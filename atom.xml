<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Vanilla-Beauty&#39;s Blog</title>
  
  
  <link href="http://example.com/atom.xml" rel="self"/>
  
  <link href="http://example.com/"/>
  <updated>2026-05-28T07:43:34.981Z</updated>
  <id>http://example.com/</id>
  
  <author>
    <name>Vanilla-Beauty</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>CURP共识算法详解-02-测试框架设计</title>
    <link href="http://example.com/2026/05/28/%E9%A1%B9%E7%9B%AE/tiny-distributed-kv/CURP%E5%85%B1%E8%AF%86%E7%AE%97%E6%B3%95%E8%AF%A6%E8%A7%A3-02-%E6%B5%8B%E8%AF%95%E6%A1%86%E6%9E%B6%E8%AE%BE%E8%AE%A1/"/>
    <id>http://example.com/2026/05/28/%E9%A1%B9%E7%9B%AE/tiny-distributed-kv/CURP%E5%85%B1%E8%AF%86%E7%AE%97%E6%B3%95%E8%AF%A6%E8%A7%A3-02-%E6%B5%8B%E8%AF%95%E6%A1%86%E6%9E%B6%E8%AE%BE%E8%AE%A1/</id>
    <published>2026-05-28T07:47:00.000Z</published>
    <updated>2026-05-28T07:43:34.981Z</updated>
    
    <content type="html"><![CDATA[<p>本节介绍 CURP 实现的测试框架设计，用于验证正确性、活性和性能。</p><h1 id="1-框架目标"><a href="#1-框架目标" class="headerlink" title="1 框架目标"></a>1 框架目标</h1><p>验证 CURP 实现的正确性，包括：</p><ul><li><strong>安全性（Safety）</strong>：线性一致性、数据不丢失</li><li><strong>活性（Liveness）</strong>：系统持续运行</li><li><strong>性能（Performance）</strong>：快速路径成功率</li></ul><hr><h1 id="2-核心组件"><a href="#2-核心组件" class="headerlink" title="2 核心组件"></a>2 核心组件</h1><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">┌─────────────────────────────────────────────────────────────┐</span><br><span class="line">│                      Test Controller                         │</span><br><span class="line">│  (控制测试流程、收集结果、验证正确性)                          │</span><br><span class="line">└─────────────────────────────────────────────────────────────┘</span><br><span class="line">                              │</span><br><span class="line">        ┌─────────────────────┼─────────────────────┐</span><br><span class="line">        ▼                     ▼                     ▼</span><br><span class="line">┌───────────────┐    ┌───────────────┐    ┌───────────────┐</span><br><span class="line">│ Network       │    │ Node          │    │ Consistency   │</span><br><span class="line">│ Simulator     │    │ Controller    │    │ Checker       │</span><br><span class="line">└───────────────┘    └───────────────┘    └───────────────┘</span><br><span class="line">        │                     │                     │</span><br><span class="line">        ▼                     ▼                     ▼</span><br><span class="line">┌─────────────────────────────────────────────────────────────┐</span><br><span class="line">│                     Simulated Cluster                        │</span><br><span class="line">│  ┌─────┐  ┌─────┐  ┌─────┐    ┌─────┐  ┌─────┐            │</span><br><span class="line">│  │Master│  │Backup│  │Backup│    │Witness│×f            │</span><br><span class="line">│  └─────┘  └─────┘  └─────┘    └─────┘                      │</span><br><span class="line">└─────────────────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure><hr><h1 id="3-Network-Simulator"><a href="#3-Network-Simulator" class="headerlink" title="3 Network Simulator"></a>3 Network Simulator</h1><h2 id="3-1-功能"><a href="#3-1-功能" class="headerlink" title="3.1 功能"></a>3.1 功能</h2><ul><li><strong>延迟模拟</strong>：消息延迟指定时间</li><li><strong>丢包模拟</strong>：按概率丢弃消息</li><li><strong>网络分区</strong>：模拟节点间通信中断</li><li><strong>消息重排序</strong>：打乱消息顺序</li></ul><h2 id="3-2-接口"><a href="#3-2-接口" class="headerlink" title="3.2 接口"></a>3.2 接口</h2><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">NetworkSimulator</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">set_delay</span><span class="params">(<span class="type">int</span> node1, <span class="type">int</span> node2, <span class="type">int</span> delay_ms)</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">set_packet_loss</span><span class="params">(<span class="type">double</span> probability)</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">partition</span><span class="params">(std::vector&lt;<span class="type">int</span>&gt; group1, std::vector&lt;<span class="type">int</span>&gt; group2)</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">heal</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">send_message</span><span class="params">(<span class="type">int</span> from, <span class="type">int</span> to, <span class="type">const</span> Message&amp; msg)</span></span>;</span><br><span class="line">    </span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    std::unordered_map&lt;std::pair&lt;<span class="type">int</span>,<span class="type">int</span>&gt;, <span class="type">int</span>&gt; delays_;</span><br><span class="line">    <span class="type">double</span> packet_loss_prob_ = <span class="number">0.0</span>;</span><br><span class="line">    std::set&lt;std::pair&lt;<span class="type">int</span>,<span class="type">int</span>&gt;&gt; blocked_links_;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><hr><h1 id="4-Node-Controller"><a href="#4-Node-Controller" class="headerlink" title="4 Node Controller"></a>4 Node Controller</h1><h2 id="4-1-功能"><a href="#4-1-功能" class="headerlink" title="4.1 功能"></a>4.1 功能</h2><ul><li><strong>启动&#x2F;停止节点</strong></li><li><strong>模拟崩溃</strong>：立即停止，丢失内存状态</li><li><strong>恢复节点</strong>：从持久化状态恢复</li><li><strong>控制时钟</strong>：加速&#x2F;暂停时间</li></ul><h2 id="4-2-接口"><a href="#4-2-接口" class="headerlink" title="4.2 接口"></a>4.2 接口</h2><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">NodeController</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">start_node</span><span class="params">(<span class="type">int</span> node_id)</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">stop_node</span><span class="params">(<span class="type">int</span> node_id)</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">crash_node</span><span class="params">(<span class="type">int</span> node_id)</span></span>;    <span class="comment">// 模拟崩溃（丢失内存）</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">recover_node</span><span class="params">(<span class="type">int</span> node_id)</span></span>;  <span class="comment">// 从持久化恢复</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">set_clock_speed</span><span class="params">(<span class="type">int</span> node_id, <span class="type">double</span> factor)</span></span>;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">is_running</span><span class="params">(<span class="type">int</span> node_id)</span></span>;</span><br><span class="line">    <span class="function">NodeState <span class="title">get_state</span><span class="params">(<span class="type">int</span> node_id)</span></span>;</span><br><span class="line">    </span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    std::unordered_map&lt;<span class="type">int</span>, std::unique_ptr&lt;CurpNode&gt;&gt; nodes_;</span><br><span class="line">    std::unordered_map&lt;<span class="type">int</span>, <span class="type">bool</span>&gt; running_;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><hr><h1 id="5-Consistency-Checker"><a href="#5-Consistency-Checker" class="headerlink" title="5 Consistency Checker"></a>5 Consistency Checker</h1><h2 id="5-1-功能"><a href="#5-1-功能" class="headerlink" title="5.1 功能"></a>5.1 功能</h2><ul><li><strong>线性一致性验证</strong>：使用并发算法验证</li><li><strong>数据完整性检查</strong>：验证所有写入都被持久化</li><li><strong>CURP 特性检查</strong>：验证快速路径正确性</li></ul><h2 id="5-2-方法"><a href="#5-2-方法" class="headerlink" title="5.2 方法"></a>5.2 方法</h2><ul><li>记录所有操作和响应时间戳</li><li>构建实时序关系图</li><li>检测是否存在循环依赖</li></ul><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">ConsistencyChecker</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">record_write</span><span class="params">(<span class="type">int</span> client_id, <span class="type">const</span> std::string&amp; key, </span></span></span><br><span class="line"><span class="params"><span class="function">                      <span class="type">const</span> std::vector&lt;<span class="type">uint8_t</span>&gt;&amp; value, </span></span></span><br><span class="line"><span class="params"><span class="function">                      <span class="type">uint64_t</span> start_time, <span class="type">uint64_t</span> end_time)</span></span>;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">record_read</span><span class="params">(<span class="type">int</span> client_id, <span class="type">const</span> std::string&amp; key,</span></span></span><br><span class="line"><span class="params"><span class="function">                     <span class="type">const</span> std::vector&lt;<span class="type">uint8_t</span>&gt;&amp; value,</span></span></span><br><span class="line"><span class="params"><span class="function">                     <span class="type">uint64_t</span> start_time, <span class="type">uint64_t</span> end_time)</span></span>;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">check_linearizability</span><span class="params">()</span></span>;  <span class="comment">// 检查线性一致性</span></span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">check_data_integrity</span><span class="params">()</span></span>;   <span class="comment">// 检查数据完整性</span></span><br><span class="line">    </span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    std::vector&lt;OperationRecord&gt; operations_;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 线性一致性检查算法（基于实时序）</span></span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">hasCycle</span><span class="params">(<span class="type">const</span> std::vector&lt;OperationRecord&gt;&amp; ops)</span></span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><hr><h1 id="6-测试场景"><a href="#6-测试场景" class="headerlink" title="6 测试场景"></a>6 测试场景</h1><h2 id="6-1-基本正确性"><a href="#6-1-基本正确性" class="headerlink" title="6.1 基本正确性"></a>6.1 基本正确性</h2><ul><li>单节点写读</li><li>多节点并发写</li><li>快速路径验证</li></ul><h2 id="6-2-故障恢复"><a href="#6-2-故障恢复" class="headerlink" title="6.2 故障恢复"></a>6.2 故障恢复</h2><ul><li>Master 崩溃恢复</li><li>Witness 崩溃恢复</li><li>同时崩溃恢复</li></ul><h2 id="6-3-网络故障"><a href="#6-3-网络故障" class="headerlink" title="6.3 网络故障"></a>6.3 网络故障</h2><ul><li>网络延迟</li><li>网络分区</li><li>消息丢失</li></ul><h2 id="6-4-极端情况"><a href="#6-4-极端情况" class="headerlink" title="6.4 极端情况"></a>6.4 极端情况</h2><ul><li>高并发</li><li>大量故障</li><li>恢复过程中新故障</li></ul><hr><h1 id="7-测试指标"><a href="#7-测试指标" class="headerlink" title="7 测试指标"></a>7 测试指标</h1><table><thead><tr><th>指标</th><th>说明</th><th>目标值</th></tr></thead><tbody><tr><td>快速路径成功率</td><td>1 RTT 完成的比例</td><td>&gt; 90%</td></tr><tr><td>写延迟</td><td>平均写操作延迟</td><td>&lt; 10ms</td></tr><tr><td>恢复时间</td><td>崩溃后恢复时间</td><td>&lt; 1s</td></tr><tr><td>数据丢失率</td><td>故障后数据丢失比例</td><td>0%</td></tr><tr><td>线性一致性违反</td><td>检测到的不一致次数</td><td>0</td></tr></tbody></table><hr><h1 id="8-单元测试示例"><a href="#8-单元测试示例" class="headerlink" title="8 单元测试示例"></a>8 单元测试示例</h1><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// test/dtest_witness.cpp</span></span><br><span class="line"><span class="built_in">TEST</span>(WitnessTest, RecordCommutative) &#123;</span><br><span class="line">    Witness w;</span><br><span class="line">    <span class="keyword">auto</span> r1 = w.<span class="built_in">record</span>(<span class="string">&quot;key1&quot;</span>, <span class="number">1</span>, &#123;&#125;);</span><br><span class="line">    <span class="built_in">EXPECT_EQ</span>(r1, Witness::RecordResult::ACCEPTED);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">auto</span> r2 = w.<span class="built_in">record</span>(<span class="string">&quot;key2&quot;</span>, <span class="number">2</span>, &#123;&#125;);</span><br><span class="line">    <span class="built_in">EXPECT_EQ</span>(r2, Witness::RecordResult::ACCEPTED);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">TEST</span>(WitnessTest, RejectNonCommutative) &#123;</span><br><span class="line">    Witness w;</span><br><span class="line">    w.<span class="built_in">record</span>(<span class="string">&quot;key1&quot;</span>, <span class="number">1</span>, &#123;&#125;);</span><br><span class="line">    <span class="keyword">auto</span> r = w.<span class="built_in">record</span>(<span class="string">&quot;key1&quot;</span>, <span class="number">2</span>, &#123;&#125;);</span><br><span class="line">    <span class="built_in">EXPECT_EQ</span>(r, Witness::RecordResult::REJECTED_NOT_COMMUTATIVE);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h1 id="9-集成测试示例"><a href="#9-集成测试示例" class="headerlink" title="9 集成测试示例"></a>9 集成测试示例</h1><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// test/dtest_curp.cpp</span></span><br><span class="line"><span class="built_in">TEST</span>(CurpTest, OneRoundTripWrite) &#123;</span><br><span class="line">    <span class="function">CurpClient <span class="title">client</span><span class="params">(config)</span></span>;</span><br><span class="line">    <span class="keyword">auto</span> result = client.<span class="built_in">write</span>(<span class="string">&quot;key&quot;</span>, value);</span><br><span class="line">    <span class="built_in">EXPECT_EQ</span>(result, CurpClient::WriteResult::SUCCESS);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">TEST</span>(CurpTest, RecoveryAfterCrash) &#123;</span><br><span class="line">    <span class="comment">// 1. 启动 Master + Witness</span></span><br><span class="line">    NodeController nc;</span><br><span class="line">    nc.<span class="built_in">start_node</span>(MASTER);</span><br><span class="line">    nc.<span class="built_in">start_node</span>(WITNESS);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 2. 写入数据（快速路径）</span></span><br><span class="line">    <span class="function">CurpClient <span class="title">client</span><span class="params">(config)</span></span>;</span><br><span class="line">    client.<span class="built_in">write</span>(<span class="string">&quot;key&quot;</span>, value);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 3. 杀死 Master（模拟崩溃）</span></span><br><span class="line">    nc.<span class="built_in">crash_node</span>(MASTER);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 4. 恢复 Master</span></span><br><span class="line">    nc.<span class="built_in">recover_node</span>(MASTER);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 5. 验证数据恢复</span></span><br><span class="line">    <span class="keyword">auto</span> read_value = client.<span class="built_in">read</span>(<span class="string">&quot;key&quot;</span>);</span><br><span class="line">    <span class="built_in">EXPECT_EQ</span>(read_value, value);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h1 id="10-总结"><a href="#10-总结" class="headerlink" title="10 总结"></a>10 总结</h1><p>测试框架是 CURP 实现的关键组成部分，通过：</p><ul><li><strong>Network Simulator</strong> 模拟各种网络条件</li><li><strong>Node Controller</strong> 控制节点生命周期</li><li><strong>Consistency Checker</strong> 验证系统正确性</li></ul><p>确保 CURP 实现满足安全性、活性和性能要求。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;本节介绍 CURP 实现的测试框架设计，用于验证正确性、活性和性能。&lt;/p&gt;
&lt;h1 id=&quot;1-框架目标&quot;&gt;&lt;a href=&quot;#1-框架目标&quot; class=&quot;headerlink&quot; title=&quot;1 框架目标&quot;&gt;&lt;/a&gt;1 框架目标&lt;/h1&gt;&lt;p&gt;验证 CURP 实现的正</summary>
      
    
    
    
    <category term="项目" scheme="http://example.com/categories/%E9%A1%B9%E7%9B%AE/"/>
    
    <category term="tiny-distributed-kv" scheme="http://example.com/categories/%E9%A1%B9%E7%9B%AE/tiny-distributed-kv/"/>
    
    
    <category term="C++" scheme="http://example.com/tags/C/"/>
    
    <category term="分布式系统" scheme="http://example.com/tags/%E5%88%86%E5%B8%83%E5%BC%8F%E7%B3%BB%E7%BB%9F/"/>
    
    <category term="测试框架" scheme="http://example.com/tags/%E6%B5%8B%E8%AF%95%E6%A1%86%E6%9E%B6/"/>
    
  </entry>
  
  <entry>
    <title>CURP共识算法详解-01-实现计划</title>
    <link href="http://example.com/2026/05/28/%E9%A1%B9%E7%9B%AE/tiny-distributed-kv/CURP%E5%85%B1%E8%AF%86%E7%AE%97%E6%B3%95%E8%AF%A6%E8%A7%A3-01-%E5%AE%9E%E7%8E%B0%E8%AE%A1%E5%88%92/"/>
    <id>http://example.com/2026/05/28/%E9%A1%B9%E7%9B%AE/tiny-distributed-kv/CURP%E5%85%B1%E8%AF%86%E7%AE%97%E6%B3%95%E8%AF%A6%E8%A7%A3-01-%E5%AE%9E%E7%8E%B0%E8%AE%A1%E5%88%92/</id>
    <published>2026-05-28T07:46:00.000Z</published>
    <updated>2026-05-28T07:42:45.257Z</updated>
    
    <content type="html"><![CDATA[<p>基于 tiny-distributed-kv 现有 Raft 实现扩展为 CURP。这一节介绍整体的实现计划。</p><h1 id="1-现有代码分析"><a href="#1-现有代码分析" class="headerlink" title="1 现有代码分析"></a>1 现有代码分析</h1><h2 id="1-1-已实现组件"><a href="#1-1-已实现组件" class="headerlink" title="1.1 已实现组件"></a>1.1 已实现组件</h2><table><thead><tr><th>组件</th><th>文件</th><th>状态</th><th>说明</th></tr></thead><tbody><tr><td>RaftNode</td><td><code>src/raft/raft.cpp</code></td><td>✓ 完成</td><td>Raft 核心状态机</td></tr><tr><td>RaftServiceImpl</td><td><code>src/raft/raft_rpc.cpp</code></td><td>✓ 完成</td><td>Raft RPC 服务</td></tr><tr><td>LogVec</td><td><code>src/storage/log_vec.cpp</code></td><td>✓ 完成</td><td>持久化日志</td></tr><tr><td>Timer</td><td><code>src/utils/timer.cpp</code></td><td>✓ 完成</td><td>定时器</td></tr><tr><td>gRPC Server</td><td><code>src/grpc/grpc_server.cpp</code></td><td>✓ 完成</td><td>RPC 服务框架</td></tr></tbody></table><h2 id="1-2-Raft-实现特点"><a href="#1-2-Raft-实现特点" class="headerlink" title="1.2 Raft 实现特点"></a>1.2 Raft 实现特点</h2><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 现有 Raft 状态机</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">RaftNode</span> &#123;</span><br><span class="line">    RaftState role;           <span class="comment">// LEADER/FOLLOWER/CANDIDATE</span></span><br><span class="line">    <span class="type">int</span> currentTerm;</span><br><span class="line">    <span class="type">int</span> votedFor;</span><br><span class="line">    LogVec log;               <span class="comment">// 持久化日志</span></span><br><span class="line">    vector&lt;<span class="type">uint64_t</span>&gt; nextIndex;</span><br><span class="line">    vector&lt;<span class="type">uint64_t</span>&gt; matchIndex;</span><br><span class="line">    <span class="type">uint64_t</span> commitIndex;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 关键流程</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">Elect</span><span class="params">()</span></span>;             <span class="comment">// 选举</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">SendHeartBeats</span><span class="params">()</span></span>;    <span class="comment">// 心跳/日志复制</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">handleAppendEntries</span><span class="params">()</span></span>;<span class="comment">// 处理日志复制响应</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><hr><h1 id="2-Raft-vs-CURP-对比"><a href="#2-Raft-vs-CURP-对比" class="headerlink" title="2 Raft vs CURP 对比"></a>2 Raft vs CURP 对比</h1><h2 id="2-1-架构差异"><a href="#2-1-架构差异" class="headerlink" title="2.1 架构差异"></a>2.1 架构差异</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">Raft 架构:</span><br><span class="line">┌────────┐     ┌────────┐</span><br><span class="line">│ Client │────→│ Leader │────→ Majority</span><br><span class="line">└────────┘     └────────┘     (同步复制)</span><br><span class="line">                   ↓</span><br><span class="line">               2 RTT</span><br><span class="line"></span><br><span class="line">CURP 架构:</span><br><span class="line">┌────────┐     ┌────────┐</span><br><span class="line">│ Client │────→│ Master │────→ Backup (异步)</span><br><span class="line">└────────┘     └────────┘</span><br><span class="line">     │              ↑</span><br><span class="line">     └─────→ Witness × f</span><br><span class="line">              (并行)</span><br><span class="line">                   </span><br><span class="line">               1 RTT</span><br></pre></td></tr></table></figure><h2 id="2-2-代码层面的关键差异"><a href="#2-2-代码层面的关键差异" class="headerlink" title="2.2 代码层面的关键差异"></a>2.2 代码层面的关键差异</h2><table><thead><tr><th>方面</th><th>Raft</th><th>CURP</th></tr></thead><tbody><tr><td><strong>角色</strong></td><td>Leader&#x2F;Follower&#x2F;Candidate</td><td>Master&#x2F;Backup&#x2F;Witness</td></tr><tr><td><strong>选举</strong></td><td>复杂的多轮选举</td><td>简化的 leader 选举（可复用 Raft）</td></tr><tr><td><strong>写路径</strong></td><td>同步复制到 majority</td><td>异步复制 + Witness 记录</td></tr><tr><td><strong>可交换性检查</strong></td><td>无</td><td>核心机制</td></tr><tr><td><strong>推测执行</strong></td><td>无</td><td>执行后立即响应</td></tr><tr><td><strong>恢复</strong></td><td>从最新日志恢复</td><td>Backup + Witness 重放</td></tr></tbody></table><hr><h1 id="3-实现计划"><a href="#3-实现计划" class="headerlink" title="3 实现计划"></a>3 实现计划</h1><h2 id="Phase-1-Witness-实现（优先级最高）"><a href="#Phase-1-Witness-实现（优先级最高）" class="headerlink" title="Phase 1: Witness 实现（优先级最高）"></a>Phase 1: Witness 实现（优先级最高）</h2><p>Witness 是 CURP 的核心新组件。</p><h3 id="3-1-1-Witness-数据结构"><a href="#3-1-1-Witness-数据结构" class="headerlink" title="3.1.1 Witness 数据结构"></a>3.1.1 Witness 数据结构</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// include/curp/witness.h</span></span><br><span class="line"><span class="meta">#<span class="keyword">pragma</span> once</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unordered_map&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;mutex&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Witness 存储的请求记录</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">WitnessRecord</span> &#123;</span><br><span class="line">    <span class="type">uint64_t</span> rpc_id;        <span class="comment">// RIFL 唯一标识</span></span><br><span class="line">    std::string key;        <span class="comment">// 操作的 key（用于可交换性检查）</span></span><br><span class="line">    std::vector&lt;<span class="type">uint8_t</span>&gt; request_data;  <span class="comment">// 序列化的请求</span></span><br><span class="line">    <span class="type">uint64_t</span> timestamp;     <span class="comment">// 用于 GC</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Witness</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Witness</span>();</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// RPC 接口</span></span><br><span class="line">    <span class="keyword">enum class</span> <span class="title class_">RecordResult</span> &#123;</span><br><span class="line">        ACCEPTED,</span><br><span class="line">        REJECTED_NOT_COMMUTATIVE,</span><br><span class="line">        REJECTED_NO_SPACE</span><br><span class="line">    &#125;;</span><br><span class="line">    </span><br><span class="line">    <span class="function">RecordResult <span class="title">record</span><span class="params">(<span class="type">const</span> std::string&amp; key, <span class="type">uint64_t</span> rpc_id, </span></span></span><br><span class="line"><span class="params"><span class="function">                        <span class="type">const</span> std::vector&lt;<span class="type">uint8_t</span>&gt;&amp; request_data)</span></span>;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">garbage_collect</span><span class="params">(<span class="type">const</span> std::vector&lt;std::pair&lt;std::string, <span class="type">uint64_t</span>&gt;&gt;&amp; to_drop)</span></span>;</span><br><span class="line">    </span><br><span class="line">    <span class="function">std::vector&lt;WitnessRecord&gt; <span class="title">get_recovery_data</span><span class="params">()</span></span>;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">stop_accepting</span><span class="params">()</span></span>;  <span class="comment">// 进入恢复模式</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">reset</span><span class="params">()</span></span>;           <span class="comment">// 重置为新 Master</span></span><br><span class="line">    </span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    std::mutex mtx_;</span><br><span class="line">    <span class="type">bool</span> accepting_ = <span class="literal">true</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 按 key hash 分组的 slots</span></span><br><span class="line">    std::unordered_map&lt;std::string, WitnessRecord&gt; records_;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 可交换性检查</span></span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">isCommutative</span><span class="params">(<span class="type">const</span> std::string&amp; key)</span> <span class="type">const</span></span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h3 id="3-1-2-可交换性检查实现"><a href="#3-1-2-可交换性检查实现" class="headerlink" title="3.1.2 可交换性检查实现"></a>3.1.2 可交换性检查实现</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// src/curp/witness.cpp</span></span><br><span class="line"><span class="function">Witness::RecordResult <span class="title">Witness::record</span><span class="params">(<span class="type">const</span> std::string&amp; key, <span class="type">uint64_t</span> rpc_id,</span></span></span><br><span class="line"><span class="params"><span class="function">                                       <span class="type">const</span> std::vector&lt;<span class="type">uint8_t</span>&gt;&amp; request_data)</span> </span>&#123;</span><br><span class="line">    <span class="function">std::lock_guard&lt;std::mutex&gt; <span class="title">lock</span><span class="params">(mtx_)</span></span>;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (!accepting_) &#123;</span><br><span class="line">        <span class="keyword">return</span> RecordResult::REJECTED_NOT_COMMUTATIVE;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 检查是否与已存储的请求冲突</span></span><br><span class="line">    <span class="keyword">if</span> (records_.<span class="built_in">find</span>(key) != records_.<span class="built_in">end</span>()) &#123;</span><br><span class="line">        <span class="keyword">return</span> RecordResult::REJECTED_NOT_COMMUTATIVE;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 检查空间</span></span><br><span class="line">    <span class="keyword">if</span> (records_.<span class="built_in">size</span>() &gt;= kMaxRecords) &#123;</span><br><span class="line">        <span class="keyword">return</span> RecordResult::REJECTED_NO_SPACE;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 存储记录</span></span><br><span class="line">    records_[key] = WitnessRecord&#123;</span><br><span class="line">        .rpc_id = rpc_id,</span><br><span class="line">        .key = key,</span><br><span class="line">        .request_data = request_data,</span><br><span class="line">        .timestamp = <span class="built_in">getCurrentTime</span>()</span><br><span class="line">    &#125;;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> RecordResult::ACCEPTED;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h2 id="Phase-2-CurpMaster-实现"><a href="#Phase-2-CurpMaster-实现" class="headerlink" title="Phase 2: CurpMaster 实现"></a>Phase 2: CurpMaster 实现</h2><h3 id="3-2-1-CurpMaster-数据结构"><a href="#3-2-1-CurpMaster-数据结构" class="headerlink" title="3.2.1 CurpMaster 数据结构"></a>3.2.1 CurpMaster 数据结构</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// include/curp/curp_master.h</span></span><br><span class="line"><span class="meta">#<span class="keyword">pragma</span> once</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;../raft/raft.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;witness.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unordered_set&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;atomic&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">enum class</span> <span class="title class_">CurpState</span> &#123;</span><br><span class="line">    NORMAL,</span><br><span class="line">    RECOVERING</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">CurpMaster</span> : <span class="keyword">public</span> RaftNode &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">static</span> std::shared_ptr&lt;CurpMaster&gt; <span class="title">Create</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">        std::vector&lt;NodeConfig&gt; cluster_configs,</span></span></span><br><span class="line"><span class="params"><span class="function">        std::vector&lt;NodeConfig&gt; witness_configs,</span></span></span><br><span class="line"><span class="params"><span class="function">        std::string log_dir,</span></span></span><br><span class="line"><span class="params"><span class="function">        <span class="type">uint64_t</span> cur_node_id)</span></span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// Curp 特有的接口</span></span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">proposeUpdate</span><span class="params">(<span class="type">const</span> std::string&amp; key, <span class="type">const</span> std::vector&lt;<span class="type">uint8_t</span>&gt;&amp; value)</span></span>;</span><br><span class="line">    <span class="function">std::string <span class="title">read</span><span class="params">(<span class="type">const</span> std::string&amp; key)</span></span>;</span><br><span class="line">    </span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="comment">// Witness 配置</span></span><br><span class="line">    std::vector&lt;NodeConfig&gt; witness_configs_;</span><br><span class="line">    std::vector&lt;std::shared_ptr&lt;Witness&gt;&gt; witnesses_;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 未同步的 keys（用于可交换性检查）</span></span><br><span class="line">    std::unordered_set&lt;std::string&gt; unsynced_keys_;</span><br><span class="line">    std::mutex unsynced_mtx_;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 状态</span></span><br><span class="line">    std::atomic&lt;CurpState&gt; state_&#123;CurpState::NORMAL&#125;;</span><br><span class="line">    <span class="type">uint64_t</span> last_sync_index_&#123;<span class="number">0</span>&#125;;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 核心方法</span></span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">canSpeculativeExecute</span><span class="params">(<span class="type">const</span> std::string&amp; key)</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">markUnsynced</span><span class="params">(<span class="type">const</span> std::string&amp; key)</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">asyncSyncToBackup</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">garbageCollectWitnesses</span><span class="params">()</span></span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h3 id="3-2-2-写操作流程"><a href="#3-2-2-写操作流程" class="headerlink" title="3.2.2 写操作流程"></a>3.2.2 写操作流程</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// src/curp/curp_master.cpp</span></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">CurpMaster::proposeUpdate</span><span class="params">(<span class="type">const</span> std::string&amp; key, </span></span></span><br><span class="line"><span class="params"><span class="function">                                 <span class="type">const</span> std::vector&lt;<span class="type">uint8_t</span>&gt;&amp; value)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 1. 检查是否可以推测执行</span></span><br><span class="line">    <span class="keyword">if</span> (!<span class="built_in">canSpeculativeExecute</span>(key)) &#123;</span><br><span class="line">        <span class="comment">// 慢路径：先 sync，再执行</span></span><br><span class="line">        <span class="built_in">syncToBackup</span>();</span><br><span class="line">        <span class="built_in">executeAndRespond</span>(key, value);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 2. 推测执行（快速路径）</span></span><br><span class="line">    <span class="built_in">markUnsynced</span>(key);</span><br><span class="line">    <span class="built_in">speculativeExecute</span>(key, value);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">CurpMaster::canSpeculativeExecute</span><span class="params">(<span class="type">const</span> std::string&amp; key)</span> </span>&#123;</span><br><span class="line">    <span class="function">std::lock_guard&lt;std::mutex&gt; <span class="title">lock</span><span class="params">(unsynced_mtx_)</span></span>;</span><br><span class="line">    <span class="comment">// 如果 key 在未同步集合中，不能推测执行</span></span><br><span class="line">    <span class="keyword">return</span> unsynced_keys_.<span class="built_in">find</span>(key) == unsynced_keys_.<span class="built_in">end</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">CurpMaster::markUnsynced</span><span class="params">(<span class="type">const</span> std::string&amp; key)</span> </span>&#123;</span><br><span class="line">    <span class="function">std::lock_guard&lt;std::mutex&gt; <span class="title">lock</span><span class="params">(unsynced_mtx_)</span></span>;</span><br><span class="line">    unsynced_keys_.<span class="built_in">insert</span>(key);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h2 id="Phase-3-CurpClient-实现"><a href="#Phase-3-CurpClient-实现" class="headerlink" title="Phase 3: CurpClient 实现"></a>Phase 3: CurpClient 实现</h2><h3 id="3-3-1-Client-接口"><a href="#3-3-1-Client-接口" class="headerlink" title="3.3.1 Client 接口"></a>3.3.1 Client 接口</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// include/curp/curp_client.h</span></span><br><span class="line"><span class="meta">#<span class="keyword">pragma</span> once</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;memory&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">WitnessConfig</span> &#123;</span><br><span class="line">    std::string addr;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">CurpConfig</span> &#123;</span><br><span class="line">    std::string master_addr;</span><br><span class="line">    std::vector&lt;WitnessConfig&gt; witness_configs;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">CurpClient</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">CurpClient</span>(CurpConfig config);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 1 RTT 写操作</span></span><br><span class="line">    <span class="keyword">enum class</span> <span class="title class_">WriteResult</span> &#123;</span><br><span class="line">        SUCCESS,        <span class="comment">// 1 RTT 完成</span></span><br><span class="line">        SLOW_SUCCESS,   <span class="comment">// 2 RTT 完成（需要 sync）</span></span><br><span class="line">        FAILED</span><br><span class="line">    &#125;;</span><br><span class="line">    </span><br><span class="line">    <span class="function">WriteResult <span class="title">write</span><span class="params">(<span class="type">const</span> std::string&amp; key, <span class="type">const</span> std::vector&lt;<span class="type">uint8_t</span>&gt;&amp; value)</span></span>;</span><br><span class="line">    <span class="function">std::vector&lt;<span class="type">uint8_t</span>&gt; <span class="title">read</span><span class="params">(<span class="type">const</span> std::string&amp; key)</span></span>;</span><br><span class="line">    </span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    CurpConfig config_;</span><br><span class="line">    <span class="type">uint64_t</span> next_rpc_id_&#123;<span class="number">1</span>&#125;;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 并行发送到 Master 和 Witness</span></span><br><span class="line">    <span class="function">WriteResult <span class="title">parallelWrite</span><span class="params">(<span class="type">const</span> std::string&amp; key, </span></span></span><br><span class="line"><span class="params"><span class="function">                               <span class="type">const</span> std::vector&lt;<span class="type">uint8_t</span>&gt;&amp; value)</span></span>;</span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">recordToWitnesses</span><span class="params">(<span class="type">const</span> std::string&amp; key, <span class="type">uint64_t</span> rpc_id,</span></span></span><br><span class="line"><span class="params"><span class="function">                           <span class="type">const</span> std::vector&lt;<span class="type">uint8_t</span>&gt;&amp; request_data)</span></span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h3 id="3-3-2-并行写流程"><a href="#3-3-2-并行写流程" class="headerlink" title="3.3.2 并行写流程"></a>3.3.2 并行写流程</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// src/curp/curp_client.cpp</span></span><br><span class="line"><span class="function">CurpClient::WriteResult <span class="title">CurpClient::write</span><span class="params">(<span class="type">const</span> std::string&amp; key,</span></span></span><br><span class="line"><span class="params"><span class="function">                                          <span class="type">const</span> std::vector&lt;<span class="type">uint8_t</span>&gt;&amp; value)</span> </span>&#123;</span><br><span class="line">    <span class="type">uint64_t</span> rpc_id = next_rpc_id_++;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 并行发送</span></span><br><span class="line">    <span class="keyword">auto</span> master_future = std::<span class="built_in">async</span>([&amp;]() &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">sendToMaster</span>(key, value, rpc_id);</span><br><span class="line">    &#125;);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">auto</span> witness_future = std::<span class="built_in">async</span>([&amp;]() &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">recordToWitnesses</span>(key, rpc_id, <span class="built_in">serializeRequest</span>(key, value));</span><br><span class="line">    &#125;);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 等待 Master 响应</span></span><br><span class="line">    <span class="keyword">auto</span> master_reply = master_future.<span class="built_in">get</span>();</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 等待 Witness 记录</span></span><br><span class="line">    <span class="type">bool</span> all_witness_accepted = witness_future.<span class="built_in">get</span>();</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (master_reply.success) &#123;</span><br><span class="line">        <span class="keyword">if</span> (all_witness_accepted) &#123;</span><br><span class="line">            <span class="keyword">return</span> WriteResult::SUCCESS;  <span class="comment">// 1 RTT</span></span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (master_reply.synced) &#123;</span><br><span class="line">            <span class="keyword">return</span> WriteResult::SLOW_SUCCESS;  <span class="comment">// 2 RTT</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> WriteResult::FAILED;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h2 id="Phase-4-恢复机制"><a href="#Phase-4-恢复机制" class="headerlink" title="Phase 4: 恢复机制"></a>Phase 4: 恢复机制</h2><h3 id="3-4-1-恢复流程"><a href="#3-4-1-恢复流程" class="headerlink" title="3.4.1 恢复流程"></a>3.4.1 恢复流程</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// src/curp/curp_recovery.cpp</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">CurpMaster::recover</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    state_ = CurpState::RECOVERING;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// Phase 1: 从 Backup 恢复有序数据（复用 Raft 恢复）</span></span><br><span class="line">    <span class="built_in">recoverFromBackup</span>();</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// Phase 2: 从 Witness 重放无序请求</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">auto</span>&amp; witness : witnesses_) &#123;</span><br><span class="line">        <span class="keyword">if</span> (witness-&gt;<span class="built_in">isAlive</span>()) &#123;</span><br><span class="line">            <span class="built_in">replayFromWitness</span>(witness);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// Phase 3: 同步到 Backup，完成恢复</span></span><br><span class="line">    <span class="built_in">syncToBackup</span>();</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// Phase 4: 重置 Witness</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">auto</span>&amp; witness : witnesses_) &#123;</span><br><span class="line">        witness-&gt;<span class="built_in">reset</span>();</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    state_ = CurpState::NORMAL;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">CurpMaster::replayFromWitness</span><span class="params">(std::shared_ptr&lt;Witness&gt; witness)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 停止接受新请求</span></span><br><span class="line">    witness-&gt;<span class="built_in">stop_accepting</span>();</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 获取所有记录的请求</span></span><br><span class="line">    <span class="keyword">auto</span> records = witness-&gt;<span class="built_in">get_recovery_data</span>();</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 重放（顺序不重要，因为可交换）</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">const</span> <span class="keyword">auto</span>&amp; record : records) &#123;</span><br><span class="line">        <span class="keyword">if</span> (!<span class="built_in">alreadyExecuted</span>(record.rpc_id)) &#123;</span><br><span class="line">            <span class="built_in">replayRequest</span>(record);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h1 id="4-文件结构规划"><a href="#4-文件结构规划" class="headerlink" title="4 文件结构规划"></a>4 文件结构规划</h1><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line">tiny-distributed-kv/</span><br><span class="line">├── proto/</span><br><span class="line">│   ├── raft.proto          # 已有</span><br><span class="line">│   ├── node.proto          # 已有</span><br><span class="line">│   └── witness.proto       # 新增：Witness RPC</span><br><span class="line">│</span><br><span class="line">├── include/</span><br><span class="line">│   ├── raft/               # 已有，保持不变</span><br><span class="line">│   ├── curp/</span><br><span class="line">│   │   ├── witness.h       # 新增</span><br><span class="line">│   │   ├── curp_master.h   # 新增</span><br><span class="line">│   │   └── curp_client.h   # 新增</span><br><span class="line">│   └── grpc/</span><br><span class="line">│       └── witness_service_impl.h  # 新增</span><br><span class="line">│</span><br><span class="line">├── src/</span><br><span class="line">│   ├── raft/               # 已有</span><br><span class="line">│   ├── curp/</span><br><span class="line">│   │   ├── witness.cpp     # 新增</span><br><span class="line">│   │   ├── curp_master.cpp # 新增</span><br><span class="line">│   │   └── curp_client.cpp # 新增</span><br><span class="line">│   └── grpc/</span><br><span class="line">│       └── witness_service_impl.cpp  # 新增</span><br><span class="line">│</span><br><span class="line">└── test/</span><br><span class="line">    ├── dtest_curp.cpp      # 新增</span><br><span class="line">    └── dtest_witness.cpp   # 新增</span><br></pre></td></tr></table></figure><hr><h1 id="5-实现顺序"><a href="#5-实现顺序" class="headerlink" title="5 实现顺序"></a>5 实现顺序</h1><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line">Phase 1: Witness（基础）</span><br><span class="line">├── [1] proto/witness.proto          # Witness RPC 定义</span><br><span class="line">├── [2] include/curp/witness.h       # Witness 数据结构</span><br><span class="line">├── [3] src/curp/witness.cpp         # Witness 实现</span><br><span class="line">├── [4] grpc/witness_service_impl    # Witness RPC 服务</span><br><span class="line">└── [5] test/dtest_witness.cpp       # Witness 单元测试</span><br><span class="line"></span><br><span class="line">Phase 2: CurpMaster（核心）</span><br><span class="line">├── [6] include/curp/curp_master.h   # CurpMaster 定义</span><br><span class="line">├── [7] src/curp/curp_master.cpp     # CurpMaster 实现</span><br><span class="line">└── [8] 扩展 Raft -&gt; CurpMaster 继承</span><br><span class="line"></span><br><span class="line">Phase 3: CurpClient（接口）</span><br><span class="line">├── [9] include/curp/curp_client.h   # Client 定义</span><br><span class="line">└── [10] src/curp/curp_client.cpp    # 并行写实现</span><br><span class="line"></span><br><span class="line">Phase 4: 恢复与完善</span><br><span class="line">├── [11] 恢复机制</span><br><span class="line">├── [12] GC 机制</span><br><span class="line">├── [13] 集成测试</span><br><span class="line">└── [14] 性能测试</span><br></pre></td></tr></table></figure><hr><h1 id="6-教学版简化策略"><a href="#6-教学版简化策略" class="headerlink" title="6 教学版简化策略"></a>6 教学版简化策略</h1><p>作为教学版实现，可以做以下简化：</p><table><thead><tr><th>方面</th><th>完整实现</th><th>教学简化</th></tr></thead><tbody><tr><td><strong>RIFL</strong></td><td>完整的去重机制</td><td>简化的 RPC ID 检查</td></tr><tr><td><strong>Witness 数量</strong></td><td>f 个独立</td><td>单个 Witness 即可演示</td></tr><tr><td><strong>批量同步</strong></td><td>自适应批大小</td><td>固定周期同步</td></tr><tr><td><strong>GC</strong></td><td>复杂的 GC 策略</td><td>简单的超时清理</td></tr><tr><td><strong>配置管理</strong></td><td>动态配置变更</td><td>静态配置</td></tr></tbody></table><hr><h1 id="7-下一步"><a href="#7-下一步" class="headerlink" title="7 下一步"></a>7 下一步</h1><p>下一节开始实现 <strong>Phase 1: Witness</strong>：</p><ol><li>创建 <code>proto/witness.proto</code></li><li>实现 <code>Witness</code> 类和可交换性检查</li><li>实现 <code>WitnessServiceImpl</code></li><li>编写单元测试验证</li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;基于 tiny-distributed-kv 现有 Raft 实现扩展为 CURP。这一节介绍整体的实现计划。&lt;/p&gt;
&lt;h1 id=&quot;1-现有代码分析&quot;&gt;&lt;a href=&quot;#1-现有代码分析&quot; class=&quot;headerlink&quot; title=&quot;1 现有代码分析&quot;&gt;&lt;/a</summary>
      
    
    
    
    <category term="项目" scheme="http://example.com/categories/%E9%A1%B9%E7%9B%AE/"/>
    
    <category term="tiny-distributed-kv" scheme="http://example.com/categories/%E9%A1%B9%E7%9B%AE/tiny-distributed-kv/"/>
    
    
    <category term="C++" scheme="http://example.com/tags/C/"/>
    
    <category term="分布式系统" scheme="http://example.com/tags/%E5%88%86%E5%B8%83%E5%BC%8F%E7%B3%BB%E7%BB%9F/"/>
    
    <category term="共识协议" scheme="http://example.com/tags/%E5%85%B1%E8%AF%86%E5%8D%8F%E8%AE%AE/"/>
    
  </entry>
  
  <entry>
    <title>CURP共识算法详解-00-CURP理论</title>
    <link href="http://example.com/2026/05/28/%E9%A1%B9%E7%9B%AE/tiny-distributed-kv/CURP%E5%85%B1%E8%AF%86%E7%AE%97%E6%B3%95%E8%AF%A6%E8%A7%A3-00-CURP%E7%90%86%E8%AE%BA/"/>
    <id>http://example.com/2026/05/28/%E9%A1%B9%E7%9B%AE/tiny-distributed-kv/CURP%E5%85%B1%E8%AF%86%E7%AE%97%E6%B3%95%E8%AF%A6%E8%A7%A3-00-CURP%E7%90%86%E8%AE%BA/</id>
    <published>2026-05-28T07:45:00.000Z</published>
    <updated>2026-05-28T07:41:54.318Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p><strong>论文来源</strong>: <em>Exploiting Commutativity For Practical Fast Replication</em><br><strong>作者</strong>: Seo Jin Park, John Ousterhout (Stanford University)<br><strong>会议</strong>: NSDI ‘19</p></blockquote><p>本系列文章记录我基于现有 Raft 实现扩展为 CURP 共识协议的过程。这一节先介绍 CURP 的核心理论。</p><h1 id="1-概述"><a href="#1-概述" class="headerlink" title="1 概述"></a>1 概述</h1><p>CURP（Consistent Unordered Replication Protocol）是一种创新的复制协议，<strong>将大多数写操作的延迟从 2 RTT 降低到 1 RTT</strong>。</p><h2 id="1-1-核心目标"><a href="#1-1-核心目标" class="headerlink" title="1.1 核心目标"></a>1.1 核心目标</h2><ul><li><strong>线性一致性</strong>：保证强一致性</li><li><strong>低延迟</strong>：正常情况下 1 RTT 完成</li><li><strong>容错性</strong>：支持 f 个节点故障</li><li><strong>通用性</strong>：可应用于 primary-backup 和 consensus 系统</li></ul><h2 id="1-2-性能提升（论文实测）"><a href="#1-2-性能提升（论文实测）" class="headerlink" title="1.2 性能提升（论文实测）"></a>1.2 性能提升（论文实测）</h2><table><thead><tr><th>系统</th><th>无复制</th><th>传统复制</th><th>CURP 复制</th></tr></thead><tbody><tr><td>RAMCloud 写延迟</td><td>6.1 µs</td><td>14 µs</td><td><strong>7.1 µs</strong></td></tr><tr><td>Redis</td><td>非持久化</td><td>高开销持久化</td><td><strong>低开销持久化</strong></td></tr></tbody></table><hr><h1 id="2-核心思想：分离持久性与排序"><a href="#2-核心思想：分离持久性与排序" class="headerlink" title="2 核心思想：分离持久性与排序"></a>2 核心思想：分离持久性与排序</h1><h2 id="2-1-传统复制的问题"><a href="#2-1-传统复制的问题" class="headerlink" title="2.1 传统复制的问题"></a>2.1 传统复制的问题</h2><p>传统复制协议（Raft、Paxos、primary-backup）必须：</p><ol><li><strong>先排序</strong>：确定操作的执行顺序</li><li><strong>再持久化</strong>：将有序操作复制到副本</li></ol><p>这导致 <strong>至少 2 RTT</strong>：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Client → Master (1 RTT)</span><br><span class="line">Master → Backup (1 RTT)</span><br></pre></td></tr></table></figure><h2 id="2-2-CURP-的创新"><a href="#2-2-CURP-的创新" class="headerlink" title="2.2 CURP 的创新"></a>2.2 CURP 的创新</h2><p>CURP 发现：<strong>如果操作是可交换的（commutative），它们的执行顺序就不重要！</strong></p><p>因此：</p><ul><li><strong>持久性可以独立完成</strong>（无需等待排序）</li><li><strong>排序可以延迟执行</strong>（异步进行）</li></ul><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Client → Master + Witness (并行，1 RTT)</span><br><span class="line">Master → Backup (异步，不影响客户端)</span><br></pre></td></tr></table></figure><hr><h1 id="3-架构组件"><a href="#3-架构组件" class="headerlink" title="3 架构组件"></a>3 架构组件</h1><p>CURP 在传统 primary-backup 基础上引入 <strong>Witness（见证者）</strong>。</p><h2 id="3-1-节点角色"><a href="#3-1-节点角色" class="headerlink" title="3.1 节点角色"></a>3.1 节点角色</h2><table><thead><tr><th>角色</th><th>数量</th><th>功能</th></tr></thead><tbody><tr><td><strong>Master</strong></td><td>1</td><td>接收请求、执行操作、协调复制</td></tr><tr><td><strong>Backup</strong></td><td>f</td><td>存储有序数据，用于恢复</td></tr><tr><td><strong>Witness</strong></td><td>f</td><td>临时存储未排序请求，保证持久性</td></tr></tbody></table><h2 id="3-2-容错能力"><a href="#3-2-容错能力" class="headerlink" title="3.2 容错能力"></a>3.2 容错能力</h2><ul><li><strong>f + 1 replicas</strong>（Master + f Backups）</li><li><strong>f witnesses</strong></li><li>系统可容忍 <strong>f 个任意节点故障</strong></li></ul><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">            ┌─────────────┐</span><br><span class="line">            │   Witness   │ × f  (无序持久化)</span><br><span class="line">            └─────────────┘</span><br><span class="line">                  ↓</span><br><span class="line">┌─────────────────────────────────────┐</span><br><span class="line">│              Client                 │</span><br><span class="line">└─────────────────────────────────────┘</span><br><span class="line">          ↓               ↓</span><br><span class="line">    ┌──────────┐    ┌──────────┐</span><br><span class="line">    │  Master  │ →  │  Backup  │ × f  (有序持久化)</span><br><span class="line">    └──────────┘    └──────────┘</span><br></pre></td></tr></table></figure><hr><h1 id="4-正常操作流程"><a href="#4-正常操作流程" class="headerlink" title="4 正常操作流程"></a>4 正常操作流程</h1><h2 id="4-1-快速路径（Fast-Path）-1-RTT"><a href="#4-1-快速路径（Fast-Path）-1-RTT" class="headerlink" title="4.1 快速路径（Fast Path）- 1 RTT"></a>4.1 快速路径（Fast Path）- 1 RTT</h2><p><strong>条件</strong>：操作与所有未同步操作可交换</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">时间线:</span><br><span class="line">─────────────────────────────────────────────────→</span><br><span class="line"></span><br><span class="line">Client                    Master                   Witness</span><br><span class="line">  │                         │                         │</span><br><span class="line">  │──── update request ────→│                         │</span><br><span class="line">  │                         │                         │</span><br><span class="line">  │───── record request ───────────────────────────→│</span><br><span class="line">  │                         │                         │</span><br><span class="line">  │                         │── execute (speculative) │</span><br><span class="line">  │                         │                         │</span><br><span class="line">  │←───── response ─────────│                         │</span><br><span class="line">  │                         │                         │</span><br><span class="line">  │←─── ACCEPTED ───────────────────────────────────│</span><br><span class="line">  │                         │                         │</span><br><span class="line">  │  ✓ 操作完成             │                         │</span><br><span class="line">  │                         │─── async sync ─────────→│ Backup</span><br></pre></td></tr></table></figure><p><strong>步骤</strong>：</p><ol><li>Client <strong>并行发送</strong>请求到 Master 和所有 Witness</li><li>Witness 检查可交换性，接受则返回 ACCEPTED</li><li>Master <strong>推测执行</strong>（speculative execution），立即响应</li><li>Client 收到 Master 响应 + 所有 Witness ACCEPTED → <strong>完成</strong></li><li>Master <strong>异步同步</strong>到 Backup</li></ol><h2 id="4-2-慢速路径（Slow-Path）-2-RTT"><a href="#4-2-慢速路径（Slow-Path）-2-RTT" class="headerlink" title="4.2 慢速路径（Slow Path）- 2 RTT"></a>4.2 慢速路径（Slow Path）- 2 RTT</h2><p><strong>触发条件</strong>：</p><ul><li>Witness 拒绝（可交换性冲突或空间不足）</li><li>Master 发现冲突（操作与未同步操作不可交换）</li></ul><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">时间线:</span><br><span class="line">─────────────────────────────────────────────────→</span><br><span class="line"></span><br><span class="line">Client                    Master                   Backup</span><br><span class="line">  │                         │                         │</span><br><span class="line">  │──── update request ────→│                         │</span><br><span class="line">  │                         │                         │</span><br><span class="line">  │←───── CONFLICT ─────────│                         │</span><br><span class="line">  │                         │                         │</span><br><span class="line">  │────── sync request ────→│                         │</span><br><span class="line">  │                         │──── sync ──────────────→│</span><br><span class="line">  │                         │←─── ack ───────────────│</span><br><span class="line">  │                         │                         │</span><br><span class="line">  │←───── synced response ──│                         │</span><br><span class="line">  │                         │                         │</span><br><span class="line">  │  ✓ 操作完成             │                         │</span><br></pre></td></tr></table></figure><hr><h1 id="5-Witness-详解"><a href="#5-Witness-详解" class="headerlink" title="5 Witness 详解"></a>5 Witness 详解</h1><h2 id="5-1-Witness-的职责"><a href="#5-1-Witness-的职责" class="headerlink" title="5.1 Witness 的职责"></a>5.1 Witness 的职责</h2><p>Witness 是 <strong>轻量级的临时持久化存储</strong>：</p><table><thead><tr><th>操作</th><th>说明</th></tr></thead><tbody><tr><td><strong>record</strong></td><td>接收并存储客户端请求（检查可交换性）</td></tr><tr><td><strong>gc</strong></td><td>清理已同步到 Backup 的请求</td></tr><tr><td><strong>getRecoveryData</strong></td><td>恢复时返回所有存储的请求</td></tr></tbody></table><h2 id="5-2-可交换性检查"><a href="#5-2-可交换性检查" class="headerlink" title="5.2 可交换性检查"></a>5.2 可交换性检查</h2><p><strong>Witness 只接受与已存储操作可交换的新操作</strong>。</p><p>对于 KV 存储：</p><ul><li>操作不同 key → 可交换 ✓</li><li>操作相同 key → 不可交换 ✗</li></ul><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 示例：Witness 检查逻辑</span></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">Witness::canAccept</span><span class="params">(<span class="type">const</span> Request&amp; new_req)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">const</span> <span class="keyword">auto</span>&amp; saved : saved_requests) &#123;</span><br><span class="line">        <span class="keyword">if</span> (!<span class="built_in">isCommutative</span>(new_req, saved)) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">false</span>;  <span class="comment">// 拒绝</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;  <span class="comment">// 接受</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">isCommutative</span><span class="params">(<span class="type">const</span> Request&amp; a, <span class="type">const</span> Request&amp; b)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 不同 key 的写操作可交换</span></span><br><span class="line">    <span class="keyword">return</span> a.keys != b.keys;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h1 id="6-Master-详解"><a href="#6-Master-详解" class="headerlink" title="6 Master 详解"></a>6 Master 详解</h1><h2 id="6-1-推测执行（Speculative-Execution）"><a href="#6-1-推测执行（Speculative-Execution）" class="headerlink" title="6.1 推测执行（Speculative Execution）"></a>6.1 推测执行（Speculative Execution）</h2><p>Master 可以 <strong>在同步 Backup 之前响应客户端</strong>。</p><p><strong>安全条件</strong>：</p><ul><li>该操作与所有未同步（unsynced）操作可交换</li><li>否则必须先 sync</li></ul><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">未同步操作队列（unsynced）:</span><br><span class="line">[op1, op2, op3, ..., op_n] ← 尾部是未同步的</span><br><span class="line"></span><br><span class="line">新操作 op_new:</span><br><span class="line">- 如果 op_new 与 [op1...op_n] 全部可交换 → 推测执行 ✓</span><br><span class="line">- 否则 → 先 sync，再执行</span><br></pre></td></tr></table></figure><h2 id="6-2-异步同步（Async-Sync）"><a href="#6-2-异步同步（Async-Sync）" class="headerlink" title="6.2 异步同步（Async Sync）"></a>6.2 异步同步（Async Sync）</h2><p>Master <strong>批量同步</strong>到 Backup，提高吞吐量：</p><ul><li>减少 RPC 数量</li><li>避免 CPU 空转等待</li></ul><hr><h1 id="7-恢复机制"><a href="#7-恢复机制" class="headerlink" title="7 恢复机制"></a>7 恢复机制</h1><h2 id="7-1-恢复流程"><a href="#7-1-恢复流程" class="headerlink" title="7.1 恢复流程"></a>7.1 恢复流程</h2><p>Master 崩溃后，新 Master 恢复：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">Phase 1: 从 Backup 恢复有序数据</span><br><span class="line">───────────────────────────────────</span><br><span class="line">New Master ← Backup (传统恢复机制)</span><br><span class="line"></span><br><span class="line">Phase 2: 从 Witness 重放无序请求</span><br><span class="line">───────────────────────────────────</span><br><span class="line">1. 选择一个可用 Witness</span><br><span class="line">2. 停止该 Witness 接收新请求</span><br><span class="line">3. 获取所有存储的请求</span><br><span class="line">4. 重放请求（任意顺序，因为可交换）</span><br><span class="line">5. 同步到 Backup</span><br><span class="line">6. 重置 Witness</span><br></pre></td></tr></table></figure><h2 id="7-2-防止重复执行"><a href="#7-2-防止重复执行" class="headerlink" title="7.2 防止重复执行"></a>7.2 防止重复执行</h2><p>问题：部分请求可能已同步到 Backup，但也在 Witness 中。</p><p>解决方案：<strong>RIFL（Exactly-Once 语义）</strong></p><ul><li>每个请求有唯一 RPC ID</li><li>检测并过滤已执行的请求</li></ul><hr><h1 id="8-与-Raft-的对比"><a href="#8-与-Raft-的对比" class="headerlink" title="8 与 Raft 的对比"></a>8 与 Raft 的对比</h1><table><thead><tr><th>特性</th><th>Raft</th><th>CURP</th></tr></thead><tbody><tr><td><strong>延迟</strong></td><td>2 RTT</td><td>1 RTT（快速路径）</td></tr><tr><td><strong>排序时机</strong></td><td>先排序</td><td>排序延迟&#x2F;异步</td></tr><tr><td><strong>持久性</strong></td><td>排序后持久化</td><td>排序前持久化</td></tr><tr><td><strong>可交换性</strong></td><td>不利用</td><td>核心依赖</td></tr><tr><td><strong>额外组件</strong></td><td>无</td><td>Witness</td></tr><tr><td><strong>适用场景</strong></td><td>通用</td><td>操作可判断可交换性</td></tr></tbody></table><h3 id="从-Raft-到-CURP-的演进思路"><a href="#从-Raft-到-CURP-的演进思路" class="headerlink" title="从 Raft 到 CURP 的演进思路"></a>从 Raft 到 CURP 的演进思路</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">Raft:</span><br><span class="line">Client → Leader → Log → Majority → Execute → Response</span><br><span class="line"></span><br><span class="line">CURP:</span><br><span class="line">Client → Master + Witness → Execute → Response</span><br><span class="line">       ↓</span><br><span class="line">       (异步) Master → Backup</span><br></pre></td></tr></table></figure><hr><h1 id="9-总结"><a href="#9-总结" class="headerlink" title="9 总结"></a>9 总结</h1><p>CURP 的关键洞察：</p><blockquote><p><strong>如果操作可交换，顺序不重要；如果顺序不重要，持久化可以提前完成。</strong></p></blockquote><p>这打破了传统复制协议中 “排序 → 持久化” 的耦合，实现了 <strong>1 RTT 的线性一致性写操作</strong>。</p><p>对于 KV 存储这类天然支持可交换性检查的系统，CURP 是理想的复制协议选择。</p><hr><h1 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h1><ul><li>论文 PDF: <code>doc/curp-nsdi19.pdf</code></li><li>论文文本: <code>doc/curp-nsdi19.txt</code></li><li>NSDI ‘19 视频: <a href="https://www.usenix.org/conference/nsdi19/presentation/park">https://www.usenix.org/conference/nsdi19/presentation/park</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;论文来源&lt;/strong&gt;: &lt;em&gt;Exploiting Commutativity For Practical Fast Replication&lt;/em&gt;&lt;br&gt;&lt;strong&gt;作者&lt;/strong&gt;: Seo Jin Park</summary>
      
    
    
    
    <category term="项目" scheme="http://example.com/categories/%E9%A1%B9%E7%9B%AE/"/>
    
    <category term="tiny-distributed-kv" scheme="http://example.com/categories/%E9%A1%B9%E7%9B%AE/tiny-distributed-kv/"/>
    
    
    <category term="C++" scheme="http://example.com/tags/C/"/>
    
    <category term="分布式系统" scheme="http://example.com/tags/%E5%88%86%E5%B8%83%E5%BC%8F%E7%B3%BB%E7%BB%9F/"/>
    
    <category term="共识协议" scheme="http://example.com/tags/%E5%85%B1%E8%AF%86%E5%8D%8F%E8%AE%AE/"/>
    
  </entry>
  
  <entry>
    <title>Git 小技巧1：修改已有的 Commit</title>
    <link href="http://example.com/2026/02/13/%E5%AE%9E%E7%94%A8%E5%B7%A5%E5%85%B7%E7%AC%94%E8%AE%B0/Git%20%E5%B0%8F%E6%8A%80%E5%B7%A71%EF%BC%9A%E4%BF%AE%E6%94%B9%E5%B7%B2%E6%9C%89%E7%9A%84%20Commit/"/>
    <id>http://example.com/2026/02/13/%E5%AE%9E%E7%94%A8%E5%B7%A5%E5%85%B7%E7%AC%94%E8%AE%B0/Git%20%E5%B0%8F%E6%8A%80%E5%B7%A71%EF%BC%9A%E4%BF%AE%E6%94%B9%E5%B7%B2%E6%9C%89%E7%9A%84%20Commit/</id>
    <published>2026-02-13T08:42:08.000Z</published>
    <updated>2026-05-28T07:37:46.665Z</updated>
    
    <content type="html"><![CDATA[<h1 id="0-背景"><a href="#0-背景" class="headerlink" title="0 背景"></a>0 背景</h1><p>在日常开发中，我们肯定遇到过这样的情况：</p><ul><li>提交信息写错了（比如拼写错误）；</li><li>提交信息太模糊（如 “fix bug”）；</li><li>想把多个零散提交合并成一个有意义的提交；</li><li>或者只是想让 Git 历史看起来更整洁？</li></ul><p>如果你不熟悉git, 可能会使用<code>reset</code>的方式撤销提交后再手动创建新的<code>commit</code>。。。单实际上， <strong>交互式 rebase（<code>git rebase -i</code>）</strong>。可以轻松修改、合并、删除甚至重排历史提交——包括已经提交但尚未推送到远程仓库的记录。</p><p>今天，我们就来重点介绍如何用 <code>git rebase - interactive</code> 来 <strong>修改已有 commit 的信息</strong>。</p><h1 id="1-基本原理"><a href="#1-基本原理" class="headerlink" title="1 基本原理"></a>1 基本原理</h1><p>Git 中的每个 commit 都包含作者、时间、父提交和提交信息（message）。一旦你执行了 <code>git commit</code>，这个 commit 就被“固化”了。</p><p>但如果你还没把这个 commit 推送到共享仓库（比如 GitHub、GitLab），那么你完全可以在本地“重写历史”——这正是 <code>git rebase -i</code> 的用武之地。</p><blockquote><p>⚠️ 注意：<strong>不要对已经推送到公共分支且他人可能基于其工作的 commit 执行 rebase</strong>，否则会引发协作混乱。</p></blockquote><h1 id="2-实际案例"><a href="#2-实际案例" class="headerlink" title="2 实际案例"></a>2 实际案例</h1><p>假设我现在有这样的提交：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">&gt; git <span class="built_in">log</span></span><br><span class="line">commit c9a40f6bb060fa5c1a7f07089c438d5fb3376fbd (HEAD -&gt; master, origin/master, origin/HEAD, bugfix)</span><br><span class="line">Author: Vanilla-Beauty &lt;807077266@qq.com&gt;</span><br><span class="line">Date:   Fri Feb 13 16:24:06 2026 +0800</span><br><span class="line"></span><br><span class="line">    fix: 修复部分环境下的编译报错</span><br><span class="line"></span><br><span class="line">commit ef06417fd0e807ee3815875ae92c3327765fbd0c</span><br><span class="line">Author: Vanilla-Beauty &lt;807077266@qq.com&gt;</span><br><span class="line">Date:   Fri Feb 13 16:28:37 2026 +0800</span><br><span class="line"></span><br><span class="line">    docs: update tools <span class="keyword">in</span> Readme</span><br><span class="line"></span><br><span class="line">commit b4449ed14e4323f87f22d8135a440a9ef601e03b</span><br><span class="line">Author: Vanilla-Beauty &lt;807077266@qq.com&gt;</span><br><span class="line">Date:   Sat Nov 22 19:30:41 2025 +0800</span><br><span class="line"></span><br><span class="line">    修复wal清理检查的bug</span><br></pre></td></tr></table></figure><p>这里倒数第三个提交不符合规范, 我们想在提交信息前加上 <code>feat:</code> 前缀使其符合开源规范, 我们可以通过如下几个步骤完成:</p><h2 id="2-1-启动交互式-rebase"><a href="#2-1-启动交互式-rebase" class="headerlink" title="2.1 启动交互式 rebase"></a>2.1 启动交互式 rebase</h2><p>先运行</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git rebase -i HEAD~3</span><br></pre></td></tr></table></figure><p>这里的 <code>HEAD~3</code> 表示从当前 HEAD 往前数 3 个提交（包括当前），也就是说你会看到最近的 3 个提交。如果你想修改的是倒数第 3 个，那至少要包含它。</p><p>执行后，Git 会打开默认编辑器（如 Vim、Nano），显示类似内容：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">pick c9a40f6 fix: 修复部分环境下的编译报错</span><br><span class="line">pick ef06417 docs: update tools in Readme</span><br><span class="line">pick b4449ed 修复wal清理检查的bug</span><br></pre></td></tr></table></figure><h2 id="2-2-将-pick-改为-reword"><a href="#2-2-将-pick-改为-reword" class="headerlink" title="2.2 将 pick 改为 reword"></a>2.2 将 <code>pick</code> 改为 <code>reword</code></h2><p>找到你想修改的那一行（比如第二行），把 <code>pick</code> 改成 <code>reword</code>（或简写 <code>r</code>）：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">pick c9a40f6 fix: 修复部分环境下的编译报错</span><br><span class="line">pick ef06417 docs: update tools in Readme</span><br><span class="line">r b4449ed 修复wal清理检查的bug</span><br></pre></td></tr></table></figure><p>保存并退出编辑器。</p><h2 id="2-3-输入新的提交信息"><a href="#2-3-输入新的提交信息" class="headerlink" title="2.3 输入新的提交信息"></a>2.3 输入新的提交信息</h2><p>Git 会立即弹出另一个编辑窗口，让你修改该 commit 的 message。这是我们就可以修改commit信息：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">feat: 修复wal清理检查的bug</span><br><span class="line"></span><br><span class="line">- Check email format</span><br><span class="line">- Add password strength hint</span><br></pre></td></tr></table></figure><p>修改完成后保存退出。如果一切顺利，Git 会自动完成 rebase。你的 commit 信息就更新成功了！</p><p>你可以用 <code>git log</code> 查看结果：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">commit f72d75d57a7f8443022d0cbf1fc45466e73c3407 (HEAD -&gt; master)</span><br><span class="line">Author: Vanilla-Beauty &lt;807077266@qq.com&gt;</span><br><span class="line">Date:   Fri Feb 13 16:24:06 2026 +0800</span><br><span class="line"></span><br><span class="line">    fix: 修复部分环境下的编译报错</span><br><span class="line"></span><br><span class="line">commit 642feafc6e958bb9aa3a57eedd04b01b566ac737</span><br><span class="line">Author: Vanilla-Beauty &lt;807077266@qq.com&gt;</span><br><span class="line">Date:   Fri Feb 13 16:28:37 2026 +0800</span><br><span class="line"></span><br><span class="line">    docs: update tools <span class="keyword">in</span> Readme</span><br><span class="line"></span><br><span class="line">commit 346f843b8806a0a2ad982d74b5c6f07039a3dad5</span><br><span class="line">Author: Vanilla-Beauty &lt;807077266@qq.com&gt;</span><br><span class="line">Date:   Sat Nov 22 19:30:41 2025 +0800</span><br><span class="line"></span><br><span class="line">    feat: 修复wal清理检查的bug</span><br></pre></td></tr></table></figure><p>你会发现那个 commit 的 hash 值变了（因为内容变了），但历史更清晰了。</p><h1 id="3-rebase-介绍"><a href="#3-rebase-介绍" class="headerlink" title="3 rebase 介绍"></a>3 rebase 介绍</h1><h2 id="3-1-基本原理"><a href="#3-1-基本原理" class="headerlink" title="3.1 基本原理"></a>3.1 基本原理</h2><p><code>git rebase</code> 是 Git 中一个用于<strong>整合分支历史</strong>的命令，它的主要作用是将一个分支上的提交“移动”或“重放”到另一个分支的顶端，从而让项目历史看起来更线性、整洁。假设你有如下历史：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">A---B---C   main</span><br><span class="line">     \</span><br><span class="line">      D---E   feature</span><br></pre></td></tr></table></figure><p>如果你在 <code>feature</code> 分支上执行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git rebase main</span><br></pre></td></tr></table></figure><p>Git 会把 <code>feature</code> 分支上自与 <code>main</code> 分叉以来的所有提交（即 D 和 E）“重新应用”到 <code>main</code> 的最新提交 C 之后，结果变成：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">A---B---C         main</span><br><span class="line">         \</span><br><span class="line">          D&#x27;---E&#x27;   feature</span><br></pre></td></tr></table></figure><p>注意：D’ 和 E’ 是<strong>新的提交对象</strong>（因为它们的父提交变了），虽然内容一样，但哈希值不同。</p><h2 id="3-2-rebase-vs-merge"><a href="#3-2-rebase-vs-merge" class="headerlink" title="3.2 rebase vs merge"></a>3.2 rebase vs merge</h2><table><thead><tr><th>对比项</th><th><code>git merge</code></th><th><code>git rebase</code></th></tr></thead><tbody><tr><td>历史结构</td><td>保留真实合并历史，可能有分叉</td><td>生成线性历史，无分叉</td></tr><tr><td>-commit 数量</td><td>可能增加一个“合并提交”</td><td>不产生额外合并提交</td></tr><tr><td>安全性</td><td>安全，适合共享分支</td><td><strong>不建议在已推送的公共分支上使用</strong>（会改写历史）</td></tr><tr><td>可读性</td><td>真实反映开发过程</td><td>更干净、易读</td></tr></tbody></table><h2 id="3-3-常见用法"><a href="#3-3-常见用法" class="headerlink" title="3.3 常见用法"></a>3.3 常见用法</h2><ol><li><p><strong>同步上游更新（推荐在本地分支使用）</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git checkout feature</span><br><span class="line">git rebase main</span><br></pre></td></tr></table></figure></li><li><p><strong>交互式 rebase（整理提交）</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git rebase -i HEAD~3</span><br></pre></td></tr></table></figure><p>可以 squash（合并）、edit（修改）、drop（删除）提交等。</p></li><li><p><strong>变基到远程分支</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git pull --rebase origin main</span><br></pre></td></tr></table></figure><p>相当于先 fetch 再 rebase，避免不必要的 merge commit。</p></li></ol><h3 id="4-注意点"><a href="#4-注意点" class="headerlink" title="4 注意点"></a>4 注意点</h3><blockquote><p><code>git rebase</code> 就是“把你的工作重新放到别人最新的工作后面”，让历史更干净，但要谨慎使用，尤其在多人协作时。</p></blockquote>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;0-背景&quot;&gt;&lt;a href=&quot;#0-背景&quot; class=&quot;headerlink&quot; title=&quot;0 背景&quot;&gt;&lt;/a&gt;0 背景&lt;/h1&gt;&lt;p&gt;在日常开发中，我们肯定遇到过这样的情况：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;提交信息写错了（比如拼写错误）；&lt;/li&gt;
&lt;li&gt;提交</summary>
      
    
    
    
    <category term="实用工具" scheme="http://example.com/categories/%E5%AE%9E%E7%94%A8%E5%B7%A5%E5%85%B7/"/>
    
    
    <category term="git" scheme="http://example.com/tags/git/"/>
    
  </entry>
  
  <entry>
    <title>Bear-全能的代码索引工具</title>
    <link href="http://example.com/2026/02/07/%E5%AE%9E%E7%94%A8%E5%B7%A5%E5%85%B7%E7%AC%94%E8%AE%B0/Bear-%E5%85%A8%E8%83%BD%E7%9A%84%E4%BB%A3%E7%A0%81%E7%B4%A2%E5%BC%95%E5%B7%A5%E5%85%B7/"/>
    <id>http://example.com/2026/02/07/%E5%AE%9E%E7%94%A8%E5%B7%A5%E5%85%B7%E7%AC%94%E8%AE%B0/Bear-%E5%85%A8%E8%83%BD%E7%9A%84%E4%BB%A3%E7%A0%81%E7%B4%A2%E5%BC%95%E5%B7%A5%E5%85%B7/</id>
    <published>2026-02-07T14:43:28.000Z</published>
    <updated>2026-05-28T07:37:46.665Z</updated>
    
    <content type="html"><![CDATA[<h1 id="0-序言-LSP"><a href="#0-序言-LSP" class="headerlink" title="0 序言: LSP"></a>0 序言: LSP</h1><p>现代 C&#x2F;C++ 开发中，代码补全、跳转定义等功能是提升开发效率的关键, 今年来的IDE 都支持 LSP 来实现这些功能，而 clangd 是目前最流行的 C&#x2F;C++ 语言服务器。</p><p>如果你使用过像 clangd、ccls 或 cquery 这样的语言服务器（LSP），你一定对<code>compile_commands.json</code>这个文件不陌生——它记录了每个源文件是如何被编译的（包括头文件路径、宏定义、编译选项等）, 例如：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">[</span></span><br><span class="line">  <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;directory&quot;</span><span class="punctuation">:</span> <span class="string">&quot;/home/user/myproject&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;gcc -I./include -DDEBUG -c src/main.c -o src/main.o&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;file&quot;</span><span class="punctuation">:</span> <span class="string">&quot;src/main.c&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">]</span></span><br></pre></td></tr></table></figure><p>通常, 现代化项目的编译脚本都会支持 <code>compile_commands.json</code> 文件的生成，但对于那些没有原生生成 <code>compile_commands.json</code> 能力的构建系统（比如 Makefile、CMake 未启用特定选项、或者手写的构建脚本），我们该怎么办?</p><p>这时，Bear 就派上用场了。</p><h1 id="1-Bear简介"><a href="#1-Bear简介" class="headerlink" title="1 Bear简介"></a>1 Bear简介</h1><p><code>Bear</code>（Build EAR）是一个命令行工具，通过拦截构建过程中对编译器（如 <code>gcc</code>、<code>clang</code>）的调用，记录完整的编译命令，并将其转换为标准的 <code>compile_commands.json</code> 文件。</p><p>具体来说, <code>Bear</code> 并不解析你的 <code>Makefile</code> 或构建脚本，而是利用 动态链接库预加载（<code>LD_PRELOAD</code>，在 Linux&#x2F;macOS 上）或 进程监控 技术，在你运行构建命令（如 make）时：</p><ol><li>劫持（intercept）所有子进程中对编译器（如 gcc、g++、clang 等）的调用；</li><li>记录每个源文件对应的完整命令行、工作目录和环境；</li><li>构建完成后，将这些信息整理成符合 <code>Clang Compilation Database</code> 格式的 <code>JSON</code> 文件——即 <code>compile_commands.json</code>。</li></ol><p>后面我们会结合具体的例子来说明他的原理。</p><p>Bear的安装参考仓库主页： <a href="https://github.com/rizsotto/Bear">https://github.com/rizsotto/Bear</a></p><h1 id="2-Bear-的使用"><a href="#2-Bear-的使用" class="headerlink" title="2 Bear 的使用"></a>2 Bear 的使用</h1><h2 id="2-1-基础使用"><a href="#2-1-基础使用" class="headerlink" title="2.1 基础使用"></a>2.1 基础使用</h2><p><code>Bear</code> 的使用非常简单, 只需要在你正常构建的脚本前加上<code>bear --</code>即可。例如, 对于一个使用 <code>Makefile</code> 的项目：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">bear -- make</span><br></pre></td></tr></table></figure><p>对这个命令来说, <code>Bear</code> 实际做了：</p><ul><li>启动 <code>make</code>；</li><li>在 <code>make</code> 调用命令时拦截并记录</li></ul><p>假设你的<code>make</code>执行时有<code>gcc -I./include -DDEBUG -c src/main.c -o src/main.o</code> 这个命令，<code>Bear</code> 会捕获到这个调用，并记录下来, 其对应<code>compile_commands.json</code> 中的条目如下：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;directory&quot;</span><span class="punctuation">:</span> <span class="string">&quot;/path/to/project&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;gcc -I./include -DDEBUG -c foo.c -o foo.o&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;file&quot;</span><span class="punctuation">:</span> <span class="string">&quot;foo.c&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>需要说明的是, <code>Bear</code> 并非仅仅用于<code>make</code>, 它基本上支持所有的构建系统:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">bear -- ninja</span><br><span class="line">bear -- cmake --build .</span><br><span class="line">bear -- ./build.sh</span><br><span class="line">bear -- ./build.py</span><br></pre></td></tr></table></figure><h2 id="2-2-高级选项"><a href="#2-2-高级选项" class="headerlink" title="2.2 高级选项"></a>2.2 高级选项</h2><ul><li>–output FILE：指定输出文件名（默认为 <code>compile_commands.json</code>）</li><li>–append：追加到已有的 <code>compile_commands.json</code></li><li>–verbose：显示详细日志</li></ul><p>实际效果：配合 clangd 使用</p><p>一旦生成了 <code>compile_commands.json</code>，只需将其放在项目根目录，<code>clangd</code> 就会自动读取。此时，在 <code>VS Code</code>（安装 <code>clangd</code> 插件）、Neovim（配置较为复杂…）等编辑器中，你将获得：</p><ul><li>精准的代码跳转（Go to Definition）</li><li>智能补全（包括宏和模板）</li><li>实时错误诊断</li><li>查找所有引用（Find References）</li></ul><p>这大大提升了阅读大型 C&#x2F;C++ 项目（如 Linux 内核、Postgresql 等）的体验。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;0-序言-LSP&quot;&gt;&lt;a href=&quot;#0-序言-LSP&quot; class=&quot;headerlink&quot; title=&quot;0 序言: LSP&quot;&gt;&lt;/a&gt;0 序言: LSP&lt;/h1&gt;&lt;p&gt;现代 C&amp;#x2F;C++ 开发中，代码补全、跳转定义等功能是提升开发效率的关键, 今年</summary>
      
    
    
    
    <category term="实用工具" scheme="http://example.com/categories/%E5%AE%9E%E7%94%A8%E5%B7%A5%E5%85%B7/"/>
    
    
    <category term="clangd" scheme="http://example.com/tags/clangd/"/>
    
    <category term="cpp" scheme="http://example.com/tags/cpp/"/>
    
  </entry>
  
  <entry>
    <title>C++ 模板约束进阶指南</title>
    <link href="http://example.com/2025/11/23/C++/C++%20%E6%A8%A1%E6%9D%BF%E7%BA%A6%E6%9D%9F%E8%BF%9B%E9%98%B6%E6%8C%87%E5%8D%97/"/>
    <id>http://example.com/2025/11/23/C++/C++%20%E6%A8%A1%E6%9D%BF%E7%BA%A6%E6%9D%9F%E8%BF%9B%E9%98%B6%E6%8C%87%E5%8D%97/</id>
    <published>2025-11-23T01:10:54.000Z</published>
    <updated>2026-05-28T07:37:46.658Z</updated>
    
    <content type="html"><![CDATA[<p>在现代 C++（尤其是 C++20 引入 Concepts 之后）中，模板约束（Template Constraints）已成为编写安全、清晰且高效泛型代码的重要工具。本文将系统梳理 C++ 模板约束的核心知识点，帮助你深入理解 <code>concept</code>、<code>requires</code> 表达式、约束优先级以及标准库中的常用工具变量模板。</p><h1 id="1-Concept-的约束表达式：原子约束、合取式与析取式"><a href="#1-Concept-的约束表达式：原子约束、合取式与析取式" class="headerlink" title="1. Concept 的约束表达式：原子约束、合取式与析取式"></a>1. Concept 的约束表达式：原子约束、合取式与析取式</h1><p>C++20 中引入的 <strong>Concepts</strong> 允许我们对模板参数施加编译期约束，从而提升错误信息可读性并实现更精准的重载解析。一个 <code>concept</code> 的定义本质上是一个 <strong>布尔常量表达式</strong>，其内部可以包含以下三种基本形式：</p><h2 id="1-1-原子约束（Atomic-Constraint）"><a href="#1-1-原子约束（Atomic-Constraint）" class="headerlink" title="1.1 原子约束（Atomic Constraint）"></a>1.1 原子约束（Atomic Constraint）</h2><p>原子约束是最基本的约束单元，通常是一个 <strong>类型谓词（type trait）</strong> 或 <strong>requires 表达式</strong>，其结果为 <code>true</code> 或 <code>false</code>。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">concept</span> Integral = std::is_integral_v&lt;T&gt;;</span><br></pre></td></tr></table></figure><p>这里的 <code>std::is_integral_v&lt;T&gt;</code> 就是一个原子约束。</p><h2 id="1-2-合取式（Conjunction）"><a href="#1-2-合取式（Conjunction）" class="headerlink" title="1.2 合取式（Conjunction）"></a>1.2 合取式（Conjunction）</h2><p>多个约束通过逻辑 <strong>与（&amp;&amp;）</strong> 连接，表示所有条件必须同时满足。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">concept</span> SignedIntegral = Integral&lt;T&gt; &amp;&amp; std::is_signed_v&lt;T&gt;;</span><br></pre></td></tr></table></figure><p>只有当 <code>T</code> 既是整数类型又是有符号类型时，该 concept 才成立。</p><h2 id="1-3-析取式（Disjunction）"><a href="#1-3-析取式（Disjunction）" class="headerlink" title="1.3 析取式（Disjunction）"></a>1.3 析取式（Disjunction）</h2><p>多个约束通过逻辑 <strong>或（||）</strong> 连接，表示满足任一条件即可。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">concept</span> Number = Integral&lt;T&gt; || std::is_floating_point_v&lt;T&gt;;</span><br></pre></td></tr></table></figure><p><code>Number</code> 对整数或浮点类型都成立。</p><blockquote><p>⚠️ 注意：C++ 标准规定，<strong>合取式的优先级高于析取式</strong>，类似于逻辑运算符的常规优先级。但为了代码清晰，建议使用括号显式分组。</p></blockquote><h1 id="2-requires-表达式（Requires-Expression）"><a href="#2-requires-表达式（Requires-Expression）" class="headerlink" title="2 requires 表达式（Requires Expression）"></a>2 <code>requires</code> 表达式（Requires Expression）</h1><p>这是一种特殊的布尔表达式，用于检查一组操作是否对给定类型合法。其语法如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">requires</span> (参数列表) &#123; <span class="comment">// 表达式结果必须为 bool 类型</span></span><br><span class="line">    要求满足的表达式<span class="number">1</span>（称为“requirement”）</span><br><span class="line">    要求满足的表达式<span class="number">2</span></span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>常见 requirement 类型包括：</p><ul><li><strong>Simple requirement</strong>：直接写表达式，如 <code>x + y;</code>。这类要求只验证表达式是否能够合法编译，而不关心其执行结果或返回值类型。它主要用于确保某些操作符或函数调用对给定类型是有效的。</li><li><strong>Type requirement</strong>：<code>typename T::value_type;</code>。用于验证某个类型是否有效存在，比如检查模板参数是否具有特定的嵌套类型别名。</li><li><strong>Compound requirement</strong>：带返回类型约束，如 <code>&#123; x.size() &#125; -&gt; std::same_as&lt;std::size_t&gt;;</code>。不仅验证表达式是否合法，还进一步约束其返回值类型。</li><li><strong>Nested requirement</strong>：嵌套的 <code>requires</code> 表达式。允许在 requirements 中使用更复杂的编译期布尔表达式。</li></ul><h2 id="2-1-Simple-Requirements"><a href="#2-1-Simple-Requirements" class="headerlink" title="2.1 Simple Requirements"></a>2.1 Simple Requirements</h2><p><code>Simple requirements</code> 是最基本的约束形式，其唯一目的是验证表达式在给定类型上是否能够通过编译。需要注意的是，这些表达式实际上并不会被执行，它们仅仅用于编译期检查。</p><p>示例：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">concept</span> Hashable = <span class="built_in">requires</span>(T a) &#123;</span><br><span class="line">    &#123; std::hash&lt;T&gt;&#123;&#125;(a) &#125;; <span class="comment">// 确保可以使用 std::hash 计算 T 的哈希值</span></span><br><span class="line">    a + a;                 <span class="comment">// 确保支持 operator+</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 此处 a + a 并不会真正执行，只是验证这个表达式对类型 T 是有效的</span></span><br></pre></td></tr></table></figure><p>重要的是要理解，<code>simple requirement</code> 的结果值并不重要，也不会被捕获或使用。它的作用仅仅是让编译器验证相关表达式是否符合语法规则且能够成功完成名字查找和模板参数替换等编译过程。</p><h2 id="2-2-Type-Requirements"><a href="#2-2-Type-Requirements" class="headerlink" title="2.2 Type Requirements"></a>2.2 Type Requirements</h2><p><code>Type requirements</code> 用来检查某个类型是否存在。这对于验证模板参数是否具备某些嵌套类型非常有用。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">concept</span> Container = <span class="keyword">requires</span> &#123;</span><br><span class="line">    <span class="keyword">typename</span> T::value_type;     <span class="comment">// 要求 T 有一个 value_type 类型别名</span></span><br><span class="line">    <span class="keyword">typename</span> T::iterator;       <span class="comment">// 要求 T 有一个 iterator 类型别名</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h2 id="2-3-Compound-Requirements"><a href="#2-3-Compound-Requirements" class="headerlink" title="2.3 Compound Requirements"></a>2.3 Compound Requirements</h2><p><code>Compound requirements</code> 不仅验证表达式的合法性，还可以指定表达式的返回类型。这使得我们可以更加精确地约束接口行为。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">concept</span> Addable = <span class="built_in">requires</span>(T a, T b) &#123;</span><br><span class="line">    a + b;                          <span class="comment">// simple requirement</span></span><br><span class="line">    &#123; a += b &#125; -&gt; std::same_as&lt;T&amp;&gt;; <span class="comment">// compound requirement - 返回 T&amp;</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>在这个例子中，<code>&#123; a += b &#125; -&gt; std::same_as&lt;T&amp;&gt;</code> 不仅验证 <code>a += b</code> 是合法的，还要求该表达式返回 <code>T&amp;</code> 类型。</p><h2 id="2-4-Nested-requirement"><a href="#2-4-Nested-requirement" class="headerlink" title="2.4 Nested requirement"></a>2.4 Nested requirement</h2><p><code>Nested requirements</code> 是在 <code>requires</code> 表达式内部使用 <code>requires</code> 子句的形式，它允许我们在 <code>requirements</code> 中引入更复杂的编译期断言。这种形式特别适用于需要基于模板参数的编译期计算来建立约束的情况。</p><p><code>Nested requirements</code> 的基本语法是：<code>requires constant-expression</code>，其中 <code>constant-expression</code> 必须是一个可以在编译期求值的布尔表达式。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">print_twice</span><span class="params">(T x)</span> <span class="keyword">requires</span> Addable&lt;T&gt; </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; x + x &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>也可以直接内联写 <code>requires</code> 表达式：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(T x)</span> <span class="keyword">requires</span> <span class="title">requires</span><span class="params">(T t)</span> </span>&#123; t.<span class="built_in">begin</span>(); t.<span class="built_in">end</span>(); &#125; &#123;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><blockquote><p>💡 提示：虽然语法上允许 <code>requires requires</code>，但建议将复杂的 <code>requires</code> 表达式封装为命名 concept 以提高可读性。</p></blockquote><h1 id="3-多个模板约束之间的优先级判断"><a href="#3-多个模板约束之间的优先级判断" class="headerlink" title="3. 多个模板约束之间的优先级判断"></a>3. 多个模板约束之间的优先级判断</h1><p>当存在多个重载模板且都满足调用条件时，C++ 编译器会根据 <strong>约束的严格程度（constraint subsumption）</strong> 来选择最匹配的一个。</p><h2 id="3-1-约束蕴含（Subsumption）"><a href="#3-1-约束蕴含（Subsumption）" class="headerlink" title="3.1 约束蕴含（Subsumption）"></a>3.1 约束蕴含（Subsumption）</h2><p>如果约束 A <strong>逻辑上蕴含</strong> 约束 B（即 A 成立 ⇒ B 成立），则称 A <strong>比 B 更严格</strong>，优先选择 A。</p><p>例如：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">concept</span> Animal = <span class="literal">true</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">concept</span> Mammal = Animal&lt;T&gt; &amp;&amp; <span class="comment">/* 其他条件 */</span>;</span><br></pre></td></tr></table></figure><p>那么 <code>Mammal</code> 蕴含 <code>Animal</code>，因此对于同时满足两者的类型，编译器会选择 <code>Mammal</code> 版本。</p><h2 id="3-2-实际规则"><a href="#3-2-实际规则" class="headerlink" title="3.2 实际规则"></a>3.2 实际规则</h2><ul><li>编译器会尝试找出 <strong>最特殊化（most constrained）</strong> 的候选。</li><li>如果两个约束互不蕴含（即无法比较严格程度），则产生二义性错误。</li></ul><p>示例：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">f</span><span class="params">(T)</span> <span class="keyword">requires</span> Integral&lt;T&gt; </span>&#123; std::cout &lt;&lt; <span class="string">&quot;Integral\n&quot;</span>; &#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">f</span><span class="params">(T)</span> <span class="keyword">requires</span> Number&lt;T&gt; </span>&#123; std::cout &lt;&lt; <span class="string">&quot;Number\n&quot;</span>; &#125;</span><br></pre></td></tr></table></figure><p>调用 <code>f(42)</code> 时，因为 <code>Integral&lt;int&gt;</code> 蕴含于 <code>Number&lt;int&gt;</code>（<code>Integral ⇒ Number</code>），所以选择第一个版本。</p><blockquote><p>🔍 注意：蕴含关系仅在 <strong>同一表达式结构下</strong> 才能自动推导。复杂逻辑（如涉及 <code>||</code>）可能无法被正确识别，此时需谨慎设计 concept 层次。</p></blockquote><h2 id="4-C-标准库中的常见类型特征（Type-Traits）与变量模板"><a href="#4-C-标准库中的常见类型特征（Type-Traits）与变量模板" class="headerlink" title="4. C++ 标准库中的常见类型特征（Type Traits）与变量模板"></a>4. C++ 标准库中的常见类型特征（Type Traits）与变量模板</h2><p>C++ 标准库在 <code>&lt;type_traits&gt;</code> 头文件中提供了大量用于编译期类型查询和变换的 <strong>类型特征类模板（trait class templates）</strong>。从 C++17 开始，为每个返回布尔值的 trait 类模板都配套引入了同名的 <strong>变量模板（variable template）</strong>，后缀为 <code>_v</code>（针对 <code>value</code> 成员）或 <code>_t</code>（针对 <code>type</code> 成员）。</p><p>这些工具是编写 <strong>约束（concepts）</strong>、<strong>SFINAE</strong> 和 <strong>泛型代码</strong> 的基础。</p><h3 id="4-1-常见的布尔型类型特征（及其-v-变量模板）"><a href="#4-1-常见的布尔型类型特征（及其-v-变量模板）" class="headerlink" title="4.1 常见的布尔型类型特征（及其 _v 变量模板）"></a>4.1 常见的布尔型类型特征（及其 <code>_v</code> 变量模板）</h3><table><thead><tr><th>原始形式类模板（C++11 起）</th><th>对应的变量模板（C++17 起）</th><th>功能说明</th></tr></thead><tbody><tr><td><code>std::is_void&lt;T&gt;</code></td><td><code>std::is_void_v&lt;T&gt;</code></td><td>是否为 <code>void</code></td></tr><tr><td><code>std::is_integral&lt;T&gt;</code></td><td><code>std::is_integral_v&lt;T&gt;</code></td><td>是否为整数类型（如 <code>int</code>, <code>char</code>, <code>bool</code>）</td></tr><tr><td><code>std::is_floating_point&lt;T&gt;</code></td><td><code>std::is_floating_point_v&lt;T&gt;</code></td><td>是否为浮点类型（<code>float</code>, <code>double</code> 等）</td></tr><tr><td><code>std::is_arithmetic&lt;T&gt;</code></td><td><code>std::is_arithmetic_v&lt;T&gt;</code></td><td>是否为算术类型（整数或浮点）</td></tr><tr><td><code>std::is_pointer&lt;T&gt;</code></td><td><code>std::is_pointer_v&lt;T&gt;</code></td><td>是否为指针类型</td></tr><tr><td><code>std::is_reference&lt;T&gt;</code></td><td><code>std::is_reference_v&lt;T&gt;</code></td><td>是否为引用类型</td></tr><tr><td><code>std::is_lvalue_reference&lt;T&gt;</code></td><td><code>std::is_lvalue_reference_v&lt;T&gt;</code></td><td>是否为左值引用</td></tr><tr><td><code>std::is_rvalue_reference&lt;T&gt;</code></td><td><code>std::is_rvalue_reference_v&lt;T&gt;</code></td><td>是否为右值引用</td></tr><tr><td><code>std::is_const&lt;T&gt;</code></td><td><code>std::is_const_v&lt;T&gt;</code></td><td>是否为 const 限定类型</td></tr><tr><td><code>std::is_volatile&lt;T&gt;</code></td><td><code>std::is_volatile_v&lt;T&gt;</code></td><td>是否为 volatile 限定类型</td></tr><tr><td><code>std::is_trivial&lt;T&gt;</code></td><td><code>std::is_trivial_v&lt;T&gt;</code></td><td>是否为平凡类型</td></tr><tr><td><code>std::is_standard_layout&lt;T&gt;</code></td><td><code>std::is_standard_layout_v&lt;T&gt;</code></td><td>是否为标准布局类型</td></tr><tr><td><code>std::is_pod&lt;T&gt;</code></td><td><code>std::is_pod_v&lt;T&gt;</code></td><td>（C++20 起弃用）是否为 POD 类型</td></tr><tr><td><code>std::is_empty&lt;T&gt;</code></td><td><code>std::is_empty_v&lt;T&gt;</code></td><td>是否为空类（无非静态成员）</td></tr><tr><td><code>std::is_polymorphic&lt;T&gt;</code></td><td><code>std::is_polymorphic_v&lt;T&gt;</code></td><td>是否包含虚函数（多态类型）</td></tr><tr><td><code>std::has_virtual_destructor&lt;T&gt;</code></td><td><code>std::has_virtual_destructor_v&lt;T&gt;</code></td><td>是否有虚析构函数</td></tr><tr><td><code>std::is_abstract&lt;T&gt;</code></td><td><code>std::is_abstract_v&lt;T&gt;</code></td><td>是否为抽象类</td></tr><tr><td><code>std::is_final&lt;T&gt;</code></td><td><code>std::is_final_v&lt;T&gt;</code></td><td>是否用 <code>final</code> 修饰（C++14 起）</td></tr><tr><td><code>std::is_signed&lt;T&gt;</code></td><td><code>std::is_signed_v&lt;T&gt;</code></td><td>是否为有符号算术类型</td></tr><tr><td><code>std::is_unsigned&lt;T&gt;</code></td><td><code>std::is_unsigned_v&lt;T&gt;</code></td><td>是否为无符号算术类型</td></tr></tbody></table><p>原始形式（如 <code>std::is_integral&lt;T&gt;</code>）是一个 类模板，其内部有一个静态常量成员 <code>value</code>（类型为 bool）。<br><code>_v</code> 形式（如 <code>std::is_integral_v&lt;T&gt;</code>）是 C++17 引入的 变量模板，等价于 <code>std::is_integral&lt;T&gt;::value</code>。</p><p>🔧 示例：使用传统形式 vs 变量模板形式</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;type_traits&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用原始形式（繁琐）</span></span><br><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">check_integral_old</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (std::is_integral&lt;T&gt;::value) &#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Type is integral (old style)\n&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用 _v 变量模板（简洁）</span></span><br><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">check_integral_new</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (std::is_integral_v&lt;T&gt;) &#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Type is integral (new style)\n&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">check_integral_old</span>&lt;<span class="type">int</span>&gt;();   <span class="comment">// 输出 &quot;Type is integral (old style)&quot;</span></span><br><span class="line">    <span class="built_in">check_integral_new</span>&lt;<span class="type">int</span>&gt;();   <span class="comment">// 输出 &quot;Type is integral (new style)&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>💡 几乎所有现代 C++ 泛型代码都应优先使用 _v 形式，特别是在 concept 定义中。</p><h3 id="4-2-关系型类型特征"><a href="#4-2-关系型类型特征" class="headerlink" title="4.2 关系型类型特征"></a>4.2 关系型类型特征</h3><table><thead><tr><th>原始形式类模板</th><th>变量模板</th><th>说明</th></tr></thead><tbody><tr><td><code>std::is_same&lt;T, U&gt;</code></td><td><code>std::is_same_v&lt;T, U&gt;</code></td><td><code>T</code> 与 <code>U</code> 是否为同一类型</td></tr><tr><td><code>std::is_base_of&lt;Base, Derived&gt;</code></td><td><code>std::is_base_of_v&lt;Base, Derived&gt;</code></td><td><code>Base</code> 是否是 <code>Derived</code> 的基类（含相同类型）</td></tr><tr><td><code>std::is_convertible&lt;From, To&gt;</code></td><td><code>std::is_convertible_v&lt;From, To&gt;</code></td><td><code>From</code> 是否可隐式转换为 <code>To</code></td></tr><tr><td><code>std::is_assignable&lt;T, U&gt;</code></td><td><code>std::is_assignable_v&lt;T, U&gt;</code></td><td>表达式 <code>declval&lt;T&gt;() = declval&lt;U&gt;()</code> 是否合法</td></tr><tr><td><code>std::is_constructible&lt;T, Args...&gt;</code></td><td><code>std::is_constructible_v&lt;T, Args...&gt;</code></td><td><code>T</code> 是否可用 <code>Args...</code> 构造</td></tr><tr><td><code>std::is_default_constructible&lt;T&gt;</code></td><td><code>std::is_default_constructible_v&lt;T&gt;</code></td><td>是否可默认构造</td></tr><tr><td><code>std::is_copy_constructible&lt;T&gt;</code></td><td><code>std::is_copy_constructible_v&lt;T&gt;</code></td><td>是否可拷贝构造</td></tr><tr><td><code>std::is_move_constructible&lt;T&gt;</code></td><td><code>std::is_move_constructible_v&lt;T&gt;</code></td><td>是否可移动构造</td></tr><tr><td><code>std::is_copy_assignable&lt;T&gt;</code></td><td><code>std::is_copy_assignable_v&lt;T&gt;</code></td><td>是否可拷贝赋值</td></tr><tr><td><code>std::is_move_assignable&lt;T&gt;</code></td><td><code>std::is_move_assignable_v&lt;T&gt;</code></td><td>是否可移动赋值</td></tr><tr><td><code>std::is_destructible&lt;T&gt;</code></td><td><code>std::is_destructible_v&lt;T&gt;</code></td><td>是否可析构</td></tr></tbody></table><blockquote><p>✅ 注意：<code>is_base_of_v&lt;Base, Derived&gt;</code> 中，若 <code>Base == Derived</code>，结果也为 <code>true</code>。</p></blockquote><p>原始形式（如 <code>std::is_same&lt;T, U&gt;</code>）是一个 类模板，其内部有一个静态常量成员 <code>value</code>（类型为 <code>bool</code>）。 _v 形式（如 <code>std::is_same_v&lt;T, U&gt;</code>）是 C++17 引入的 变量模板，等价于 <code>std::is_same&lt;T, U&gt;::value</code>。</p><p>🔧 示例：在模板约束中使用关系型类型特征</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;type_traits&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用原始形式（繁琐）</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T, <span class="keyword">typename</span> U&gt;</span><br><span class="line"><span class="keyword">typename</span> std::enable_if&lt;std::is_same&lt;T, U&gt;::value, <span class="type">void</span>&gt;::<span class="function">type</span></span><br><span class="line"><span class="function"><span class="title">print_if_same_old</span><span class="params">(<span class="type">const</span> T&amp; t, <span class="type">const</span> U&amp; u)</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;Same types: &quot;</span> &lt;&lt; t &lt;&lt; <span class="string">&quot; and &quot;</span> &lt;&lt; u &lt;&lt; <span class="string">&quot;\n&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用 _v 变量模板（简洁）</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T, <span class="keyword">typename</span> U&gt;</span><br><span class="line">std::<span class="type">enable_if_t</span>&lt;std::is_same_v&lt;T, U&gt;&gt;</span><br><span class="line"><span class="built_in">print_if_same_new</span>(<span class="type">const</span> T&amp; t, <span class="type">const</span> U&amp; u) &#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;Same types: &quot;</span> &lt;&lt; t &lt;&lt; <span class="string">&quot; and &quot;</span> &lt;&lt; u &lt;&lt; <span class="string">&quot;\n&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 在 concept 中使用</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">concept</span> Integral = std::is_integral_v&lt;T&gt;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T, <span class="keyword">typename</span> U&gt;</span><br><span class="line"><span class="keyword">concept</span> SameType = std::is_same_v&lt;T, U&gt;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">template</span>&lt;Integral T, Integral U&gt;</span></span><br><span class="line"><span class="function"><span class="keyword">requires</span> SameType&lt;T, U&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">process_integers</span><span class="params">(<span class="type">const</span> T&amp; t, <span class="type">const</span> U&amp; u)</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;Processing same integer types: &quot;</span> &lt;&lt; t &lt;&lt; <span class="string">&quot; and &quot;</span> &lt;&lt; u &lt;&lt; <span class="string">&quot;\n&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">print_if_same_old</span>(<span class="number">1</span>, <span class="number">2</span>);        <span class="comment">// 输出 &quot;Same types: 1 and 2&quot;</span></span><br><span class="line">    <span class="built_in">print_if_same_new</span>(<span class="number">1</span>, <span class="number">2</span>);        <span class="comment">// 输出 &quot;Same types: 1 and 2&quot;</span></span><br><span class="line">    <span class="built_in">process_integers</span>(<span class="number">5</span>, <span class="number">10</span>);        <span class="comment">// 输出 &quot;Processing same integer types: 5 and 10&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="4-3-类型变换（Transformation-Traits）与-t-别名模板"><a href="#4-3-类型变换（Transformation-Traits）与-t-别名模板" class="headerlink" title="4.3 类型变换（Transformation Traits）与 _t 别名模板"></a>4.3 类型变换（Transformation Traits）与 <code>_t</code> 别名模板</h3><p>除了布尔判断，标准库还提供类型变换工具，通常配合 <code>_t</code> 使用：</p><table><thead><tr><th>类模板</th><th>别名模板（C++14 起）</th><th>功能</th></tr></thead><tbody><tr><td><code>std::remove_cv&lt;T&gt;</code></td><td><code>std::remove_cv_t&lt;T&gt;</code></td><td>移除 const&#x2F;volatile</td></tr><tr><td><code>std::remove_reference&lt;T&gt;</code></td><td><code>std::remove_reference_t&lt;T&gt;</code></td><td>移除引用</td></tr><tr><td><code>std::decay&lt;T&gt;</code></td><td><code>std::decay_t&lt;T&gt;</code></td><td>应用退化规则（如数组转指针，函数转函数指针）</td></tr><tr><td><code>std::add_const&lt;T&gt;</code></td><td><code>std::add_const_t&lt;T&gt;</code></td><td>添加 const</td></tr><tr><td><code>std::make_signed&lt;T&gt;</code></td><td><code>std::make_signed_t&lt;T&gt;</code></td><td>转为对应的有符号类型</td></tr><tr><td><code>std::underlying_type&lt;T&gt;</code></td><td><code>std::underlying_type_t&lt;T&gt;</code></td><td>获取枚举的底层类型</td></tr><tr><td><code>std::enable_if&lt;B, T&gt;</code></td><td><code>std::enable_if_t&lt;B, T&gt;</code></td><td>SFINAE 控制重载（当 <code>B</code> 为 true 时类型为 <code>T</code>）</td></tr></tbody></table><p>原始形式（如 <code>std::remove_reference&lt;T&gt;</code>）是一个 类模板，其内部定义了一个 <code>type</code> 成员类型（如 using type &#x3D; …;）。<br><code>_t</code> 形式（如 <code>std::remove_reference_t&lt;T&gt;</code>）是 C++14 引入的 类型别名模板，等价于 <code>typename std::remove_reference&lt;T&gt;::type</code>。</p><p>🔧 示例：实现通用的值传递函数（去除引用）</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">编辑</span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;type_traits&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用原始形式（繁琐）</span></span><br><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo_old</span><span class="params">(T&amp;&amp; arg)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">using</span> Decayed = <span class="keyword">typename</span> std::decay&lt;<span class="keyword">typename</span> std::remove_reference&lt;T&gt;::type&gt;::type;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;Old style: &quot;</span> &lt;&lt; <span class="built_in">typeid</span>(Decayed).<span class="built_in">name</span>() &lt;&lt; <span class="string">&quot;\n&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用 _t 别名模板（简洁）</span></span><br><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo_new</span><span class="params">(T&amp;&amp; arg)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">using</span> CleanType = std::<span class="type">decay_t</span>&lt;std::<span class="type">remove_reference_t</span>&lt;T&gt;&gt;;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;New style: &quot;</span> &lt;&lt; <span class="built_in">typeid</span>(CleanType).<span class="built_in">name</span>() &lt;&lt; <span class="string">&quot;\n&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> x = <span class="number">42</span>;</span><br><span class="line">    <span class="built_in">foo_old</span>(x);   <span class="comment">// 输出 int</span></span><br><span class="line">    <span class="built_in">foo_new</span>(x);   <span class="comment">// 输出 int</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>💡 几乎所有现代 C++ 泛型代码都应优先使用 <code>_t</code> 形式。</p><h2 id="使用示例"><a href="#使用示例" class="headerlink" title="使用示例"></a>使用示例</h2><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T, <span class="keyword">typename</span> U&gt;</span><br><span class="line"><span class="keyword">concept</span> DerivedFrom = std::is_base_of_v&lt;T, U&gt;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> Base, <span class="keyword">typename</span> Derived&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">check_inheritance</span><span class="params">()</span> <span class="keyword">requires</span> DerivedFrom&lt;Base, Derived&gt; </span>&#123;</span><br><span class="line">    <span class="built_in">static_assert</span>(std::is_base_of_v&lt;Base, Derived&gt;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这些变量模板不仅简洁，而且在编译期求值，零运行时开销，是构建高质量 concept 的基石。</p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><ul><li><strong>Concepts</strong> 让模板约束变得清晰、安全、可组合。</li><li><strong>原子约束、合取、析取</strong> 构成了约束表达式的基本逻辑。</li><li><strong><code>requires</code> 表达式</strong> 可验证类型是否支持特定操作。</li><li><strong>约束优先级</strong> 由蕴含关系决定，影响重载解析。</li><li><strong>标准库变量模板</strong>（如 <code>is_base_of_v</code>）是编写 concept 的得力助手。</li></ul><p>掌握这些知识，你就能写出更具表达力、更易维护的泛型 C++ 代码。随着 C++20&#x2F;23 的普及，模板约束正成为现代 C++ 开发的必备技能。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;在现代 C++（尤其是 C++20 引入 Concepts 之后）中，模板约束（Template Constraints）已成为编写安全、清晰且高效泛型代码的重要工具。本文将系统梳理 C++ 模板约束的核心知识点，帮助你深入理解 &lt;code&gt;concept&lt;/code&gt;、&lt;</summary>
      
    
    
    
    <category term="C++随笔" scheme="http://example.com/categories/C-%E9%9A%8F%E7%AC%94/"/>
    
    
    <category term="C++" scheme="http://example.com/tags/C/"/>
    
    <category term="面经" scheme="http://example.com/tags/%E9%9D%A2%E7%BB%8F/"/>
    
    <category term="C++20" scheme="http://example.com/tags/C-20/"/>
    
  </entry>
  
  <entry>
    <title>C++ 中 typename 关键字的完整指南</title>
    <link href="http://example.com/2025/11/16/C++/C++%20%E4%B8%AD%20typename%20%E5%85%B3%E9%94%AE%E5%AD%97%E7%9A%84%E5%AE%8C%E6%95%B4%E6%8C%87%E5%8D%97/"/>
    <id>http://example.com/2025/11/16/C++/C++%20%E4%B8%AD%20typename%20%E5%85%B3%E9%94%AE%E5%AD%97%E7%9A%84%E5%AE%8C%E6%95%B4%E6%8C%87%E5%8D%97/</id>
    <published>2025-11-16T09:22:27.000Z</published>
    <updated>2026-05-28T07:37:46.658Z</updated>
    
    <content type="html"><![CDATA[<p>在 C++ 模板编程中，<code>typename</code> 是一个看似简单却极易被误解的关键字。它不仅用于声明模板类型参数，更在处理嵌套依赖类型时扮演着“编译器翻译官”的角色。本文将系统梳理 <code>typename</code> 的所有用法、规则与陷阱，帮助你彻底掌握这一核心概念。</p><hr><h1 id="一、typename-的两种核心用途"><a href="#一、typename-的两种核心用途" class="headerlink" title="一、typename 的两种核心用途"></a>一、<code>typename</code> 的两种核心用途</h1><p><code>typename</code> 在 C++ 中主要有两个作用：</p><ol><li><strong>声明模板中的类型参数</strong>（常见）</li><li><strong>指明依赖名称是一个类型</strong>（关键但易错）</li></ol><p>下面分别展开说明。</p><h1 id="二、用法-1：声明模板类型参数"><a href="#二、用法-1：声明模板类型参数" class="headerlink" title="二、用法 1：声明模板类型参数"></a>二、用法 1：声明模板类型参数</h1><p>这是最直观的用法：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Container</span> &#123;</span><br><span class="line">    T data;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 等价于</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Container</span> &#123; <span class="comment">/* ... */</span> &#125;;</span><br></pre></td></tr></table></figure><ul><li><code>typename</code> 和 <code>class</code> 在此处完全等价。</li><li>但习惯上，当 <code>T</code> 可能是非类类型（如 <code>int</code>、<code>float</code>）时，更推荐使用 <code>typename</code>，语义更准确。</li></ul><blockquote><p>✅ 小贴士：虽然 <code>class</code> 也能用于基本类型，但 <code>typename</code> 表达了“任意类型”的含义，更具通用性。</p></blockquote><h1 id="三、用法-2：指明依赖名称是类型（重点！）"><a href="#三、用法-2：指明依赖名称是类型（重点！）" class="headerlink" title="三、用法 2：指明依赖名称是类型（重点！）"></a>三、用法 2：指明依赖名称是类型（重点！）</h1><p>这一用法是许多 C++ 程序员容易混淆的地方。理解它需要先了解“依赖名称”的概念。</p><h2 id="3-1-什么是“依赖名称”？"><a href="#3-1-什么是“依赖名称”？" class="headerlink" title="3.1 什么是“依赖名称”？"></a>3.1 什么是“依赖名称”？</h2><p>在模板中，如果一个名称<strong>依赖于模板参数</strong>，就称为 <strong>dependent name（依赖名称）</strong>。</p><p>例如：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    T::value_type x;  <span class="comment">// ← T::value_type 依赖于 T，是依赖名称</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="3-2-为什么需要-typename？"><a href="#3-2-为什么需要-typename？" class="headerlink" title="3.2 为什么需要 typename？"></a>3.2 为什么需要 <code>typename</code>？</h2><p>C++ 编译器在解析模板定义时，<strong>还不知道 <code>T</code> 具体是什么类型</strong>。因此，对于像 <code>T::value_type</code> 这样的表达式，编译器无法确定它是：</p><ul><li>一个<strong>嵌套类型</strong>（如 <code>std::vector&lt;int&gt;::value_type</code>）</li><li>还是一个<strong>静态成员变量&#x2F;常量</strong>（如 <code>MyClass::MAX_SIZE</code>）</li></ul><p>根据 C++ 标准，<strong>编译器默认假设依赖名称不是类型</strong>（即当作值处理）。</p><p>👉 所以，如果你明确知道它是类型，就必须用 <code>typename</code> 告诉编译器！</p><h2 id="3-3-正确写法示例"><a href="#3-3-正确写法示例" class="headerlink" title="3.3 正确写法示例"></a>3.3 正确写法示例</h2><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">print_value_type</span><span class="params">(<span class="type">const</span> T&amp; container)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 错误 ❌：缺少 typename</span></span><br><span class="line">    <span class="comment">// T::value_type val;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 正确 ✅：</span></span><br><span class="line">    <span class="keyword">typename</span> T::value_type val = *container.<span class="built_in">begin</span>();</span><br><span class="line">    std::cout &lt;&lt; val &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>调用：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">std::vector&lt;<span class="type">int</span>&gt; v = &#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>&#125;;</span><br><span class="line"><span class="built_in">print_value_type</span>(v);  <span class="comment">// T = std::vector&lt;int&gt;, T::value_type = int</span></span><br></pre></td></tr></table></figure><p>如果没有 <code>typename</code>，编译器会尝试将 <code>T::value_type</code> 解析为一个值，导致类似以下错误：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">error: expected &#x27;;&#x27; before &#x27;val&#x27;</span><br><span class="line">error: &#x27;T::value_type&#x27; is not a type</span><br></pre></td></tr></table></figure><hr><h1 id="四、何时必须使用-typename？"><a href="#四、何时必须使用-typename？" class="headerlink" title="四、何时必须使用 typename？"></a>四、何时必须使用 <code>typename</code>？</h1><p><strong>规则</strong>：  </p><blockquote><p>如果一个<strong>嵌套名称</strong>（形式为 <code>X::Y</code>）出现在模板中，且 <code>X</code> 依赖于模板参数，并且你<strong>将其用作类型</strong>，那么必须在前面加上 <code>typename</code>。</p></blockquote><h2 id="常见场景"><a href="#常见场景" class="headerlink" title="常见场景"></a>常见场景</h2><table><thead><tr><th>表达式</th><th>是否需要 <code>typename</code>？</th><th>说明</th></tr></thead><tbody><tr><td><code>T x;</code></td><td>❌</td><td><code>T</code> 本身就是类型参数</td></tr><tr><td><code>T::iterator it;</code></td><td>✅</td><td><code>iterator</code> 是嵌套依赖类型</td></tr><tr><td><code>std::vector&lt;T&gt;::size_type n;</code></td><td>✅</td><td>依赖 <code>T</code>，且 <code>size_type</code> 是类型</td></tr><tr><td><code>MyClass::static_val</code></td><td>❌</td><td><code>MyClass</code> 不依赖模板参数（非依赖名）</td></tr><tr><td><code>using U = T::type;</code></td><td>✅</td><td>类型别名，<code>T::type</code> 是依赖类型</td></tr></tbody></table><h2 id="实际工程例子"><a href="#实际工程例子" class="headerlink" title="实际工程例子"></a>实际工程例子</h2><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> Container&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">process</span><span class="params">(Container&amp; c)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">using</span> ValueType = <span class="keyword">typename</span> Container::value_type;        <span class="comment">// ✅</span></span><br><span class="line">    <span class="keyword">typename</span> Container::iterator it = c.<span class="built_in">begin</span>();            <span class="comment">// ✅</span></span><br><span class="line">    <span class="keyword">typename</span> std::allocator_traits&lt;</span><br><span class="line">        <span class="keyword">typename</span> Container::allocator_type                  <span class="comment">// ✅ 嵌套依赖</span></span><br><span class="line">    &gt;::pointer ptr = &amp;(*it);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h1 id="五、例外：禁止使用-typename-的地方"><a href="#五、例外：禁止使用-typename-的地方" class="headerlink" title="五、例外：禁止使用 typename 的地方"></a>五、例外：禁止使用 <code>typename</code> 的地方</h1><p>尽管大多数依赖类型前要加 <code>typename</code>，但在某些上下文中<strong>不允许使用</strong>，因为编译器已经知道那里必须是类型。</p><h2 id="5-1-基类声明（Base-Class-List）"><a href="#5-1-基类声明（Base-Class-List）" class="headerlink" title="5.1 基类声明（Base Class List）"></a>5.1 基类声明（Base Class List）</h2><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Derived</span> : <span class="keyword">public</span> T::nested_type &#123;  <span class="comment">// ✅ 正确</span></span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// ❌ 错误：不能写成</span></span><br><span class="line"><span class="comment">// class Derived : public typename T::nested_type</span></span><br></pre></td></tr></table></figure><blockquote><p>原因：继承列表中只能是类型，所以 C++ 隐含了 <code>typename</code>。</p></blockquote><h2 id="5-2-成员初始化列表中的基类构造"><a href="#5-2-成员初始化列表中的基类构造" class="headerlink" title="5.2 成员初始化列表中的基类构造"></a>5.2 成员初始化列表中的基类构造</h2><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">D</span> : Base&lt;T&gt; &#123;</span><br><span class="line">    <span class="built_in">D</span>() : Base&lt;T&gt;::<span class="built_in">Base</span>() &#123;&#125;  <span class="comment">// 这里不加 typename</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h2 id="5-3-模板实参（某些情况）"><a href="#5-3-模板实参（某些情况）" class="headerlink" title="5.3 模板实参（某些情况）"></a>5.3 模板实参（某些情况）</h2><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">template</span>&lt;<span class="keyword">typename</span>&gt; <span class="keyword">class</span> <span class="title class_">C</span>, <span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    C&lt;T&gt; obj;  <span class="comment">// C 是模板模板参数，T 是类型，无需 typename</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h1 id="六、常见误区与陷阱"><a href="#六、常见误区与陷阱" class="headerlink" title="六、常见误区与陷阱"></a>六、常见误区与陷阱</h1><h2 id="误区-1：认为-typename-是“定义新类型”"><a href="#误区-1：认为-typename-是“定义新类型”" class="headerlink" title="误区 1：认为 typename 是“定义新类型”"></a>误区 1：认为 <code>typename</code> 是“定义新类型”</h2><p>❌ 错误理解：  </p><blockquote><p>“<code>typename T::value_type</code> 是在定义一个叫 <code>value_type</code> 的新类型。”</p></blockquote><p>✅ 正确理解：  </p><blockquote><p>它只是<strong>告诉编译器</strong> <code>T::value_type</code> <strong>已经是一个类型名</strong>，请按类型解析。</p></blockquote><hr><h2 id="误区-2：在非模板代码中滥用-typename"><a href="#误区-2：在非模板代码中滥用-typename" class="headerlink" title="误区 2：在非模板代码中滥用 typename"></a>误区 2：在非模板代码中滥用 <code>typename</code></h2><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">MyList</span> &#123;</span><br><span class="line">    <span class="keyword">using</span> value_type = <span class="type">int</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">test</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">// ❌ 不需要 typename，因为不在模板中，且 MyList 已知</span></span><br><span class="line">    <span class="keyword">typename</span> MyList::value_type x;  <span class="comment">// 虽然能编译，但冗余！</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// ✅ 直接写：</span></span><br><span class="line">    MyList::value_type x;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><blockquote><p>只有在<strong>模板中 + 依赖名称</strong>时才需要 <code>typename</code>。</p></blockquote><hr><h2 id="误区-3：混淆-typename-和-class-在模板参数中的区别"><a href="#误区-3：混淆-typename-和-class-在模板参数中的区别" class="headerlink" title="误区 3：混淆 typename 和 class 在模板参数中的区别"></a>误区 3：混淆 <code>typename</code> 和 <code>class</code> 在模板参数中的区别</h2><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;   <span class="comment">// OK</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt; <span class="comment">// 也 OK，且更推荐</span></span><br></pre></td></tr></table></figure><p>两者在声明类型参数时完全等价。但注意：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">template</span>&lt;<span class="keyword">typename</span>&gt; <span class="keyword">class</span> <span class="title class_">C</span>&gt;  <span class="comment">// 必须用 class（C++17 前）</span></span><br><span class="line"><span class="comment">// C++17 起允许写 template&lt;template&lt;typename&gt; typename C&gt;</span></span><br></pre></td></tr></table></figure><hr><h1 id="七、现代-C-中的变化（C-17-）"><a href="#七、现代-C-中的变化（C-17-）" class="headerlink" title="七、现代 C++ 中的变化（C++17+）"></a>七、现代 C++ 中的变化（C++17+）</h1><p>C++17 放宽了部分限制：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// C++17 起合法</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">template</span>&lt;<span class="keyword">typename</span>&gt; <span class="keyword">typename</span> Container&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyWrapper</span>;</span><br></pre></td></tr></table></figure><p>但 <code>typename</code> 在依赖类型中的核心作用<strong>没有改变</strong>。</p><p>此外，C++20 引入的 <strong>Concepts</strong> 可以减少对 <code>enable_if + typename</code> 的依赖，但底层机制仍相同。</p><hr><h1 id="八、总结：一张表掌握-typename"><a href="#八、总结：一张表掌握-typename" class="headerlink" title="八、总结：一张表掌握 typename"></a>八、总结：一张表掌握 <code>typename</code></h1><table><thead><tr><th>场景</th><th>是否需要 <code>typename</code></th><th>示例</th></tr></thead><tbody><tr><td>模板参数声明</td><td>可选（推荐用）</td><td><code>template&lt;typename T&gt;</code></td></tr><tr><td>依赖嵌套类型（模板内）</td><td>✅ 必须</td><td><code>typename T::iterator</code></td></tr><tr><td>非依赖名称</td><td>❌ 不需要</td><td><code>MyClass::value_type</code></td></tr><tr><td>基类列表</td><td>❌ 禁止</td><td><code>class D : public T::Base</code></td></tr><tr><td>函数返回类型（依赖）</td><td>✅ 需要</td><td><code>typename T::result_type func();</code></td></tr><tr><td><code>using</code> 类型别名（依赖）</td><td>✅ 需要</td><td><code>using U = typename T::type;</code></td></tr></tbody></table><p><code>typename</code> 是 C++ 模板元编程的基石之一。理解它不仅是写出正确模板代码的前提，更是掌握 SFINAE、类型萃取、泛型库设计的关键。记住核心原则：</p><blockquote><p><strong>“依赖名称默认不是类型；若它是类型，请用 <code>typename</code> 明确告知。”</strong></p></blockquote>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;在 C++ 模板编程中，&lt;code&gt;typename&lt;/code&gt; 是一个看似简单却极易被误解的关键字。它不仅用于声明模板类型参数，更在处理嵌套依赖类型时扮演着“编译器翻译官”的角色。本文将系统梳理 &lt;code&gt;typename&lt;/code&gt; 的所有用法、规则与陷阱，帮助你</summary>
      
    
    
    
    <category term="C++随笔" scheme="http://example.com/categories/C-%E9%9A%8F%E7%AC%94/"/>
    
    
    <category term="C++" scheme="http://example.com/tags/C/"/>
    
    <category term="面经" scheme="http://example.com/tags/%E9%9D%A2%E7%BB%8F/"/>
    
  </entry>
  
  <entry>
    <title>C++新特性梳理-Modules</title>
    <link href="http://example.com/2025/11/09/C++/C++%E6%96%B0%E7%89%B9%E6%80%A7%E6%A2%B3%E7%90%86-Modules/"/>
    <id>http://example.com/2025/11/09/C++/C++%E6%96%B0%E7%89%B9%E6%80%A7%E6%A2%B3%E7%90%86-Modules/</id>
    <published>2025-11-09T04:02:57.000Z</published>
    <updated>2026-05-28T07:37:46.658Z</updated>
    
    <content type="html"><![CDATA[<h1 id="1-Modules是什么？"><a href="#1-Modules是什么？" class="headerlink" title="1 Modules是什么？"></a>1 Modules是什么？</h1><p>C++20 引入了一个重要的新特性：模块（<code>Modules</code>），这是对 C++ 编译模型的一次重大革新，旨在解决传统头文件（header files）机制带来的诸多问题，如编译速度慢、宏污染、重复包含、依赖管理混乱等。</p><p>这么说可能有点抽象, 那么我们说人话就是:</p><ul><li>有了模块之后, C++就不需要不需要将源文件和头文件分离了</li><li>你甚至可以不需要头文件</li><li>你的C++的文件结构变得类似<code>Java</code>和<code>Python</code>, <code>import</code>时导入你想要的代码就行了, 不需要考虑头文件这个东西</li></ul><p>以上只是引入<code>Modules</code>后代码直观结果, 当然<code>Modules</code>的功能不仅仅如此</p><h1 id="2-头文件存在的问题"><a href="#2-头文件存在的问题" class="headerlink" title="2 头文件存在的问题"></a>2 头文件存在的问题</h1><h2 id="2-1-传统头文件机制的代码案例"><a href="#2-1-传统头文件机制的代码案例" class="headerlink" title="2.1 传统头文件机制的代码案例"></a>2.1 传统头文件机制的代码案例</h2><p>在 C++20 之前，我们通常采用“声明-定义分离”模式：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// math.h</span></span><br><span class="line"><span class="meta">#<span class="keyword">ifndef</span> MATH_H</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> MATH_H</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span></span>;</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// main.cpp</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;math.h&quot;</span></span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> </span>&#123; <span class="keyword">return</span> a + b; &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// math.cpp</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;math.h&quot;</span></span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> </span>&#123; <span class="keyword">return</span> a + b; &#125;</span><br></pre></td></tr></table></figure><p>这个例子中, 也就是传统的 C++ 编译模型中，头文件实际上是一种文本替换机制。当预处理器遇到 <code>#include</code> 指令时，它会将指定的头文件内容直接复制到 <code>#include</code> 所在的位置。这种机制也被称为”包含模型”(include model)。<br>例如，在上面的例子中，预处理器处理后的 math.cpp 文件实际上变成了：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 预处理器处理后的 math.cpp</span></span><br><span class="line"><span class="meta">#<span class="keyword">ifndef</span> MATH_H    <span class="comment">// 来自 math.h</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> MATH_H    <span class="comment">// 来自 math.h</span></span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span></span>;  <span class="comment">// 来自 math.h</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span>            <span class="comment">// 来自 math.h</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> </span>&#123; <span class="keyword">return</span> a + b; &#125;</span><br></pre></td></tr></table></figure><p>这个过程中, 头文件的作用是: <strong>生命一个编译时期可见的符号</strong>, 在这个例子中, 头文件<code>math.h</code>声明了函数<code>add</code>, 使得<code>math.cpp</code>可以调用这个函数, 为了理解这句话, 我们还需要知道C++ 程序从源代码到可执行文件需要经历的步骤:</p><p><strong>编译阶段</strong></p><ol><li>预处理阶段(Preprocessing)：<ol><li>处理 #include、#define 等预处理指令</li><li>展开头文件内容，替换宏定义</li><li>条件编译处理</li></ol></li><li>编译阶段(Compilation)：<ol><li>将预处理后的文件转换为汇编代码</li><li>每个源文件(.cpp)独立编译成目标文件(.obj&#x2F;.o)</li><li>进行词法分析、语法分析、语义分析</li><li>生成符号表</li></ol></li><li>汇编阶段(Assembly)：<ol><li>将汇编代码转换为机器码</li><li>生成目标文件(.obj&#x2F;.o)</li></ol></li></ol><p><strong>链接阶段</strong></p><ol><li>合并多个目标文件</li><li>解析外部引用（如函数调用）</li><li>重定位地址</li><li>生成最终的可执行文件</li></ol><p>这里还需要知道一个重要的知识点, C++编译的基础单元就是一个.cpp文件（严格说是翻译单元 <code>translation unit</code>）, 而.cpp文件会生成一个.obj文件, 一个或多个.obj文件经处理会生成一个.exe文件</p><p>在这个过程中, 一个编译单元引用其他编译单元的符号如何确保其正确和有效呢? 在传统方法中, 就是通过头文件告知编译器。 例如这里<code>main.cpp</code>需要调用<code>add</code>函数, 那么它就需要包含<code>math.h</code>, 这样编译器在编译<code>main.cpp</code>时就能够从头文件知道<code>add</code>函数的存在, 先将其地址标记为可重定位。最后在链接阶段, 链接器会找到<code>add</code>函数的实现并将之前的可重定位符号替换为真实的地址。</p><h2 id="2-2-传统头文件机制的问题"><a href="#2-2-传统头文件机制的问题" class="headerlink" title="2.2 传统头文件机制的问题"></a>2.2 传统头文件机制的问题</h2><p>基于上述编译过程，我们可以发现传统头文件机制好像没啥问题?</p><p>但如果是下面几个场景呢?</p><ol><li>一个头文件在大型项目中被多个源文件包含<ol><li>每次编译这些源文件时, 头文件内容都会被重复处理</li><li>即使头文件发生微小变化，所有包含它的源文件都需要重新编译</li></ol></li><li>不同代码由不同的团队编写, 可能存在冲突的符号<ol><li>虽然可以使用<code>include guards</code>（如#ifndef）或<code>#pragma once</code>来防止重复包含，但这增加了复杂性，并且仍需要预处理器进行检查</li></ol></li><li>模板支持不佳<ol><li>模板实现必须放在头文件中才能被正确实例化，这进一步增加了头文件的大小和编译负担。</li></ol></li></ol><h1 id="3-Modules-所解决的问题"><a href="#3-Modules-所解决的问题" class="headerlink" title="3 Modules 所解决的问题"></a>3 Modules 所解决的问题</h1><p>C++20引入的Modules特性旨在从根本上解决上述问题：</p><h2 id="3-1-编译速度提升"><a href="#3-1-编译速度提升" class="headerlink" title="3.1 编译速度提升"></a>3.1 编译速度提升</h2><p>Modules通过引入模块接口单元（module interface unit）来替代头文件。模块只编译一次，生成中间表示形式，其他编译单元可以直接导入这个预编译的结果：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// math.cpp (模块接口)</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">module</span> math;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">export</span> <span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a + b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// main.cpp</span></span><br><span class="line"><span class="keyword">import</span> math; <span class="comment">// 直接导入，无需文本替换</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">add</span>(<span class="number">1</span>, <span class="number">2</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><blockquote><p>这里给出的名词和代码案例先不进行详细介绍, 后面的章节中会进行详细介绍</p></blockquote><h2 id="3-2-符号污染问题"><a href="#3-2-符号污染问题" class="headerlink" title="3.2 符号污染问题"></a>3.2 符号污染问题</h2><p>Modules具有明确的导出控制，只有明确标记为<code>export</code>的声明才会对外可见，定义的符号默认不会泄露到导入模块的编译单元中。例如</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">module</span> math;</span><br><span class="line"><span class="function"><span class="keyword">export</span> <span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a + b;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">inner_add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a + b;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里的<code>inner_add</code>函数没有使用<code>export</code>关键字，不会被导出，因此不会被其他模块访问。</p><h1 id="4-Modules-语法详解"><a href="#4-Modules-语法详解" class="headerlink" title="4 Modules 语法详解"></a>4 Modules 语法详解</h1><h2 id="4-1-模块声明"><a href="#4-1-模块声明" class="headerlink" title="4.1 模块声明"></a>4.1 模块声明</h2><ul><li><p><strong>默认 → 普通单元</strong></p><ul><li>不使用 <code>module</code> 关键字的翻译单元是普通单元（即传统 <code>.cpp</code> 文件）</li><li>可以包含头文件，使用 <code>#include</code></li></ul></li><li><p><strong>加 <code>module</code> 关键字 → 模块单元</strong></p><ul><li>使用 <code>module</code> 声明的单元称为模块单元</li><li>分为两种子类型：<ul><li><strong>默认 → 模块实现单元</strong><ul><li>仅定义内部逻辑，不对外暴露接口</li><li>通常用于实现细节</li></ul></li><li><strong>加 <code>export</code> 关键字 → 模块接口单元</strong><ul><li>使用 <code>export module ModuleName;</code> 定义模块接口</li><li>是模块的入口点，供其他模块导入</li></ul></li></ul></li></ul></li></ul><blockquote><p>示例：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">module</span> mymath;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">export</span> <span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> </span>&#123; <span class="keyword">return</span> a + b; &#125;</span><br></pre></td></tr></table></figure></blockquote><h2 id="4-2-导入导出的方法"><a href="#4-2-导入导出的方法" class="headerlink" title="4.2 导入导出的方法"></a>4.2 导入导出的方法</h2><p>这里的导入导出的含义就是, 如何让外部模块访问模块中的符号</p><h3 id="4-2-1-导出（Export）"><a href="#4-2-1-导出（Export）" class="headerlink" title="4.2.1 导出（Export）"></a>4.2.1 导出（Export）</h3><ul><li><strong>默认 → 不可见</strong><ul><li>模块单元中定义的符号默认对其他模块不可见</li></ul></li><li><strong>加 <code>export</code> 关键字 → 可见</strong><ul><li>显式导出函数、类、变量等，使其可以被其他模块使用</li></ul></li></ul><blockquote><p>示例：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">export</span> <span class="type">int</span> <span class="title">multiply</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> </span>&#123; <span class="keyword">return</span> a * b; &#125; <span class="comment">// 可被导入</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">helper</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> <span class="number">42</span>; &#125; <span class="comment">// 不可见</span></span><br></pre></td></tr></table></figure></blockquote><h3 id="4-2-2-导入（Import）"><a href="#4-2-2-导入（Import）" class="headerlink" title="4.2.2 导入（Import）"></a>4.2.2 导入（Import）</h3><ul><li><p><strong>加 <code>import</code> 关键字 → 导入只在本编译单元可见</strong></p><ul><li>使用 <code>import ModuleName;</code> 引入整个模块</li><li>导入后，该模块的导出内容可在当前单元中使用</li></ul></li><li><p><strong>普通&#x2F;模块单元区别：导入头文件</strong></p><ul><li><strong>模块单元无法使用 <code>#include</code> 引入头文件</strong><ul><li>必须使用 <code>import &quot;header.h&quot;</code> 或 <code>import &lt;header&gt;</code> 方式导入</li></ul></li><li><strong>问题：预处理宏访问限制</strong><ul><li>在模块单元中，通过 <code>import</code> 导入的头文件中定义的宏，<strong>不能被其他 <code>import</code> 的头文件访问</strong></li><li>因为模块机制绕过了传统的预处理器行为</li></ul></li></ul></li></ul><blockquote><p>示例：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> math;           <span class="comment">// 导入模块</span></span><br><span class="line"><span class="keyword">import</span> <span class="string">&quot;myconfig.h&quot;</span>;   <span class="comment">// 导入自定义头文件（作为模块）</span></span><br></pre></td></tr></table></figure><p>补充说明：</p><ul><li><code>import math;</code> 用于导入名为 <code>math</code> 的模块，这与传统的 <code>#include</code> 方式不同，它不会进行文本替换，而是直接导入模块的接口</li><li><code>import &quot;myconfig.h&quot;;</code> 用于将以传统头文件形式存在的代码作为模块导入，这为逐步迁移现有代码提供了便利</li><li>与 <code>#include</code> 不同，<code>import</code> 导入的头文件中的宏定义不会泄露到全局命名空间，避免了宏污染问题</li><li>模块导入具有原子性，要么全部导入成功，要么全部失败，不会出现部分导入的情况</li></ul></blockquote><h2 id="4-3-全局和私有模块片段"><a href="#4-3-全局和私有模块片段" class="headerlink" title="4.3 全局和私有模块片段"></a>4.3 全局和私有模块片段</h2><ul><li>模块支持定义<strong>全局模块片段</strong>（Global Module Fragment）<ul><li>用 <code>module;</code> 表示</li><li>用于在模块中声明不依赖于模块名的全局内容</li></ul></li><li>支持<strong>私有模块片段</strong>（Private Module Fragment）<ul><li>用于定义仅在模块内部使用的符号</li><li>无需 <code>export</code>，自动隐藏</li></ul></li></ul><blockquote><p>示例：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">module</span> mylib;</span><br><span class="line"></span><br><span class="line"><span class="keyword">module</span>; <span class="comment">// 全局模块片段开始</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span> <span class="comment">// 在全局模块片段中包含头文件</span></span></span><br><span class="line"><span class="type">int</span> global_counter = <span class="number">0</span>; <span class="comment">// 全局变量，模块内可见</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">export</span> <span class="type">int</span> <span class="title">get_counter</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> global_counter;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">module</span> : <span class="keyword">private</span>; <span class="comment">// 私有模块片段开始</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">internal_helper</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 仅在当前模块内可用的辅助函数</span></span><br><span class="line">    global_counter++;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">export</span> <span class="type">void</span> <span class="title">increment_counter</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">internal_helper</span>(); <span class="comment">// 可以调用私有函数</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在这个例子中：</p><ul><li><code>module;</code> 开始全局模块片段，可以包含头文件和全局声明</li><li><code>module : private;</code> 开始私有模块片段，其中定义的符号仅在当前模块内可见</li><li><code>global_counter</code> 在全局模块片段中定义，对整个模块可见但不导出</li><li><code>internal_helper</code> 函数在私有模块片段中定义，只能在当前模块内使用</li></ul></blockquote><h2 id="4-4-子模块划分"><a href="#4-4-子模块划分" class="headerlink" title="4.4 子模块划分"></a>4.4 子模块划分</h2><ul><li>模块支持<strong>子模块</strong>（Submodules）或<strong>模块分区</strong>（Module Partitions）</li><li>用于将大型模块拆分为多个部分，便于组织和维护</li><li>语法：<ul><li><code>export module mylib:part1;</code> —— 子模块接口</li><li><code>module mylib:impl;</code> —— 实现部分（非导出）</li></ul></li></ul><blockquote><p>示例：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// mylib.cppm</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">module</span> mylib;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">export</span> <span class="type">int</span> <span class="title">main_func</span><span class="params">()</span></span>; <span class="comment">// 接口</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// mylib:impl.cppm</span></span><br><span class="line"><span class="keyword">module</span> mylib:impl;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main_func</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> <span class="number">42</span>; &#125; <span class="comment">// 实现</span></span><br></pre></td></tr></table></figure></blockquote><blockquote><p>注意：主模块必须先声明，子模块才能引用。</p></blockquote><hr><h2 id="4-5-所有权问题"><a href="#4-5-所有权问题" class="headerlink" title="4.5 所有权问题"></a>4.5 所有权问题</h2><ul><li><strong>模块的所有权属于其定义者</strong></li><li><strong>不能随意修改已发布的模块</strong></li><li><strong>导入模块时，不能重定义其符号</strong></li><li><strong>模块间的依赖关系明确且强绑定</strong></li><li><strong>避免循环依赖</strong>：模块间应保持清晰的依赖链</li></ul><blockquote><p>⚠️ 重要提示：</p><ul><li>模块一旦发布，其接口应尽量稳定</li><li>修改模块接口可能破坏所有依赖它的代码</li><li>模块是“编译期实体”，不像头文件那样容易“偷偷”引入副作用</li></ul></blockquote><h1 id="5-实战-Demo"><a href="#5-实战-Demo" class="headerlink" title="5 实战 Demo"></a>5 实战 Demo</h1><p>下面是一个 使用 <code>xmake</code> 构建 C++20 Modules 的完整真实案例，涵盖了模块的大部分常用功能：</p><ul><li>主模块接口（Primary Module Interface）</li><li>模块实现单元（Module Implementation Unit）</li><li>模块分区（Module Partition）</li><li>导入标准库模块（如 <iostream>）</li><li>与传统 .cpp 文件混合编译</li></ul><p>完整代码请参考: <a href="https://github.com/Vanilla-Beauty/cpp_notes_code/tree/master/modules_demo">https://github.com/Vanilla-Beauty/cpp_notes_code/tree/master/modules_demo</a></p><blockquote><p>这里说明为什么使用 <code>xmake</code> 而不是 <code>cmake</code>, 原因当然是<code>xmake</code>配置最简单了…同时我个人非常不喜欢<code>cmake</code>的语法…</p></blockquote><p><strong>📁 0 项目结构</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">bash</span><br><span class="line">modules_demo/</span><br><span class="line">├── src</span><br><span class="line">│   ├── main.cpp # </span><br><span class="line">│   └── math</span><br><span class="line">│       ├── math.ixx</span><br><span class="line">│       ├── math_impl.cpp</span><br><span class="line">│       └── utils.ixx</span><br><span class="line">└── xmake.lua</span><br></pre></td></tr></table></figure><p><strong>🔧 1. xmake.lua 配置文件</strong></p><figure class="highlight lua"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- xmake.lua</span></span><br><span class="line">set_languages(<span class="string">&quot;c++20&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 启用模块支持（关键！）</span></span><br><span class="line">set_policy(<span class="string">&quot;build.c++.modules&quot;</span>, <span class="literal">true</span>)</span><br><span class="line"></span><br><span class="line">target(<span class="string">&quot;modules_demo&quot;</span>)</span><br><span class="line">    set_kind(<span class="string">&quot;binary&quot;</span>)</span><br><span class="line">    add_files(<span class="string">&quot;src/main.cpp&quot;</span>)</span><br><span class="line">    add_files(<span class="string">&quot;src/math/math.ixx&quot;</span>)           <span class="comment">-- 主模块接口</span></span><br><span class="line">    add_files(<span class="string">&quot;src/math/math_impl.cpp&quot;</span>)      <span class="comment">-- 模块实现单元</span></span><br><span class="line">    add_files(<span class="string">&quot;src/math/utils.ixx&quot;</span>)     <span class="comment">-- 模块分区</span></span><br></pre></td></tr></table></figure><p><strong>📄 2. 模块分区：src&#x2F;math&#x2F;utils.ixx</strong>\</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// src/math/utils.ixx</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">module</span> math:utils;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">namespace</span> math::utils &#123;</span><br><span class="line">    <span class="function"><span class="keyword">inline</span> <span class="type">int</span> <span class="title">square</span><span class="params">(<span class="type">int</span> x)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> x * x;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>📄 3. 主模块接口：src&#x2F;math&#x2F;math.ixx</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// src//math/math.ixx</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">module</span> math;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 导入模块分区</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">import</span> :utils;  <span class="comment">// 等价于 import math:utils;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">namespace</span> math &#123;</span><br><span class="line">    <span class="comment">// 导出函数声明（定义在实现单元）</span></span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span></span>;</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">multiply</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 内联函数可直接定义并导出</span></span><br><span class="line">    <span class="function"><span class="keyword">inline</span> <span class="type">int</span> <span class="title">power2</span><span class="params">(<span class="type">int</span> x)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> utils::<span class="built_in">square</span>(x);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>📄 4. 模块实现单元：src&#x2F;math&#x2F;math_impl.cpp</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// src/math/math_impl.cpp</span></span><br><span class="line"><span class="keyword">module</span> math;  <span class="comment">// 注意：这里不加 export！</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 实现主模块中声明的函数</span></span><br><span class="line"><span class="keyword">namespace</span> math &#123;</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> a + b;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">multiply</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> a * b;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>⚠️ 实现单元使用 module math;（无 export），表示这是 math 模块的一部分，但不导出新符号。</p><blockquote><p>这里模块接口定义单元和模块实现单元类似传统的头文件与源文件</p></blockquote><p><strong>📄 5. 主程序：src&#x2F;main.cpp</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// src/main.cpp</span></span><br><span class="line"><span class="keyword">import</span> math; <span class="comment">// 导入我们自己的模块</span></span><br><span class="line"><span class="keyword">import</span> &lt;iostream&gt;; <span class="comment">// C++20 标准库模块（需编译器支持）</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> math;</span><br><span class="line"></span><br><span class="line">cout &lt;&lt; <span class="string">&quot;2 + 3 = &quot;</span> &lt;&lt; <span class="built_in">add</span>(<span class="number">2</span>, <span class="number">3</span>) &lt;&lt; endl;</span><br><span class="line">cout &lt;&lt; <span class="string">&quot;4 5 = &quot;</span> &lt;&lt; <span class="built_in">multiply</span>(<span class="number">4</span>, <span class="number">5</span>) &lt;&lt; endl;</span><br><span class="line">cout &lt;&lt; <span class="string">&quot;7^2 = &quot;</span> &lt;&lt; <span class="built_in">power2</span>(<span class="number">7</span>) &lt;&lt; endl; <span class="comment">// 来自内联定义</span></span><br><span class="line">cout &lt;&lt; <span class="string">&quot;9^2 (via utils) = &quot;</span> &lt;&lt; utils::<span class="built_in">square</span>(<span class="number">9</span>) &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>✅ 注意：import <iostream>; 要求编译器支持标准库模块（MSVC 支持较好，Clang&#x2F;GCC 需额外配置或暂时回退到 #include）。</p><p>如果你的编译器不支持 import <iostream>，可临时改为：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="comment">// 并移除 import &lt;iostream&gt;;</span></span><br></pre></td></tr></table></figure><p><strong>▶️ 6. 构建与运行</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 进入项目目录</span></span><br><span class="line"><span class="built_in">cd</span> modules_demo</span><br><span class="line"><span class="comment"># 配置（自动检测编译器，推荐 clang++ 或 cl.exe）</span></span><br><span class="line">xmake f -c</span><br><span class="line"><span class="comment"># 构建</span></span><br><span class="line">xmake</span><br><span class="line"><span class="comment"># 运行</span></span><br><span class="line">xmake run</span><br></pre></td></tr></table></figure><p>预期输出：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">2 + 3 = 5</span><br><span class="line">4 5 = 20</span><br><span class="line">7^2 = 49</span><br><span class="line">9^2 (via utils) = 81</span><br></pre></td></tr></table></figure><p>如果输出符合预期, 那么恭喜, 你已经成功使用 C++20 Modules 构建了一个完整的项目。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;1-Modules是什么？&quot;&gt;&lt;a href=&quot;#1-Modules是什么？&quot; class=&quot;headerlink&quot; title=&quot;1 Modules是什么？&quot;&gt;&lt;/a&gt;1 Modules是什么？&lt;/h1&gt;&lt;p&gt;C++20 引入了一个重要的新特性：模块（&lt;code</summary>
      
    
    
    
    <category term="C++随笔" scheme="http://example.com/categories/C-%E9%9A%8F%E7%AC%94/"/>
    
    
    <category term="C++" scheme="http://example.com/tags/C/"/>
    
    <category term="面经" scheme="http://example.com/tags/%E9%9D%A2%E7%BB%8F/"/>
    
    <category term="C++20" scheme="http://example.com/tags/C-20/"/>
    
  </entry>
  
  <entry>
    <title>C++新特性梳理-Concepts</title>
    <link href="http://example.com/2025/09/21/C++/C++%E6%96%B0%E7%89%B9%E6%80%A7%E6%A2%B3%E7%90%8604-Concepts/"/>
    <id>http://example.com/2025/09/21/C++/C++%E6%96%B0%E7%89%B9%E6%80%A7%E6%A2%B3%E7%90%8604-Concepts/</id>
    <published>2025-09-21T09:20:57.000Z</published>
    <updated>2026-05-28T07:37:46.659Z</updated>
    
    <content type="html"><![CDATA[<h1 id="1-Concepts-简介"><a href="#1-Concepts-简介" class="headerlink" title="1 Concepts 简介"></a>1 Concepts 简介</h1><p>C++20 引入了 <strong>Concepts</strong>，这是一个革命性的特性，旨在解决模板编程中长期存在的问题，特别是编译错误信息晦涩难懂和对模板参数缺乏明确约束的问题。Concepts 提供了一种直接、清晰的方式来<strong>定义模板参数必须满足的接口要求</strong>。</p><p>在 C++20 之前，模板是“隐式契约”：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span></span><br><span class="line"><span class="function">T <span class="title">add</span><span class="params">(T a, T b)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a + b; <span class="comment">// 隐含要求：T 必须支持 operator+</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果传入一个不支持 <code>+</code> 的类型（如 <code>std::thread</code>），编译器会报出一长串难以理解的错误信息，追溯到 <code>operator+</code> 调用失败。</p><p><strong>Concepts 的目标就是将这种“隐式契约”变成“显式契约”</strong>：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line">    <span class="keyword">requires</span> Addable&lt;T&gt; <span class="comment">// 显式要求 T 满足 Addable 概念</span></span><br><span class="line"><span class="function">T <span class="title">add</span><span class="params">(T a, T b)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a + b;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>现在，如果 <code>T</code> 不满足 <code>Addable</code>，编译器会直接告诉你“<code>T</code> 不满足 <code>Addable</code> 概念”，错误信息清晰明了。</p><hr><h1 id="2-如何定义Concept"><a href="#2-如何定义Concept" class="headerlink" title="2 如何定义Concept"></a>2 如何定义Concept</h1><h2 id="2-1-一些定义-Concept-的案例"><a href="#2-1-一些定义-Concept-的案例" class="headerlink" title="2.1 一些定义 Concept 的案例"></a>2.1 一些定义 Concept 的案例</h2><p>使用 <code>template</code> 和 <code>concept</code> 关键字，结合 <code>requires</code> 表达式来定义。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;concepts&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;type_traits&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义一个名为 Addable 的 concept</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">concept</span> Addable = <span class="built_in">requires</span>(T a, T b) &#123;</span><br><span class="line">    a + b; <span class="comment">// 要求表达式 a + b 是合法的</span></span><br><span class="line">    <span class="comment">// 可以添加更多要求，如返回类型</span></span><br><span class="line">    &#123; a + b &#125; -&gt; std::same_as&lt;T&gt;; <span class="comment">// 要求 a + b 的返回类型是 T</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义一个名为 Printable 的 concept</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">concept</span> Printable = <span class="built_in">requires</span>(T t, std::ostream&amp; os) &#123;</span><br><span class="line">    os &lt;&lt; t; <span class="comment">// 要求 T 支持输出到 ostream</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义一个更复杂的 concept：Number</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">concept</span> Number = std::is_arithmetic_v&lt;T&gt;; <span class="comment">// 基于类型特征</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 或者使用 requires 表达式</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">concept</span> Integral = <span class="built_in">requires</span>(T a) &#123;</span><br><span class="line">    <span class="keyword">requires</span> std::is_integral_v&lt;T&gt;;</span><br><span class="line">    <span class="comment">// 或者更直接地检查操作</span></span><br><span class="line">    &#123; a / <span class="number">2</span> &#125; -&gt; std::convertible_to&lt;T&gt;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h2 id="2-2-需求的语法"><a href="#2-2-需求的语法" class="headerlink" title="2.2 需求的语法"></a>2.2 需求的语法</h2><p>从前面的案例我们观察到, <code>Concept</code> 的定义中，我们使用了 <code>requires</code> 关键字，其后的块中我们定义了多个限制条件, 这些限制条件我们称为需求(<code>requirement</code>), 其有下面几种语法:</p><hr><h3 id="2-2-1-简单需求-Simple-Requirements"><a href="#2-2-1-简单需求-Simple-Requirements" class="headerlink" title="2.2.1 简单需求 (Simple Requirements)"></a>2.2.1 简单需求 (Simple Requirements)</h3><p><strong>语法：</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">expression;</span><br></pre></td></tr></table></figure><p><strong>作用：</strong><br>检查 <code>expression</code> 是否是一个<strong>语法上合法的表达式</strong>。它只关心表达式能否被正确解析和编译，而不关心其返回类型、值类别或语义。</p><p><strong>示例：</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">concept</span> Addable = <span class="built_in">requires</span>(T a, T b) &#123;</span><br><span class="line">    a + b; <span class="comment">// 检查 &quot;a + b&quot; 这个表达式是否语法合法</span></span><br><span class="line">           <span class="comment">// 即 T 类型的对象是否重载了 operator+</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><ul><li>如果 <code>T</code> 定义了 <code>operator+</code>，这个需求就满足。</li><li>它不关心 <code>a + b</code> 返回的是 <code>T</code>、<code>T&amp;</code>、<code>int</code> 还是其他任何类型，只要表达式能写出来就行。</li></ul><hr><h3 id="2-2-2-类型需求-Type-Requirements"><a href="#2-2-2-类型需求-Type-Requirements" class="headerlink" title="2.2.2 类型需求 (Type Requirements)"></a>2.2.2 类型需求 (Type Requirements)</h3><p><strong>语法：</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typename</span> type-id;</span><br></pre></td></tr></table></figure><p><strong>作用：</strong><br>检查某个类型是否存在。常用于检查嵌套类型（如 <code>value_type</code>, <code>iterator</code>）。</p><p><strong>示例：</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">concept</span> HasValueType = <span class="keyword">requires</span> &#123;</span><br><span class="line">    <span class="keyword">typename</span> T::value_type; <span class="comment">// 检查 T 是否有名为 value_type 的嵌套类型</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">concept</span> Iterator = <span class="built_in">requires</span>(T it) &#123;</span><br><span class="line">    <span class="keyword">typename</span> std::iterator_traits&lt;T&gt;::value_type; <span class="comment">// 检查迭代器的 value_type</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><hr><h3 id="2-2-3-复合需求-Compound-Requirements"><a href="#2-2-3-复合需求-Compound-Requirements" class="headerlink" title="2.2.3 复合需求 (Compound Requirements)"></a>2.2.3 复合需求 (Compound Requirements)</h3><p>这是最复杂也最强大的一种，使用花括号 <code>&#123;&#125;</code> 包围表达式，并可以附加约束。</p><p><strong>基本语法：</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&#123; expression &#125; [<span class="keyword">noexcept</span>] [-&gt; type-constraint];</span><br></pre></td></tr></table></figure><p>这里的 <code>-&gt;</code> <strong>不是</strong> JavaScript 的箭头函数，而是 C++ <code>requires</code> 表达式中用于指定<strong>返回类型约束</strong>的语法。</p><p><strong>a: 只检查表达式合法性（等同于简单需求，但更灵活</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&#123; a + b &#125;;</span><br></pre></td></tr></table></figure><p>这与 <code>a + b;</code> 效果相同，检查 <code>a + b</code> 是否语法合法。</p><p><strong>b: 检查表达式的返回类型</strong></p><p>这才是复合需求的精髓：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&#123; a + b &#125; -&gt; std::same_as&lt;T&gt;;</span><br></pre></td></tr></table></figure><p><strong>分解解释：</strong></p><ul><li><code>&#123; a + b &#125;</code>: 被检查的表达式。</li><li><code>-&gt;</code>: 引入对表达式结果的约束。</li><li><code>std::same_as&lt;T&gt;</code>: 一个 <strong>type-constraint</strong>（类型约束），它是一个 concept，要求左侧的类型与 <code>T</code> <strong>完全相同</strong>。</li></ul><p><strong>这意味着：</strong><br><code>a + b</code> 不仅要语法合法，其<strong>求值结果的类型</strong>还必须与 <code>T</code> 是同一个类型。</p><p><strong>对比：</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 只检查语法</span></span><br><span class="line">a + b;                    <span class="comment">// OK if operator+ exists, regardless of return type</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 检查语法 AND return type</span></span><br><span class="line">&#123; a + b &#125; -&gt; std::same_as&lt;T&gt;; <span class="comment">// Fails if operator+ returns int, float, or T&amp;, but not exactly T</span></span><br></pre></td></tr></table></figure><p><strong>c: 更多返回类型约束的例子</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">concept</span> AddableStrict = <span class="built_in">requires</span>(T a, T b) &#123;</span><br><span class="line">    &#123; a + b &#125; -&gt; std::convertible_to&lt;T&gt;; <span class="comment">// 要求返回类型可隐式转换为 T</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">concept</span> EqualityComparable = <span class="built_in">requires</span>(T a, T b) &#123;</span><br><span class="line">    &#123; a == b &#125; -&gt; std::convertible_to&lt;<span class="type">bool</span>&gt;; <span class="comment">// 要求 a == b 返回可转为 bool 的类型</span></span><br><span class="line">    &#123; a != b &#125; -&gt; std::same_as&lt;<span class="type">bool</span>&gt;;        <span class="comment">// 要求 a != b 返回 exactly bool</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">concept</span> Callable = <span class="built_in">requires</span>(T f) &#123;</span><br><span class="line">    &#123; <span class="built_in">f</span>() &#125; -&gt; std::same_as&lt;<span class="type">int</span>&gt;; <span class="comment">// 要求 f() 调用返回 int</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p><strong>d:检查异常规范</strong></p><p>复合需求还可以检查表达式是否 <code>noexcept</code>：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">&#123; a + b &#125; <span class="keyword">noexcept</span>; <span class="comment">// 要求 a + b 是 noexcept 表达式</span></span><br><span class="line">&#123; a + b &#125; <span class="keyword">noexcept</span> -&gt; std::same_as&lt;T&gt;; <span class="comment">// 要求 noexcept 且返回 T</span></span><br></pre></td></tr></table></figure><hr><h3 id="2-2-4-嵌套需求-Nested-Requirements"><a href="#2-2-4-嵌套需求-Nested-Requirements" class="headerlink" title="2.2.4 嵌套需求 (Nested Requirements)"></a>2.2.4 嵌套需求 (Nested Requirements)</h3><p>使用 <code>requires</code> 关键字在内部引入额外的逻辑条件。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">concept</span> SignedIntegral = <span class="built_in">requires</span>(T a) &#123;</span><br><span class="line">    <span class="keyword">requires</span> std::integral&lt;T&gt;;        <span class="comment">// 嵌套要求：T 必须是整型</span></span><br><span class="line">    <span class="keyword">requires</span> std::is_signed_v&lt;T&gt;;     <span class="comment">// 嵌套要求：T 必须是有符号的</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>注意：嵌套需求中的 <code>requires</code> 后面直接跟一个布尔常量表达式（通常是另一个 concept 或 <code>bool</code> 值），而不需要分号结束（它本身就是一个声明）。</p><hr><h4 id="2-2-5-总结对比表"><a href="#2-2-5-总结对比表" class="headerlink" title="2.2.5 总结对比表"></a>2.2.5 总结对比表</h4><table><thead><tr><th align="left">语法形式</th><th align="left">示例</th><th align="left">检查内容</th></tr></thead><tbody><tr><td align="left"><strong>简单需求</strong></td><td align="left"><code>a + b;</code></td><td align="left"><code>a + b</code> 是否是合法表达式</td></tr><tr><td align="left"><strong>复合需求 (无 -&gt;)</strong></td><td align="left"><code>&#123; a + b &#125;;</code></td><td align="left">同上，等价于简单需求</td></tr><tr><td align="left"><strong>复合需求 (带 -&gt;)</strong></td><td align="left"><code>&#123; a + b &#125; -&gt; std::same_as&lt;T&gt;;</code></td><td align="left"><code>a + b</code> 合法 <strong>且</strong> 返回类型 <strong>完全等于</strong> <code>T</code></td></tr><tr><td align="left"><strong>复合需求 (带 -&gt;)</strong></td><td align="left"><code>&#123; a + b &#125; -&gt; std::convertible_to&lt;T&gt;;</code></td><td align="left"><code>a + b</code> 合法 <strong>且</strong> 返回类型 <strong>可隐式转换为</strong> <code>T</code></td></tr><tr><td align="left"><strong>复合需求 (noexcept)</strong></td><td align="left"><code>&#123; a + b &#125; noexcept;</code></td><td align="left"><code>a + b</code> 合法 <strong>且</strong> 是 <code>noexcept</code></td></tr><tr><td align="left"><strong>类型需求</strong></td><td align="left"><code>typename T::value_type;</code></td><td align="left"><code>T</code> 是否有 <code>value_type</code> 嵌套类型</td></tr><tr><td align="left"><strong>嵌套需求</strong></td><td align="left"><code>requires std::integral&lt;T&gt;;</code></td><td align="left">额外的布尔条件</td></tr></tbody></table><hr><p><strong>为什么需要 <code>-&gt;</code> 形式？</strong></p><p>因为<strong>许多操作符的返回类型是有约定的</strong>。例如：</p><ul><li><code>operator+</code> 通常返回一个新对象（值类型），而不是引用。</li><li><code>operator==</code> 应该返回 <code>bool</code>。</li></ul><p>使用 <code>&#123; expr &#125; -&gt; constraint;</code> 可以精确地强制这些约定，使你的 concept 更加严谨和符合直觉。而简单的 <code>expr;</code> 只保证了操作符的存在，无法保证其行为的正确性。</p><p>因此，当你需要对表达式的<strong>返回类型</strong>进行约束时，就必须使用复合需求的 <code>-&gt;</code> 形式。</p><h1 id="3-使用-Concepts"><a href="#3-使用-Concepts" class="headerlink" title="3 使用 Concepts"></a>3 使用 Concepts</h1><p>有多种语法形式：</p><p><strong>形式一：函数模板参数列表中使用 (推荐)</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 使用 concept 作为模板参数的约束</span></span><br><span class="line"><span class="function"><span class="keyword">template</span>&lt;Addable T&gt;</span></span><br><span class="line"><span class="function">T <span class="title">add</span><span class="params">(T a, T b)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a + b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 多个 concept 约束 (使用逻辑运算符)</span></span><br><span class="line"><span class="function"><span class="keyword">template</span>&lt;Addable T&gt;</span></span><br><span class="line"><span class="function">    <span class="keyword">requires</span> Printable&lt;T&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">print_add</span><span class="params">(T a, T b)</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="built_in">add</span>(a, b) &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 等价的简化语法 (C++20)</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">print_add</span><span class="params">(Addable <span class="keyword">auto</span> a, Addable <span class="keyword">auto</span> b)</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; (a + b) &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>形式二：类模板约束</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;Printable T&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Logger</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">log</span><span class="params">(<span class="type">const</span> T&amp; value)</span> </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Log: &quot;</span> &lt;&lt; value &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用</span></span><br><span class="line">Logger&lt;<span class="type">int</span>&gt; logger1;     <span class="comment">// OK</span></span><br><span class="line">Logger&lt;std::string&gt; logger2; <span class="comment">// OK</span></span><br><span class="line"><span class="comment">// Logger&lt;std::thread&gt; logger3; // 错误！std::thread 不满足 Printable</span></span><br></pre></td></tr></table></figure><p><strong>形式三：函数参数约束</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 直接约束函数参数</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">process</span><span class="params">(Integral <span class="keyword">auto</span> value)</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;Processing integer: &quot;</span> &lt;&lt; value &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用</span></span><br><span class="line"><span class="built_in">process</span>(<span class="number">42</span>);    <span class="comment">// OK</span></span><br><span class="line"><span class="built_in">process</span>(<span class="number">3.14</span>);  <span class="comment">// 错误！double 不是 Integral</span></span><br></pre></td></tr></table></figure><hr><h1 id="标准库中的常用-Concepts"><a href="#标准库中的常用-Concepts" class="headerlink" title="标准库中的常用 Concepts"></a>标准库中的常用 Concepts</h1><p>C++20 标准库在 <code>&lt;concepts&gt;</code> 头文件中定义了许多预定义的 Concepts，可以直接使用：</p><ul><li><p><strong>基本类型特征相关</strong>:</p><ul><li><code>std::integral&lt;T&gt;</code>: <code>T</code> 是整型</li><li><code>std::floating_point&lt;T&gt;</code>: <code>T</code> 是浮点型</li><li><code>std::arithmetic&lt;T&gt;</code>: <code>T</code> 是算术类型（整型或浮点型）</li><li><code>std::same_as&lt;T, U&gt;</code>: <code>T</code> 和 <code>U</code> 是同一类型</li><li><code>std::derived_from&lt;Base, Derived&gt;</code>: <code>Derived</code> 从 <code>Base</code> 派生</li></ul></li><li><p><strong>对象类别相关</strong>:</p><ul><li><code>std::movable&lt;T&gt;</code>: <code>T</code> 可移动</li><li><code>std::copyable&lt;T&gt;</code>: <code>T</code> 可拷贝</li><li><code>std::destructible&lt;T&gt;</code>: <code>T</code> 可析构</li></ul></li><li><p><strong>可调用相关</strong>:</p><ul><li><code>std::invocable&lt;F, Args...&gt;</code>: <code>F</code> 可以用 <code>Args...</code> 调用</li><li><code>std::predicate&lt;F, Args...&gt;</code>: <code>F</code> 是一个返回布尔值的可调用对象</li></ul></li><li><p><strong>容器和迭代器相关</strong>:</p><ul><li><code>std::default_constructible&lt;T&gt;</code></li><li><code>std::swappable&lt;T&gt;</code></li><li><code>std::ranges::input_iterator&lt;It&gt;</code> (在 <code>&lt;ranges&gt;</code> 中)</li></ul></li></ul><hr><h1 id="优势"><a href="#优势" class="headerlink" title="优势"></a>优势</h1><ol><li><strong>清晰的编译错误</strong>：当类型不满足概念时，错误信息直接指出哪个概念未满足，而不是深入模板实例化的细节。</li><li><strong>更好的代码文档</strong>：模板的意图和要求一目了然。</li><li><strong>函数重载和特化</strong>：可以根据不同的 concept 选择最合适的函数重载或类特化。<figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">template</span>&lt;std::integral T&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(T)</span> </span>&#123; <span class="comment">/* 处理整型 */</span> &#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">template</span>&lt;std::floating_point T&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(T)</span> </span>&#123; <span class="comment">/* 处理浮点型 */</span> &#125;</span><br></pre></td></tr></table></figure></li><li><strong>提高代码安全性</strong>：在编译期强制执行接口契约。</li></ol><hr><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>C++20 的 <strong>Concepts</strong> 是模板编程的重大进步。它通过<strong>显式声明模板参数的约束条件</strong>，极大地提升了模板代码的可读性、可维护性和错误诊断能力。掌握 Concepts 是现代 C++ 编程的关键技能之一。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;1-Concepts-简介&quot;&gt;&lt;a href=&quot;#1-Concepts-简介&quot; class=&quot;headerlink&quot; title=&quot;1 Concepts 简介&quot;&gt;&lt;/a&gt;1 Concepts 简介&lt;/h1&gt;&lt;p&gt;C++20 引入了 &lt;strong&gt;Concepts</summary>
      
    
    
    
    <category term="C++随笔" scheme="http://example.com/categories/C-%E9%9A%8F%E7%AC%94/"/>
    
    
    <category term="C++" scheme="http://example.com/tags/C/"/>
    
    <category term="面经" scheme="http://example.com/tags/%E9%9D%A2%E7%BB%8F/"/>
    
    <category term="C++20" scheme="http://example.com/tags/C-20/"/>
    
  </entry>
  
  <entry>
    <title>新人的Shell配置指南</title>
    <link href="http://example.com/2025/07/05/%E5%AE%9E%E7%94%A8%E5%B7%A5%E5%85%B7%E7%AC%94%E8%AE%B0/%E6%96%B0%E4%BA%BA%E7%9A%84Shell%E9%85%8D%E7%BD%AE%E6%8C%87%E5%8D%97/"/>
    <id>http://example.com/2025/07/05/%E5%AE%9E%E7%94%A8%E5%B7%A5%E5%85%B7%E7%AC%94%E8%AE%B0/%E6%96%B0%E4%BA%BA%E7%9A%84Shell%E9%85%8D%E7%BD%AE%E6%8C%87%E5%8D%97/</id>
    <published>2025-07-05T11:59:55.000Z</published>
    <updated>2026-05-28T07:37:46.666Z</updated>
    
    <content type="html"><![CDATA[<p>本人将为新人介绍常用的 <code>Shell</code> 配置和工具，帮助提升命令行效率。本文将涵盖 <code>Zsh</code>、<code>Oh My Zsh</code>、<code>Zoxide</code>、<code>FZF</code> 和 <code>Ripgrep</code> 等工具的安装和配置和简单教学。</p><hr><h1 id="1-Zsh-安装和配置"><a href="#1-Zsh-安装和配置" class="headerlink" title="1 Zsh 安装和配置"></a>1 Zsh 安装和配置</h1><p>你可能已经习惯了 <code>Linux</code> 默认的 <code>Bash Shell</code>，但现在是时候考虑升级到 <strong>Zsh</strong> 了。<code>Zsh</code> 提供了许多 <code>Bash</code> 不具备的强大功能，例如：</p><ul><li><strong>更强大的自动补全</strong>：<code>Zsh</code> 的自动补全功能非常智能，可以补全命令、文件路径、选项、甚至是 Git 分支等，大大减少你的输入量。</li><li><strong>命令历史共享</strong>：在多个 <code>Zsh</code> 会话中共享命令历史，方便你随时查找和重复执行之前的命令。</li><li><strong>主题和插件支持</strong>：通过各种主题美化你的终端界面，并通过插件扩展 <code>Zsh</code> 的功能，使其更加强大。</li></ul><h2 id="1-1-如何安装-Zsh"><a href="#1-1-如何安装-Zsh" class="headerlink" title="1.1 如何安装 Zsh"></a>1.1 如何安装 Zsh</h2><p>根据你的操作系统，安装 Zsh 的方法略有不同：</p><ul><li><strong>macOS</strong>: Zsh 通常已经预装。你可以通过运行 <code>zsh --version</code> 来检查。如果未安装，可以使用 Homebrew：<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">brew install zsh</span><br></pre></td></tr></table></figure></li><li><strong>Ubuntu&#x2F;Debian</strong>:<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">sudo apt update</span><br><span class="line">sudo apt install zsh</span><br></pre></td></tr></table></figure></li><li><strong>Fedora&#x2F;RHEL</strong>:<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo dnf install zsh</span><br></pre></td></tr></table></figure></li></ul><h2 id="1-2-设置-Zsh-为默认-Shell"><a href="#1-2-设置-Zsh-为默认-Shell" class="headerlink" title="1.2 设置 Zsh 为默认 Shell"></a>1.2 设置 Zsh 为默认 Shell</h2><p>安装完成后，你需要将 Zsh 设置为你的默认 Shell：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">chsh -s $(<span class="built_in">which</span> zsh)</span><br></pre></td></tr></table></figure><p>然后，你需要退出当前终端会话并重新登录，或者重新打开终端，Zsh 就会生效。</p><blockquote><p>如果你选择安装后续的<code>Oh My Zsh</code>, 你可以跳过此步骤, 因为<code>Oh My Zsh</code> 会自动将 <code>Zsh</code> 设置为默认 <code>Shell</code>。</p></blockquote><hr><h1 id="2-Oh-My-Zsh：Zsh-配置框架"><a href="#2-Oh-My-Zsh：Zsh-配置框架" class="headerlink" title="2 Oh My Zsh：Zsh 配置框架"></a>2 Oh My Zsh：Zsh 配置框架</h1><p><strong>Oh My Zsh</strong> 是一个开源的、社区驱动的 Zsh 配置框架，它使得管理 Zsh 配置变得异常简单。它包含了数百个有用的函数、助手、主题和插件，可以帮助你快速构建一个功能强大的 <code>Shell</code> 环境。</p><p>官网: <a href="https://ohmyz.sh/">Oh My Zsh</a></p><h2 id="2-1-安装-Oh-My-Zsh"><a href="#2-1-安装-Oh-My-Zsh" class="headerlink" title="2.1 安装 Oh My Zsh"></a>2.1 安装 Oh My Zsh</h2><p>安装 Oh My Zsh 非常简单，只需执行以下命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sh -c <span class="string">&quot;<span class="subst">$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)</span>&quot;</span></span><br></pre></td></tr></table></figure><p>根据脚本的提升选择性按<code>y</code>或<code>n</code>进行配置即可, 通常全部默认选<code>y</code>即可</p><h2 id="2-2-强大的插件系统"><a href="#2-2-强大的插件系统" class="headerlink" title="2.2 强大的插件系统"></a>2.2 强大的插件系统</h2><p><code>Oh My Zsh</code> 的核心优势之一在于其丰富的插件生态。你可以在 <code>~/.zshrc</code> 文件中配置要启用的插件。找到 <code>plugins=(git)</code> 这一行，然后添加你需要的插件。</p><p>以下是一些常用且推荐的插件：</p><ul><li><strong>git</strong>: 提供了大量的 Git 别名和辅助函数，极大提高了 Git 操作的效率。</li><li><strong>zsh-autosuggestions</strong>: 根据你的历史命令自动建议命令，按右箭头键即可补全。<ul><li><strong>安装</strong>:<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/zsh-users/zsh-autosuggestions <span class="variable">$&#123;ZSH_CUSTOM:-~/.oh-my-zsh/custom&#125;</span>/plugins/zsh-autosuggestions</span><br></pre></td></tr></table></figure></li><li><strong>启用</strong>: 在 <code>~/.zshrc</code> 中添加 <code>zsh-autosuggestions</code> 到 <code>plugins</code> 数组。</li></ul></li><li><strong>zsh-syntax-highlighting</strong>: 为命令提供语法高亮，让你的命令更易读，并能及时发现输入错误。<ul><li><strong>安装</strong>:<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/zsh-users/zsh-syntax-highlighting.git <span class="variable">$&#123;ZSH_CUSTOM:-~/.oh-my-zsh/custom&#125;</span>/plugins/zsh-syntax-highlighting</span><br></pre></td></tr></table></figure></li><li><strong>启用</strong>: 在 <code>~/.zshrc</code> 中添加 <code>zsh-syntax-highlighting</code> 到 <code>plugins</code> 数组。</li></ul></li></ul><p><strong>配置示例</strong>:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"># ~/.zshrc</span><br><span class="line">plugins=(</span><br><span class="line">    git</span><br><span class="line">    zsh-autosuggestions</span><br><span class="line">    zsh-syntax-highlighting</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>修改 <code>~/.zshrc</code> 后，记得运行 <code>source ~/.zshrc</code> 来使更改生效。</p><h2 id="2-3-Zsh-默认主题概览"><a href="#2-3-Zsh-默认主题概览" class="headerlink" title="2.3 Zsh 默认主题概览"></a>2.3 Zsh 默认主题概览</h2><p><img src="/../../images/tools/zsh.png" alt="zsh"></p><p>上图显示的是 <code>Zsh</code> 默认主题。可以看到, 其相比较传统的<code>bash</code>提供了更多有用的信息和功能:</p><ol><li>在安装<code>git</code>插件后, 当你处于<code>git</code>仓库目录时, <code>zsh</code>会自动将在<code>prompt</code>中显示当前分支名。</li><li><code>zsh</code>提供丰富的智能不全, 比如安装<code>git</code>插件后, 可以补全<code>git</code>的子命令</li><li>该默认主题箭头的颜色表达了上一个命令的执行状态: <ul><li>绿色表示上一个命令成功执行</li><li>红色表示上一个命令执行失败</li></ul></li></ol><blockquote><p>更多的插件请参考 <a href="https://github.com/ohmyzsh/ohmyzsh">Oh My Zsh 插件列表</a></p></blockquote><h1 id="3-Zoxide：更智能的目录切换"><a href="#3-Zoxide：更智能的目录切换" class="headerlink" title="3 Zoxide：更智能的目录切换"></a>3 Zoxide：更智能的目录切换</h1><p>你是否厌倦了在命令行中频繁地 <code>cd</code> 到深层目录？<strong>Zoxide</strong> 是一个更智能的 <code>cd</code> 命令替代品，它会记住你访问过的目录，并允许你通过模糊匹配快速切换。</p><h2 id="3-1-安装-Zoxide"><a href="#3-1-安装-Zoxide" class="headerlink" title="3.1 安装 Zoxide"></a>3.1 安装 Zoxide</h2><ul><li><strong>macOS (Homebrew)</strong>:<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">brew install zoxide</span><br></pre></td></tr></table></figure></li><li><strong>Ubuntu&#x2F;Debian</strong>:<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo apt install zoxide</span><br></pre></td></tr></table></figure></li><li><strong>其他方式</strong>: 请参考 Zoxide 的 GitHub 仓库获取更详细的安装指南。</li></ul><h2 id="3-2-配置-Zoxide"><a href="#3-2-配置-Zoxide" class="headerlink" title="3.2 配置 Zoxide"></a>3.2 配置 Zoxide</h2><p>安装完成后，你需要将 Zoxide 的初始化脚本添加到你的 Shell 配置文件中（对于 Zsh 是 <code>~/.zshrc</code>）。在 <code>~/.zshrc</code> 文件的末尾添加以下行：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"># ~/.zshrc</span><br><span class="line">eval &quot;$(zoxide init zsh)&quot;</span><br></pre></td></tr></table></figure><p>然后运行 <code>source ~/.zshrc</code>。</p><h2 id="3-3-如何使用-Zoxide"><a href="#3-3-如何使用-Zoxide" class="headerlink" title="3.3 如何使用 Zoxide"></a>3.3 如何使用 Zoxide</h2><p>现在，你可以直接输入目录名的部分或模糊匹配来切换目录，例如：</p><ul><li><code>z myproj</code>：切换到你最近访问过的名为 <code>myproj</code> 的目录。</li><li><code>z code</code>：切换到包含 <code>code</code> 关键字的目录。</li><li><code>zi</code>：启动交互式选择界面，通过模糊匹配选择要切换的目录。</li></ul><hr><h1 id="4-FZF：命令行模糊查找利器"><a href="#4-FZF：命令行模糊查找利器" class="headerlink" title="4 FZF：命令行模糊查找利器"></a>4 FZF：命令行模糊查找利器</h1><p><strong>FZF</strong>（Fuzzy Finder）是一个通用的命令行模糊查找器，它可以与各种命令结合使用，实现快速、交互式的列表过滤和选择。无论是文件、历史命令、Git 分支，FZF 都能让你轻松找到所需。</p><h2 id="4-1-安装-FZF"><a href="#4-1-安装-FZF" class="headerlink" title="4.1 安装 FZF"></a>4.1 安装 FZF</h2><ul><li><strong>macOS (Homebrew)</strong>:<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">brew install fzf</span><br></pre></td></tr></table></figure></li><li><strong>Linux (推荐 Git 克隆)</strong>:<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">clone</span> --depth 1 https://github.com/junegunn/fzf.git ~/.fzf</span><br><span class="line">~/.fzf/install</span><br></pre></td></tr></table></figure>在安装过程中，它会询问你是否将 FZF 的键绑定和自动补全添加到你的 Shell 配置中，请选择 <code>y</code>。</li></ul><h2 id="4-2-FZF-的常用场景"><a href="#4-2-FZF-的常用场景" class="headerlink" title="4.2 FZF 的常用场景"></a>4.2 FZF 的常用场景</h2><ul><li><p><strong>文件查找</strong>:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">vim $(fzf) <span class="comment"># 使用 fzf 查找文件并用 vim 打开</span></span><br></pre></td></tr></table></figure></li><li><p><strong>历史命令搜索</strong>: 按下 <code>Ctrl + R</code>（在 Zsh 中默认已配置），然后输入关键词进行历史命令的模糊搜索。</p></li><li><p><strong>进程查找</strong>:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ps aux | fzf <span class="comment"># 查找并过滤进程</span></span><br></pre></td></tr></table></figure></li><li><p><strong>历史命令搜索</strong>:<br><code>fzf</code>可以搜索历史命令，只需按下 <code>Ctrl + R</code>（在 Zsh 中默认已配置），然后输入关键词进行历史命令的模糊搜索。例如:</p><p><img src="/../../images/tools/fzf-ctrl-R.png" alt="fzf-ctrl-R"></p></li></ul><hr><h1 id="5-Ripgrep：极速文件内容搜索"><a href="#5-Ripgrep：极速文件内容搜索" class="headerlink" title="5 Ripgrep：极速文件内容搜索"></a>5 Ripgrep：极速文件内容搜索</h1><p><strong>Ripgrep</strong> 是一个极速的行式搜索工具，它递归地在当前目录中搜索正则表达式模式。它比 <code>grep</code> 更快，并且默认会忽略 <code>.gitignore</code> 中的文件，极大提高了搜索效率。</p><h2 id="5-1-安装-Ripgrep"><a href="#5-1-安装-Ripgrep" class="headerlink" title="5.1 安装 Ripgrep"></a>5.1 安装 Ripgrep</h2><ul><li><strong>macOS (Homebrew)</strong>:<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">brew install ripgrep</span><br></pre></td></tr></table></figure></li><li><strong>Ubuntu&#x2F;Debian</strong>:<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo apt install ripgrep</span><br></pre></td></tr></table></figure></li><li><strong>Fedora&#x2F;RHEL</strong>:<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo dnf install ripgrep</span><br></pre></td></tr></table></figure></li></ul><h2 id="5-2-如何使用-Ripgrep"><a href="#5-2-如何使用-Ripgrep" class="headerlink" title="5.2 如何使用 Ripgrep"></a>5.2 如何使用 Ripgrep</h2><p>Ripgrep 的基本用法与 <code>grep</code> 类似，但速度更快且功能更强大。</p><ul><li><strong>搜索文件内容</strong>:<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rg <span class="string">&quot;your_pattern&quot;</span> <span class="comment"># 在当前目录递归搜索 &quot;your_pattern&quot;</span></span><br></pre></td></tr></table></figure></li><li><strong>显示行号</strong>:<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rg -n <span class="string">&quot;your_pattern&quot;</span></span><br></pre></td></tr></table></figure></li><li><strong>只搜索特定类型文件</strong>:<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rg <span class="string">&quot;your_pattern&quot;</span> -t js <span class="comment"># 只搜索 JavaScript 文件</span></span><br></pre></td></tr></table></figure></li><li><strong>不区分大小写</strong>:<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rg -i <span class="string">&quot;your_pattern&quot;</span></span><br></pre></td></tr></table></figure></li></ul><h2 id="5-3-Ripgrep-与-Grep-的不同之处及默认行为"><a href="#5-3-Ripgrep-与-Grep-的不同之处及默认行为" class="headerlink" title="5.3 Ripgrep 与 Grep 的不同之处及默认行为"></a>5.3 Ripgrep 与 Grep 的不同之处及默认行为</h2><p>Ripgrep 在设计上旨在提供比传统 <code>grep</code> 更优越的体验。除了速度上的显著优势外，它还有一些默认行为与 <code>grep</code> 不同，这些差异通常能更好地满足现代开发的需求：</p><ul><li><strong>默认忽略 <code>.gitignore</code> 文件</strong>: Ripgrep 会自动读取当前目录及其父目录中的 <code>.gitignore</code> 文件，并忽略其中指定的文件和目录。这意味着在搜索代码仓库时，您不必担心搜索到编译生成的文件、依赖包目录（如 <code>node_modules</code>）或其他无关文件，从而使搜索结果更加聚焦和准确。如果您需要搜索被 <code>.gitignore</code> 忽略的文件，可以使用 <code>-u</code> 或 <code>--unrestricted</code> 选项：<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rg -u <span class="string">&quot;your_pattern&quot;</span> <span class="comment"># 搜索所有文件，包括被 .gitignore 忽略的</span></span><br></pre></td></tr></table></figure></li><li><strong>默认忽略隐藏文件和二进制文件</strong>: Ripgrep 默认情况下会跳过隐藏文件（以 <code>.</code> 开头的文件和目录）以及被识别为二进制的文件。这可以避免在搜索结果中出现大量无关的二进制数据，并提高搜索效率。</li><li><strong>智能识别文件类型</strong>: Ripgrep 能够智能地识别多种文件类型（例如 <code>js</code>、<code>py</code>、<code>md</code> 等），并支持按文件类型进行过滤搜索，这比 <code>grep</code> 更为方便。</li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;本人将为新人介绍常用的 &lt;code&gt;Shell&lt;/code&gt; 配置和工具，帮助提升命令行效率。本文将涵盖 &lt;code&gt;Zsh&lt;/code&gt;、&lt;code&gt;Oh My Zsh&lt;/code&gt;、&lt;code&gt;Zoxide&lt;/code&gt;、&lt;code&gt;FZF&lt;/code&gt; 和 &lt;code</summary>
      
    
    
    
    <category term="实用工具" scheme="http://example.com/categories/%E5%AE%9E%E7%94%A8%E5%B7%A5%E5%85%B7/"/>
    
    
    <category term="WSL" scheme="http://example.com/tags/WSL/"/>
    
  </entry>
  
  <entry>
    <title>C++从零开始实现LSM-Tree-KV存储-22-代码覆盖率</title>
    <link href="http://example.com/2025/05/27/%E9%A1%B9%E7%9B%AE/Tiny-LSM/C++%E4%BB%8E%E9%9B%B6%E5%BC%80%E5%A7%8B%E5%AE%9E%E7%8E%B0LSM-Tree-KV%E5%AD%98%E5%82%A8-22-%E4%BB%A3%E7%A0%81%E8%A6%86%E7%9B%96%E7%8E%87/"/>
    <id>http://example.com/2025/05/27/%E9%A1%B9%E7%9B%AE/Tiny-LSM/C++%E4%BB%8E%E9%9B%B6%E5%BC%80%E5%A7%8B%E5%AE%9E%E7%8E%B0LSM-Tree-KV%E5%AD%98%E5%82%A8-22-%E4%BB%A3%E7%A0%81%E8%A6%86%E7%9B%96%E7%8E%87/</id>
    <published>2025-05-27T03:10:16.000Z</published>
    <updated>2026-05-28T07:37:46.670Z</updated>
    
    <content type="html"><![CDATA[<p>我们的主线内容其实都已经结束了, 这里先补充一些项目建设性方面的内容, 首先就是根据单元测试生成代码覆盖率的测试报告。</p><p>代码仓库：<a href="https://github.com/Vanilla-Beauty/tiny-lsm">Vanilla-Beauty&#x2F;tiny-lsm: A KV storage engine based on LSM Tree, supporting Redis RESP</a> 感谢您的 <code>Star</code>!</p><p>欢迎支持本项目同步更新的从零开始实现的视频教程：<a href="https://avo6166ew2u.feishu.cn/docx/LXmVdezdsoTBRaxC97WcHwGunOc">https://avo6166ew2u.feishu.cn/docx/LXmVdezdsoTBRaxC97WcHwGunOc</a></p><p>欢迎加入讨论群：<a href="https://qm.qq.com/q/lkIzCPvcdM">Tiny-LSM项目交流群</a></p><h1 id="1-为什么需要代码覆盖率？"><a href="#1-为什么需要代码覆盖率？" class="headerlink" title="1 为什么需要代码覆盖率？"></a>1 为什么需要代码覆盖率？</h1><p>代码覆盖率测试是软件开发过程中验证测试完整性的重要手段，它通过统计程序中被执行的代码比例来评估测试用例的有效性。常见的代码覆盖率类型包括：</p><ul><li><strong>行覆盖率（Line Coverage）</strong>：衡量源文件中被测试执行的代码行数。</li><li><strong>函数覆盖率（Function Coverage）</strong>：确认每个函数是否都被调用。</li><li><strong>分支覆盖率（Branch Coverage）</strong>：检查程序中的条件判断是否都被覆盖。</li></ul><h2 id="常见工具"><a href="#常见工具" class="headerlink" title="常见工具"></a>常见工具</h2><p>以下是几种主流的代码覆盖率测试工具，按语言和技术栈分类说明：</p><table><thead><tr><th>工具</th><th>语言&#x2F;平台</th><th>简要说明</th></tr></thead><tbody><tr><td><strong>gcov &#x2F; lcov</strong></td><td>C&#x2F;C++ (GCC)</td><td><code>gcov</code> 是 GCC 自带的覆盖率分析工具，<code>lcov</code> 是其可视化前端，可生成 HTML 报告。</td></tr><tr><td><strong>Valgrind + Gcov</strong></td><td>C&#x2F;C++</td><td>结合使用 Valgrind 的 callgrind 工具进行更精确的覆盖率分析。</td></tr><tr><td><strong>Coverage.py</strong></td><td>Python</td><td>针对 Python 的代码覆盖率工具，支持分支覆盖率，并集成 unittest 和 pytest。</td></tr><tr><td><strong>JaCoCo</strong></td><td>Java</td><td>Java 平台的标准覆盖率工具，支持 Ant、Maven、Gradle 构建系统，能与 Jenkins 等 CI 工具集成。</td></tr><tr><td><strong>Istanbul &#x2F; nyc</strong></td><td>JavaScript</td><td>常用于 Node.js 项目，支持 ES6+，并能生成详细的 HTML 报告。</td></tr><tr><td><strong>dotCover</strong></td><td>.NET</td><td>JetBrains 提供的商业工具，用于 .NET 框架和 .NET Core 的单元测试覆盖率分析。</td></tr><tr><td><strong>OpenCppCoverage</strong></td><td>C&#x2F;C++ (Windows)</td><td>支持 Windows 平台的代码覆盖率工具，适合 MSVC 编译器环境。</td></tr></tbody></table><h2 id="常见的工作流"><a href="#常见的工作流" class="headerlink" title="常见的工作流"></a>常见的工作流</h2><ol><li><p><strong>自动化测试流程整合</strong>：</p><ul><li>可将覆盖率测试脚本集成到 CI&#x2F;CD 流水线（如 GitHub Actions、GitLab CI、Jenkins）中，确保每次提交都自动运行并生成报告。</li></ul></li><li><p><strong>质量门禁设置</strong>：</p><ul><li>利用工具提供的阈值配置功能（例如 <code>lcov</code> 配合 <code>genhtml</code> 或 <code>coverage.py</code>），设定最低覆盖率要求，低于该值则构建失败。</li></ul></li><li><p><strong>持续改进</strong>：</p><ul><li>定期查看覆盖率报告，针对未覆盖代码设计补充测试用例，提升整体测试质量和代码可靠性。</li></ul></li></ol><hr><h1 id="2-工具链使用介绍"><a href="#2-工具链使用介绍" class="headerlink" title="2 工具链使用介绍"></a>2 工具链使用介绍</h1><h2 id="2-1-工具链说明"><a href="#2-1-工具链说明" class="headerlink" title="2.1 工具链说明"></a>2.1 工具链说明</h2><p>本项目由于采用了 <code>xmake</code> 构建系统，因此我们将使用 <code>xmake</code>、<code>lcov</code> 和 <code>genhtml</code> 等工具来生成代码覆盖率报告。以下是这些工具的简要说明：</p><table><thead><tr><th>工具</th><th>说明</th></tr></thead><tbody><tr><td><strong>xmake</strong></td><td>一款现代构建系统，简洁灵活，支持多平台和构建模式（如覆盖率模式）。 （这个系列用了这么久就不再过多介绍了  ）</td></tr><tr><td><strong>lcov</strong></td><td><code>gcov</code> 的图形化封装，生成 <code>.info</code> 格式的覆盖率数据文件。</td></tr><tr><td><strong>genhtml</strong></td><td>读取 <code>.info</code> 文件并生成 HTML 格式的可视化代码覆盖率报告。</td></tr><tr><td><strong>gcov</strong></td><td>GCC 的代码覆盖率分析工具，生成 <code>.gcno</code>、<code>.gcda</code> 数据文件供 <code>lcov</code> 使用。</td></tr></tbody></table><h2 id="2-2-工具链安装"><a href="#2-2-工具链安装" class="headerlink" title="2.2 工具链安装"></a>2.2 工具链安装</h2><p>为了顺利使用 <code>xmake</code>、<code>lcov</code> 和 <code>genhtml</code> 工具生成代码覆盖率报告，需要在系统中安装这些工具。以下是各工具的<strong>安装说明</strong>，按不同操作系统分类。</p><h3 id="2-2-1-lcov-安装"><a href="#2-2-1-lcov-安装" class="headerlink" title="2.2.1 lcov 安装"></a>2.2.1 lcov 安装</h3><p><code>lcov</code> 是基于 <code>gcov</code> 的前端工具，用于收集和处理覆盖率数据。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">sudo apt-get install lcov <span class="comment"># Ubuntu/Debian</span></span><br><span class="line">sudo dnf install lcov <span class="comment"># Fedora</span></span><br><span class="line">brew install lcov <span class="comment"># macOS（Homebrew）</span></span><br></pre></td></tr></table></figure><p>验证是否安装成功：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">lcov --version</span><br></pre></td></tr></table></figure><hr><h3 id="2-2-2-genhtml-安装"><a href="#2-2-2-genhtml-安装" class="headerlink" title="2.2.2 genhtml 安装"></a>2.2.2 genhtml 安装</h3><p><code>genhtml</code> 是 <code>lcov</code> 的一部分，安装 <code>lcov</code> 后会自动包含 <code>genhtml</code>，无需单独安装。</p><p>验证是否可用：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">genhtml --version</span><br></pre></td></tr></table></figure><hr><h3 id="2-2-3-gcov-安装（GCC-工具链）"><a href="#2-2-3-gcov-安装（GCC-工具链）" class="headerlink" title="2.2.3 gcov 安装（GCC 工具链）"></a>2.2.3 gcov 安装（GCC 工具链）</h3><p><code>gcov</code> 是 GCC 自带的覆盖率分析工具，通常随编译器一起安装。</p><h4 id="Linux"><a href="#Linux" class="headerlink" title="Linux"></a>Linux</h4><p>确保已安装 GCC：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo apt-get install gcc g++</span><br></pre></td></tr></table></figure><h4 id="macOS"><a href="#macOS" class="headerlink" title="macOS"></a>macOS</h4><p>默认可能使用的是 Clang，需手动安装 GCC（例如通过 Homebrew）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">brew install gcc</span><br></pre></td></tr></table></figure><p>验证是否安装成功：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">gcov --version</span><br></pre></td></tr></table></figure><blockquote><p>提示：</p><ul><li>若使用的是 CI 环境（如 GitHub Actions），可在 <code>.yml</code> 文件中添加上述安装命令。</li><li>对于 Windows 平台若使用 MSVC 编译器，可考虑使用 <a href="https://github.com/OpenCppCoverage/OpenCppCoverage">OpenCppCoverage</a> 替代方案。</li></ul></blockquote><h1 id="3-获取代码覆盖率报告"><a href="#3-获取代码覆盖率报告" class="headerlink" title="3 获取代码覆盖率报告"></a>3 获取代码覆盖率报告</h1><h2 id="3-1-配置文件修改"><a href="#3-1-配置文件修改" class="headerlink" title="3.1 配置文件修改"></a>3.1 配置文件修改</h2><p>为了获取代码覆盖率测试, 我们在编译和链接文件时需要附上<code>--coverage</code>标志, 我们不妨再设置一个编译模式:</p><figure class="highlight lua"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 在 coverage 模式下设置 flags</span></span><br><span class="line">add_rules(<span class="string">&quot;mode.debug&quot;</span>, <span class="string">&quot;mode.release&quot;</span>, <span class="string">&quot;mode.coverage&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> is_mode(<span class="string">&quot;coverage&quot;</span>) <span class="keyword">then</span></span><br><span class="line">    add_cxxflags(<span class="string">&quot;--coverage&quot;</span>)</span><br><span class="line">    add_ldflags(<span class="string">&quot;--coverage&quot;</span>)</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure><p>在 <code>coverage</code> 模式下：</p><ul><li>使用 <code>--coverage</code> 编译器参数，以生成用于覆盖率分析的额外信息。</li><li>使用 <code>--coverage</code> 链接器参数，以确保链接了必要的覆盖率运行库（如 <code>gcov</code>）。</li></ul><h2 id="3-2-测试脚本说明"><a href="#3-2-测试脚本说明" class="headerlink" title="3.2 测试脚本说明"></a>3.2 测试脚本说明</h2><p>脚本文件示例：<code>generate_coverage.sh</code></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#!/usr/bin/bash</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 清理旧的构建和覆盖率报告</span></span><br><span class="line"><span class="built_in">rm</span> -rf build/*</span><br><span class="line"><span class="built_in">rm</span> -rf coverage-report</span><br><span class="line"></span><br><span class="line"><span class="comment"># 配置为覆盖率构建模式</span></span><br><span class="line">xmake f -m coverage</span><br><span class="line"></span><br><span class="line"><span class="comment"># 编译项目</span></span><br><span class="line">xmake</span><br><span class="line"></span><br><span class="line"><span class="comment"># 执行所有测试（确保所有逻辑路径被触发）</span></span><br><span class="line">xmake run-all-tests</span><br><span class="line"></span><br><span class="line"><span class="comment"># 收集覆盖率数据</span></span><br><span class="line">lcov --capture --directory . --output-file coverage.info</span><br><span class="line"></span><br><span class="line"><span class="comment"># 去除无关路径（如系统库、xmake 缓存、测试代码等）</span></span><br><span class="line">lcov --remove coverage.info <span class="string">&#x27;/usr/*&#x27;</span> <span class="string">&#x27;*/.xmake/*&#x27;</span> <span class="string">&#x27;*/test/*&#x27;</span> -o coverage.cleaned.info</span><br><span class="line"></span><br><span class="line"><span class="comment"># 生成 HTML 格式的覆盖率报告</span></span><br><span class="line">genhtml -o coverage-report coverage.cleaned.info</span><br></pre></td></tr></table></figure><p>为了更好地理解 <code>generate_coverage.sh</code>, 以下是对整个流程中关键步骤的详细解释。</p><h2 id="3-3-获取代码覆盖率的基本原理"><a href="#3-3-获取代码覆盖率的基本原理" class="headerlink" title="3.3 获取代码覆盖率的基本原理"></a>3.3 获取代码覆盖率的基本原理</h2><h3 id="3-3-1-编译阶段：插入覆盖率信息"><a href="#3-3-1-编译阶段：插入覆盖率信息" class="headerlink" title="3.3.1 编译阶段：插入覆盖率信息"></a>3.3.1 编译阶段：插入覆盖率信息</h3><p>在使用 GCC 编译器时，通过添加编译选项（如 <code>-fprofile-arcs</code> 和 <code>-ftest-coverage</code> 或直接使用 <code>--coverage</code>），编译器会在生成的目标文件中插入额外的探针（instrumentation code）：</p><ul><li><p><strong><code>.gcno</code> 文件</strong>：</p><ul><li>在编译阶段生成。</li><li>包含程序结构信息（如函数、基本块、分支等）。</li><li>用于将运行时数据映射回源码。</li></ul></li><li><p><strong><code>__gcov_init</code>, <code>__gcov_exit</code> 等调用</strong>：</p><ul><li>插入到函数入口和出口。</li><li>用于记录执行路径。</li></ul></li></ul><h3 id="3-3-2-运行阶段：收集执行路径数据"><a href="#3-3-2-运行阶段：收集执行路径数据" class="headerlink" title="3.3.2 运行阶段：收集执行路径数据"></a>3.3.2 运行阶段：收集执行路径数据</h3><p>当程序运行时，这些探针会记录每条语句和分支是否被执行，并在程序正常退出时将结果写入 <code>.gcda</code> 文件：</p><ul><li><strong><code>.gcda</code> 文件</strong>：<ul><li>每个 <code>.o</code> 文件对应一个 <code>.gcda</code> 文件。</li><li>包含该模块中各行代码&#x2F;分支的实际执行次数。</li></ul></li></ul><blockquote><p>⚠️ 注意：程序必须正常退出（如调用 <code>exit()</code> 或从 <a href="file://d:\CSLearn\CSBlog\ch_img_shape.py#L31-L41">main</a> 返回），否则可能无法正确生成 <code>.gcda</code> 文件。</p></blockquote><h3 id="3-3-3-数据采集与过滤"><a href="#3-3-3-数据采集与过滤" class="headerlink" title="3.3.3 数据采集与过滤"></a>3.3.3 数据采集与过滤</h3><p>脚本使用 <code>lcov</code> 工具进行数据采集：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">lcov --capture --directory . --output-file coverage.info</span><br></pre></td></tr></table></figure><ul><li>遍历当前目录及其子目录，收集所有 <code>.gcno</code> 和 <code>.gcda</code> 文件。</li><li>将其转换为统一的 <code>coverage.info</code> 格式，便于后续处理。</li></ul><p>随后通过 <code>--remove</code> 命令过滤掉不关心的路径（如系统头文件、构建缓存、测试代码）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">lcov --remove coverage.info <span class="string">&#x27;/usr/*&#x27;</span> <span class="string">&#x27;*/.xmake/*&#x27;</span> <span class="string">&#x27;*/test/*&#x27;</span> -o coverage.cleaned.info</span><br></pre></td></tr></table></figure><p>这一步有助于聚焦项目核心代码，避免干扰。</p><h3 id="3-3-4-报告生成"><a href="#3-3-4-报告生成" class="headerlink" title="3.3.4 报告生成"></a>3.3.4 报告生成</h3><p>最后，使用 <code>genhtml</code> 将 <code>.info</code> 文件转换为 HTML 可视化报告：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">genhtml -o coverage-report coverage.cleaned.info</span><br></pre></td></tr></table></figure><p>生成的 HTML 页面中：</p><ul><li>每个源文件都有对应的覆盖率统计。</li><li>行号颜色区分已覆盖（绿色）、未覆盖（红色）。</li><li>支持点击进入具体函数或文件查看细节。</li></ul><h1 id="4-gcov相关知识点"><a href="#4-gcov相关知识点" class="headerlink" title="4 gcov相关知识点"></a>4 gcov相关知识点</h1><p>为了更清晰地理解代码覆盖率工具链中涉及的命令、参数和文件类型，以下是对 <code>gcov</code> 相关知识点的<strong>结构化整理</strong>，包括编译参数、生成文件、常用命令及其作用。</p><hr><h2 id="4-1-编译参数说明"><a href="#4-1-编译参数说明" class="headerlink" title="4.1 编译参数说明"></a>4.1 编译参数说明</h2><table><thead><tr><th>参数</th><th>含义</th></tr></thead><tbody><tr><td><code>-fprofile-arcs</code></td><td>记录程序执行路径（分支信息），用于分析控制流。</td></tr><tr><td><code>-ftest-coverage</code></td><td>生成 <code>.gcno</code> 文件，记录源码结构信息，供后续覆盖率分析使用。</td></tr><tr><td><code>--coverage</code></td><td>GCC 提供的快捷选项，等价于同时启用 <code>-fprofile-arcs</code> 和 <code>-ftest-coverage</code>，并链接 <code>gcov</code> 运行时库。</td></tr></tbody></table><blockquote><p>✅ 推荐在构建测试版本时使用 <code>--coverage</code>，确保编译器和链接器都正确插入探针。</p></blockquote><hr><h2 id="4-2-生成文件说明"><a href="#4-2-生成文件说明" class="headerlink" title="4.2 生成文件说明"></a>4.2 生成文件说明</h2><table><thead><tr><th>文件类型</th><th>来源阶段</th><th>描述</th></tr></thead><tbody><tr><td><code>.gcno</code></td><td>编译阶段</td><td>包含函数、基本块、分支等源码结构信息，用于将运行时数据映射回源文件。</td></tr><tr><td><code>.gcda</code></td><td>运行阶段</td><td>程序执行后生成，记录每条语句和分支的实际执行次数。</td></tr><tr><td><code>.info</code></td><td><code>lcov</code> 输出</td><td>由 <code>lcov</code> 收集 <code>.gcno</code> 和 <code>.gcda</code> 生成的中间格式文件，便于后续处理。</td></tr></tbody></table><h2 id="4-3-常用命令说明"><a href="#4-3-常用命令说明" class="headerlink" title="4.3 常用命令说明"></a>4.3 常用命令说明</h2><h3 id="4-3-1-lcov-命令"><a href="#4-3-1-lcov-命令" class="headerlink" title="4.3.1 lcov 命令"></a>4.3.1 lcov 命令</h3><table><thead><tr><th>命令</th><th>功能描述</th></tr></thead><tbody><tr><td><code>lcov --capture --directory . --output-file coverage.info</code></td><td>收集当前目录下所有模块的 <code>.gcno</code> 和 <code>.gcda</code> 文件，合并为一个 <code>.info</code> 文件。</td></tr><tr><td><code>lcov --remove coverage.info &#39;/usr/*&#39; &#39;*/.xmake/*&#39; &#39;*/test/*&#39; -o coverage.cleaned.info</code></td><td>去除与项目无关的路径（如系统库、缓存、测试代码），提升报告准确性。</td></tr><tr><td><code>lcov --list coverage.info</code></td><td>查看 <code>.info</code> 文件中的覆盖信息内容。</td></tr><tr><td><code>lcov --extract coverage.info &#39;src/*&#39; -o filtered.info</code></td><td>提取特定路径下的覆盖率数据。</td></tr></tbody></table><h3 id="4-3-2-genhtml-命令"><a href="#4-3-2-genhtml-命令" class="headerlink" title="4.3.2 genhtml 命令"></a>4.3.2 genhtml 命令</h3><table><thead><tr><th>命令</th><th>功能描述</th></tr></thead><tbody><tr><td><code>genhtml -o coverage-report coverage.cleaned.info</code></td><td>将 <code>.info</code> 文件转换为 HTML 格式报告，支持浏览器查看。</td></tr><tr><td><code>genhtml --title &quot;My Project Coverage&quot; -o coverage-report coverage.cleaned.info</code></td><td>自定义报告标题。</td></tr><tr><td><code>genhtml --legend -o coverage-report coverage.cleaned.info</code></td><td>显示详细图例，包括未覆盖行数和百分比。</td></tr></tbody></table><hr><h2 id="4-4-流程简要回顾"><a href="#4-4-流程简要回顾" class="headerlink" title="4.4 流程简要回顾"></a>4.4 流程简要回顾</h2><ol><li><strong>编译阶段</strong>：使用 <code>--coverage</code> 编译，生成 <code>.gcno</code>。</li><li><strong>运行阶段</strong>：执行测试用例，生成 <code>.gcda</code>。</li><li><strong>采集阶段</strong>：使用 <code>lcov --capture</code> 收集 <code>.gcno</code> 和 <code>.gcda</code>。</li><li><strong>过滤阶段</strong>：使用 <code>lcov --remove</code> 移除不关心的路径。</li><li><strong>生成阶段</strong>：使用 <code>genhtml</code> 生成 HTML 报告，可视化展示。</li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;我们的主线内容其实都已经结束了, 这里先补充一些项目建设性方面的内容, 首先就是根据单元测试生成代码覆盖率的测试报告。&lt;/p&gt;
&lt;p&gt;代码仓库：&lt;a href=&quot;https://github.com/Vanilla-Beauty/tiny-lsm&quot;&gt;Vanilla-Beau</summary>
      
    
    
    
    
  </entry>
  
  <entry>
    <title>番外-我做了一个 LSM 存储引擎 Lab 等你来挑战</title>
    <link href="http://example.com/2025/05/08/%E9%A1%B9%E7%9B%AE/Tiny-LSM/%E7%95%AA%E5%A4%96-%E6%88%91%E5%81%9A%E4%BA%86%E4%B8%80%E4%B8%AA%20LSM%20%E5%AD%98%E5%82%A8%E5%BC%95%E6%93%8E%20Lab%20%E7%AD%89%E4%BD%A0%E6%9D%A5%E6%8C%91%E6%88%98/"/>
    <id>http://example.com/2025/05/08/%E9%A1%B9%E7%9B%AE/Tiny-LSM/%E7%95%AA%E5%A4%96-%E6%88%91%E5%81%9A%E4%BA%86%E4%B8%80%E4%B8%AA%20LSM%20%E5%AD%98%E5%82%A8%E5%BC%95%E6%93%8E%20Lab%20%E7%AD%89%E4%BD%A0%E6%9D%A5%E6%8C%91%E6%88%98/</id>
    <published>2025-05-08T09:47:35.000Z</published>
    <updated>2026-05-28T07:37:46.670Z</updated>
    
    <content type="html"><![CDATA[<h2 id="🚀-用-C-从零手写一个「键值数据库」？我真写了！"><a href="#🚀-用-C-从零手写一个「键值数据库」？我真写了！" class="headerlink" title="🚀 用 C++ 从零手写一个「键值数据库」？我真写了！"></a>🚀 用 C++ 从零手写一个「键值数据库」？我真写了！</h2><p>前段时间我做了一件 <em>“硬核又浪漫”</em> 的事：<br>👨‍💻 <strong>自己写了一个 C++ 的 Lab 教程，手把手带你实现一个 LSM-Tree 键值数据库！</strong></p><p>没错，是那种数据库底层原理 + 事务 + 崩溃恢复 + Redis 协议兼容全都有的 <strong>“真·存储引擎”Lab</strong>！</p><p>👉 项目主页：<a href="https://vanilla-beauty.github.io/Tiny-LSM/book/">https://Vanilla-Beauty.github.io/Tiny-LSM/book/</a></p><hr><h3 id="🎯-这个-Lab-是什么？"><a href="#🎯-这个-Lab-是什么？" class="headerlink" title="🎯 这个 Lab 是什么？"></a>🎯 这个 Lab 是什么？</h3><p>这是一个实践型的数据库实验项目，灵感来自 <code>CMU 15445</code> 的 <code>Bustub</code>。<br>但我把它用 <strong>现代 C++17 + Xmake 构建 + 极简风交互文档</strong> 完全重构，适合：</p><p>✅ 想搞懂数据库底层结构的开发者<br>✅ 想用真实项目打磨简历的同学<br>✅ 想提升系统设计和编码能力的朋友</p><hr><h3 id="💡-Lab-你能学到什么？"><a href="#💡-Lab-你能学到什么？" class="headerlink" title="💡 Lab 你能学到什么？"></a>💡 Lab 你能学到什么？</h3><p>每个实验模块从 <strong>基础到进阶</strong>，一步步带你构建一个完整的键值存储引擎：</p><ul><li>🧠 理解 LSM Tree 如何组织磁盘数据，读写高效还耐操</li><li>💾 实现自己的 <strong>WAL（预写日志）机制</strong>，支撑崩溃恢复</li><li>🔀 加入事务 + MVCC，多版本并发控制不再是黑魔法</li><li>🔌 和 Redis 协议对接，把你写的数据库变“Redis”！</li></ul><hr><h3 id="🧑‍🎓-学习之外，它还能帮你："><a href="#🧑‍🎓-学习之外，它还能帮你：" class="headerlink" title="🧑‍🎓 学习之外，它还能帮你："></a>🧑‍🎓 学习之外，它还能帮你：</h3><p>📌 <strong>简历加分项</strong>：</p><blockquote><p>“实现一个带事务支持的 LSM 键值存储引擎，兼容 Redis 协议”<br>这句写进简历，比“做过 todo app”管用太多！</p></blockquote><p>📌 <strong>刷面试原理题不再死记硬背</strong>：<br>什么是 WAL？为什么用 SkipList？Compaction 怎么做？<br>做过一遍，面试问到直接开讲！</p><p>📌 <strong>还能交朋友！</strong><br>我建了 QQ 群和 GitHub Issues 讨论区，很多同学一起交流学习、优化代码，说不定还能组队参加比赛或者写论文～</p><hr><h3 id="📚-适合人群："><a href="#📚-适合人群：" class="headerlink" title="📚 适合人群："></a>📚 适合人群：</h3><ul><li>有 C++ 基础，想挑战硬核项目的开发者</li><li>后端 &#x2F; 数据库方向的求职者</li><li>想写点“能进开源推荐栏”的项目集训者</li><li>大三大四想做毕设的同学（这真是宝藏选题）</li></ul><hr><h3 id="💬-最后说一句："><a href="#💬-最后说一句：" class="headerlink" title="💬 最后说一句："></a>💬 最后说一句：</h3><p>我是真心希望这个项目能帮到更多在路上的开发者。<br>如果你喜欢硬核技术，但又不知道从哪里开始 —— <strong>欢迎来做这个 Tiny-LSM Lab！</strong></p><p>主页在这：<a href="https://vanilla-beauty.github.io/Tiny-LSM/book/">https://Vanilla-Beauty.github.io/Tiny-LSM/book/</a><br>如果你做完了别忘了来告诉我，我们一起优化下一版！</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;🚀-用-C-从零手写一个「键值数据库」？我真写了！&quot;&gt;&lt;a href=&quot;#🚀-用-C-从零手写一个「键值数据库」？我真写了！&quot; class=&quot;headerlink&quot; title=&quot;🚀 用 C++ 从零手写一个「键值数据库」？我真写了！&quot;&gt;&lt;/a&gt;🚀 用 C</summary>
      
    
    
    
    <category term="项目" scheme="http://example.com/categories/%E9%A1%B9%E7%9B%AE/"/>
    
    <category term="Tiny-LSM" scheme="http://example.com/categories/%E9%A1%B9%E7%9B%AE/Tiny-LSM/"/>
    
    
    <category term="C++" scheme="http://example.com/tags/C/"/>
    
    <category term="KV存储" scheme="http://example.com/tags/KV%E5%AD%98%E5%82%A8/"/>
    
  </entry>
  
  <entry>
    <title>C++从零开始实现LSM-Tree-KV存储-21-全局迭代器</title>
    <link href="http://example.com/2025/05/06/%E9%A1%B9%E7%9B%AE/Tiny-LSM/C++%E4%BB%8E%E9%9B%B6%E5%BC%80%E5%A7%8B%E5%AE%9E%E7%8E%B0LSM-Tree-KV%E5%AD%98%E5%82%A8-21-%E5%85%A8%E5%B1%80%E8%BF%AD%E4%BB%A3%E5%99%A8/"/>
    <id>http://example.com/2025/05/06/%E9%A1%B9%E7%9B%AE/Tiny-LSM/C++%E4%BB%8E%E9%9B%B6%E5%BC%80%E5%A7%8B%E5%AE%9E%E7%8E%B0LSM-Tree-KV%E5%AD%98%E5%82%A8-21-%E5%85%A8%E5%B1%80%E8%BF%AD%E4%BB%A3%E5%99%A8/</id>
    <published>2025-05-06T07:39:23.000Z</published>
    <updated>2026-05-28T07:37:46.670Z</updated>
    
    <content type="html"><![CDATA[<p>本章是对目前的<code>LSM Tree</code>功能的补全, 提供全局的迭代器遍历功能。</p><p>代码仓库：<a href="https://github.com/Vanilla-Beauty/tiny-lsm">Vanilla-Beauty&#x2F;tiny-lsm: A KV storage engine based on LSM Tree, supporting Redis RESP</a> 感谢您的 <code>Star</code>!</p><p>欢迎支持本项目同步更新的从零开始实现的视频教程：<a href="https://avo6166ew2u.feishu.cn/docx/LXmVdezdsoTBRaxC97WcHwGunOc">https://avo6166ew2u.feishu.cn/docx/LXmVdezdsoTBRaxC97WcHwGunOc</a></p><p>欢迎加入讨论群：<a href="https://qm.qq.com/q/lkIzCPvcdM">Tiny-LSM项目交流群</a></p><h1 id="1-架构回顾"><a href="#1-架构回顾" class="headerlink" title="1 架构回顾"></a>1 架构回顾</h1><p>首先回顾<code>LSM Tree</code>的架构:</p><p><img src="/../../../images/Proj/LSM/Tiny-LSM-arch.drawio.png" alt="LSM Tree架构"></p><p>可以看到, 越靠近左上角的组件, 优先级越高, 在查询时我们按照优先级依次从<code>active skiplist</code>、<code>frzozen skiplist</code>、<code>l0 sst</code>、<code>l1 sst</code>中查找, 如果查到就返回, 否则继续向下查找。</p><p>但我们要实现全局迭代器就会存在问题, 有点<code>key</code>只在底层的<code>sst</code>中存在, 有的<code>key</code>同时在多个<code>sst</code>和<code>skiplist</code>中存在（此时需要按照优先级进行滤除）。同时，我们还需要考虑到事务<code>id</code>的可见性对某些重复的键值对进行屏蔽。因此，我们需要实现一个全局的迭代器，将以上组件进行串联，最终实现全局迭代器。</p><h1 id="2-全局迭代器的设计"><a href="#2-全局迭代器的设计" class="headerlink" title="2 全局迭代器的设计"></a>2 全局迭代器的设计</h1><h2 id="2-1-数据结构设计"><a href="#2-1-数据结构设计" class="headerlink" title="2.1 数据结构设计"></a>2.1 数据结构设计</h2><p>由于前述分析可知, 我们的这个迭代器就是按照优先级的不同, 将不同组件的迭代器进行逐次遍历, 因此其需要将不同组件的优先级进行区分, 这里可以采用将各个组件的迭代器按序存入一个<code>vector</code>实现, 每个迭代器在<code>vector</code>中的位置就是其优先级, 因此, 我们如下定义头文件:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// include/lsm/level_iterator.h</span></span><br><span class="line"><span class="meta">#<span class="keyword">pragma</span> once</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;../iterator/iterator.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;memory&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;optional&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;shared_mutex&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">LSMEngine</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Level_Iterator</span> : <span class="keyword">public</span> BaseIterator &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="built_in">Level_Iterator</span>() = <span class="keyword">default</span>;</span><br><span class="line">  <span class="built_in">Level_Iterator</span>(std::shared_ptr&lt;LSMEngine&gt; engine_, <span class="type">uint64_t</span> max_tranc_id);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">virtual</span> BaseIterator &amp;<span class="keyword">operator</span>++() <span class="keyword">override</span>;</span><br><span class="line">  <span class="keyword">virtual</span> <span class="type">bool</span> <span class="keyword">operator</span>==(<span class="type">const</span> BaseIterator &amp;other) <span class="type">const</span> <span class="keyword">override</span>;</span><br><span class="line">  <span class="keyword">virtual</span> <span class="type">bool</span> <span class="keyword">operator</span>!=(<span class="type">const</span> BaseIterator &amp;other) <span class="type">const</span> <span class="keyword">override</span>;</span><br><span class="line">  <span class="keyword">virtual</span> value_type <span class="keyword">operator</span>*() <span class="type">const</span> <span class="keyword">override</span>;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> IteratorType <span class="title">get_type</span><span class="params">()</span> <span class="type">const</span> <span class="keyword">override</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="type">uint64_t</span> <span class="title">get_tranc_id</span><span class="params">()</span> <span class="type">const</span> <span class="keyword">override</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="type">bool</span> <span class="title">is_end</span><span class="params">()</span> <span class="type">const</span> <span class="keyword">override</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="type">bool</span> <span class="title">is_valid</span><span class="params">()</span> <span class="type">const</span> <span class="keyword">override</span></span>;</span><br><span class="line"></span><br><span class="line">  BaseIterator::pointer <span class="keyword">operator</span>-&gt;() <span class="type">const</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">  std::shared_ptr&lt;LSMEngine&gt; engine_;</span><br><span class="line">  std::vector&lt;std::shared_ptr&lt;BaseIterator&gt;&gt; iter_vec;</span><br><span class="line">  <span class="type">size_t</span> cur_idx_;</span><br><span class="line">  <span class="type">uint64_t</span> max_tranc_id_;</span><br><span class="line">  <span class="keyword">mutable</span> std::optional&lt;value_type&gt; cached_value; <span class="comment">// 缓存当前值</span></span><br><span class="line">  std::shared_lock&lt;std::shared_mutex&gt; rlock_;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">  <span class="function"><span class="type">void</span> <span class="title">update_current</span><span class="params">()</span> <span class="type">const</span></span>;</span><br><span class="line">  <span class="function">std::pair&lt;<span class="type">size_t</span>, std::string&gt; <span class="title">get_min_key_idx</span><span class="params">()</span> <span class="type">const</span></span>;</span><br><span class="line">  <span class="function"><span class="type">void</span> <span class="title">skip_key</span><span class="params">(<span class="type">const</span> std::string &amp;key)</span></span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>其中, 这里最重要的数据结构就是:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">std::vector&lt;std::shared_ptr&lt;BaseIterator&gt;&gt; iter_vec</span><br></pre></td></tr></table></figure><p>其存储了不同组件的迭代器指针, 因为这里所有迭代器都是<code>BaseIterator</code>的父类, 所以我们可以对各种算法或子迭代器类进行良好的代码复用。</p><p>另外<code>cur_idx_</code>标记当前迭代器应该选择哪一个组件的迭代器进行输出, 也就是当前生效的迭代器在<code>iter_vec</code>中的索引。其余数据结构就是一些简单的记录, 比如获取父<code>engine</code>的智能指针、当前查询事务<code>id</code>、锁和缓存键值对等。</p><h2 id="2-2-构造函数"><a href="#2-2-构造函数" class="headerlink" title="2.2 构造函数"></a>2.2 构造函数</h2><p>构造函数中需要构建这个<code>item_vec</code>, 也就是初始化各个组件的迭代器, 代码如下:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// src/lsm/level_iterator.cpp</span></span><br><span class="line">Level_Iterator::<span class="built_in">Level_Iterator</span>(std::shared_ptr&lt;LSMEngine&gt; engine,</span><br><span class="line">                               <span class="type">uint64_t</span> max_tranc_id)</span><br><span class="line">    : <span class="built_in">engine_</span>(engine), <span class="built_in">max_tranc_id_</span>(max_tranc_id), <span class="built_in">rlock_</span>(engine_-&gt;ssts_mtx) &#123;</span><br><span class="line">  <span class="comment">// 成员变量获取sst读锁</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// 1. 获取内存部分迭代器</span></span><br><span class="line">  <span class="comment">// <span class="doctag">TODO:</span> 这里最好修改 memtable.begin 使其返回一个指针, 避免多余的内存拷贝</span></span><br><span class="line">  <span class="keyword">auto</span> mem_iter = engine_-&gt;memtable.<span class="built_in">begin</span>(max_tranc_id_);</span><br><span class="line">  std::shared_ptr&lt;HeapIterator&gt; mem_iter_ptr = std::<span class="built_in">make_shared</span>&lt;HeapIterator&gt;();</span><br><span class="line">  *mem_iter_ptr = mem_iter;</span><br><span class="line">  iter_vec.<span class="built_in">push_back</span>(mem_iter_ptr);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 2. 获取 L0 层的迭代器</span></span><br><span class="line">  std::vector&lt;SearchItem&gt; item_vec;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">auto</span> &amp;sst_id : engine_-&gt;level_sst_ids[<span class="number">0</span>]) &#123;</span><br><span class="line">    <span class="keyword">auto</span> sst = engine_-&gt;ssts[sst_id];</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">auto</span> iter = sst-&gt;<span class="built_in">begin</span>(max_tranc_id_);</span><br><span class="line">         iter.<span class="built_in">is_valid</span>() &amp;&amp; iter != sst-&gt;<span class="built_in">end</span>(); ++iter) &#123;</span><br><span class="line">      <span class="comment">// 这里越新的sst的idx越大, 我们需要让新的sst优先在堆顶</span></span><br><span class="line">      <span class="comment">// 让新的sst(拥有更大的idx)排序在前面, 反转符号就行了</span></span><br><span class="line">      <span class="keyword">if</span> (max_tranc_id_ != <span class="number">0</span> &amp;&amp; iter.<span class="built_in">get_tranc_id</span>() &gt; max_tranc_id_) &#123;</span><br><span class="line">        <span class="comment">// 如果开启了事务, 比当前事务 id 更大的记录是不可见的</span></span><br><span class="line">        <span class="keyword">continue</span>;</span><br><span class="line">      &#125;</span><br><span class="line">      item_vec.<span class="built_in">emplace_back</span>(iter.<span class="built_in">key</span>(), iter.<span class="built_in">value</span>(), -sst_id, <span class="number">0</span>,</span><br><span class="line">                            iter.<span class="built_in">get_tranc_id</span>());</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  std::shared_ptr&lt;HeapIterator&gt; l0_iter_ptr =</span><br><span class="line">      std::<span class="built_in">make_shared</span>&lt;HeapIterator&gt;(item_vec, max_tranc_id);</span><br><span class="line">  iter_vec.<span class="built_in">push_back</span>(l0_iter_ptr);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 3. 获取其他层的迭代器</span></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">auto</span> &amp;[level, sst_id_list] : engine_-&gt;level_sst_ids) &#123;</span><br><span class="line">    <span class="keyword">if</span> (level == <span class="number">0</span>) &#123;</span><br><span class="line">      <span class="keyword">continue</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    std::vector&lt;std::shared_ptr&lt;SST&gt;&gt; ssts;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">auto</span> sst_id : sst_id_list) &#123;</span><br><span class="line">      <span class="keyword">auto</span> sst = engine_-&gt;ssts[sst_id];</span><br><span class="line">      ssts.<span class="built_in">push_back</span>(sst);</span><br><span class="line">      std::shared_ptr&lt;ConcactIterator&gt; level_i_iter =</span><br><span class="line">          std::<span class="built_in">make_shared</span>&lt;ConcactIterator&gt;(ssts, max_tranc_id);</span><br><span class="line">      iter_vec.<span class="built_in">push_back</span>(level_i_iter);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里迭代器包括:</p><ol><li>内存迭代器<code>mem_iter</code>: 就是遍历整个内存中的键值对, 并使用<code>HeapIterator</code>进行排序和去重, <code>HeapIterator</code>就是使用了一个堆结构, 将所有键值对按照优先级进行排序, 并且去重, 具体内容可以参见之前的第8章。</li><li><code>L0</code>层迭代器<code>l0_iter_ptr</code>: 由于<code>L0</code>这一层的<code>SST</code>之间不是排序且无重叠的, 因此这里同样要求进行去重, 我们仍然使用了<code>HeapIterator</code>实现去重和排序, 这一实现方案存在一定缺陷, 因为<code>L0</code>的数据量还是不小的, 后续可以考虑在这一层嵌套一个<code>Level_Iterator</code>, 因为<code>L0</code>这一层的<code>SST</code>也是按照先后优先级进行排序的, 符合<code>Level_Iterator</code>的迭代器设计原则, 可以减少内存开销。</li><li>其余<code>Level</code>的迭代器<code>level_i_iter</code>: 其余层的迭代器都是按照<code>SST</code>的先后顺序进行排序的, 因此这里直接使用<code>ConcactIterator</code>进行迭代即可, <code>ConcactIterator</code>就是将多个迭代器进行拼接, 并且按照<code>sst</code>的<code>id</code>顺序作为优先级进行排序, 不需要进行额外的去重操作, 且支持惰性的文件<code>IO</code>读取, 避免了文件<code>IO</code>开销。<code>ConcactIterator</code>的内容可以参考第19章中的介绍。</li></ol><blockquote><p>注意代码中的<code>...</code>, 这是非常重要的步骤, 后续进行介绍</p></blockquote><h2 id="2-2-自增运算符重载"><a href="#2-2-自增运算符重载" class="headerlink" title="2.2 自增运算符重载"></a>2.2 自增运算符重载</h2><h3 id="2-2-1-整体重载逻辑"><a href="#2-2-1-整体重载逻辑" class="headerlink" title="2.2.1 整体重载逻辑"></a>2.2.1 整体重载逻辑</h3><p>这里的自增操作的逻辑就是将<code>iter_vec</code>中解引用后等于当前<code>key</code>的迭代器进行自增, 这里的逻辑是:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// src/lsm/level_iterator.cpp</span></span><br><span class="line">BaseIterator &amp;Level_Iterator::<span class="keyword">operator</span>++() &#123;</span><br><span class="line">  <span class="comment">// 先跳过和当前 key 相同的部分</span></span><br><span class="line">  <span class="built_in">skip_key</span>(cached_value-&gt;first);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 重新选择key最小的迭代器</span></span><br><span class="line">  <span class="keyword">while</span> (!<span class="built_in">is_end</span>()) &#123;</span><br><span class="line">    <span class="keyword">auto</span> [min_idx, _] = <span class="built_in">get_min_key_idx</span>();</span><br><span class="line">    cur_idx_ = min_idx;</span><br><span class="line">    <span class="built_in">update_current</span>();</span><br><span class="line">    <span class="keyword">if</span> (cached_value-&gt;second.<span class="built_in">size</span>() == <span class="number">0</span>) &#123;</span><br><span class="line">      <span class="comment">// 如果当前值为空, 说明当前key已经被删除了</span></span><br><span class="line">      <span class="comment">// 需要跳过这个key</span></span><br><span class="line">      <span class="built_in">skip_key</span>(cached_value-&gt;first);</span><br><span class="line">      <span class="keyword">continue</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="comment">// 找到一个合法的键值对, 跳出循环</span></span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> *<span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>首先, 需要将当前活跃的<code>key</code>所属的迭代器进行自增, 也就是<code>skip_key</code>函数所做的事儿:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// src/lsm/level_iterator.cpp</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">Level_Iterator::skip_key</span><span class="params">(<span class="type">const</span> std::string &amp;key)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="type">size_t</span> i = <span class="number">0</span>; i &lt; iter_vec.<span class="built_in">size</span>(); ++i) &#123;</span><br><span class="line">    <span class="keyword">while</span> ((*iter_vec[i]).<span class="built_in">is_valid</span>() &amp;&amp; (**iter_vec[i]).first == key) &#123;</span><br><span class="line">      <span class="comment">// 如果找到当前key, 则跳过这个key</span></span><br><span class="line">      ++(*iter_vec[i]);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里不仅要将当前<code>key</code>所属的活跃迭代器进行自增, 其余解引用后拥有相同<code>key</code>的迭代器也要进行自增, 因为多个迭代器存在相同的<code>key</code>是允许的情况, 只是由于不同迭代器在数组中索引不同或其事务<code>id</code>不同, 才导致只有一个活跃迭代器被选中(即<code>cur_idx_</code>指向的迭代器)。</p><h3 id="2-2-2-重新选择迭代器"><a href="#2-2-2-重新选择迭代器" class="headerlink" title="2.2.2 重新选择迭代器"></a>2.2.2 重新选择迭代器</h3><p>在对原本的<code>当前迭代器</code>的<code>key</code>进行自增后, 需要重新选择当前<code>key</code>最小的迭代器, 也就是重新选择当前<code>key</code>所属的迭代器, 也就是<code>Level_Iterator::operator++()</code>代码中的<code>while</code>循环部分。这里的逻辑是，只要当前迭代器没有到达<code>end</code>, 就选择所有迭代器中<code>key</code>最小的迭代器作为当前迭代器, 这家就是<code>get_min_key_idx</code>的逻辑:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// src/lsm/level_iterator.cpp</span></span><br><span class="line"><span class="function">std::pair&lt;<span class="type">size_t</span>, std::string&gt; <span class="title">Level_Iterator::get_min_key_idx</span><span class="params">()</span> <span class="type">const</span> </span>&#123;</span><br><span class="line">  <span class="type">size_t</span> min_idx = <span class="number">0</span>;</span><br><span class="line">  std::string min_key = <span class="string">&quot;&quot;</span>;</span><br><span class="line">  <span class="keyword">for</span> (<span class="type">size_t</span> i = <span class="number">0</span>; i &lt; iter_vec.<span class="built_in">size</span>(); ++i) &#123;</span><br><span class="line">    <span class="keyword">if</span> (!iter_vec[i]-&gt;<span class="built_in">is_valid</span>()) &#123;</span><br><span class="line">      <span class="comment">// 如果当前迭代器无效, 则跳过</span></span><br><span class="line">      <span class="keyword">continue</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (min_key == <span class="string">&quot;&quot;</span>) &#123;</span><br><span class="line">      <span class="comment">// 第一次初始化</span></span><br><span class="line">      min_key = (**iter_vec[i]).first;</span><br><span class="line">      min_idx = i;</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> ((**iter_vec[i]).first &lt; (**iter_vec[min_idx]).first) &#123;</span><br><span class="line">      <span class="comment">// 更新最小key和索引</span></span><br><span class="line">      min_key = (**iter_vec[i]).first;</span><br><span class="line">      min_idx = i;</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> ((**iter_vec[i]).first == min_key) &#123;</span><br><span class="line">      <span class="comment">// key相同时, 事务id大的排前面</span></span><br><span class="line">      <span class="keyword">if</span> (max_tranc_id_ != <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> ((*iter_vec[i]).<span class="built_in">get_tranc_id</span>() &gt;</span><br><span class="line">            (*iter_vec[min_idx]).<span class="built_in">get_tranc_id</span>()) &#123;</span><br><span class="line">          min_idx = i;</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> std::<span class="built_in">make_pair</span>(min_idx, min_key);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>get_min_key_idx</code>在比较<code>key</code>之前, 还需判断迭代器是否有效, 比较<code>key</code>之后还需要判断事物<code>id</code>的可见性, 这里的<code>max_tranc_id_</code>就是当前事务<code>id</code>, 其查询到的事务的<code>id</code>不能比这个事务<code>id</code>大, 否则该事务的记录是不可见的。</p><h3 id="2-2-3-构造函数的补全"><a href="#2-2-3-构造函数的补全" class="headerlink" title="2.2.3 构造函数的补全"></a>2.2.3 构造函数的补全</h3><p>之前提到了, 构造函数中的<code>...</code>部分需要进行补全, 原因就是, 默认的<code>cur_idx_</code>为0, 但第0个迭代器可能是删除标记，也可能齐全<code>key</code>不是最小的, 又或者其事务<code>id</code>对当前迭代器不可见, 因此也需要进行初始迭代器的选举和滤除:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// src/lsm/level_iterator.cpp</span></span><br><span class="line">Level_Iterator::<span class="built_in">Level_Iterator</span>(std::shared_ptr&lt;LSMEngine&gt; engine,</span><br><span class="line">                               <span class="type">uint64_t</span> max_tranc_id)</span><br><span class="line">    : <span class="built_in">engine_</span>(engine), <span class="built_in">max_tranc_id_</span>(max_tranc_id), <span class="built_in">rlock_</span>(engine_-&gt;ssts_mtx) &#123;</span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">  <span class="comment">// 之前部分的代码</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">while</span> (!<span class="built_in">is_end</span>()) &#123;</span><br><span class="line">    <span class="keyword">auto</span> [min_idx, _] = <span class="built_in">get_min_key_idx</span>();</span><br><span class="line">    cur_idx_ = min_idx;</span><br><span class="line">    <span class="built_in">update_current</span>();</span><br><span class="line">    <span class="keyword">auto</span> cached_kv = *cached_value;</span><br><span class="line">    <span class="keyword">if</span> (cached_kv.second.<span class="built_in">size</span>() == <span class="number">0</span>) &#123;</span><br><span class="line">      <span class="comment">// 如果当前值为空, 说明当前key已经被删除了</span></span><br><span class="line">      <span class="comment">// 需要跳过这个key</span></span><br><span class="line">      <span class="built_in">skip_key</span>(cached_value-&gt;first);</span><br><span class="line">      <span class="keyword">continue</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="comment">// 找到一个合法的键值对, 跳出循环</span></span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="2-3-解引用运算符重载"><a href="#2-3-解引用运算符重载" class="headerlink" title="2.3 解引用运算符重载"></a>2.3 解引用运算符重载</h2><p>这部分就很简单了, 直接获取缓存值即可, 缓存值会在构造函数以及后续每一次自增时进行更新:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// src/lsm/level_iterator.cpp</span></span><br><span class="line">BaseIterator::value_type Level_Iterator::<span class="keyword">operator</span>*() <span class="type">const</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> (!cached_value.<span class="built_in">has_value</span>()) &#123;</span><br><span class="line">    <span class="keyword">throw</span> std::<span class="built_in">runtime_error</span>(<span class="string">&quot;Level_Iterator is invalid&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> *cached_value;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="3-全局迭代器的接口"><a href="#3-全局迭代器的接口" class="headerlink" title="3 全局迭代器的接口"></a>3 全局迭代器的接口</h1><p>现在我们可以在上层组件中暴露<code>begin</code>和<code>end</code>来获取当前迭代器的全局接口了:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// src/lsm/engine.cpp</span></span><br><span class="line"><span class="function">Level_Iterator <span class="title">LSMEngine::begin</span><span class="params">(<span class="type">uint64_t</span> tranc_id)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="built_in">Level_Iterator</span>(<span class="built_in">shared_from_this</span>(), tranc_id);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function">Level_Iterator <span class="title">LSMEngine::end</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> Level_Iterator&#123;&#125;; &#125;</span><br></pre></td></tr></table></figure><p>我们可以在外部如下使用全局迭代器:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// example/main.cpp</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;../include/lsm/engine.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;../include/lsm/level_iterator.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="comment">// create lsm instance, data_dir is the directory to store data</span></span><br><span class="line">  <span class="function">LSM <span class="title">lsm</span><span class="params">(<span class="string">&quot;example_data&quot;</span>)</span></span>;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// iterator</span></span><br><span class="line">  std::cout &lt;&lt; <span class="string">&quot;All key-value pairs:&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">  <span class="comment">// begin(id): id means transaction id, 0 means disable mvcc</span></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">auto</span> it = lsm.<span class="built_in">begin</span>(<span class="number">0</span>); it != lsm.<span class="built_in">end</span>(); ++it) &#123;</span><br><span class="line">    std::cout &lt;&lt; it-&gt;first &lt;&lt; <span class="string">&quot;: &quot;</span> &lt;&lt; it-&gt;second &lt;&lt; std::endl;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="4-小节"><a href="#4-小节" class="headerlink" title="4 小节"></a>4 小节</h1><p>本小节没有什么复杂的内容, 就是赋予不同组件的迭代器不同的优先级, 将其整合形成一个更高级的迭代器。通过这章的学习，相比大家对于<code>C++</code>中迭代器的理解应该会更加清晰, 迭代器是不同组件信息交流的桥梁, 非常重要。</p><p>本项目的初版差不多已经完结了，后续会不定期更新一些功能的补全。然后会开一个新坑————基于目前的单机的存储引擎<code>Tiny-LSM</code>, 进行<code>raft</code>或<code>curp</code>共识算法的封装, 最终形成一个分布式数据库。（不知道什么时候开坑，速度可能有点慢，但肯定不会鸽）。</p><p>如果本项目有帮到你，请给个<code>star</code>吧，您的支持是我继续维护和开发这个项目的动力。如果你想自己从零开始手敲代码实现这个项目，欢迎支持我的付费视频课程：<a href="https://avo6166ew2u.feishu.cn/docx/LXmVdezdsoTBRaxC97WcHwGunOc">https://avo6166ew2u.feishu.cn/docx/LXmVdezdsoTBRaxC97WcHwGunOc</a></p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;本章是对目前的&lt;code&gt;LSM Tree&lt;/code&gt;功能的补全, 提供全局的迭代器遍历功能。&lt;/p&gt;
&lt;p&gt;代码仓库：&lt;a href=&quot;https://github.com/Vanilla-Beauty/tiny-lsm&quot;&gt;Vanilla-Beauty&amp;#x2F;tin</summary>
      
    
    
    
    <category term="项目" scheme="http://example.com/categories/%E9%A1%B9%E7%9B%AE/"/>
    
    <category term="Tiny-LSM" scheme="http://example.com/categories/%E9%A1%B9%E7%9B%AE/Tiny-LSM/"/>
    
    
    <category term="C++" scheme="http://example.com/tags/C/"/>
    
    <category term="KV存储" scheme="http://example.com/tags/KV%E5%AD%98%E5%82%A8/"/>
    
  </entry>
  
  <entry>
    <title>Consul学习笔记1-简介与Demo</title>
    <link href="http://example.com/2025/04/30/%E5%88%86%E5%B8%83%E5%BC%8F/Consul/Consul%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B01-%E7%AE%80%E4%BB%8B%E4%B8%8EDemo/"/>
    <id>http://example.com/2025/04/30/%E5%88%86%E5%B8%83%E5%BC%8F/Consul/Consul%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B01-%E7%AE%80%E4%BB%8B%E4%B8%8EDemo/</id>
    <published>2025-04-30T09:48:18.000Z</published>
    <updated>2026-05-28T07:37:46.665Z</updated>
    
    <content type="html"><![CDATA[<h1 id="1-Consul-简介"><a href="#1-Consul-简介" class="headerlink" title="1. Consul 简介"></a>1. Consul 简介</h1><p><code>Consul</code> 是 <code>HashiCorp</code> 开发的分布式服务管理框架，提供服务发现、健康检查、配置中心和完整的分布式并行性维护技术。它特别适合应用于小型到大型的分布式集群环境，通过同步机制维护集群内定义好的服务和信息。</p><p>官方文档: <a href="https://www.consul.io/">https://www.consul.io/</a><br>代码仓库: <a href="https://github.com/hashicorp/consul">https://github.com/hashicorp/consul</a></p><p><strong>核心特性</strong>：</p><ul><li><strong>服务发现</strong>：支持服务注册与自动发现，便于其他服务快速找到可用的服务实例。</li><li><strong>健康检查</strong>：内置对服务和节点的健康状态监控，能动态剔除不健康的节点。</li><li><strong>KV 存储</strong>：提供键值存储功能，可用于动态配置管理和协调分布式系统的行为。</li><li><strong>多数据中心支持</strong>：天然支持多数据中心部署，确保跨区域服务的高可用性和一致性。</li><li><strong>安全通信</strong>：通过加密和访问控制保障服务间通信的安全性。</li></ul><p><strong>应用场景</strong>：</p><ul><li>微服务架构中的服务注册与发现。</li><li>动态负载均衡和服务路由。</li><li>分布式系统的配置管理。</li><li>健康监控与故障转移。</li></ul><p><code>Consul</code> 的设计目标是简化分布式系统的管理难度，并提供一套统一的工具链来提升开发和运维效率。<code>Consul</code> 自推出以来，因其强大的功能和易用性被广泛应用于多个知名项目和企业中, 例如<code>Netflix</code>在其微服务架构中使用 Consul 进行服务发现与健康检查。</p><p>但由于<code>Consul</code>的特性和功能太多, 而官方的<code>Tutorial</code>使用了<code>AWS</code>等云服务进行讲解, 对于国内的小伙伴不是很友好, 因此这里我使用<code>VMWare</code>虚拟机做一个<code>Consul</code>的小<code>demo</code>, 从这个<code>demo</code>的开发演示中，学习如何使用<code>Consul</code>进行服务注册与发现。</p><p>本章是学习<code>Consul</code>的第一部分, 因此除了<code>demo</code>外不会涉及太多琐碎的知识点, 目的是让大家对<code>Consul</code>的功能、使用方式有一个大体的局具象感知，更详细的使用和原理介绍将在后续章节中进行。</p><h1 id="2-基于虚拟机的-Demo"><a href="#2-基于虚拟机的-Demo" class="headerlink" title="2. 基于虚拟机的 Demo"></a>2. 基于虚拟机的 Demo</h1><p>由于官方的<code>Tutorial</code>比较复杂, 这里我采用一个极简的<code>demo</code>进行演示。</p><h2 id="2-1-环境准备"><a href="#2-1-环境准备" class="headerlink" title="2.1 环境准备"></a>2.1 环境准备</h2><p>环境准备包括以下关键步骤：</p><h3 id="2-1-1-VMWare虚拟机的下载与安装"><a href="#2-1-1-VMWare虚拟机的下载与安装" class="headerlink" title="2.1.1 VMWare虚拟机的下载与安装**"></a>2.1.1 VMWare虚拟机的下载与安装**</h3><p><code>VMWare Workstation Player</code> 可以从官网免费下载（适用于非商业用途），安装后可用于创建和运行虚拟机。</p><p>官方下载地址：<a href="https://www.vmware.com/products/workstation-player.html">https://www.vmware.com/products/workstation-player.html</a></p><blockquote><p>这里采用<code>VMWare</code>而不是<code>WSL</code>的原因是, <code>VMWare</code>默认会给创建的虚拟机分配一个随机的 <code>IP</code> 地址，且不同虚拟机实例的<code>IP</code>不同, 而<code>WSL</code>需要进行一定的配置, 相对麻烦一点。另一方面， <code>VMWare</code>提供虚拟机的快速克隆, 我们可以在一台虚拟机上部署好所有环境, 然后直接克隆即可, 不需要在4个虚拟机实例上分别布置环境依赖</p></blockquote><h3 id="2-1-2-创建4份-Ubuntu-22-04-Server-实例-安装"><a href="#2-1-2-创建4份-Ubuntu-22-04-Server-实例-安装" class="headerlink" title="2.1.2 创建4份 Ubuntu 22.04 Server 实例 安装"></a>2.1.2 创建4份 Ubuntu 22.04 Server 实例 安装</h3><p>创建 4 台 <code>Ubuntu Server 22.04</code> 虚拟机，用于部署 <code>Consul</code> 集群。这里之所以要创建4台虚拟机, 是因为<code>consul</code>的<code>sever</code>集群采用的是基于<code>raft</code>的<code>KV</code>存储, 因此至少要3个节点, 才能形成<code>server</code>集群, 另外我们还需要一个<code>client</code>节点，其是被<code>server</code>服务的对象。</p><blockquote><p>这里不推荐<code>Desktop</code>版本的<code>Ubuntu</code>, 因为我们要安装4份虚拟机实例, 要是使用<code>Desktop</code>版本的话, 内存压力会非常大! 同时, <code>VMWare</code>虚拟机的安装方式我也不介绍了, 默认大家有基础</p></blockquote><h3 id="2-1-3-安装开发工具链（Go、jq-等）"><a href="#2-1-3-安装开发工具链（Go、jq-等）" class="headerlink" title="2.1.3 安装开发工具链（Go、jq 等）"></a>2.1.3 安装开发工具链（Go、jq 等）</h3><ul><li><code>Go</code>: 用于编译和运行服务注册代码, 我们的服务用<code>Go</code>写起来最方便, 因为<code>consul</code>本身使用<code>Go</code>作为开发语言, 可以直接<code>import</code>官方源码仓库的<code>api</code>在<code>go</code>代码中直接进行集群相关操作。</li><li><code>jq</code>: 用于命令行下对 JSON 数据格式化查看，便于调试和验证 <code>Consul</code> 网络接口返回的数据。</li><li><code>OpenSSh-Server</code>: 用于在宿主机和虚拟机之间进行 <code>SSH</code> 登录，方便调试和验证, 总不可能在虚拟机这样丑陋的环境里敲命令吧…</li><li><code>VSCode</code>：远程连接虚拟机编写配置文件和代码</li></ul><blockquote><p>至于 <code>SSH</code>秘钥配置、<code>VSCode</code>插件配置, <code>jq</code>的使用方法等, 我就不具体介绍了, 默认大家有一定的基础</p></blockquote><h3 id="2-1-4-获取-Consul-可执行文件"><a href="#2-1-4-获取-Consul-可执行文件" class="headerlink" title="2.1.4 获取 Consul 可执行文件"></a>2.1.4 获取 Consul 可执行文件</h3><p>可以直接从 <a href="https://www.consul.io/downloads">Consul 官网</a> 下载对应操作系统的二进制文件，解压后将 <code>consul</code> 放入系统路径 <code>/usr/local/bin/</code> 即可使用；也可以通过源码编译生成可执行文件。</p><h2 id="2-2-启动-agent"><a href="#2-2-启动-agent" class="headerlink" title="2.2 启动 agent"></a>2.2 启动 agent</h2><h3 id="2-2-1-agent-的基本概念"><a href="#2-2-1-agent-的基本概念" class="headerlink" title="2.2.1 agent 的基本概念"></a>2.2.1 agent 的基本概念</h3><p>这里先对 <code>Consul</code> 中的重要概念 <code>agent</code> 进行介绍：</p><p><code>Consul</code> 以 <strong>agent</strong> 的形式运行在每一个节点上，agent 可以是 <strong>server</strong> 或 <strong>client</strong> 角色。<code>server</code> 负责参与 Raft 协议、维护集群状态和处理请求；<code>client</code> 则负责注册服务、运行健康检查，并将请求转发给 <code>server</code> 集群。</p><p>每个 <code>agent</code> 独立运行，通过配置文件（如 <code>.hcl</code> 文件）定义其行为，包括节点名、IP 地址、运行模式、数据目录、通信端口等信息。agent 启动后会尝试加入集群，并持续与其他节点保持通信以维护集群视图和服务状态。</p><p>在本 <code>demo</code> 中，我们分别启动了 3 个<code> server agent</code> 和 1 个 <code>client agent</code>，构成一个最小化的 Consul 集群。</p><h3 id="2-2-2-server-agent-配置文件"><a href="#2-2-2-server-agent-配置文件" class="headerlink" title="2.2.2 server agent 配置文件"></a>2.2.2 server agent 配置文件</h3><p>这里<code>server</code>的<code>consul aent</code>的启动命令如下：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">consul agent -config-file=server.hcl</span><br></pre></td></tr></table></figure><p>这里的<code>server.hcl</code>是配置文件, 其内容如下：</p><figure class="highlight toml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">datacenter</span> = <span class="string">&quot;dc1&quot;</span></span><br><span class="line"><span class="attr">node_name</span>  = <span class="string">&quot;consul-server-1&quot;</span></span><br><span class="line"><span class="attr">data_dir</span>   = <span class="string">&quot;/home/toni/consul-tutorial/consul-data&quot;</span></span><br><span class="line"><span class="attr">bind_addr</span>  = <span class="string">&quot;192.168.110.129&quot;</span> <span class="comment"># ! 当前节点的内网地址</span></span><br><span class="line"><span class="attr">client_addr</span> = <span class="string">&quot;0.0.0.0&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="attr">server</span> = <span class="literal">true</span></span><br><span class="line"><span class="attr">bootstrap_expect</span> = <span class="number">3</span></span><br><span class="line"></span><br><span class="line">ui_config &#123;</span><br><span class="line">  <span class="attr">enabled</span> = <span class="literal">true</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="attr">log_level</span> = <span class="string">&quot;INFO&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># ! 其他 Server 节点的地址, 需要排除自身</span></span><br><span class="line"><span class="attr">retry_join</span> = [</span><br><span class="line">    <span class="string">&quot;192.168.110.130&quot;</span>,</span><br><span class="line">    <span class="string">&quot;192.168.110.132&quot;</span></span><br><span class="line">]</span><br></pre></td></tr></table></figure><p>这里说明如下:</p><ul><li><code>datacenter</code>: 设置当前节点所属的数据中心名称为 <code>dc1</code>。Consul 支持多数据中心部署，该配置用于标识该节点归属于哪个数据中心。数据中心是部署的最高级别单位，通常对应于一个物理数据中心或云提供商的一个区域<br>-<code>node_name</code>: 指定当前 <code>Consul</code> 节点（<code>agent</code>）的唯一名称，在集群中通过这个名称来识别不同的节点。</li><li><code>data_dir</code>: 定义 <code>Consul</code> 持久化数据的存储目录，例如节点状态、服务信息和 <code>Raft</code> 日志等都保存在此路径下。</li><li><code>bind_addr</code>: 设置当前节点与其他 <code>Consul agent</code> 通信时绑定的 IP 地址。不需要指定端口, 内部会指定一个默认端口。</li><li><code>client_addr</code>: 指定客户端接口监听的地址，用于接收 HTTP&#x2F;DNS&#x2F;gRPC 等客户端请求。设置为 <code>0.0.0.0</code> 表示允许从任意 IP 访问 API 接口。</li><li><code>server = true</code>: 表示该节点是以 <strong>server</strong> 角色运行。<code>server</code> 是 <code>Consul</code> 集群的核心组件，负责参与 <code>Raft</code> 协议、维护集群一致性等关键任务。</li><li><code>bootstrap_expect</code>: 声明集群中预期加入的 <code>server</code> 节点总数。当第一个 <code>server</code> 启动时，它会等待直到有 3 个 <code>server</code> 加入后才进行 <code>Raft</code> 初始化，从而避免脑裂问题。</li><li><code>ui_config</code>: 启用内置 Web UI 界面，可以通过浏览器访问 <code>Consul</code> 的可视化界面，查看节点、服务、健康状态等信息。</li><li><code>log_level</code>: 设置日志输出级别为 <code>INFO</code>，即显示正常运行日志。也可以设为 <code>DEBUG</code> 或 <code>ERROR</code> 来控制日志详细程度。</li><li><code>retry_join</code>: 配置自动重试加入的其他 <code>Consul</code> 节点地址。在启动过程中，如果首次连接失败，<code>Consul</code> 会持续尝试与这些节点建立连接，以加入集群。此处列出的是其他两个 <code>server</code> 节点的 IP 地址。</li></ul><blockquote><p>注意：每个 <code>server</code> 节点的 <code>bind_addr</code> 和 <code>node_name</code> 应根据实际机器进行修改，而 <code>retry_join</code> 列表应包含除自身外的所有 server 节点地址。</p></blockquote><h2 id="2-2-3-client-agent-配置文件"><a href="#2-2-3-client-agent-配置文件" class="headerlink" title="2.2.3 client agent 配置文件"></a>2.2.3 client agent 配置文件</h2><p>这里<code>client</code>的<code>consul aent</code>的启动命令如下：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">consul agent -config-file=client.hcl</span><br></pre></td></tr></table></figure><p>这里的<code>client.hcl</code>是配置文件, 其内容如下：</p><figure class="highlight toml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">datacenter</span> = <span class="string">&quot;dc1&quot;</span></span><br><span class="line"><span class="attr">node_name</span>  = <span class="string">&quot;consul-client-1&quot;</span></span><br><span class="line"><span class="attr">data_dir</span>   = <span class="string">&quot;/home/toni/consul-tutorial/consul-data&quot;</span></span><br><span class="line"><span class="attr">bind_addr</span>  = <span class="string">&quot;192.168.110.133&quot;</span></span><br><span class="line"><span class="attr">client_addr</span> = <span class="string">&quot;0.0.0.0&quot;</span></span><br><span class="line"><span class="attr">log_level</span> = <span class="string">&quot;INFO&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="attr">retry_join</span> = [</span><br><span class="line">  <span class="string">&quot;192.168.110.129&quot;</span>,</span><br><span class="line">  <span class="string">&quot;192.168.110.130&quot;</span>,</span><br><span class="line">  <span class="string">&quot;192.168.110.132&quot;</span></span><br><span class="line">]</span><br></pre></td></tr></table></figure><p><code>client</code>这里的配置文件内容和 <code>server</code> 配置文件基本一致，只是去掉了 <code>server = true</code> 和 <code>bootstrap_expect</code> 这两个配置项。并且需要指定整个<code>server</code>的 <code>retry_join</code> 列表，以包含所有 <code>server</code> 节点地址。</p><h3 id="2-2-4-启动集群后的状态"><a href="#2-2-4-启动集群后的状态" class="headerlink" title="2.2.4 启动集群后的状态"></a>2.2.4 启动集群后的状态</h3><p>在3个节点启动<code>server agnet</code>以及1个节点启动<code>client agent</code>后, 我们的集群就已经正常搭建好了。可以在任意一个节点上云霄下面的命令查看集群节点状态：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">l$ consul members</span><br><span class="line">Node             Address               Status  Type    Build      Protocol  DC   Partition  Segment</span><br><span class="line">consul-server-1  192.168.110.129:8301  alive   server  1.22.0dev  2         dc1  default    &lt;all&gt;</span><br><span class="line">consul-server-2  192.168.110.130:8301  alive   server  1.22.0dev  2         dc1  default    &lt;all&gt;</span><br><span class="line">consul-server-3  192.168.110.132:8301  alive   server  1.22.0dev  2         dc1  default    &lt;all&gt;</span><br><span class="line">consul-client-1  192.168.110.131:8301  alive   client  1.22.0dev  2         dc1  default    &lt;default&gt;</span><br></pre></td></tr></table></figure><p>其输出包含健康状态、IP、类型等信息。</p><p>我们也可以只查看<code>server</code>这样参与选举的节点信息：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">$ consul operator raft list-peers</span><br><span class="line">Node             ID                                    Address               State     Voter  RaftProtocol  Commit Index  Trails Leader By</span><br><span class="line">consul-server-3  83d7a1c9-442d-beaf-82dd-8409952bd0bb  192.168.110.132:8300  follower  <span class="literal">true</span>   3             486           0 commits</span><br><span class="line">consul-server-2  6a464d5c-eda5-4d93-11e6-842292468c1d  192.168.110.130:8300  leader    <span class="literal">true</span>   3             486           -</span><br><span class="line">consul-server-1  e61233cd-e9f4-3fac-b76b-016f1125b76b  192.168.110.129:8300  follower  <span class="literal">true</span>   3             486           0 commits</span><br></pre></td></tr></table></figure><p>这是我们可以杀掉其中某个&#96;&#96;server&#96;节点，然后观察集群状态变化:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">$ consul operator raft list-peers</span><br><span class="line">Node             ID                                    Address               State     Voter  RaftProtocol  Commit Index  Trails Leader By</span><br><span class="line">consul-server-3  83d7a1c9-442d-beaf-82dd-8409952bd0bb  192.168.110.132:8300  follower  <span class="literal">true</span>   3             1195          0 commits</span><br><span class="line">consul-server-1  e61233cd-e9f4-3fac-b76b-016f1125b76b  192.168.110.129:8300  leader    <span class="literal">true</span>   3             1195          -</span><br></pre></td></tr></table></figure><p>发现掉了一个<code>leader</code>节<code>consul-server-2</code>点之后触发了重新选举, 集群仍然可以正常工作(这方面的只是可以搜索关键字<code>raft</code>了解)</p><p>当我们重启<code>consul-server-2</code>后再次查询:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">$ consul operator raft list-peers</span><br><span class="line">Node             ID                                    Address               State     Voter  RaftProtocol  Commit Index  Trails Leader By</span><br><span class="line">consul-server-3  83d7a1c9-442d-beaf-82dd-8409952bd0bb  192.168.110.132:8300  follower  <span class="literal">true</span>   3             1213          0 commits</span><br><span class="line">consul-server-1  e61233cd-e9f4-3fac-b76b-016f1125b76b  192.168.110.129:8300  leader    <span class="literal">true</span>   3             1213          -</span><br><span class="line">consul-server-2  6a464d5c-eda5-4d93-11e6-842292468c1d  192.168.110.130:8300  follower  <span class="literal">false</span>  3             1213          0 commits</span><br></pre></td></tr></table></figure><p>其能够在此加入集群。</p><blockquote><p>你也可以测试杀掉<code>client</code>节点试试看看</p></blockquote><h2 id="2-3-服务注册与发现"><a href="#2-3-服务注册与发现" class="headerlink" title="2.3 服务注册与发现"></a>2.3 服务注册与发现</h2><p>现在我们有了集群, 但还没有服务, 我们需要先编写一个服务注册的代码, 然后运行这个服务并注册</p><h3 id="2-3-1-Go-服务代码"><a href="#2-3-1-Go-服务代码" class="headerlink" title="2.3.1 Go 服务代码"></a>2.3.1 Go 服务代码</h3><p>这里我们用<code>Go</code>写一段简单的代码:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line"><span class="string">&quot;fmt&quot;</span></span><br><span class="line"><span class="string">&quot;net/http&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">http.HandleFunc(<span class="string">&quot;/&quot;</span>, <span class="function"><span class="keyword">func</span><span class="params">(w http.ResponseWriter, r *http.Request)</span></span> &#123;</span><br><span class="line">fmt.Fprintln(w, <span class="string">&quot;Hello from service A!&quot;</span>)</span><br><span class="line">&#125;)</span><br><span class="line">fmt.Println(<span class="string">&quot;Starting server on :5000...&quot;</span>)</span><br><span class="line">http.ListenAndServe(<span class="string">&quot;0.0.0.0:5000&quot;</span>, <span class="literal">nil</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这段 <code>Go</code> 代码实现了一个简单的 <code>HTTP Web</code> 服务，主要作用是监听本地的 <code>5000</code> 端口，并对所有访问根路径 &#x2F; 的请求返回 “Hello from service A!” 的响应。</p><p>写完之后我们直接:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">go build -o app</span><br><span class="line">./app  &gt;/dev/null &amp;</span><br></pre></td></tr></table></figure><blockquote><p>这段代码当然是在 <code>consul-client-1</code> 这个节点上编写并运行的。</p></blockquote><h2 id="3-2-命令行方式注册服务"><a href="#3-2-命令行方式注册服务" class="headerlink" title="3.2 命令行方式注册服务"></a>3.2 命令行方式注册服务</h2><p>我们现在在<code>consul-client-1</code> 这个节点已经运行了一个服务, 我们现在需要将其注册到<code>consul</code>集群中。<code>Consul</code>支持通过<code>http</code>接口发送注册服务的请求, 这里我们把<code>Post</code>请求的<code>body</code>写在一个<code>json</code>文件里，然后通过<code>curl</code>发送请求:<br><strong>service.json</strong>:</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;Name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;service-a&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;ID&quot;</span><span class="punctuation">:</span> <span class="string">&quot;service-a-1&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;Address&quot;</span><span class="punctuation">:</span> <span class="string">&quot;192.168.110.131&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;Port&quot;</span><span class="punctuation">:</span> <span class="number">5000</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;Check&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;HTTP&quot;</span><span class="punctuation">:</span> <span class="string">&quot;http://192.168.110.131:5000/&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;Interval&quot;</span><span class="punctuation">:</span> <span class="string">&quot;10s&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>这里重点讲解<code>Check</code>, 其就是规定了<code>Consul</code>通过什么网络接口判断服务是否健康。</p><p>发送请求:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">curl --request PUT   --data @service.json   http://127.0.0.1:8500/v1/agent/service/register</span><br></pre></td></tr></table></figure><blockquote><p>8500 是<code>consul</code>的<code>http</code>端口</p></blockquote><p>然后我们就可以在<code>consul</code>集群中看到这个服务了:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">$ consul catalog services</span><br><span class="line">consul</span><br><span class="line">service-a</span><br></pre></td></tr></table></figure><p>另一方面, 我们也可以通过<code>http</code>查询服务的具体健康信息:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line">$ curl http://localhost:8500/v1/health/service/service-a | jq</span><br><span class="line">  % Total    % Received % Xferd  Average Speed   Time    Time     Time  Current</span><br><span class="line">                                 Dload  Upload   Total   Spent    Left  Speed</span><br><span class="line">100  1448  100  1448    0     0  88948      0 --:--:-- --:--:-- --:--:-- 90500</span><br><span class="line">[</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="string">&quot;Node&quot;</span>: &#123;</span><br><span class="line">      ...</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="string">&quot;Service&quot;</span>: &#123;</span><br><span class="line">      <span class="string">&quot;ID&quot;</span>: <span class="string">&quot;service-a-1&quot;</span>,</span><br><span class="line">      <span class="string">&quot;Service&quot;</span>: <span class="string">&quot;service-a&quot;</span>,</span><br><span class="line">      <span class="string">&quot;Tags&quot;</span>: [],</span><br><span class="line">      ...</span><br><span class="line">      ...</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="string">&quot;Checks&quot;</span>: [</span><br><span class="line">      &#123;</span><br><span class="line">        ...</span><br><span class="line">      &#125;,</span><br><span class="line">      &#123;</span><br><span class="line">        <span class="string">&quot;Node&quot;</span>: <span class="string">&quot;consul-client-1&quot;</span>,</span><br><span class="line">        <span class="string">&quot;CheckID&quot;</span>: <span class="string">&quot;service:service-a-1&quot;</span>,</span><br><span class="line">        <span class="string">&quot;Name&quot;</span>: <span class="string">&quot;Service &#x27;service-a&#x27; check&quot;</span>,</span><br><span class="line">        <span class="string">&quot;Status&quot;</span>: <span class="string">&quot;passing&quot;</span>,</span><br><span class="line">        ...</span><br><span class="line">      &#125;</span><br><span class="line">    ]</span><br><span class="line">  &#125;</span><br><span class="line">]</span><br></pre></td></tr></table></figure><p>原始的结果比较长, 这里我省略了部分, 需要重点关注的就是<code>Checks</code>字段的<code>Status</code>, 案例的<code>Status</code>字段为<code>passing</code>, 表示服务正常。</p><p>现在我们杀掉服务:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">$ <span class="built_in">kill</span> -9 $(ps aux | grep <span class="string">&#x27;app&#x27;</span> | awk <span class="string">&#x27;&#123;print $2&#125;&#x27;</span>)</span><br><span class="line">$ curl http://localhost:8500/v1/health/service/service-a | jq</span><br></pre></td></tr></table></figure><p>此时返回的结果中:</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line"><span class="attr">&quot;Node&quot;</span><span class="punctuation">:</span> <span class="string">&quot;consul-client-1&quot;</span><span class="punctuation">,</span></span><br><span class="line"><span class="attr">&quot;CheckID&quot;</span><span class="punctuation">:</span> <span class="string">&quot;service:service-a-1&quot;</span><span class="punctuation">,</span></span><br><span class="line"><span class="attr">&quot;Name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Service &#x27;service-a&#x27; check&quot;</span><span class="punctuation">,</span></span><br><span class="line"><span class="attr">&quot;Status&quot;</span><span class="punctuation">:</span> <span class="string">&quot;critical&quot;</span><span class="punctuation">,</span></span><br><span class="line">...</span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p><code>Status</code>字段为<code>critical</code>, 表示服务已经挂了。</p><p>当我们重新启动服务后再次检查, <code>Status</code>字段变为<code>passing</code>, 这就是<code>Consul</code>对服务状态维护的检测功能</p><h2 id="3-3-Go-代码引入第三方库注册服务"><a href="#3-3-Go-代码引入第三方库注册服务" class="headerlink" title="3.3 Go 代码引入第三方库注册服务"></a>3.3 Go 代码引入第三方库注册服务</h2><p>尽管我们也可以通过<code>http</code>接口注册服务, 但<code>Consul</code>提供了<code>SDK</code>来简化操作, 我们可以引入第三方库来注册服务，这样运行代码就可以自动进行注册服务等基础的操作， 不需要额外通过人工命令行的方式进行操作。</p><p>安装 SDK:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">go get github.com/hashicorp/consul/api</span><br></pre></td></tr></table></figure><p>服务代码：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line"><span class="string">&quot;fmt&quot;</span></span><br><span class="line"><span class="string">&quot;log&quot;</span></span><br><span class="line"><span class="string">&quot;net/http&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="string">&quot;github.com/hashicorp/consul/api&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"><span class="comment">// 注册服务到 Consul</span></span><br><span class="line">registerServiceWithConsul()</span><br><span class="line"></span><br><span class="line">http.HandleFunc(<span class="string">&quot;/&quot;</span>, helloHandler)</span><br><span class="line"></span><br><span class="line">fmt.Println(<span class="string">&quot;Starting server on :5000...&quot;</span>)</span><br><span class="line">err := http.ListenAndServe(<span class="string">&quot;0.0.0.0:5000&quot;</span>, <span class="literal">nil</span>)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">fmt.Println(<span class="string">&quot;Error starting server:&quot;</span>, err)</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">helloHandler</span><span class="params">(w http.ResponseWriter, r *http.Request)</span></span> &#123;</span><br><span class="line">fmt.Fprintln(w, <span class="string">&quot;Hello from service A!&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">registerServiceWithConsul</span><span class="params">()</span></span> &#123;</span><br><span class="line"><span class="comment">// 创建 Consul 客户端</span></span><br><span class="line">config := api.DefaultConfig()</span><br><span class="line">config.Address = <span class="string">&quot;127.0.0.1:8500&quot;</span> <span class="comment">// Consul agent 地址</span></span><br><span class="line">client, err := api.NewClient(config)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">log.Fatalf(<span class="string">&quot;Failed to create Consul client: %v&quot;</span>, err)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义服务注册信息</span></span><br><span class="line">registration := &amp;api.AgentServiceRegistration&#123;</span><br><span class="line">ID:      <span class="string">&quot;service-a-1&quot;</span>,</span><br><span class="line">Name:    <span class="string">&quot;service-a&quot;</span>,</span><br><span class="line">Address: <span class="string">&quot;127.0.0.1&quot;</span>,</span><br><span class="line">Port:    <span class="number">5000</span>,</span><br><span class="line">Check: &amp;api.AgentServiceCheck&#123;</span><br><span class="line">HTTP:     <span class="string">&quot;http://127.0.0.1:5000/&quot;</span>,</span><br><span class="line">Interval: <span class="string">&quot;10s&quot;</span>,</span><br><span class="line">&#125;,</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 注册服务</span></span><br><span class="line">err = client.Agent().ServiceRegister(registration)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">log.Fatalf(<span class="string">&quot;Failed to register service with Consul: %v&quot;</span>, err)</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>现在我们编译后运行服务，其会自动进行注册</p><h1 id="3-小结"><a href="#3-小结" class="headerlink" title="3 小结"></a>3 小结</h1><p>通过本章的实践，我们完成了以下关键内容：</p><ul><li><p><strong>了解 Consul 的基本概念与核心功能</strong><br>Consul 是一个分布式服务管理工具，提供服务发现、健康检查、KV 存储、多数据中心支持和安全通信等能力。它适用于从单机开发环境到大规模生产集群的各种场景。</p></li><li><p><strong>搭建 Consul 集群环境</strong><br>我们基于 VMWare 创建了 4 台 Ubuntu 虚拟机，其中 3 台作为 server agent 组成 Raft 集群，1 台作为 client agent 接入集群。通过 <code>.hcl</code> 配置文件分别配置其角色和网络信息，并成功启动了 Consul 集群。</p></li><li><p><strong>理解 Agent 的运行机制</strong><br>Consul 以 agent 的形式运行在每个节点上，server agent 负责集群状态维护，client agent 负责注册服务并转发请求。agent 启动后会持续与其他节点通信，维护集群一致性。</p></li><li><p><strong>配置 Consul Server 与 Client</strong><br>详细讲解了 <code>server.hcl</code> 和 <code>client.hcl</code> 配置文件中各个参数的作用，包括数据中心设置、节点名称、数据目录、监听地址、集群加入方式等，为搭建高可用集群打下基础。</p></li><li><p><strong>验证集群状态与容错能力</strong><br>使用 <code>consul members</code> 和 <code>consul operator raft list-peers</code> 查看集群成员与 Raft 状态。测试 kill 掉某个 server 节点后，集群仍能正常进行 leader 选举和状态同步，体现了 Consul 的高可用特性。</p></li><li><p><strong>实现服务注册与健康检查</strong><br>编写了一个简单的 HTTP Web 服务，并通过两种方式将其注册到 Consul：</p><ul><li><strong>命令行方式</strong>：使用 <code>curl</code> 发送 JSON 请求注册服务，并配置健康检查。</li><li><strong>Go SDK 方式</strong>：引入 <code>github.com/hashicorp/consul/api</code> 包，在代码中自动完成服务注册与健康上报。</li></ul></li><li><p><strong>演示 Consul 的服务健康监测能力</strong><br>当服务正常运行时，Consul 显示其状态为 <code>passing</code>；当服务被 kill 掉后，Consul 检测到异常并将状态变为 <code>critical</code>，实现了对服务状态的动态感知。</p></li></ul><p>本章通过一个极简但完整的 Demo，展示了 Consul 在服务注册与发现中的核心作用，也为我们后续深入学习其原理和高级用法打下了良好基础。</p><p>但我们目前还缺少对这个服务的具体应用，<code>Consul</code>是一个功能非常多的微服务管理框架, 不同微服务简可以通过<code>Consul</code>机械能各种类型的通信, 这些将在后续章节中进一步介绍。（如果我不鸽。。。。。。）</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;1-Consul-简介&quot;&gt;&lt;a href=&quot;#1-Consul-简介&quot; class=&quot;headerlink&quot; title=&quot;1. Consul 简介&quot;&gt;&lt;/a&gt;1. Consul 简介&lt;/h1&gt;&lt;p&gt;&lt;code&gt;Consul&lt;/code&gt; 是 &lt;code&gt;Hash</summary>
      
    
    
    
    <category term="分布式" scheme="http://example.com/categories/%E5%88%86%E5%B8%83%E5%BC%8F/"/>
    
    <category term="Consul" scheme="http://example.com/categories/%E5%88%86%E5%B8%83%E5%BC%8F/Consul/"/>
    
    
    <category term="分布式" scheme="http://example.com/tags/%E5%88%86%E5%B8%83%E5%BC%8F/"/>
    
    <category term="服务注册与发现" scheme="http://example.com/tags/%E6%9C%8D%E5%8A%A1%E6%B3%A8%E5%86%8C%E4%B8%8E%E5%8F%91%E7%8E%B0/"/>
    
    <category term="Consul" scheme="http://example.com/tags/Consul/"/>
    
  </entry>
  
  <entry>
    <title>Python管理工具uv简易教程</title>
    <link href="http://example.com/2025/04/28/%E5%AE%9E%E7%94%A8%E5%B7%A5%E5%85%B7%E7%AC%94%E8%AE%B0/Python%E7%AE%A1%E7%90%86%E5%B7%A5%E5%85%B7uv%E7%AE%80%E6%98%93%E6%95%99%E7%A8%8B/"/>
    <id>http://example.com/2025/04/28/%E5%AE%9E%E7%94%A8%E5%B7%A5%E5%85%B7%E7%AC%94%E8%AE%B0/Python%E7%AE%A1%E7%90%86%E5%B7%A5%E5%85%B7uv%E7%AE%80%E6%98%93%E6%95%99%E7%A8%8B/</id>
    <published>2025-04-28T04:06:11.000Z</published>
    <updated>2026-05-28T07:37:46.665Z</updated>
    
    <content type="html"><![CDATA[<h1 id="1-什么是uv"><a href="#1-什么是uv" class="headerlink" title="1 什么是uv?"></a>1 什么是uv?</h1><h2 id="1-1-uv诞生的背景"><a href="#1-1-uv诞生的背景" class="headerlink" title="1.1 uv诞生的背景"></a>1.1 uv诞生的背景</h2><p>Python 语言因其灵活性和庞大的生态系统而广受欢迎。然而，长期以来，Python 的依赖管理和环境管理一直是开发者们讨论和面临挑战的领域。从 <code>pip</code> 到 <code>virtualenv</code>&#x2F;<code>venv</code>，再到后来的 <code>pip-tools</code>、<code>Poetry</code>、<code>PDM</code> 等，社区一直在寻求更高效、更可靠的解决方案。</p><p>在这场持续的探索中，一个引人注目的新工具出现了—— **<code>uv</code>**。由著名的 Pydantic 和 Rye 工具的创建者 Astral 团队开发，<code>uv</code> 以其惊人的速度和对现有工作流程的兼容性，迅速获得了关注。</p><p>本文将带你深入了解 <code>uv</code> 是什么，它能做什么，为什么你可能想尝试它，以及如何开始使用它。</p><h2 id="1-2-uv的优势"><a href="#1-2-uv的优势" class="headerlink" title="1.2 uv的优势"></a>1.2 uv的优势</h2><p>简单来说，<code>uv</code> 是一个用 Rust 编写的现代 Python 打包工具。它的核心目标是<strong>极速</strong>地替代 <code>pip</code>、<code>pip-tools</code> 和 <code>venv</code> 等工具的功能。</p><p>它之所以特别，主要有以下几个原因：</p><ol><li><strong>极致的速度：</strong> 这是 <code>uv</code> 最引以为傲的特性。由于使用 Rust 编写并进行了大量的性能优化，<code>uv</code> 在依赖解析、下载、安装和环境创建等方面比传统的 Python 工具快几个数量级。无论是处理小型项目还是拥有数百个依赖的大型项目，<code>uv</code> 都能提供几乎即时的反馈。</li><li><strong>单一的可执行文件：</strong> <code>uv</code> 打包成一个独立的二进制文件，安装简单方便，没有额外的依赖项。这避免了“用什么来安装包管理器”的依赖套娃问题。</li><li><strong>兼容性强：</strong> <code>uv</code> 的设计哲学之一是兼容现有的 Python 生态系统。它理解并支持标准的 <code>requirements.txt</code> 文件格式，并采用了类似于 <code>pip-tools</code> 的 <code>.in</code> 和 <code>.txt</code>（锁定文件）的工作流，这意味着许多现有项目可以非常容易地迁移到 <code>uv</code>。</li><li><strong>统一工具：</strong> <code>uv</code> 旨在将虚拟环境管理（<code>venv</code>）、依赖安装（<code>pip install</code>）、依赖解析和锁定（<code>pip-tools compile</code>）以及环境同步（<code>pip-tools sync</code>）等功能集成到一个工具中，简化开发者的工具链。</li><li><strong>可靠的依赖解析：</strong> <code>uv</code> 使用了一个全新的依赖解析器，它被设计得更加健壮和可靠，能够更准确地找到兼容的依赖版本组合，减少版本冲突问题。</li></ol><p>可以把 <code>uv</code> 看作是 Python 包管理领域的一辆“跑车”，它专注于将最核心、最频繁的操作（创建环境、安装依赖、管理依赖版本）做到极致的快和稳。</p><h2 id="1-3-uv-能替代哪些工具？"><a href="#1-3-uv-能替代哪些工具？" class="headerlink" title="1.3 uv 能替代哪些工具？"></a>1.3 uv 能替代哪些工具？</h2><p><code>uv</code> 目前主要针对以下工具的功能进行替代：</p><ul><li><strong><code>venv</code> &#x2F; <code>virtualenv</code>:</strong> 用于创建和管理独立的 Python 虚拟环境。</li><li><strong><code>pip install</code> &#x2F; <code>pip uninstall</code> &#x2F; <code>pip list</code>:</strong> 用于安装、卸载和查看包。</li><li><strong><code>pip-compile</code> &#x2F; <code>pip-sync</code> (来自 <code>pip-tools</code>):</strong> 用于从抽象的依赖列表 (<code>requirements.in</code>) 生成精确锁定的依赖列表 (<code>requirements.txt</code>)，并根据锁定文件同步环境。</li></ul><p>值得注意的是，<code>uv</code> <strong>不是</strong>一个完整的项目管理器，它目前不处理发布包、运行脚本、管理项目元数据（如 <code>pyproject.toml</code> 中的 <code>[project]</code> 部分）等功能。这些是 <code>Poetry</code> 或 <code>PDM</code> 等工具的长处。<code>uv</code> 更专注于底层的依赖和环境操作，并且可以作为其他高级工具（比如 <code>Rye</code> 就内置使用了 <code>uv</code>）的后端引擎。</p><h1 id="2-安装-uv"><a href="#2-安装-uv" class="headerlink" title="2 安装 uv"></a>2 安装 uv</h1><p>安装 <code>uv</code> 非常简单，因为它是一个独立的二进制文件。推荐使用以下方法之一：</p><p><strong>方法 1：使用 <code>curl</code> (推荐)</strong></p><p>这是最直接的方式，它会下载适用于你操作系统的 <code>uv</code> 可执行文件并将其放置在用户可执行路径中。</p><p>在 Linux 或 macOS 上：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">curl -LsSf https://astral.sh/uv/install.sh | sh</span><br></pre></td></tr></table></figure><p>在 Windows 上，可以使用 PowerShell：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">irm</span> https://astral.sh/uv/install.ps1 | <span class="built_in">iex</span></span><br></pre></td></tr></table></figure><p><strong>方法 2：使用 <code>pipx</code></strong></p><p>如果你已经安装了 <code>pipx</code>（一个用于安装和运行 Python 应用程序的工具），你也可以用它来安装 <code>uv</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">pipx install uv</span><br></pre></td></tr></table></figure><p>安装完成后，打开新的终端窗口或运行 <code>source ~/.profile</code> (或其他 shell 配置文件) 使 <code>uv</code> 命令生效。你可以通过运行 <code>uv --version</code> 来验证安装是否成功。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">uv --version</span><br><span class="line"><span class="comment"># 输出类似 uv 0.1.x</span></span><br></pre></td></tr></table></figure><h1 id="3-uv-的核心工作流与常用命令"><a href="#3-uv-的核心工作流与常用命令" class="headerlink" title="3 uv 的核心工作流与常用命令"></a>3 uv 的核心工作流与常用命令</h1><p><code>uv</code> 采用了一个清晰的工作流，尤其擅长处理基于 <code>requirements.in</code> 和 <code>requirements.txt</code> 的项目。以下是 <code>uv</code> 的核心命令和它们的作用：</p><h2 id="3-1-创建虚拟环境：uv-venv"><a href="#3-1-创建虚拟环境：uv-venv" class="headerlink" title="3.1 创建虚拟环境：uv venv"></a>3.1 创建虚拟环境：<code>uv venv</code></h2><p>在开始安装包之前，通常需要创建一个独立的 Python 环境，以避免项目之间的依赖冲突。<code>uv venv</code> 命令用于此目的。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">uv venv</span><br></pre></td></tr></table></figure><p>这个命令会在当前目录下创建一个名为 <code>.venv</code> 的文件夹（这是社区推荐的虚拟环境名称），并在其中设置一个新的 Python 环境。</p><p><strong>详细解释：</strong><br><code>uv venv</code> 会查找系统中可用的 Python 解释器，并复制或创建指向必要文件的链接，构建一个隔离的环境。在这个环境中安装的包不会影响到系统全局或其他项目的 Python 环境。与 <code>python -m venv</code> 相比，<code>uv venv</code> 通常快很多，尤其是在创建大量虚拟环境时。</p><p>除了在当前目录创建默认的 <code>.venv</code> 环境外，<code>uv venv</code> 还提供了更灵活的选项：</p><ul><li><p><strong>指定虚拟环境的名称或路径：</strong> 你可以在命令后面加上一个路径，来指定虚拟环境的创建位置和名称。这允许你在项目的不同位置创建环境，或者给环境一个自定义的名字。</p>  <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 在当前目录下创建一个名为 my-project-env 的虚拟环境</span></span><br><span class="line">uv venv my-project-env</span><br><span class="line"></span><br><span class="line"><span class="comment"># 在指定的路径下创建一个虚拟环境 (例如，在用户主目录下的 .venvs 文件夹中)</span></span><br><span class="line">uv venv ~/.venvs/another-env</span><br></pre></td></tr></table></figure></li><li><p><strong>指定 Python 版本：</strong> 如果你的系统中安装了多个 Python 版本，并且你希望为这个虚拟环境指定一个特定的 Python 版本（例如 Python 3.9），你可以使用 <code>--python</code> 选项。<code>uv</code> 会在你的系统环境中查找符合指定版本要求的 Python 解释器来创建环境。</p>  <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 使用系统中的 Python 3.9 创建虚拟环境 (uv 会尝试查找 python3.9, python3 等)</span></span><br><span class="line">uv venv --python 3.9 .venv</span><br><span class="line"></span><br><span class="line"><span class="comment"># 同时指定环境名称和 Python 版本</span></span><br><span class="line">uv venv my-env --python 3.10</span><br></pre></td></tr></table></figure><p>  <code>uv</code> 在查找 Python 解释器时非常智能，你可以指定主版本号 (<code>3.9</code>)，也可以指定次版本号 (<code>3.9.10</code>)，甚至可以使用可执行文件的名称 (<code>python3.9</code>)。</p></li></ul><p><strong>激活环境：</strong><br>创建环境后，你需要激活它才能使用其中安装的包。激活环境的方式取决于你使用的 shell：</p><ul><li><strong>Bash&#x2F;Zsh:</strong> <code>source .venv/bin/activate</code></li><li><strong>Fish:</strong> <code>source .venv/bin/activate.fish</code></li><li><strong>Nushell:</strong> <code>source .venv/bin/activate.nu</code></li><li><strong>PowerShell:</strong> <code>. .venv\Scripts\Activate.ps1</code></li><li><strong>Cmd:</strong> <code>.venv\Scripts\activate.bat</code></li></ul><p>激活环境后，你的终端提示符前通常会显示环境的名称（如 <code>(.venv)</code>），并且此时运行 <code>python</code> 或 <code>pip</code> 等命令时，使用的是虚拟环境中的版本。</p><h2 id="3-2-兼容-pip-的常用命令-uv-pip"><a href="#3-2-兼容-pip-的常用命令-uv-pip" class="headerlink" title="3.2 兼容 pip 的常用命令 (uv pip)"></a>3.2 兼容 pip 的常用命令 (<code>uv pip</code>)</h2><p><code>uv</code> 的一个设计目标是与 <code>pip</code> 的常用命令行接口保持兼容性，让用户能够快速上手。大多数你习惯使用的 <code>pip</code> 命令，都可以在 <code>uv pip</code> 下找到对应的功能，并且通常速度更快。</p><p>以下是一些常用的 <code>uv pip</code> 命令及其说明：</p><ul><li><p><strong>安装依赖：<code>uv pip install</code></strong></p><p>  这是最常用的命令，用于安装一个或多个 Python 包到当前激活的环境中。你可以指定包的名称，也可以指定特定的版本或版本范围。<code>uv pip install</code> 会负责解析依赖关系并安装所有必需的包。</p>  <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 安装一个包</span></span><br><span class="line">uv pip install requests</span><br><span class="line"></span><br><span class="line"><span class="comment"># 安装多个包</span></span><br><span class="line">uv pip install requests flask</span><br><span class="line"></span><br><span class="line"><span class="comment"># 安装指定版本的包</span></span><br><span class="line">uv pip install <span class="string">&quot;requests==2.28.1&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 安装一个包的最新兼容版本</span></span><br><span class="line">uv pip install <span class="string">&quot;requests&gt;=2.20,&lt;3.0&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 从 requirements 文件安装 (同 3.3 节内容，但此处作为兼容 pip 的用法提及)</span></span><br><span class="line">uv pip install -r requirements.txt</span><br></pre></td></tr></table></figure><p>  <strong>详细解释：</strong><br>  <code>uv pip install</code> 会在当前的虚拟环境（如果已激活）或全局环境中（如果不使用虚拟环境，但通常不推荐）查找、下载并安装指定的包及其依赖。<code>uv</code> 的依赖解析器会负责找出所有包之间兼容的版本组合。它的速度优势在这里体现得淋𝓮无论是安装单个包还是从 requirements 文件批量安装，<code>uv</code> 都比 <code>pip</code> 快很多。</p></li><li><p><strong>卸载包：<code>uv pip uninstall</code></strong></p><p>  用于从当前环境中移除一个或多个已安装的包。</p>  <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">uv pip uninstall requests flask</span><br></pre></td></tr></table></figure><p>  <strong>详细解释：</strong><br>  <code>uv pip uninstall</code> 会从当前激活的虚拟环境中移除指定的包。需要注意的是，手动卸载包后，你的虚拟环境可能就不再与 <code>requirements.txt</code> 文件精确同步了。通常更推荐的工作流是修改 <code>requirements.in</code> 文件（删除你不再需要的顶级依赖），然后重新运行 <code>uv pip compile</code> 更新 <code>requirements.txt</code>，最后运行 <code>uv pip sync</code> 来清理环境，以保持环境与锁定文件的一致。</p></li><li><p><strong>查看已安装的包：<code>uv pip list</code></strong></p><p>  列出当前环境中已安装的所有包及其版本。这是检查环境状态和已安装依赖的便捷方式。</p>  <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">uv pip list</span><br></pre></td></tr></table></figure><p>  <strong>详细解释：</strong><br>  这个命令提供了当前虚拟环境中所有包的快照，方便你查看安装了哪些包以及它们的具体版本。</p></li><li><p><strong>查看包的依赖树：<code>uv pip tree</code></strong></p><p>  显示当前环境中已安装包的依赖关系树。这有助于理解为什么某个包会被安装以及包之间的相互依赖关系。</p>  <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">uv pip tree</span><br></pre></td></tr></table></figure><p>  <strong>详细解释：</strong><br>  当你项目的依赖比较复杂时，<code>uv pip tree</code> 可以帮助你理解依赖的来源（它是哪个顶级依赖的子依赖），以及整个依赖图。这对于调试依赖冲突或意外安装的包非常有帮助。</p></li></ul><h2 id="3-3-依赖编译与锁定：uv-pip-compile"><a href="#3-3-依赖编译与锁定：uv-pip-compile" class="headerlink" title="3.3 依赖编译与锁定：uv pip compile"></a>3.3 依赖编译与锁定：<code>uv pip compile</code></h2><p>这是 <code>uv</code>（以及 <code>pip-tools</code>）工作流中非常重要的一步，用于生成一个<strong>精确锁定</strong>项目依赖及其所有子依赖版本的 <code>requirements.txt</code> 文件。这确保了项目的构建和部署在任何地方都能获得完全相同的依赖环境，是实现<strong>可重复构建 (Reproducible Builds)</strong> 的关键。</p><p>你通常会创建一个 <code>requirements.in</code> 文件，其中只包含你项目<strong>直接需要</strong>的顶级依赖：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"># requirements.in</span><br><span class="line">requests</span><br><span class="line">flask</span><br><span class="line">pytest</span><br></pre></td></tr></table></figure><p>然后运行 <code>uv pip compile</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">uv pip compile requirements.in -o requirements.txt</span><br></pre></td></tr></table></figure><p><strong>详细解释：</strong><br><code>uv pip compile</code> 会读取 <code>requirements.in</code> 文件中的顶级依赖，然后递归地查找这些依赖所依赖的所有包。它会解析出<strong>整个依赖树</strong>中每一个包的<strong>确切版本</strong>，甚至包括它们的哈希值（用于进一步验证）。最后，它将这个完整的、精确锁定的依赖列表写入 <code>requirements.txt</code> 文件。</p><p>生成的 <code>requirements.txt</code> 文件看起来会像这样（内容会多很多，包含所有间接依赖）：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"># This file is autogenerated by uv</span><br><span class="line"># Becomes: requirements.in</span><br><span class="line"></span><br><span class="line">anyio==3.7.1 \</span><br><span class="line">    --hash=sha256:f0d8f...</span><br><span class="line">certifi==2023.11.17 \</span><br><span class="line">    --hash=sha256:6f8f7...</span><br><span class="line">charset-normalizer==3.3.2 \</span><br><span class="line">    --hash=sha256:a34d1...</span><br><span class="line">click==8.1.7 \</span><br><span class="line">    --hash=sha256:1c80c...</span><br><span class="line"># ... 很多很多行 ...</span><br><span class="line">requests==2.31.0 \</span><br><span class="line">    --hash=sha256:79127...</span><br><span class="line">sniffio==1.3.0 \</span><br><span class="line">    --hash=sha256:c7364...</span><br><span class="line">Werkzeug==3.0.1 \</span><br><span class="line">    --hash=sha256:3d45f...</span><br></pre></td></tr></table></figure><p>有了这个 <code>requirements.txt</code> 文件，任何人在任何机器上使用 <code>uv pip sync</code> 都可以安装到与你开发时完全相同的依赖环境。</p><p><strong>常见选项：</strong></p><ul><li><code>-o &lt;output_file&gt;</code>: 指定输出的锁定文件（通常是 <code>requirements.txt</code>）。</li><li><code>--upgrade-package &lt;package&gt;</code>: 在编译时升级指定的包到最新兼容版本，即使它已经在 <code>.txt</code> 文件中被锁定。这在你只想升级某个特定依赖时很有用。</li><li><code>--no-header</code>: 不在输出文件顶部添加自动生成的注释。</li></ul><h2 id="3-4-同步环境：uv-pip-sync"><a href="#3-4-同步环境：uv-pip-sync" class="headerlink" title="3.4 同步环境：uv pip sync"></a>3.4 同步环境：<code>uv pip sync</code></h2><p><code>uv pip sync</code> 的作用是让当前活动的虚拟环境的状态与一个或多个精确锁定的 <code>requirements.txt</code> 文件<strong>完全一致</strong>。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">uv pip <span class="built_in">sync</span> requirements.txt</span><br></pre></td></tr></table></figure><p><strong>详细解释：</strong><br>运行 <code>uv pip sync</code> 后，<code>uv</code> 会读取 <code>requirements.txt</code> 文件中列出的所有包和它们的精确版本。然后，它会检查当前虚拟环境中<strong>实际安装</strong>了哪些包。</p><ul><li>如果锁定文件中有的包，但环境中没有，<code>uv</code> 会安装它。</li><li>如果锁定文件中有的包，环境中也有，但版本或哈希不匹配，<code>uv</code> 会重新安装正确版本的包。</li><li>如果环境中有包，但锁定文件中<strong>没有</strong>（这意味着这个包不是项目需要的依赖），<code>uv</code> 会<strong>卸载</strong>它。</li></ul><p>这个过程确保了虚拟环境的内容<strong>精确</strong>地反映了锁定文件的内容，不多不少，版本完全一致。这对于保证开发、测试和生产环境的一致性至关重要。</p><p><strong><code>uv pip sync</code> 和 <code>uv pip install -r</code> 的区别：</strong><br>虽然 <code>uv pip install -r requirements.txt</code> 也可以安装锁定文件中的包，但它<strong>不会</strong>卸载环境中多余的包。<code>uv pip sync</code> 的核心在于实现环境与锁定文件的<strong>完全同步</strong>，包括删除不需要的包。因此，在团队协作或部署时，<code>uv pip sync</code> 是更推荐的命令，以确保环境的纯净和一致。</p><h2 id="3-5-本地项目包管理：uv-add"><a href="#3-5-本地项目包管理：uv-add" class="headerlink" title="3.5 本地项目包管理：uv add"></a>3.5 本地项目包管理：<code>uv add</code></h2><p>如果你需要管理本地项目依赖，可以使用 <code>uv add</code> 命令。这种情况下, <code>uv</code>会将依赖包安装在项目目录下的虚拟环境中, 而不是添加到全局的<code>Python</code>解释器所在的环境。</p><p><strong>本地项目依赖管理</strong></p><p>并且通过 <code>uv add &lt;package&gt;</code> 添加依赖时，UV 会自动更新项目的 <code>pyproject.toml</code> 文件，记录包名及版本约束（如 <code>requests&gt;=2.32.3</code>），并生成或更新锁文件 <code>uv.lock</code>，精确锁定所有直接依赖和子依赖的版本。例如：  </p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br></pre></td><td class="code"><pre><span class="line">uv add requests</span><br><span class="line">```  </span><br><span class="line">此操作确保团队协作或部署时，所有环境依赖版本完全一致，避免“在我机器上能运行”的问题。  </span><br><span class="line"></span><br><span class="line">**开发依赖管理**  </span><br><span class="line"></span><br><span class="line">支持通过 `--dev` 标志区分开发环境依赖（如测试框架），这些依赖会被记录在 `pyproject.toml` 的 `[dependency-groups]` 部分，例如：  </span><br><span class="line">```bash</span><br><span class="line">uv add pytest --dev</span><br><span class="line">```  </span><br><span class="line">开发依赖不会影响生产环境的轻量化部署。  </span><br><span class="line"></span><br><span class="line">**虚拟环境集成**  </span><br><span class="line"></span><br><span class="line">若项目未初始化虚拟环境，`uv add` 会自动创建 `.venv` 目录并安装依赖，无需手动激活环境。通过 `uv run` 可直接在隔离环境中执行脚本，例如：  </span><br><span class="line">```bash</span><br><span class="line">uv run example.py</span><br><span class="line">```  </span><br><span class="line">此特性简化了开发流程，尤其适合单文件脚本的依赖管理。  </span><br><span class="line"></span><br><span class="line"></span><br><span class="line">**与`uv pip install` 的区别**</span><br><span class="line"></span><br><span class="line">| 特性               | `uv add`                              | `uv pip install`                     |  </span><br><span class="line">|------------------------|-------------------------------------------|-------------------------------------------|  </span><br><span class="line">| 配置文件修改       | 更新 `pyproject.toml` 和 `uv.lock` | 不修改任何配置文件                |  </span><br><span class="line">| 依赖解析逻辑       | 主动生成确定性锁文件，确保版本一致性 | 安装当前兼容的最新版本，无版本锁定 |  </span><br><span class="line">| 适用场景           | 长期项目管理、团队协作            | 临时测试、全局工具安装            |  </span><br><span class="line">| 虚拟环境管理       | 自动创建或同步项目虚拟环境        | 依赖手动激活的现有环境            |  </span><br><span class="line">| 性能优化           | 依赖解析速度更快（Rust 实现）     | 安装速度提升，但逻辑与传统 pip 对齐   |  </span><br><span class="line"></span><br><span class="line">**使用建议**  </span><br><span class="line">- 优先使用 `uv add`：在需要维护项目依赖声明、锁定版本或协作开发时，应通过 `uv add` 管理依赖，确保环境一致性。  </span><br><span class="line">- 选择性使用 `uv pip install`：在临时调试、安装全局工具（如 `ruff`、`black`）或需要兼容现有 `pip` 脚本时使用，避免污染项目配置。  </span><br><span class="line">- 混合工作流：例如，使用 `uv pip install` 安装 PyTorch 的 GPU 版本后，可手动将依赖添加到 `pyproject.toml`，再通过 `uv <span class="built_in">sync</span>` 同步环境。</span><br><span class="line"></span><br><span class="line"><span class="comment"># 4 结合使用：典型的 uv 工作流</span></span><br><span class="line"></span><br><span class="line">一个使用 `uv` 管理 Python 项目依赖的典型工作流如下：</span><br><span class="line"></span><br><span class="line">1.  **创建环境：**</span><br><span class="line">    ```bash</span><br><span class="line">    uv venv</span><br><span class="line">    <span class="built_in">source</span> .venv/bin/activate</span><br></pre></td></tr></table></figure><ol start="2"><li><strong>定义顶级依赖：</strong> 在项目根目录创建 <code>requirements.in</code> 文件，列出你项目直接需要的库。<figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"># requirements.in</span><br><span class="line">fastapi</span><br><span class="line">uvicorn[standard]</span><br><span class="line">pydantic_settings</span><br><span class="line">httpx</span><br></pre></td></tr></table></figure></li><li><strong>编译生成锁定文件：</strong> 运行 <code>uv pip compile</code> 生成精确锁定的 <code>requirements.txt</code>。<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">uv pip compile requirements.in -o requirements.txt</span><br></pre></td></tr></table></figure>第一次运行会稍慢一些，因为它需要解析所有依赖并从 PyPI 下载元数据。但比 <code>pip-compile</code> 快得多。</li><li><strong>同步环境：</strong> 根据生成的 <code>requirements.txt</code> 安装依赖。<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">uv pip <span class="built_in">sync</span> requirements.txt</span><br></pre></td></tr></table></figure>这个步骤通常会非常快，因为它只是按照锁定文件执行安装&#x2F;卸载操作，无需再次解析。</li><li><strong>开发过程中添加新依赖：</strong><ul><li>编辑 <code>requirements.in</code>，添加新的顶级依赖。</li><li>重新运行 <code>uv pip compile requirements.in -o requirements.txt</code> 更新锁定文件。</li><li>重新运行 <code>uv pip sync requirements.txt</code> 将新依赖安装到环境中。</li></ul></li><li><strong>开发过程中移除依赖：</strong><ul><li>编辑 <code>requirements.in</code>，移除不再需要的顶级依赖。</li><li>重新运行 <code>uv pip compile requirements.in -o requirements.txt</code> 更新锁定文件。</li><li>重新运行 <code>uv pip sync requirements.txt</code> 将不再需要的依赖从环境中移除。</li></ul></li></ol><p>这个工作流清晰地分离了“我想要什么”（<code>requirements.in</code>）和“我实际安装了什么”（<code>requirements.txt</code>），并利用 <code>uv</code> 的高速特性，使得依赖管理变得高效且可控。</p><h1 id="5-与-pyenv-的结合使用"><a href="#5-与-pyenv-的结合使用" class="headerlink" title="5 与 pyenv 的结合使用"></a>5 与 <code>pyenv</code> 的结合使用</h1><p>需要重点说明的是, <code>uv</code>本身并不会直接管理不同<code>Python</code>解释器的版本，而是基于系统已有的 <code>Python</code> 虚拟环境进行管理。因此, 通常我们需要结合其他的<code>Python</code>虚拟环境管理工具来使用。这里最常见的方式就是使用轻量化的 <code>pyenv</code> 。（都用<code>uv</code>了肯定不再用重量级的<code>anaconda</code>了）</p><p>这里简单介绍<code>pyenv</code>的使用方法:</p><h2 id="5-1-安装与基础配置"><a href="#5-1-安装与基础配置" class="headerlink" title="5.1 安装与基础配置"></a>5.1 安装与基础配置</h2><p><strong>Windows</strong><br>通过 Chocolatey 安装社区维护的 <code>pyenv-win</code>：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">choco install pyenv<span class="literal">-win</span> <span class="literal">-y</span></span><br><span class="line">refreshenv</span><br></pre></td></tr></table></figure><p><strong>Linux&#x2F;macOS</strong><br>直接使用原生 <code>pyenv</code>，并确保在 Shell 配置文件（如 <code>.bashrc</code> 或 <code>.zshrc</code>）中添加初始化代码：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">export</span> PATH=<span class="string">&quot;<span class="variable">$HOME</span>/.pyenv/bin:<span class="variable">$PATH</span>&quot;</span></span><br><span class="line"><span class="built_in">eval</span> <span class="string">&quot;<span class="subst">$(pyenv init -)</span>&quot;</span></span><br></pre></td></tr></table></figure><p><strong>验证安装</strong><br>运行 <code>pyenv versions</code> 查看已安装的 Python 版本。</p><h2 id="5-2-pyenv-的-Python-版本管理"><a href="#5-2-pyenv-的-Python-版本管理" class="headerlink" title="5.2 pyenv 的 Python 版本管理"></a>5.2 pyenv 的 Python 版本管理</h2><h3 id="5-2-1-通过-pyenv-安装多版本解释器"><a href="#5-2-1-通过-pyenv-安装多版本解释器" class="headerlink" title="5.2.1 通过 pyenv 安装多版本解释器"></a>5.2.1 通过 <code>pyenv</code> 安装多版本解释器</h3><p>例如安装 Python 3.10 和 3.12：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">pyenv install 3.10.11</span><br><span class="line">pyenv install 3.12.0</span><br></pre></td></tr></table></figure><h3 id="5-2-2-切换全局或项目本地版本"><a href="#5-2-2-切换全局或项目本地版本" class="headerlink" title="5.2.2 切换全局或项目本地版本"></a>5.2.2 切换全局或项目本地版本</h3><ul><li>全局设置：<code>pyenv global 3.12.0</code>  </li><li>项目本地设置：在项目目录中运行 <code>pyenv local 3.10.11</code>，生成 <code>.python-version</code> 文件。</li></ul><h3 id="5-2-3-与-uv-的版本同步"><a href="#5-2-3-与-uv-的版本同步" class="headerlink" title="5.2.3 与 uv 的版本同步"></a>5.2.3 与 <code>uv</code> 的版本同步</h3><p><code>uv</code> 通过读取 <code>.python-version</code> 或 <code>pyproject.toml</code> 中的 <code>requires-python</code> 字段自动选择解释器版本。例如：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">uv venv --python 3.10.11  <span class="comment"># 显式指定版本创建虚拟环境</span></span><br></pre></td></tr></table></figure><h1 id="6-小节"><a href="#6-小节" class="headerlink" title="6 小节"></a>6 小节</h1><h2 id="6-1-为什么选择-uv？与现有工具的对比"><a href="#6-1-为什么选择-uv？与现有工具的对比" class="headerlink" title="6.1 为什么选择 uv？与现有工具的对比"></a>6.1 为什么选择 uv？与现有工具的对比</h2><p><code>uv</code> 并不是第一个 Python 包管理工具，那么它在众多工具中有什么优势呢？</p><ul><li><strong>vs <code>pip</code> + <code>venv</code>:</strong> 这是最基础的组合。<code>uv</code> 将两者的功能整合，提供更快的环境创建和依赖安装速度。更重要的是，<code>uv</code> 的 <code>compile</code> 和 <code>sync</code> 提供了 <code>pip</code> 本身不具备的强大依赖锁定和环境同步能力，解决了 <code>pip</code> 在可重复构建方面的不足。</li><li><strong>vs <code>pip-tools</code>:</strong> <code>uv</code> 的 <code>pip compile</code> 和 <code>pip sync</code> 直接对标 <code>pip-tools</code> 的同名命令。<code>uv</code> 的主要优势在于<strong>压倒性的速度</strong>。对于大型项目或在 CI&#x2F;CD 环境中，<code>uv</code> 可以显著缩短构建时间。此外，<code>uv</code> 是一个单一工具，而 <code>pip-tools</code> 是一个依赖于 <code>pip</code> 和 Python 环境的 Python 包。</li><li><strong>vs <code>Poetry</code> &#x2F; <code>PDM</code>:</strong> <code>Poetry</code> 和 <code>PDM</code> 是功能更全面的项目管理器。它们使用 <code>pyproject.toml</code> 作为项目配置中心，处理依赖、构建、发布、运行脚本等。<code>uv</code> 目前不涉及这些高级项目管理功能。<code>uv</code> 更专注于底层的依赖解析和安装。对于习惯使用 <code>requirements.txt</code> 工作流或只需要一个快速的 <code>pip</code>&#x2F;<code>venv</code>&#x2F;<code>pip-tools</code> 替代品的项目来说，<code>uv</code> 是一个极好的选择。对于追求 All-in-one 项目管理的团队，Poetry 或 PDM 仍然是强有力的竞争者，但未来它们也可能集成 <code>uv</code> 作为其内部的解析和安装引擎，以获得速度提升。</li><li><strong>vs <code>Rye</code>:</strong> <code>Rye</code> 是 <code>uv</code> 的同门师兄，也是由 Astral 开发的更高级的项目管理器。<code>Rye</code> 专注于简化 Python 工具链的管理（包括 Python 版本本身、虚拟环境、依赖等），并在内部使用了 <code>uv</code> 作为其依赖管理引擎。如果你正在寻找一个全新的、集成的 Python 项目开发环境，<code>Rye</code> 可能更适合你；如果你只想替换现有的 <code>pip</code>&#x2F;<code>venv</code>&#x2F;<code>pip-tools</code> 流程，<code>uv</code> 是一个更直接的切入点。</li></ul><p>总的来说，<code>uv</code> 的最大亮点在于其<strong>无与伦比的速度</strong>和对现有标准的良好支持。它是一个高性能的“引擎”，可以作为现有工作流（特别是基于 <code>requirements.txt</code> 的）的直接加速器，或者作为更高级工具（如 <code>Rye</code>）的基础。</p><h2 id="6-2-潜在的注意事项和未来展望"><a href="#6-2-潜在的注意事项和未来展望" class="headerlink" title="6.2 潜在的注意事项和未来展望"></a>6.2 潜在的注意事项和未来展望</h2><p><code>uv</code> 是一个相对较新的工具，尽管它已经非常强大和稳定，但仍有一些地方需要注意：</p><ul><li><strong>生态系统集成：</strong> 虽然 <code>uv</code> 兼容 <code>requirements.txt</code>，但一些依赖于特定 <code>pip</code> 行为或内部结构的工具可能需要时间来完全支持 <code>uv</code>。</li><li><strong>功能覆盖：</strong> 尽管 <code>uv</code> 覆盖了核心的包管理功能，但 <code>pip</code> 有一些更高级或边缘的功能（如通过 VCS 安装、本地目录安装的复杂选项等）<code>uv</code> 可能尚未完全支持或行为略有不同。在使用前最好查阅官方文档确认。</li><li><strong>持续发展：</strong> <code>uv</code> 正在快速迭代中，新版本可能会带来功能改进或行为变化。</li></ul><p>未来，<code>uv</code> 有潜力成为 Python 打包领域的一个基础性工具，其核心的快速解析和安装能力可能会被更广泛地集成到各种 Python 开发工具和平台中。</p><h2 id="6-3-总结"><a href="#6-3-总结" class="headerlink" title="6.3 总结"></a>6.3 总结</h2><p><code>uv</code> 是 Python 包管理领域令人兴奋的新进展。它以 Rust 带来的极致速度为核心优势，旨在成为 <code>pip</code>、<code>venv</code> 和 <code>pip-tools</code> 的高性能替代品。通过支持标准的 <code>requirements.txt</code> 文件和类似于 <code>pip-tools</code> 的编译&#x2F;同步工作流，<code>uv</code> 提供了一条平滑的迁移路径，让开发者能够轻松地获得更快的环境管理和依赖处理体验。</p><p>如果你厌倦了等待 <code>pip</code> 解析和安装依赖，或者希望在你的 CI&#x2F;CD 流程中大幅缩短构建时间，那么 <code>uv</code> 绝对值得你尝试。开始使用 <code>uv venv</code> 和 <code>uv pip install</code>，体验一下“瞬间”完成依赖安装的快感吧！</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;1-什么是uv&quot;&gt;&lt;a href=&quot;#1-什么是uv&quot; class=&quot;headerlink&quot; title=&quot;1 什么是uv?&quot;&gt;&lt;/a&gt;1 什么是uv?&lt;/h1&gt;&lt;h2 id=&quot;1-1-uv诞生的背景&quot;&gt;&lt;a href=&quot;#1-1-uv诞生的背景&quot; class=&quot;</summary>
      
    
    
    
    <category term="实用工具" scheme="http://example.com/categories/%E5%AE%9E%E7%94%A8%E5%B7%A5%E5%85%B7/"/>
    
    
    <category term="Python" scheme="http://example.com/tags/Python/"/>
    
  </entry>
  
  <entry>
    <title>25届的C++开发学习路线经验分享</title>
    <link href="http://example.com/2025/04/23/%E9%9A%8F%E7%AC%94/25%E5%B1%8A%E7%9A%84C++%E5%BC%80%E5%8F%91%E5%AD%A6%E4%B9%A0%E8%B7%AF%E7%BA%BF%E7%BB%8F%E9%AA%8C%E5%88%86%E4%BA%AB/"/>
    <id>http://example.com/2025/04/23/%E9%9A%8F%E7%AC%94/25%E5%B1%8A%E7%9A%84C++%E5%BC%80%E5%8F%91%E5%AD%A6%E4%B9%A0%E8%B7%AF%E7%BA%BF%E7%BB%8F%E9%AA%8C%E5%88%86%E4%BA%AB/</id>
    <published>2025-04-23T01:35:07.000Z</published>
    <updated>2026-05-28T07:37:46.667Z</updated>
    
    <content type="html"><![CDATA[<h1 id="0-前言"><a href="#0-前言" class="headerlink" title="0 前言"></a>0 前言</h1><p>这篇文章记录了我<code>C++</code>开发的学习路线，领域包含互联网后端、数据库、存储、操作系统等。虽然我最后秋招的<code>Offer</code>是<code>C++</code>后端开发, 但也有一些其他领域的求职和学习经验, 在这里给大家分享一下。</p><p>既然这篇文章是介绍<code>C++</code>的, 那么首先我从个人的角度回答一下, <code>C++</code>和<code>Java</code>应该学哪个这个热点问题。我水的<code>C++</code>群都在唱衰<code>C++</code>, 我水的<code>Java</code>群都在Diss <code>Java</code>。就我个人经验而言，可以如下做一个总结：</p><p><strong>C++的优势&#x2F;Java的劣势</strong></p><ol><li><code>C++</code>可以做底层开发, 相对而言可替代性比较低; 而<code>Java</code>确实人太多太卷，相对而言可替代性高。</li><li><code>C++</code>就业领域更广, 除后端外还有<code>嵌入式</code>, <code>操作系统</code>, <code>数据库</code>, <code>存储</code>, <code>音视频</code>, <code>编译器</code>, <code>量化开发</code>, <code>AI infra</code>, <code>HPC</code>等; 而<code>Java</code>的除后端外应用领域相对较少.</li><li><code>C++</code>的八股相对少, 基本上就是语言本身的知识; <code>Java</code>的八股更庞杂, 除了语言本身外, 还有<code>Spring</code>全家桶以及各种中间件</li></ol><p><strong>C++的劣势&#x2F;Java的优势</strong></p><ol><li><code>C++</code>学习难度大, 语法复杂, 学习曲线陡峭; 而<code>Java</code>相对简单, 学习曲线平缓. 可以认为, <code>C++</code>的八股<code>少而难</code>, <code>Java</code>大八股<code>庞而杂</code></li><li><code>C++</code>不同领域的知识点差异大, 不同领域不容易互转, 例如<code>音视频</code>和<code>AI infra</code>的专业领域知识完全不同, 很难互转; <code>Java</code>领域局限且统一</li><li><code>C++</code>就业时的门槛确实相对较高</li></ol><p>不过有一点需要澄清, 尽管<code>C++</code>和<code>Java</code>的语言选择确实一定程度上影响求职面试, 但其权重远不如你四大件和笔试的影响大, 求职最重要的还是你个人的能力和项目经验, 语言只是一个工具而已。</p><h1 id="1-阶段1-基础语法学习"><a href="#1-阶段1-基础语法学习" class="headerlink" title="1 阶段1-基础语法学习"></a>1 阶段1-基础语法学习</h1><p>我从研0阶段开始学习<code>C++</code>的基础语法, 由于一开始没有领路人全靠自学, <code>C++</code>语言本身我前后至少学了3遍:</p><p><strong>第一遍: 看黑马的视频课程</strong><br>由于自己一开始不懂, 直接看的黑马的视频课程, 但其缺少常见新特性和并发编程的知识, 仅仅适合新人入门, 距离你找工作差得很远</p><p><strong>第二遍: 刷CS106L</strong><br>后来发现了<a href="https://csdiy.wiki/">csdiy</a>这个自学指南, 就刷这里推荐的公开课, 这里我刷的是<code>CS106L</code>, 同时建议把杜克大学的C语言的课程<a href="https://www.coursera.org/specializations/c-programming">Introductory C Programming Specialization</a>也刷了</p><p><strong>第三遍: 阅读电子书学习现代C++特性</strong><br>这一阶段, 我学习的目标主要是涉及<code>C++</code>的新特性, 包括智能指针、右值引用、模板元编程这一类</p><p><strong>推荐的学习方法</strong><br>其实我列举出我的3遍<code>C++</code>学习路线, 不是为了说明<code>C++</code>真的要学三遍, 而是为了说明方法很重要, 我学3遍的原因还不是因为没有找到合适的途径. 现在要推荐的话, 我如下推荐:</p><ol><li>电子教程 or 刷视频快速学习<code>C++</code>基础知识, 视频推荐<a href="https://www.bilibili.com/video/BV1o8411x7K3/?spm_id_from=333.1387.upload.video_card.click&vd_source=213e02ac84a7c6ef251b9f5d6691e21c">B站</a>, 电子书推荐(零基础C++)[<a href="https://www.yuque.com/lianlianfengchen-cvvh2/zack]">https://www.yuque.com/lianlianfengchen-cvvh2/zack]</a>, 其中这份电子书不仅仅是基础, 包括后续的高阶知识, 强烈推荐</li><li>刷视频课 or 电子书重点学习<code>C++</code>的新特性, 这里的电子书包括: <a href="https://changkun.de/modern-cpp/zh-cn/00-preface/">现代C++教程</a>, 视频课推荐之前提到的<a href="https://web.stanford.edu/class/cs106l/">CS106L</a></li></ol><blockquote><p>顺带提一句, 我个人十分不推荐使用<code>C++ Primer Plus</code>这些经典书籍进行学习, 因为他们的可交互性太差了。学习编程语言，敲代码才能熟练。电子书或者视频可见的代码内容能一键复制，极大地提高了效率，而纸质书则非常并不方便</p></blockquote><h1 id="2-阶段2-基础四大件学习"><a href="#2-阶段2-基础四大件学习" class="headerlink" title="2 阶段2-基础四大件学习"></a>2 阶段2-基础四大件学习</h1><p>无论是什么语言，基础四大件都是学习不可缺少的，我基础四大件基本上都是经典教材+刷公开课or训练营的方式学习的。因此我虽然不是计算机科班，但我的计算机基础知识其实还挺扎实的：</p><h2 id="2-1-数据结构与算法"><a href="#2-1-数据结构与算法" class="headerlink" title="2.1 数据结构与算法"></a>2.1 数据结构与算法</h2><p>这个没什么好说的， 坚持每天刷<code>LeetCode</code>就行了。我到去年秋招时刷了差不多300道题，足够应付正常难度的笔试了. 不过有一些要点需要关注:</p><ol><li>如果没有时间， 就优先刷<code>Hot100</code></li><li>刷题时尽量选择ACM模式, 贴近笔试的实际场景</li><li><code>LeetCode</code>缺少图论的题, 可从其他途径补充图论的算法题</li></ol><p>刷题重要知识点梳理:</p><ol><li>字符串<ol><li><code>KMP</code>算法</li><li>双指针</li></ol></li><li>树<ol><li>不同遍历方法</li><li>反转、重构</li><li>树与数组的转化</li></ol></li><li>图<ol><li>最短路径算法</li><li>查并集</li><li>最小生成树</li></ol></li><li>回溯</li><li>贪心</li><li>动态规划</li></ol><h2 id="2-2-操作系统"><a href="#2-2-操作系统" class="headerlink" title="2.2 操作系统"></a>2.2 操作系统</h2><p>操作系统曾是我主要技术栈之一，我的学习路线是：</p><p><strong>第一步： 经典教材阅读</strong><br>这里我强烈推荐《操作系统导论》</p><p><img src="/../../images/Notes/OS-three.png" alt="操作系统导论"></p><blockquote><p>这里还有一个本教程推荐: 《操作系统真相还原》</p></blockquote><p><strong>第二步: 刷 MIT6.S081</strong><br>这里我刷了知名课程<a href="https://pdos.csail.mit.edu/6.S081/2020/schedule.html">MIT6.S081</a>并做完了<code>xv6</code>的课程<code>lab</code>, 收获很多。其实熟读完《操作系统导论》后， <code>MIT6.S081</code>的课程知识很多都是重复的，这里的重点学习内容是<code>xv6</code>的课程作业, 通过代码来实践与加强对操作系统的理解。</p><blockquote><p>这里不一定刷<code>MIT6.S081</code>这一课程, 南京大学蒋炎岩老师的<a href="https://jyywiki.cn/OS/2023/index.html">操作系统：设计与实现</a>或者UCB的<a href="https://cs162.org/">CS162</a>也很不错</p></blockquote><p><strong>第三步: 参见OS训练营</strong><br>之后我又参与了清华大学举办的开源操作系统训练营, 并获得了优秀学员, 这里是用<code>Rust</code>完成各项作业. 这个训练营分为三个阶段:</p><ol><li>阶段一: <code>rustlings</code>熟悉<code>Rust</code>语法和基本编程概念</li><li>阶段二: <code>Rust</code>开发一个简单的操作系统<code>rCore</code></li><li>阶段三: 按照不同方向进行进一步的线上实习(也同样是上课做项目), 包括<ol><li>内核进阶</li><li>虚拟化</li><li>组件化的<code>ArceOS</code></li><li>Linux内核驱动开发项目: <a href="https://github.com/rust-for-linux/">Rust for Linux</a></li></ol></li></ol><p>关于这个训练营，我还有一个专栏记录自己的学习过程：<a href="https://www.zhihu.com/column/c_1738250998752292864">清华大学开源操作系统训练营</a></p><p>可以看到, 学习操作系统的过程中, 我基础打得非常扎实, 因为我当时是把操作系统和驱动开发作为未来的求职方向之一，不过如果你对深入学习操作系统没有兴趣， 仅仅是为了满足对操作系统的基础学习的话，这里完成我的第一步， 读一下《操作系统导论》就可以了。</p><h2 id="2-3-计算机网络"><a href="#2-3-计算机网络" class="headerlink" title="2.3 计算机网络"></a>2.3 计算机网络</h2><p>计算机网络也是我基础四大件之一，且这直接对应于互联网后端这一岗位，非常重要。我学习路线是：</p><ol><li>阅读《计算机网络： 自顶向下方法》</li><li>刷CS144</li></ol><p><img src="/../../images/Notes/CSNetWorking.png" alt="计算机网络-自顶向下方法"></p><blockquote><p>这里说明一下, 中科大的计算机网络课程也是使用的《计算机网络： 自顶向下方法》这本教材, 且更精炼一些, 如果时间不够或者读教材困难, 可以选择直接刷<a href="https://www.bilibili.com/video/BV1JV411t7ow/?spm_id_from=333.337.search-card.all.click">中科大的计算机网络视频课</a></p></blockquote><p>同样, 时间不多的同学可以只看一下《计算机网络： 自顶向下方法》就可以了, 不需要再去刷公开课的<code>Lab</code>, 反正后面你大概率会通过C++网络编程的学习巩固这些知识</p><h2 id="2-4-数据库"><a href="#2-4-数据库" class="headerlink" title="2.4 数据库"></a>2.4 数据库</h2><p>数据库同样是我花了很多时间和精力去学习的，我学习路线是：<br><strong>步骤1： 阅读《MySQL是怎样运行的》</strong><br>阅读这本书，你会对数据库有一个基本的认识，并且大致了解其运行机制和实现原理。</p><p><strong>步骤2: 刷CMU 15445</strong><br>我是直接看了<code>CMU 15445</code>所有的视频课程, 并自己独立完成了<code>CMU 15445</code>的课程作业<code>bustub</code>。独立做完这个课程项目后，你的数据库方面的知识已经能够比绝大多数非数据库领域的校招生强了。</p><blockquote><p>其实我是先做的<code>CMU 15445</code>再看的《MySQL是怎样运行的》, 后者只要是为了应付秋招, 毕竟<code>CMU 15445</code>是宏观介绍所有的数据库基本知识, 而《MySQL是怎样运行的》聚焦于<code>MySQL</code>(国内最热门的数据库)</p></blockquote><p><strong>步骤3: 刷MIT6.824</strong><br>严格来说，数据库不等同于分布式系统，不过实际上他们结合很紧密，我这里就放在一起说了。<code>MIT6.824</code>是经典的分布式系统课程，基本上也是数据库领域校招生必学的课程。这里我同样完成了<code>MIT6.824</code>全套的<code>Lab</code>, 同样记录在<a href="https://www.zhihu.com/column/c_1725883332322226177">专栏: MIT6.8540(6.824)合集</a>和<a href="https://github.com/Vanilla-Beauty/MIT6.5840">Github</a></p><p><strong>步骤4: 刷Talent Plan的TinyKV</strong><br><code>Talent Plan</code>是国内数据库公司<code>PingCap</code>举办的线上训练营, 其中的<code>TinyKV</code>项目是<code>MIT6.824</code>的完善版本, 实现了基于<code>Raft</code>的分布式KV数据库</p><p>数据库领域也是我之前学习的主攻方向之一, 最后秋招去了业务后端岗还挺遗憾的。如果对这方面感兴趣，也可以看看我自己原创的<code>LSM Tree KV存储引擎</code>项目：<a href="https://github.com/Vanilla-Beauty/tiny-lsm">Tiny-LSM</a></p><blockquote><p>虽然这里的<code>MIT6.824</code>和<code>TinyKV</code>都是<code>Go</code>的项目而不是<code>C++</code>, 但他们的知识都是互通的, 且<code>C++</code>选手也会辅修<code>Go</code>的</p></blockquote><h1 id="3-阶段3-项目实践"><a href="#3-阶段3-项目实践" class="headerlink" title="3 阶段3-项目实践"></a>3 阶段3-项目实践</h1><p>在完成了四大件的学习后, 基本上就可以开始做项目了。这里的项目可以直接用之前学习四大件时候的公开课项目来做, 也可以自己找网上的项目。</p><h2 id="3-1-TinyWebServer-烂大街-做过"><a href="#3-1-TinyWebServer-烂大街-做过" class="headerlink" title="3.1 TinyWebServer(烂大街&#x2F;做过)"></a>3.1 TinyWebServer(烂大街&#x2F;做过)</h2><p><a href="https://github.com/qinguoyi/TinyWebServer">TinyWebServer</a>虽然已经烂大街了, 但是项目本身的内容还是非常值得学习的。同时，这个项目基本上就是《Linux高性能服务器编程》这本书附带项目的进阶版本， 因此强烈建议阅读这本书。</p><p><img src="/../../images/Notes/HPLSP.png" alt="HPLSP"></p><h2 id="3-2-协程库-做过"><a href="#3-2-协程库-做过" class="headerlink" title="3.2 协程库(做过)"></a>3.2 协程库(做过)</h2><p>网上的协程库项目非常多，尤其是在<code>C++ 20</code>以前, 协程库是热门轮子项目。这里最推荐的是：<a href="https://github.com/sylar-yin/sylar">sylar</a>, 其本身是一个C++高性能分布式服务器框架, 协程只是其中的一个模块, 该项目有全套的视频课程, 可以参考。</p><h2 id="3-3-RPC框架-看过没做过"><a href="#3-3-RPC框架-看过没做过" class="headerlink" title="3.3 RPC框架(看过没做过)"></a>3.3 RPC框架(看过没做过)</h2><p><code>RPC</code>框架也是非常热门的轮子项目, 这里推荐一个开源的<code>RPC</code>框架：<a href="https://github.com/Gooddbird/tinyrpc">tinyrpc</a>, 同样是文档齐全易于学习。（不过我只是看过没做过，毕竟人的精力是有限的）</p><h2 id="3-4-单机KV存储引擎-个人广告"><a href="#3-4-单机KV存储引擎-个人广告" class="headerlink" title="3.4 单机KV存储引擎(个人广告)"></a>3.4 单机KV存储引擎(个人广告)</h2><p>这一小节是给自己打个广告, 我自己基于<code>LSM Tree</code>开发了一个KV存储引擎, 且兼容<code>Redis</code>的<code>Resp</code>协议, 换句话说可以代替<code>redis-server</code>作为<code>redis</code>的后端(相信大家知道这个创新性了), 欢迎大家来学习: <a href="https://github.com/Vanilla-Beauty/tiny-lsm">Tiny-LSM</a></p><h1 id="4-阶段4-常用工具学习-秋招-实现面试必考"><a href="#4-阶段4-常用工具学习-秋招-实现面试必考" class="headerlink" title="4 阶段4-常用工具学习(秋招&#x2F;实现面试必考)"></a>4 阶段4-常用工具学习(秋招&#x2F;实现面试必考)</h1><p>在完成上面所有的学习路线后, 就可以开始准备秋招或者实习了, 但还需要额外学习一些常用工具, 也就是组件中间件什么的:</p><h2 id="4-1-Docker-K8S"><a href="#4-1-Docker-K8S" class="headerlink" title="4.1 Docker&#x2F;K8S"></a>4.1 Docker&#x2F;K8S</h2><p>我学习<code>Docker</code>是基于这个视频课程: <a href="https://kodekloud.com/courses/docker-for-the-absolute-beginner/">https://kodekloud.com/courses/docker-for-the-absolute-beginner/</a></p><p><code>K8S</code>相对<code>Docker</code>来说更复杂, 但属于是锦上添花的技术栈, 这里推荐在线教程 <a href="https://k8s-tutorials.pages.dev/">Kubernetes 练习手册</a></p><h2 id="4-2-Redis"><a href="#4-2-Redis" class="headerlink" title="4.2 Redis"></a>4.2 Redis</h2><p><code>Redis</code>的学习分为2部分:</p><p><strong>第一部分: Redis的使用</strong><br>这一部分直接到Bilibili上搜一份视频课程就可以了, 你英文好也可以看官方文档</p><p><strong>第二部分: Redis的原理</strong><br>这部分如果有时间, 推荐阅读书籍《Redis设计与实现》</p><p><img src="/../../images/Notes/redis-book.png" alt="redis-book"></p><h2 id="4-3-Nginx"><a href="#4-3-Nginx" class="headerlink" title="4.3 Nginx"></a>4.3 Nginx</h2><p>和<code>Redis</code>类似, 基础的知识找个视频教程就可以了, 进阶学习我推荐《深入理解Nginx》</p><p><img src="/../../images/Notes/Nginx-book.png" alt="Nginx-book"></p><h2 id="4-4-Linux环境工具链"><a href="#4-4-Linux环境工具链" class="headerlink" title="4.4 Linux环境工具链"></a>4.4 Linux环境工具链</h2><p>这里的工具链包括<code>Git/Cmake/Make/GDB</code>等, 每一个都不难, 只需要搜索对应的视频教程学习就好, 这里不展开介绍了</p><h1 id="5-小节"><a href="#5-小节" class="headerlink" title="5 小节"></a>5 小节</h1><p>我上面的学习路线是我个人的学习路线, 仅供参考, 具体的学习路线还是要根据自己的情况来定. </p><p>我个人的学习路线非常庞杂, 如果你想参考我的学习路线但是时间不够的话, 建议进行适当的删减, 例如去除掉刷公开课的环境, 直觉阅读教程或者八股.</p><p>希望我的经验能够帮助到你们, 祝大家都能顺利找到心仪的工作, 也希望大家能多多交流学习经验, 共同进步!</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;0-前言&quot;&gt;&lt;a href=&quot;#0-前言&quot; class=&quot;headerlink&quot; title=&quot;0 前言&quot;&gt;&lt;/a&gt;0 前言&lt;/h1&gt;&lt;p&gt;这篇文章记录了我&lt;code&gt;C++&lt;/code&gt;开发的学习路线，领域包含互联网后端、数据库、存储、操作系统等。虽然我最后秋招</summary>
      
    
    
    
    <category term="随笔" scheme="http://example.com/categories/%E9%9A%8F%E7%AC%94/"/>
    
    
    <category term="C++" scheme="http://example.com/tags/C/"/>
    
    <category term="秋招" scheme="http://example.com/tags/%E7%A7%8B%E6%8B%9B/"/>
    
    <category term="实习" scheme="http://example.com/tags/%E5%AE%9E%E4%B9%A0/"/>
    
  </entry>
  
  <entry>
    <title>C++从零开始实现LSM-Tree-KV存储-20-Python-SDK开发</title>
    <link href="http://example.com/2025/04/22/%E9%A1%B9%E7%9B%AE/Tiny-LSM/C++%E4%BB%8E%E9%9B%B6%E5%BC%80%E5%A7%8B%E5%AE%9E%E7%8E%B0LSM-Tree-KV%E5%AD%98%E5%82%A8-20-Python-SDK%E5%BC%80%E5%8F%91/"/>
    <id>http://example.com/2025/04/22/%E9%A1%B9%E7%9B%AE/Tiny-LSM/C++%E4%BB%8E%E9%9B%B6%E5%BC%80%E5%A7%8B%E5%AE%9E%E7%8E%B0LSM-Tree-KV%E5%AD%98%E5%82%A8-20-Python-SDK%E5%BC%80%E5%8F%91/</id>
    <published>2025-04-22T07:48:11.000Z</published>
    <updated>2026-05-28T07:37:46.670Z</updated>
    
    <content type="html"><![CDATA[<p>这一章是加餐，主要是给大家介绍一下如何使用<code>Python SDK</code>来操作我们之前实现的LSM-Tree-KV存储。</p><p>代码仓库：<a href="https://github.com/Vanilla-Beauty/tiny-lsm">Vanilla-Beauty&#x2F;tiny-lsm: A KV storage engine based on LSM Tree, supporting Redis RESP</a> 感谢您的 <code>Star</code>!</p><p>欢迎支持本项目同步更新的从零开始实现的视频教程：<a href="https://avo6166ew2u.feishu.cn/docx/LXmVdezdsoTBRaxC97WcHwGunOc">https://avo6166ew2u.feishu.cn/docx/LXmVdezdsoTBRaxC97WcHwGunOc</a></p><p>欢迎加入讨论群：<a href="https://qm.qq.com/q/lkIzCPvcdM">Tiny-LSM项目交流群</a></p><h1 id="1-SDK-的作用"><a href="#1-SDK-的作用" class="headerlink" title="1 SDK 的作用"></a>1 SDK 的作用</h1><p><code>Python SDK</code>的作用是提供一个简单易用的接口，让用户可以使用<code>Python</code>这样的脚本语言方便地使用我们实现的<code>LSM-Tree-KV</code>存储。通常来说, 成熟的数据库等基础设施都会提供类似<code>Python</code>或者<code>JavaScript</code>这样的脚本语言的SDK，方便用户进行二次开发和使用。否则， 就只能使用<code>C++</code>这样的编译型语言来进行开发，或者调用动态链接库的接口，这样会比较麻烦。</p><p>这里我选择<code>Python</code>的<code>SDk</code>, 主要原因是<code>Python</code>的语法简单易懂，且有丰富的第三方库支持, 并且脚本语言也非常方便, 我自己调试的时候也更想用<code>Python</code>先验证一些流程的正确性, 然后再到具体的错误流程中进行<code>C++</code>的单步调试。</p><p><code>C/C++</code>到<code>Python</code>的SDK开发主要是通过<code>pybind11</code>这个库来实现的。<code>pybind11</code>是一个轻量级的库，可以让我们很方便地将<code>C++</code>代码暴露给<code>Python</code>，并且支持大部分的<code>C++</code>特性，包括类、函数、模板等。本章接下来的内容主要是介绍如何使用<code>pybind11</code>来实现一个简单的<code>Python SDK</code>，并且提供一些简单的示例代码来演示如何使用这个SDK。</p><h1 id="2-pybind11-简介"><a href="#2-pybind11-简介" class="headerlink" title="2 pybind11 简介"></a>2 pybind11 简介</h1><h2 id="2-1-pybind11-是什么"><a href="#2-1-pybind11-是什么" class="headerlink" title="2.1 pybind11 是什么"></a>2.1 pybind11 是什么</h2><p><code>pybind11</code> 是一个用于将 <strong>C++ 函数、类、模块</strong> 暴露给 <strong>Python 调用</strong> 的轻量级头文件库。它的目标是：</p><ul><li><strong>简洁语法</strong>：像写 Python 模块一样写 C++ 绑定。</li><li><strong>无依赖性</strong>：只需要头文件，无需链接库。</li><li><strong>性能极高</strong>：性能几乎与纯 C++ 接近。</li></ul><p>适用于以下场景：</p><ul><li>Python 调用高性能 C++ 模块（如计算密集型任务）</li><li>Python 扩展模块编写（替代 CPython 的复杂 C API）</li><li>将现有 C++ 库封装为 Python 模块</li></ul><h2 id="2-2-安装与环境配置"><a href="#2-2-安装与环境配置" class="headerlink" title="2.2 安装与环境配置"></a>2.2 安装与环境配置</h2><p>安装 <code>Python</code> 端 <code>pybind11</code>（用于提供 <code>--includes</code> 编译选项）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">pip install pybind11</span><br></pre></td></tr></table></figure><p>获取 C++ 头文件（两种方式）：</p><ol><li>使用 pip 安装版本，编译时用 <code>python -m pybind11 --includes</code></li><li>或者 clone 官方仓库：<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/pybind/pybind11.git</span><br></pre></td></tr></table></figure></li></ol><p>这里我们在项目中可以用<code>xmake</code>的包管理工具添加依赖:</p><figure class="highlight lua"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">add_requires(<span class="string">&quot;pybind11&quot;</span>)</span><br><span class="line"></span><br><span class="line">target(<span class="string">&quot;lsm_pybind&quot;</span>)</span><br><span class="line">    ...</span><br><span class="line">    add_packages(<span class="string">&quot;pybind11&quot;</span>)</span><br><span class="line">    ...</span><br></pre></td></tr></table></figure><h2 id="2-3-示例：绑定-C-函数"><a href="#2-3-示例：绑定-C-函数" class="headerlink" title="2.3 示例：绑定 C++ 函数"></a>2.3 示例：绑定 C++ 函数</h2><p><strong>📄 <code>example.cpp</code> 文件：</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pybind11/pybind11.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 普通 C++ 函数</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> i, <span class="type">int</span> j)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> i + j;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 创建 Python 模块</span></span><br><span class="line"><span class="built_in">PYBIND11_MODULE</span>(example, m) &#123;</span><br><span class="line">    m.<span class="built_in">doc</span>() = <span class="string">&quot;pybind11 example plugin&quot;</span>; <span class="comment">// 模块说明（可选）</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// m.def(&quot;python函数名&quot;, C++函数指针, &quot;函数文档字符串&quot;)</span></span><br><span class="line">    m.<span class="built_in">def</span>(<span class="string">&quot;add&quot;</span>, &amp;add, <span class="string">&quot;A function that adds two numbers&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>🚀 编译命令（Linux&#x2F;macOS）：</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">c++ -O3 -Wall -shared -std=c++11 -fPIC \</span><br><span class="line">    $(python3 -m pybind11 --includes) example.cpp \</span><br><span class="line">    -o example$(python3-config --extension-suffix)</span><br></pre></td></tr></table></figure><p>📌 <strong>说明</strong>：</p><ul><li><code>-O3</code>：优化等级</li><li><code>-fPIC</code>：生成与位置无关的代码</li><li><code>--extension-suffix</code>：生成正确的 <code>.so</code> 或 <code>.pyd</code> 文件名</li></ul><p><strong>🐍 在 Python 中使用</strong></p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> example</span><br><span class="line"><span class="built_in">print</span>(example.add(<span class="number">3</span>, <span class="number">4</span>))  <span class="comment"># 输出 7</span></span><br></pre></td></tr></table></figure><hr><h2 id="2-4-绑定类（C-类-→-Python-类）"><a href="#2-4-绑定类（C-类-→-Python-类）" class="headerlink" title="2.4 绑定类（C++ 类 → Python 类）"></a>2.4 绑定类（C++ 类 → Python 类）</h2><p><strong>📄 <code>example.cpp</code> 中绑定类：</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pybind11/pybind11.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> pybind11;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Pet</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Pet</span>(<span class="type">const</span> std::string &amp;name) : <span class="built_in">name</span>(name) &#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">setName</span><span class="params">(<span class="type">const</span> std::string &amp;name_)</span> </span>&#123; name = name_; &#125;</span><br><span class="line">    <span class="function"><span class="type">const</span> std::string &amp;<span class="title">getName</span><span class="params">()</span> <span class="type">const</span> </span>&#123; <span class="keyword">return</span> name; &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    std::string name;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="built_in">PYBIND11_MODULE</span>(example, m) &#123;</span><br><span class="line">    <span class="built_in">class_</span>&lt;Pet&gt;(m, <span class="string">&quot;Pet&quot;</span>)</span><br><span class="line">        .<span class="built_in">def</span>(<span class="built_in">init</span>&lt;<span class="type">const</span> std::string &amp;&gt;()) <span class="comment">// 绑定构造函数</span></span><br><span class="line">        .<span class="built_in">def</span>(<span class="string">&quot;setName&quot;</span>, &amp;Pet::setName)</span><br><span class="line">        .<span class="built_in">def</span>(<span class="string">&quot;getName&quot;</span>, &amp;Pet::getName);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>🐍 Python 中使用：</strong></p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> example</span><br><span class="line"></span><br><span class="line">dog = example.Pet(<span class="string">&quot;Buddy&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(dog.getName())   <span class="comment"># 输出：Buddy</span></span><br><span class="line">dog.setName(<span class="string">&quot;Rocky&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(dog.getName())   <span class="comment"># 输出：Rocky</span></span><br></pre></td></tr></table></figure><p>📌 <strong>关键语法说明</strong>：</p><ul><li><code>class_&lt;T&gt;(module, &quot;Python类名&quot;)</code>：绑定 C++ 类</li><li><code>.def(init&lt;Args...&gt;())</code>：绑定构造函数</li><li><code>.def(&quot;方法名&quot;, &amp;方法指针)</code>：绑定成员方法</li></ul><hr><h2 id="2-5-传递-STL-类型"><a href="#2-5-传递-STL-类型" class="headerlink" title="2.5 传递 STL 类型"></a>2.5 传递 STL 类型</h2><p>pybind11 支持常见 STL 类型的自动转换：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pybind11/pybind11.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pybind11/stl.h&gt;</span> <span class="comment">// STL 支持</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;vector&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function">std::vector&lt;<span class="type">int</span>&gt; <span class="title">generate</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> &#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>&#125;;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">PYBIND11_MODULE</span>(example, m) &#123;</span><br><span class="line">    m.<span class="built_in">def</span>(<span class="string">&quot;generate&quot;</span>, &amp;generate);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Python 使用：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">print</span>(example.generate())  <span class="comment"># [1, 2, 3, 4]</span></span><br></pre></td></tr></table></figure><h2 id="2-6-总结"><a href="#2-6-总结" class="headerlink" title="2.6 总结"></a>2.6 总结</h2><table><thead><tr><th>功能</th><th>语法</th><th>说明</th></tr></thead><tbody><tr><td>绑定函数</td><td><code>m.def(&quot;name&quot;, &amp;func)</code></td><td>将 C++ 函数暴露给 Python</td></tr><tr><td>绑定类</td><td><code>class_&lt;T&gt;(m, &quot;Class&quot;)</code></td><td>创建 Python 类</td></tr><tr><td>构造函数</td><td><code>.def(init&lt;Args...&gt;())</code></td><td>构造器绑定</td></tr><tr><td>成员方法</td><td><code>.def(&quot;name&quot;, &amp;method)</code></td><td>类成员方法绑定</td></tr><tr><td>STL支持</td><td><code>#include &lt;pybind11/stl.h&gt;</code></td><td>启用 vector&#x2F;map&#x2F;list 自动转换</td></tr><tr><td>NumPy支持</td><td><code>#include &lt;pybind11/numpy.h&gt;</code></td><td>绑定 NumPy 数组</td></tr></tbody></table><h1 id="3-Python-SDK-实现"><a href="#3-Python-SDK-实现" class="headerlink" title="3 Python SDK 实现"></a>3 Python SDK 实现</h1><h2 id="3-1-SDK-文件夹结构"><a href="#3-1-SDK-文件夹结构" class="headerlink" title="3.1 SDK 文件夹结构"></a>3.1 SDK 文件夹结构</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">sdk/</span><br><span class="line">├── lsm_pybind.cpp      <span class="comment"># Pybind11绑定入口</span></span><br><span class="line">├── tonilsm/</span><br><span class="line">│   ├── __init__.py     <span class="comment"># 包安装脚本</span></span><br><span class="line">│   ├── tonilsm/</span><br><span class="line">│   │    ├── __init__.py    <span class="comment"># 核心模块加载器</span></span><br><span class="line">│   │  ├── __init__.pyi   <span class="comment"># 类型提示文件</span></span><br><span class="line">│   │  └── core/          <span class="comment"># 编译产物目录</span></span><br><span class="line">├── └setup.py <span class="comment"># Python包安装脚本</span></span><br><span class="line">xmake.lua             <span class="comment"># 构建配置文件</span></span><br><span class="line">src/                  <span class="comment"># C++存储引擎源码</span></span><br><span class="line">include/              <span class="comment"># C++头文件</span></span><br></pre></td></tr></table></figure><p>这里对每个模块说明如下:</p><ul><li><code>sdk/</code>：SDK的根目录<ul><li><code>lsm_pybind.cpp</code>：SDK的绑定入口文件，主要是将C++的类和函数绑定到<code>Python</code>中</li><li><code>tonilsm/</code>：<code>Python</code>模块的目录，包含了<code>Python</code>模块的核心代码和类型提示文件，以及类型提示文件<ul><li><code>tonilsm/__init__.py</code>：<code>Python</code>模块的安装脚本，主要是将<code>tonilsm</code>模块导入到<code>Python</code>中</li><li><code>tonilsm/__init__.pyi</code>：类型提示文件，主要是给<code>Python</code>的IDE提供类型提示</li><li><code>tonilsm/core/</code>：编译产物目录，主要是存放编译后的<code>Python</code>模块</li><li><code>setup.py</code>：<code>Python</code>模块的安装脚本，主要是将<code>tonilsm</code>模块安装到<code>Python</code>中</li></ul></li></ul></li><li><code>xmake.lua</code>：<code>xmake</code>的构建配置文件，主要是配置<code>C++</code>的编译选项和依赖</li></ul><h2 id="3-2-代码绑定"><a href="#3-2-代码绑定" class="headerlink" title="3.2 代码绑定"></a>3.2 代码绑定</h2><h3 id="3-2-1-代码绑定的C-代码"><a href="#3-2-1-代码绑定的C-代码" class="headerlink" title="3.2.1 代码绑定的C++代码"></a>3.2.1 代码绑定的C++代码</h3><p>这里我们只需要将<code>lsm/engine.h</code> 中的类和函数绑定到 <code>Python</code> 中即可, 但由于我们的主类类似<code>beigin</code>这样的迭代器返回的对象是一个迭代器, 且参数中包括一些自定义的隔离级别的枚举, 所以我们需要先将这些自定义数据类型也绑定到 <code>Python</code> 中。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// sdk/lsm_pybind.cpp</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;../include/lsm/engine.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;../include/lsm/level_iterator.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pybind11/functional.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pybind11/pybind11.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pybind11/stl.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">namespace</span> py = pybind11;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 绑定 TwoMergeIterator 迭代器</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">bind_TwoMergeIterator</span><span class="params">(py::<span class="keyword">module</span> &amp;m)</span> </span>&#123;</span><br><span class="line">  py::<span class="built_in">class_</span>&lt;TwoMergeIterator&gt;(m, <span class="string">&quot;TwoMergeIterator&quot;</span>)</span><br><span class="line">      .<span class="built_in">def</span>(<span class="string">&quot;__iter__&quot;</span>, [](TwoMergeIterator &amp;it) &#123; <span class="keyword">return</span> &amp;it; &#125;)</span><br><span class="line">      .<span class="built_in">def</span>(<span class="string">&quot;__next__&quot;</span>, [](TwoMergeIterator &amp;it) &#123;</span><br><span class="line">        <span class="keyword">if</span> (!it.<span class="built_in">is_valid</span>())</span><br><span class="line">          <span class="keyword">throw</span> py::<span class="built_in">stop_iteration</span>();</span><br><span class="line">        <span class="keyword">auto</span> kv = *it;</span><br><span class="line">        ++it;</span><br><span class="line">        <span class="keyword">return</span> py::<span class="built_in">make_tuple</span>(kv.first, kv.second);</span><br><span class="line">      &#125;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">bind_Level_Iterator</span><span class="params">(py::<span class="keyword">module</span> &amp;m)</span> </span>&#123;</span><br><span class="line">  py::<span class="built_in">class_</span>&lt;Level_Iterator&gt;(m, <span class="string">&quot;Level_Iterator&quot;</span>)</span><br><span class="line">      .<span class="built_in">def</span>(<span class="string">&quot;__iter__&quot;</span>, [](Level_Iterator &amp;it) &#123; <span class="keyword">return</span> &amp;it; &#125;)</span><br><span class="line">      .<span class="built_in">def</span>(<span class="string">&quot;__next__&quot;</span>, [](Level_Iterator &amp;it) &#123;</span><br><span class="line">        <span class="keyword">if</span> (!it.<span class="built_in">is_valid</span>())</span><br><span class="line">          <span class="keyword">throw</span> py::<span class="built_in">stop_iteration</span>();</span><br><span class="line">        <span class="keyword">auto</span> kv = *it;</span><br><span class="line">        ++it;</span><br><span class="line">        <span class="keyword">return</span> py::<span class="built_in">make_tuple</span>(kv.first, kv.second);</span><br><span class="line">      &#125;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 绑定 TranContext 事务上下文</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 提前声明 TranContext（如果头文件未包含）</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">TranContext</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 绑定 TranContext</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">bind_TranContext</span><span class="params">(py::<span class="keyword">module</span> &amp;m)</span> </span>&#123;</span><br><span class="line">  py::class_&lt;TranContext, std::shared_ptr&lt;TranContext&gt;&gt;(m, <span class="string">&quot;TranContext&quot;</span>)</span><br><span class="line">      .<span class="built_in">def</span>(<span class="string">&quot;commit&quot;</span>, &amp;TranContext::commit,</span><br><span class="line">           py::<span class="built_in">arg</span>(<span class="string">&quot;test_fail&quot;</span>) = <span class="literal">false</span>) <span class="comment">// 处理默认参数</span></span><br><span class="line">      .<span class="built_in">def</span>(<span class="string">&quot;abort&quot;</span>, &amp;TranContext::abort)</span><br><span class="line">      .<span class="built_in">def</span>(<span class="string">&quot;get&quot;</span>, &amp;TranContext::get)</span><br><span class="line">      .<span class="built_in">def</span>(<span class="string">&quot;remove&quot;</span>, &amp;TranContext::remove)</span><br><span class="line">      .<span class="built_in">def</span>(<span class="string">&quot;put&quot;</span>, &amp;TranContext::put);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">bind_IsolationLevel</span><span class="params">(py::<span class="keyword">module</span> &amp;m)</span> </span>&#123;</span><br><span class="line">  py::<span class="built_in">enum_</span>&lt;IsolationLevel&gt;(m, <span class="string">&quot;IsolationLevel&quot;</span>)</span><br><span class="line">      .<span class="built_in">value</span>(<span class="string">&quot;READ_UNCOMMITTED&quot;</span>, IsolationLevel::READ_UNCOMMITTED)</span><br><span class="line">      .<span class="built_in">value</span>(<span class="string">&quot;READ_COMMITTED&quot;</span>, IsolationLevel::READ_COMMITTED)</span><br><span class="line">      .<span class="built_in">value</span>(<span class="string">&quot;REPEATABLE_READ&quot;</span>, IsolationLevel::REPEATABLE_READ)</span><br><span class="line">      .<span class="built_in">value</span>(<span class="string">&quot;SERIALIZABLE&quot;</span>, IsolationLevel::SERIALIZABLE)</span><br><span class="line">      .<span class="built_in">export_values</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>pybind11 语法解释：</p><ul><li><code>py::class_</code>: 用于将 C++ 类暴露给 Python。第一个参数是 C++ 类类型，第二个参数是 Python 类的名称。</li><li><code>def()</code>: 用于绑定方法或函数。例如，.def(“方法名”, &amp;类::方法) 将 C++ 方法绑定到 Python 方法。</li><li><code>py::arg()</code>: 允许指定默认参数。例如，py::arg(“test_fail”) &#x3D; false 设置 test_fail 参数的默认值。</li><li><code>py::make_tuple()</code>: 将 C++ 元组转换为 Python 元组，方便返回多个值。</li><li><code>py::stop_iteration()</code>: 用于在 Python 中信号迭代结束，类似于抛出 StopIteration 异常。</li><li><code>py::enum_</code>: 用于将 C++ 枚举暴露给 Python。每个 .value() 调用将 C++ 枚举常量映射到 Python 枚举常量。</li><li><code>export_values()</code>: 导出所有定义的枚举值，以便可以直接访问，而不需要使用枚举名称前缀。</li></ul><p>在此后, 我们需要将<code>LSM</code>这个对外暴露的<code>C++</code>类绑定到<code>Python</code>中, 这里我们只需要将<code>LSM</code>类的构造函数和一些对外暴露的接口绑定到<code>Python</code>中即可, 具体代码如下:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// sdk/lsm_pybind.cpp</span></span><br><span class="line"><span class="built_in">PYBIND11_MODULE</span>(lsm_pybind, m) &#123;</span><br><span class="line">  <span class="comment">// 绑定辅助类</span></span><br><span class="line">  <span class="built_in">bind_TwoMergeIterator</span>(m);</span><br><span class="line">  <span class="built_in">bind_Level_Iterator</span>(m);</span><br><span class="line">  <span class="built_in">bind_TranContext</span>(m);</span><br><span class="line">  <span class="built_in">bind_IsolationLevel</span>(m);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 主类 LSM</span></span><br><span class="line">  py::<span class="built_in">class_</span>&lt;LSM&gt;(m, <span class="string">&quot;LSM&quot;</span>)</span><br><span class="line">      .<span class="built_in">def</span>(py::<span class="built_in">init</span>&lt;<span class="type">const</span> std::string &amp;&gt;())</span><br><span class="line">      <span class="comment">// 基础操作</span></span><br><span class="line">      .<span class="built_in">def</span>(<span class="string">&quot;put&quot;</span>, &amp;LSM::put, py::<span class="built_in">arg</span>(<span class="string">&quot;key&quot;</span>), py::<span class="built_in">arg</span>(<span class="string">&quot;value&quot;</span>),</span><br><span class="line">           <span class="string">&quot;Insert a key-value pair (bytes type)&quot;</span>)</span><br><span class="line">      .<span class="built_in">def</span>(<span class="string">&quot;get&quot;</span>, &amp;LSM::get, py::<span class="built_in">arg</span>(<span class="string">&quot;key&quot;</span>),</span><br><span class="line">           <span class="string">&quot;Get value by key, returns None if not found&quot;</span>)</span><br><span class="line">      .<span class="built_in">def</span>(<span class="string">&quot;remove&quot;</span>, &amp;LSM::remove, py::<span class="built_in">arg</span>(<span class="string">&quot;key&quot;</span>), <span class="string">&quot;Delete a key&quot;</span>)</span><br><span class="line">      <span class="comment">// 批量操作</span></span><br><span class="line">      .<span class="built_in">def</span>(<span class="string">&quot;put_batch&quot;</span>, &amp;LSM::put_batch, py::<span class="built_in">arg</span>(<span class="string">&quot;kvs&quot;</span>),</span><br><span class="line">           <span class="string">&quot;Batch insert key-value pairs&quot;</span>)</span><br><span class="line">      .<span class="built_in">def</span>(<span class="string">&quot;remove_batch&quot;</span>, &amp;LSM::remove_batch, py::<span class="built_in">arg</span>(<span class="string">&quot;keys&quot;</span>),</span><br><span class="line">           <span class="string">&quot;Batch delete keys&quot;</span>)</span><br><span class="line">      <span class="comment">// 迭代器</span></span><br><span class="line">      .<span class="built_in">def</span>(<span class="string">&quot;begin&quot;</span>, &amp;LSM::begin, py::<span class="built_in">arg</span>(<span class="string">&quot;tranc_id&quot;</span>),</span><br><span class="line">           <span class="string">&quot;Start an iterator with transaction ID&quot;</span>)</span><br><span class="line">      .<span class="built_in">def</span>(<span class="string">&quot;end&quot;</span>, &amp;LSM::end, <span class="string">&quot;Get end iterator&quot;</span>)</span><br><span class="line">      <span class="comment">// 事务</span></span><br><span class="line">      .<span class="built_in">def</span>(<span class="string">&quot;begin_tran&quot;</span>, &amp;LSM::begin_tran, py::<span class="built_in">arg</span>(<span class="string">&quot;isolation_level&quot;</span>),</span><br><span class="line">           <span class="string">&quot;Start a transaction&quot;</span>)</span><br><span class="line">      <span class="comment">// 其他方法</span></span><br><span class="line">      .<span class="built_in">def</span>(<span class="string">&quot;clear&quot;</span>, &amp;LSM::clear, <span class="string">&quot;Clear all data&quot;</span>) <span class="comment">// ! Fix bugs</span></span><br><span class="line">      .<span class="built_in">def</span>(<span class="string">&quot;flush&quot;</span>, &amp;LSM::flush, <span class="string">&quot;Flush memory table to disk&quot;</span>)</span><br><span class="line">      .<span class="built_in">def</span>(<span class="string">&quot;flush_all&quot;</span>, &amp;LSM::flush_all, <span class="string">&quot;Flush all pending data&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="3-2-2-编译为动态链接库"><a href="#3-2-2-编译为动态链接库" class="headerlink" title="3.2.2 编译为动态链接库"></a>3.2.2 编译为动态链接库</h3><p>我们做的这些绑定操作都需要编译为动态链接库, 这里我们使用<code>xmake</code>来编译, 具体的<code>xmake.lua</code>文件如下:</p><figure class="highlight lua"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">target(<span class="string">&quot;lsm_pybind&quot;</span>)</span><br><span class="line">    set_kind(<span class="string">&quot;shared&quot;</span>)</span><br><span class="line">    add_files(<span class="string">&quot;sdk/lsm_pybind.cpp&quot;</span>)</span><br><span class="line">    add_packages(<span class="string">&quot;pybind11&quot;</span>)</span><br><span class="line">    add_deps(<span class="string">&quot;lsm_shared&quot;</span>)</span><br><span class="line">    add_includedirs(<span class="string">&quot;include&quot;</span>, &#123;public = <span class="literal">true</span>&#125;)</span><br><span class="line">    set_targetdir(<span class="string">&quot;$(buildir)/lib&quot;</span>)</span><br><span class="line">    set_filename(<span class="string">&quot;lsm_pybind.so&quot;</span>)  <span class="comment">-- 确保生成的文件名为 lsm_pybind.so</span></span><br><span class="line">    add_ldflags(<span class="string">&quot;-Wl,-rpath,$ORIGIN&quot;</span>)</span><br><span class="line">    add_defines(<span class="string">&quot;TONILSM_EXPORT=__attribute__((visibility(\&quot;default\&quot;)))&quot;</span>)</span><br><span class="line">    add_cxxflags(<span class="string">&quot;-fvisibility=hidden&quot;</span>)  <span class="comment">-- 隐藏非导出符号</span></span><br></pre></td></tr></table></figure><h2 id="3-3-Python包组织"><a href="#3-3-Python包组织" class="headerlink" title="3.3 Python包组织"></a>3.3 Python包组织</h2><h3 id="3-3-1-模块结构"><a href="#3-3-1-模块结构" class="headerlink" title="3.3.1 模块结构"></a>3.3.1 模块结构</h3><p>在有了这个动态链接库后, 我们需要将这个动态链接库组织成一个<code>Python</code>包, 这部分模块结构为:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">tonilsm</span><br><span class="line">├── tonilsm</span><br><span class="line">│   ├── core</span><br><span class="line">│   │   ├── __init__.py</span><br><span class="line">│   │   ├── __init__.pyi</span><br><span class="line">│   │   ├── lib</span><br><span class="line">│   │   │   ├── liblsm_shared.so</span><br><span class="line">│   │   │   └── lsm_pybind.so</span><br><span class="line">│   ├── __init__.py</span><br><span class="line">│   └── setup.py</span><br></pre></td></tr></table></figure><blockquote><p>这里忽略了自动生成和缓存的文件夹和文件</p></blockquote><p>这里简单介绍下这部分的模块:</p><ul><li><p><strong><code>tonilsm/</code></strong>: 这是整个 Python 包的根目录。</p><ul><li><strong><code>tonilsm/__init__.py</code></strong>: 这个文件用于初始化 <code>tonilsm</code> 包。它可以包含包的导入逻辑和其他初始化代码。</li></ul></li><li><p><strong><code>tonilsm/core/</code></strong>: 这个目录包含核心的实现代码和动态链接库。</p><ul><li><p><strong><code>tonilsm/core/__init__.py</code></strong>: 这个文件用于初始化 <code>core</code> 子包。通常在这个文件中导入动态链接库中的模块，以便用户可以直接从 <code>tonilsm.core</code> 中访问这些模块。</p></li><li><p><strong><code>tonilsm/core/__init__.pyi</code></strong>: 这个文件是类型提示文件（type stub），用于提供类型信息，帮助 IDE 和静态类型检查工具（如 <code>mypy</code>）更好地理解代码结构。</p></li><li><p><strong><code>tonilsm/core/lib/</code></strong>: 这个目录存放编译生成的动态链接库文件。</p><ul><li><strong><code>tonilsm/core/lib/liblsm_shared.so</code></strong>: 这是 C++ 存储引擎的共享库文件。</li><li><strong><code>tonilsm/core/lib/lsm_pybind.so</code></strong>: 这是通过 <code>pybind11</code> 绑定生成的 Python 扩展模块。</li></ul></li></ul></li></ul><h3 id="3-3-2-模块导出"><a href="#3-3-2-模块导出" class="headerlink" title="3.3.2 模块导出"></a>3.3.2 模块导出</h3><p><code>tonilsm/__init__.py</code>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># tonilsm/__init__.py</span></span><br><span class="line"><span class="keyword">from</span> .core <span class="keyword">import</span> LSM, IsolationLevel  <span class="comment"># 从 core 子模块导出 LSM 类</span></span><br><span class="line"></span><br><span class="line">__all__ = [<span class="string">&quot;LSM&quot;</span>, <span class="string">&quot;IsolationLevel&quot;</span>]</span><br></pre></td></tr></table></figure><p>这里的作用是将 <code>LSM</code>、<code>IsolationLevel</code> 模块导入到 <code>tonilsm</code> 包中，这样用户可以直接使用 <code>from tonilsm import ...</code> 的方式来访问核心功能。</p><h3 id="3-3-3-动态链接库加载"><a href="#3-3-3-动态链接库加载" class="headerlink" title="3.3.3 动态链接库加载"></a>3.3.3 动态链接库加载</h3><p>在 <code>tonilsm/core/__init__.py</code> 中，我们需要加载动态链接库 <code>lsm_pybind.so</code>，以便在 Python 中使用 C++ 实现的功能。我们可以使用 <code>importlib</code> 模块来动态加载这个库。</p><p><code>tonilsm/core/__init__.py</code>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> os</span><br><span class="line"><span class="keyword">import</span> sys</span><br><span class="line"><span class="keyword">import</span> importlib.util</span><br><span class="line"></span><br><span class="line"><span class="comment"># 获取当前目录</span></span><br><span class="line">current_dir = os.path.dirname(os.path.abspath(__file__))</span><br><span class="line"></span><br><span class="line"><span class="comment"># 添加动态链接库目录到系统路径</span></span><br><span class="line">lib_dir = os.path.join(current_dir, <span class="string">&#x27;lib&#x27;</span>)</span><br><span class="line">sys.path.append(lib_dir)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 加载动态链接库</span></span><br><span class="line">spec = importlib.util.spec_from_file_location(<span class="string">&quot;lsm_pybind&quot;</span>, os.path.join(lib_dir, <span class="string">&quot;lsm_pybind.so&quot;</span>))</span><br><span class="line">lsm_pybind = importlib.util.module_from_spec(spec)</span><br><span class="line">spec.loader.exec_module(lsm_pybind)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 导出模块中的类和函数</span></span><br><span class="line"><span class="keyword">from</span> lsm_pybind <span class="keyword">import</span> LSM, TwoMergeIterator, Level_Iterator, TranContext, IsolationLevel</span><br><span class="line"></span><br><span class="line">__all__ = [<span class="string">&#x27;LSM&#x27;</span>, <span class="string">&#x27;TwoMergeIterator&#x27;</span>, <span class="string">&#x27;Level_Iterator&#x27;</span>, <span class="string">&#x27;TranContext&#x27;</span>, <span class="string">&#x27;IsolationLevel&#x27;</span>]</span><br></pre></td></tr></table></figure><h3 id="3-3-4-类型提示文件"><a href="#3-3-4-类型提示文件" class="headerlink" title="3.3.4 类型提示文件"></a>3.3.4 类型提示文件</h3><p>类型提示文件 <code>tonilsm/core/__init__.pyi</code> 用于提供类型信息，帮助 IDE 和静态类型检查工具（如 <code>mypy</code>）更好地理解代码结构。这个文件的内容与 <code>tonilsm/core/__init__.py</code> 中的类和函数定义相对应。<br>这里我们需要定义 <code>LSM</code> 类、<code>TwoMergeIterator</code> 类、<code>Level_Iterator</code> 类、<code>TranContext</code> 类和 <code>IsolationLevel</code> 枚举类型的接口。<br><code>tonilsm/core/__init__.pyi</code> 文件的内容如下：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> typing <span class="keyword">import</span> *</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">LSM</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, path: <span class="built_in">str</span></span>) -&gt; <span class="literal">None</span>: ...</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">put</span>(<span class="params">self, key: <span class="built_in">bytes</span>, value: <span class="built_in">bytes</span></span>) -&gt; <span class="literal">None</span>: ...</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">get</span>(<span class="params">self, key: <span class="built_in">bytes</span></span>) -&gt; <span class="type">Optional</span>[<span class="built_in">bytes</span>]: ...</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">remove</span>(<span class="params">self, key: <span class="built_in">bytes</span></span>) -&gt; <span class="literal">None</span>: ...</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">put_batch</span>(<span class="params">self, kvs: <span class="type">List</span>[<span class="type">Tuple</span>[<span class="built_in">bytes</span>, <span class="built_in">bytes</span>]]</span>) -&gt; <span class="literal">None</span>: ...</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">remove_batch</span>(<span class="params">self, keys: <span class="type">List</span>[<span class="built_in">bytes</span>]</span>) -&gt; <span class="literal">None</span>: ...</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">begin</span>(<span class="params">self, tranc_id: <span class="built_in">int</span></span>) -&gt; <span class="string">&#x27;TwoMergeIterator&#x27;</span>: ...</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">end</span>(<span class="params">self</span>) -&gt; <span class="string">&#x27;TwoMergeIterator&#x27;</span>: ...</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">begin_tran</span>(<span class="params">self, isolation_level: <span class="string">&#x27;IsolationLevel&#x27;</span></span>) -&gt; <span class="string">&#x27;TranContext&#x27;</span>: ...</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">clear</span>(<span class="params">self</span>) -&gt; <span class="literal">None</span>: ...</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">flush</span>(<span class="params">self</span>) -&gt; <span class="literal">None</span>: ...</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">flush_all</span>(<span class="params">self</span>) -&gt; <span class="literal">None</span>: ...</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">TwoMergeIterator</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__iter__</span>(<span class="params">self</span>) -&gt; <span class="string">&#x27;TwoMergeIterator&#x27;</span>: ...</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__next__</span>(<span class="params">self</span>) -&gt; <span class="type">Tuple</span>[<span class="built_in">bytes</span>, <span class="built_in">bytes</span>]: ...</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Level_Iterator</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__iter__</span>(<span class="params">self</span>) -&gt; <span class="string">&#x27;Level_Iterator&#x27;</span>: ...</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__next__</span>(<span class="params">self</span>) -&gt; <span class="type">Tuple</span>[<span class="built_in">bytes</span>, <span class="built_in">bytes</span>]: ...</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">TranContext</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">commit</span>(<span class="params">self, test_fail: <span class="built_in">bool</span> = <span class="literal">False</span></span>) -&gt; <span class="literal">None</span>: ...</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">abort</span>(<span class="params">self</span>) -&gt; <span class="literal">None</span>: ...</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">get</span>(<span class="params">self, key: <span class="built_in">bytes</span></span>) -&gt; <span class="type">Optional</span>[<span class="built_in">bytes</span>]: ...</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">remove</span>(<span class="params">self, key: <span class="built_in">bytes</span></span>) -&gt; <span class="literal">None</span>: ...</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">put</span>(<span class="params">self, key: <span class="built_in">bytes</span>, value: <span class="built_in">bytes</span></span>) -&gt; <span class="literal">None</span>: ...</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">IsolationLevel</span>(<span class="title class_ inherited__">Enum</span>):</span><br><span class="line">    READ_UNCOMMITTED: <span class="string">&#x27;IsolationLevel&#x27;</span></span><br><span class="line">    READ_COMMITTED: <span class="string">&#x27;IsolationLevel&#x27;</span></span><br><span class="line">    REPEATABLE_READ: <span class="string">&#x27;IsolationLevel&#x27;</span></span><br><span class="line">    SERIALIZABLE: <span class="string">&#x27;IsolationLevel&#x27;</span></span><br></pre></td></tr></table></figure><h3 id="3-3-5-安装脚本"><a href="#3-3-5-安装脚本" class="headerlink" title="3.3.5 安装脚本"></a>3.3.5 安装脚本</h3><p>安装脚本 <code>tonilsm/setup.py</code> 用于构建和安装 Python 包。这个脚本使用 <code>setuptools</code> 构建 Python 包:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> os</span><br><span class="line"><span class="keyword">import</span> platform</span><br><span class="line"><span class="keyword">from</span> setuptools <span class="keyword">import</span> setup, find_packages</span><br><span class="line"><span class="keyword">from</span> setuptools.command.build_py <span class="keyword">import</span> build_py</span><br><span class="line"></span><br><span class="line"><span class="comment"># 版本和基本描述</span></span><br><span class="line">VERSION = <span class="string">&quot;0.1.0&quot;</span></span><br><span class="line">DESCRIPTION = <span class="string">&quot;A Python binding for Toni&#x27;s LSM Tree Storage Engine&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">CustomBuild</span>(<span class="title class_ inherited__">build_py</span>):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">run</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="comment"># 使用绝对路径确保准确性</span></span><br><span class="line">        build_lib_dir = os.path.abspath(os.path.join(os.path.dirname(__file__), <span class="string">&quot;../../build/lib&quot;</span>))</span><br><span class="line">        package_lib_dir = os.path.join(self.build_lib, <span class="string">&quot;tonilsm/core/lib&quot;</span>)</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 创建目标目录</span></span><br><span class="line">        self.mkpath(package_lib_dir)</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 动态库文件列表</span></span><br><span class="line">        lib_files = [</span><br><span class="line">            (<span class="string">&quot;liblsm_shared.so&quot;</span>, <span class="string">&quot;liblsm_shared.so&quot;</span>),</span><br><span class="line">            (<span class="string">&quot;lsm_pybind.so&quot;</span>, <span class="string">&quot;lsm_pybind.so&quot;</span>)</span><br><span class="line">        ]</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 复制文件</span></span><br><span class="line">        <span class="keyword">for</span> src_name, dst_name <span class="keyword">in</span> lib_files:</span><br><span class="line">            src = os.path.join(build_lib_dir, src_name)</span><br><span class="line">            dst = os.path.join(package_lib_dir, dst_name)</span><br><span class="line">            <span class="keyword">if</span> os.path.exists(src):</span><br><span class="line">                self.copy_file(src, dst)</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                <span class="keyword">raise</span> FileNotFoundError(<span class="string">f&quot;Missing library file: <span class="subst">&#123;src&#125;</span>&quot;</span>)</span><br><span class="line">        </span><br><span class="line">        <span class="built_in">super</span>().run()</span><br><span class="line"></span><br><span class="line">setup(</span><br><span class="line">    name=<span class="string">&quot;tonilsm&quot;</span>,</span><br><span class="line">    version=VERSION,</span><br><span class="line">    description=DESCRIPTION,</span><br><span class="line">    packages=find_packages(),</span><br><span class="line">package_data=&#123;</span><br><span class="line">    <span class="string">&quot;tonilsm.core&quot;</span>: [</span><br><span class="line">        <span class="string">&quot;lib/*.so&quot;</span>,</span><br><span class="line">        <span class="string">&quot;lib/*.dylib&quot;</span>,</span><br><span class="line">        <span class="string">&quot;lib/*.dll&quot;</span>,</span><br><span class="line">        <span class="string">&quot;lib/*.pyd&quot;</span></span><br><span class="line">    ]</span><br><span class="line">&#125;,</span><br><span class="line">    cmdclass=&#123;<span class="string">&quot;build_py&quot;</span>: CustomBuild&#125;,</span><br><span class="line">    install_requires=[<span class="string">&quot;pybind11&gt;=2.10&quot;</span>],  <span class="comment"># 需声明绑定依赖</span></span><br><span class="line">    python_requires=<span class="string">&quot;&gt;=3.8&quot;</span>,</span><br><span class="line">    author=<span class="string">&quot;Your Name&quot;</span>,</span><br><span class="line">    classifiers=[</span><br><span class="line">        <span class="string">&quot;Development Status :: 3 - Alpha&quot;</span>,</span><br><span class="line">        <span class="string">&quot;Programming Language :: Python :: 3&quot;</span>,</span><br><span class="line">    ]</span><br><span class="line">)</span><br></pre></td></tr></table></figure><h1 id="4-SDK-使用"><a href="#4-SDK-使用" class="headerlink" title="4 SDK 使用"></a>4 SDK 使用</h1><p>现在我们可以直接通过<code>Python</code>来使用这个<code>LSM</code>存储引擎了, 这里我们给出一个简单的使用示例:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> tonilsm</span><br><span class="line"></span><br><span class="line">db = tonilsm.LSM(<span class="string">&quot;test2_db&quot;</span>)</span><br><span class="line"></span><br><span class="line">db.put(<span class="string">b&quot;tomxx&quot;</span>, <span class="string">b&quot;catxx&quot;</span>)</span><br><span class="line"></span><br><span class="line">db.get(<span class="string">&quot;tomxx&quot;</span>)</span><br><span class="line"><span class="comment"># &#x27;catxx&#x27;</span></span><br><span class="line"></span><br><span class="line">t = db.begin_tran(isolation_level=tonilsm.IsolationLevel.READ_COMMITTED)</span><br><span class="line"></span><br><span class="line">t.get(<span class="string">&#x27;tomxx&#x27;</span>)</span><br><span class="line"><span class="comment"># &#x27;catxx&#x27;</span></span><br><span class="line"></span><br><span class="line">t.put(<span class="string">&#x27;tomxx&#x27;</span>, <span class="string">&#x27;1&#x27;</span>)</span><br><span class="line"></span><br><span class="line">t.get(<span class="string">&#x27;tomxx&#x27;</span>)</span><br><span class="line"><span class="comment"># &#x27;1&#x27;</span></span><br><span class="line"></span><br><span class="line">db.get(<span class="string">&quot;tomxx&quot;</span>)</span><br><span class="line"><span class="comment"># &#x27;catxx&#x27;</span></span><br><span class="line"></span><br><span class="line">t.commit()</span><br><span class="line"><span class="comment"># True</span></span><br><span class="line"></span><br><span class="line">db.get(<span class="string">&quot;tomxx&quot;</span>)</span><br><span class="line"><span class="comment"># &#x27;1&#x27;</span></span><br></pre></td></tr></table></figure><p>但这里其实有一个<code>bug</code>:</p><p><img src="/../../../images/Proj/LSM/lsm-sdk-bug.png" alt="bug"></p><p>调用<code>clear</code>方法后直接<code>core dump</code>了, 这个<code>clear</code>函数在<code>C++</code>文件中是正常使用的, 但不知道为什么到<code>Python</code>层就<code>core dump</code>了, 这个我暂时不知道什么原因。如果有网页感兴趣，可以看看这个<a href="https://github.com/Vanilla-Beauty/tiny-lsm/issues/7">issue</a></p><h1 id="5-总结"><a href="#5-总结" class="headerlink" title="5 总结"></a>5 总结</h1><p>在这一章中，我们介绍了如何使用 <code>pybind11</code> 来将<code>C++</code>代码绑定到 <code>Python</code> 中，并且实现了一个简单的 <code>Python SDK</code>。我们还介绍了如何组织 <code>Python</code> 包的结构，以及如何使用 <code>setuptools</code> 来构建和安装 <code>Python</code> 包。</p><p>这一章算是加餐内容吧, 基础的<code>Python SDK</code>已经实现了, 但有一些细节的<code>bug</code>和优化没有在之前的文章中提及, 因为比较琐碎, 写起来也比较复杂。</p><p>如果本项目有帮到你，请给个<code>star</code>吧，您的支持是我继续维护和开发这个项目的动力。如果你想自己从零开始手敲代码实现这个项目，欢迎支持我的付费视频课程：<a href="https://avo6166ew2u.feishu.cn/docx/LXmVdezdsoTBRaxC97WcHwGunOc">https://avo6166ew2u.feishu.cn/docx/LXmVdezdsoTBRaxC97WcHwGunOc</a></p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;这一章是加餐，主要是给大家介绍一下如何使用&lt;code&gt;Python SDK&lt;/code&gt;来操作我们之前实现的LSM-Tree-KV存储。&lt;/p&gt;
&lt;p&gt;代码仓库：&lt;a href=&quot;https://github.com/Vanilla-Beauty/tiny-lsm&quot;&gt;Van</summary>
      
    
    
    
    <category term="项目" scheme="http://example.com/categories/%E9%A1%B9%E7%9B%AE/"/>
    
    <category term="Tiny-LSM" scheme="http://example.com/categories/%E9%A1%B9%E7%9B%AE/Tiny-LSM/"/>
    
    
    <category term="C++" scheme="http://example.com/tags/C/"/>
    
    <category term="Python" scheme="http://example.com/tags/Python/"/>
    
    <category term="KV存储" scheme="http://example.com/tags/KV%E5%AD%98%E5%82%A8/"/>
    
  </entry>
  
  <entry>
    <title>C++从零开始实现LSM-Tree-KV存储-19-SST压缩</title>
    <link href="http://example.com/2025/04/17/%E9%A1%B9%E7%9B%AE/Tiny-LSM/C++%E4%BB%8E%E9%9B%B6%E5%BC%80%E5%A7%8B%E5%AE%9E%E7%8E%B0LSM-Tree-KV%E5%AD%98%E5%82%A8-19-SST%E5%8E%8B%E7%BC%A9/"/>
    <id>http://example.com/2025/04/17/%E9%A1%B9%E7%9B%AE/Tiny-LSM/C++%E4%BB%8E%E9%9B%B6%E5%BC%80%E5%A7%8B%E5%AE%9E%E7%8E%B0LSM-Tree-KV%E5%AD%98%E5%82%A8-19-SST%E5%8E%8B%E7%BC%A9/</id>
    <published>2025-04-17T06:42:21.000Z</published>
    <updated>2026-05-28T07:37:46.670Z</updated>
    
    <content type="html"><![CDATA[<p>之前我们已经完成了数据库内存中事务和崩溃恢复相关的功能实现, 这节课不出意外应该是<code>LSM Tree</code>核心组件的最后一节课, 即介绍<code>SSTable Compact</code>相关功能的实现。</p><p>代码仓库：<a href="https://github.com/Vanilla-Beauty/tiny-lsm">Vanilla-Beauty&#x2F;tiny-lsm: A KV storage engine based on LSM Tree, supporting Redis RESP</a> 感谢您的 <code>Star</code>!</p><p>欢迎支持本项目同步更新的从零开始实现的视频教程：<a href="https://avo6166ew2u.feishu.cn/docx/LXmVdezdsoTBRaxC97WcHwGunOc">https://avo6166ew2u.feishu.cn/docx/LXmVdezdsoTBRaxC97WcHwGunOc</a></p><p>欢迎加入讨论群：<a href="https://qm.qq.com/q/lkIzCPvcdM">Tiny-LSM项目交流群</a></p><h1 id="1-SST-Compact-基本概念"><a href="#1-SST-Compact-基本概念" class="headerlink" title="1. SST Compact 基本概念"></a>1. SST Compact 基本概念</h1><p>首先, 让我们再次回顾<code>LSM Tree</code>的架构：</p><p><img src="/../../../images/Proj/LSM/LSM-Tree-Architecture.png" alt="LSM-Tree-Architecture"></p><p>在LSM（Log-Structured Merge）树中，数据最初写入一个称为 <strong>memtable</strong> 的内存结构。一旦 memtable 达到一定大小限制，其内容会被刷写到磁盘上作为 SSTable（Sorted String Table）。随着时间的推移，可能会在 LSM 树的不同层级积累多个 SSTable。这些 SSTable 可能会导致以下问题：</p><ul><li><strong>读放大</strong>：查询一个键时，系统可能需要搜索多个不同层级的 SSTable。这会增加 I&#x2F;O 操作次数，从而降低读取性能。</li><li><strong>写放大</strong>：频繁的写操作生成新的 SSTable，最终需要与现有 SSTable 合并。如果没有压缩，这会导致过多的磁盘写入。</li><li><strong>空间放大</strong>：被删除或覆盖的键可能仍然存在于旧的 SSTable 中，直到它们通过压缩被清除。这浪费了磁盘空间。</li></ul><p>为了解决这些问题，需要进行 <strong>压缩（compaction）</strong>。压缩将较小的 SSTable 合并成较大的 SSTable，移除过时的数据（如已删除或覆盖的键），并平衡各层级之间的数据分布。</p><h1 id="2-经典的SST压缩方案"><a href="#2-经典的SST压缩方案" class="headerlink" title="2. 经典的SST压缩方案"></a>2. 经典的SST压缩方案</h1><p>这里先介绍一些经典的SST压缩方案。</p><h2 id="2-1-基于大小的分层压缩（Size-Tiered-Compaction）"><a href="#2-1-基于大小的分层压缩（Size-Tiered-Compaction）" class="headerlink" title="2.1 基于大小的分层压缩（Size-Tiered Compaction）"></a>2.1 基于大小的分层压缩（Size-Tiered Compaction）</h2><p>这种方案按大小和层级对 SSTable 进行分组。每一层包含大致相同大小的 SSTable。当某一层的 SSTable 数量超过阈值时，它们会被合并成一个或多个 SSTable 并移动到下一层。</p><ul><li><p><strong>流程</strong>：</p><ol><li>将 Level L 的 SSTable 合并成一个或多个 SSTable 并移动到 Level L+1。</li><li>如果必要，递归执行此过程。</li></ol></li><li><p><strong>优点</strong>：</p><ul><li>实现简单。</li><li>高效处理高写入吞吐量。</li></ul></li><li><p><strong>缺点</strong>：</p><ul><li>可能导致显著的读放大，因为单次查询可能需要扫描多个层级。</li></ul></li><li><p><strong>示意图</strong>：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">Level 0: [SST1] [SST2] [SST3]</span><br><span class="line">Level 1: [SST4] [SST5]</span><br><span class="line">Level 2: [SST6]</span><br><span class="line"></span><br><span class="line">压缩后：</span><br><span class="line">Level 0: []</span><br><span class="line">Level 1: [Merged(SST1,SST2,SST3)]</span><br><span class="line">Level 2: [SST6]</span><br></pre></td></tr></table></figure></li></ul><h2 id="2-2-分层压缩（Leveled-Compaction）"><a href="#2-2-分层压缩（Leveled-Compaction）" class="headerlink" title="2.2 分层压缩（Leveled Compaction）"></a>2.2 分层压缩（Leveled Compaction）</h2><p>这种方案将 SSTable 分布在不同的层级，确保每个层级包含固定数量的数据。较低层级的数据以小块形式逐步移动到较高层级。</p><ul><li><p><strong>流程</strong>：</p><ol><li>将 Level L 的 SSTable 按键范围拆分成更小的部分，并与 Level L+1 中重叠的 SSTable 合并。</li><li>每次压缩只影响 Level L+1 中的一小部分 SSTable。</li></ol></li><li><p><strong>优点</strong>：</p><ul><li>相比基于大小的分层压缩，减少了读放大。</li><li>确保每个层级的键范围不重叠。</li></ul></li><li><p><strong>缺点</strong>：</p><ul><li>实现复杂度更高。</li><li>频繁的小规模合并导致较高的写放大。</li></ul></li><li><p><strong>示意图</strong>：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">Level 0: [SST1] [SST2] [SST3]</span><br><span class="line">Level 1: [RangeA] [RangeB] [RangeC]</span><br><span class="line">Level 2: [RangeD] [RangeE]</span><br><span class="line"></span><br><span class="line">压缩后：</span><br><span class="line">Level 0: []</span><br><span class="line">Level 1: [Merged(SST1,RangeA)] [Merged(SST2,RangeB)] [Merged(SST3,RangeC)]</span><br><span class="line">Level 2: [RangeD] [RangeE]</span><br></pre></td></tr></table></figure></li></ul><h2 id="2-3-混合压缩（Hybrid-Compacti2-3）"><a href="#2-3-混合压缩（Hybrid-Compacti2-3）" class="headerlink" title="2.3 混合压缩（Hybrid Compacti2.3）"></a>2.3 混合压缩（Hybrid Compacti2.3）</h2><p>一些系统结合了基于大小的分层压缩和分层压缩的优点。例如，前几层使用基于大小的分层压缩，而较深层级切换到分层压缩。</p><ul><li><p><strong>优点</strong>：</p><ul><li>在读放大和写放大之间取得平衡。</li><li>根据工作负载特性提供灵活性。</li></ul></li><li><p><strong>缺点</strong>：</p><ul><li>管理多种压缩策略增加了复杂性。</li></ul></li></ul><h1 id="3-本项目-SST-Compact-设计"><a href="#3-本项目-SST-Compact-设计" class="headerlink" title="3 本项目 SST Compact 设计"></a>3 本项目 SST Compact 设计</h1><p>本项目的<code>compact</code>机制采用了一种混合方法，主要在第0层使用基于大小的分层压缩，而在更高层则转向分层压缩。以下是详细的设计说明：</p><h2 id="3-1-触发压缩的时机"><a href="#3-1-触发压缩的时机" class="headerlink" title="3.1 触发压缩的时机"></a>3.1 触发压缩的时机</h2><ul><li><strong>Level 0</strong>：如果 Level 0 的 SSTable 数量超过一个阈值 <code>LSM_SST_LEVEL_RATIO</code>，则触发全量压缩，将所有 Level 0 的 SSTable 移动到 Level 1。</li><li><strong>更高层</strong>：如果某一层的 SSTable 数量超过 <code>LSM_SST_LEVEL_RATIO</code>，系统会递归检查下一层是否也需要压缩，然后继续执行。</li></ul><p>这里重点解释一下 <code>LSM_SST_LEVEL_RATIO</code>的含义, 它的含义是相邻两层的单个<code>SST</code>容量之比, 同时也是单层<code>SST</code>数量的阈值。这里我为了方便压缩触发的条件判断的便捷性, 规定每一个<code>Level</code>的<code>SST</code>数量阈值恒定。例如， 每层<code>SST</code>数量超过16时就需要进行<code>compact</code>, 这一层16个<code>SST</code>被合并为下一层的单个<code>SST</code>, 因此这个单个<code>SST</code>的容量是上一个<code>Level</code>中单个<code>SST</code>的容量的16倍。这个案例中, <code>LSM_SST_LEVEL_RATIO</code>就是16。</p><h2 id="3-2-递归压缩"><a href="#3-2-递归压缩" class="headerlink" title="3.2 递归压缩"></a>3.2 递归压缩</h2><ol><li><p><strong>确定源层和目标层</strong>：</p><ul><li>确定源层 (<code>src_level</code>) 和目标层 (<code>src_level + 1</code>)。</li><li>获取这两层的 SSTable ID。</li></ul></li><li><p><strong>判断是否进行递归压缩</strong></p><ul><li>由于<code>src_level</code>层是触发了这一次<code>compact</code>操作, 因此其<code>SST</code>数量肯定大于等于<code>LSM_SST_LEVEL_RATIO</code></li><li>但还需要判断目标层(<code>src_level + 1</code>)的<code>SST</code>数量是否大于等于<code>LSM_SST_LEVEL_RATIO</code>。如果时, 则需要先将<code>src_level + 1</code>层的<code>SST</code>全部压缩到<code>src_level + 2</code>层</li><li>上述的判断递归地在更高的<code>Level</code>中进行</li></ul></li><li><p><strong>合并SSTable</strong>：</p><ul><li>对于 <code>Level 0</code>，由于其不同<code>SST</code>中的<code>key</code>有重叠, 需要使用 <code>full_l0_l1_compact</code> 处理键范围重叠的问题</li><li>对于更高层， 其<code>key</code>本来就是从小大大排布的, 使用 <code>full_common_compact</code> 合并不重叠键范围的 <code>SSTable</code>。</li></ul></li><li><p><strong>生成新SSTable</strong>：</p><ul><li>使用 <code>gen_sst_from_iter</code> 从合并后的迭代器结果生成新的 SSTable。</li><li>确保新 SSTable 的大小符合 <code>get_sst_size</code> 定义的限制。</li></ul></li><li><p><strong>更新元数据</strong>：</p><ul><li>删除旧的 SSTable（从内存和磁盘中移除）。</li><li>将新的 SSTable 添加到适当的目标层。</li><li>更新 <code>level_sst_ids</code> 并对其进行排序以便高效访问。</li></ul></li></ol><blockquote><p>这里的<code>full_l0_l1_compact</code>和<code>full_common_compact</code> 会在后文中介绍</p></blockquote><p><strong>关键特性</strong></p><ul><li><strong>事务处理</strong>：在压缩过程中，确保只将可见记录（基于事务ID）包含在新的 SSTable 中。</li><li><strong>并发控制</strong>：使用锁（<code>std::shared_mutex</code>）防止在访问共享资源（如 <code>ssts_mtx</code>）时发生竞争条件。</li><li><strong>高效迭代</strong>：利用迭代器（<code>TwoMergeIterator</code>、<code>HeapIterator</code>、<code>ConcactIterator</code>）高效遍历和合并来自多个 SSTable 的数据。</li></ul><p><strong>示例流程</strong></p><p>假设初始状态如下：</p><ul><li><p><strong>压缩前</strong>：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Level 0: [SST1] [SST2] [SST3]</span><br><span class="line">Level 1: [SST4] [SST5]</span><br><span class="line">Level 2: [SST6]</span><br></pre></td></tr></table></figure></li><li><p><strong>触发条件</strong>：Level 0 有三个 SSTable，超过了阈值（<code>LSM_SST_LEVEL_RATIO = 2</code>）。</p></li><li><p><strong>压缩过程</strong>：</p><ol><li>合并 <code>[SST1]</code>、<code>[SST2]</code> 和 <code>[SST3]</code> 成为一个迭代器。</li><li>将该迭代器的结果与 <code>[SST4]</code> 和 <code>[SST5]</code> 从 Level 1 中合并。</li><li>生成新的 SSTable 并将其分配到 Level 1。</li></ol></li><li><p><strong>压缩后</strong>：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Level 0: []</span><br><span class="line">Level 1: [NewSST1] [NewSST2]</span><br><span class="line">Level 2: [SST6]</span><br></pre></td></tr></table></figure></li></ul><h1 id="4-本项目-SST-Compact-代码实现"><a href="#4-本项目-SST-Compact-代码实现" class="headerlink" title="4 本项目 SST Compact 代码实现"></a>4 本项目 SST Compact 代码实现</h1><p>在介绍完之前的流程后, 本项目<code>compact</code>部分的代码就很好理解了</p><h2 id="4-1-触发时机"><a href="#4-1-触发时机" class="headerlink" title="4.1 触发时机"></a>4.1 触发时机</h2><p>由于本项目是一个教学项目, 教学项目最好是便于学习者进行<code>Debug</code>, 因此, 我将<code>compact</code>触发设计为惰性的同步触发机制, 这样最便于大家单步调试。因此，我们需要再每次<code>SST</code>刷盘时进行<code>compact</code>的触发检查:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// src/lsm/engine.cpp</span></span><br><span class="line"><span class="function"><span class="type">uint64_t</span> <span class="title">LSMEngine::flush</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (memtable.<span class="built_in">get_total_size</span>() == <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function">std::unique_lock&lt;std::shared_mutex&gt; <span class="title">lock</span><span class="params">(ssts_mtx)</span></span>; <span class="comment">// 写锁</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// 1. 先判断 l0 sst 是否数量超限需要concat到 l1</span></span><br><span class="line">  <span class="keyword">if</span> (level_sst_ids.<span class="built_in">find</span>(<span class="number">0</span>) != level_sst_ids.<span class="built_in">end</span>() &amp;&amp;</span><br><span class="line">      level_sst_ids[<span class="number">0</span>].<span class="built_in">size</span>() &gt;= LSM_SST_LEVEL_RATIO) &#123;</span><br><span class="line">    <span class="built_in">full_compact</span>(<span class="number">0</span>);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 2. 创建新的 SST ID</span></span><br><span class="line">  <span class="type">size_t</span> new_sst_id = next_sst_id++;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 3. 准备 SSTBuilder</span></span><br><span class="line">  <span class="function">SSTBuilder <span class="title">builder</span><span class="params">(LSM_BLOCK_SIZE, <span class="literal">true</span>)</span></span>; <span class="comment">// 4KB block size</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// 4. 将 memtable 中最旧的表写入 SST</span></span><br><span class="line">  <span class="keyword">auto</span> sst_path = <span class="built_in">get_sst_path</span>(new_sst_id, <span class="number">0</span>);</span><br><span class="line">  <span class="keyword">auto</span> new_sst =</span><br><span class="line">      memtable.<span class="built_in">flush_last</span>(builder, sst_path, new_sst_id, block_cache);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 5. 更新内存索引</span></span><br><span class="line">  ssts[new_sst_id] = new_sst;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 6. 更新 sst_ids</span></span><br><span class="line">  level_sst_ids[<span class="number">0</span>].<span class="built_in">push_front</span>(new_sst_id);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 返回新刷入的 sst 的最大的 tranc_id</span></span><br><span class="line">  <span class="keyword">return</span> new_sst-&gt;<span class="built_in">get_tranc_id_range</span>().second;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里当然存在一个并发问题, 就是刷盘时我们获取了一把锁<code>ssts_mtx</code>, 这里是对读写锁获取写锁, 因此, 在我们目前的实现中, <code>sst</code>压缩时, 其他<code>SST</code>的查询是被阻塞的。这里其实可以用如下的方案来解决这个并发问题：</p><ol><li><code>compact</code>时, 先将<code>ssts_mtx</code>锁换成读锁, 这样其他线程可以查询<code>SST</code></li><li>刷盘后的<code>sst</code>文件进行原子重命名, 因此只在文件重命名时需要获取锁</li></ol><p>这样就解决了并发问题, 不过需要注意的就是, 这里的压缩函数会递归地进行, 因此这里重命名的过程较为琐碎。介于目前的项目主要是作为教学项目存在，且本人实在精力有限， 因此我没有实现这个优化，感兴趣的朋友可尝试下，欢迎来提<code>PR</code></p><h2 id="4-2-相邻层的压缩"><a href="#4-2-相邻层的压缩" class="headerlink" title="4.2 相邻层的压缩"></a>4.2 相邻层的压缩</h2><h3 id="4-2-1-整体压缩逻辑"><a href="#4-2-1-整体压缩逻辑" class="headerlink" title="4.2.1 整体压缩逻辑"></a>4.2.1 整体压缩逻辑</h3><p><code>full_compact</code>处理相邻层压缩的整体逻辑, 即遍历相邻层的迭代器, 将键值对打到新的<code>SST</code>中:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// src/lsm/engine.cpp</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">LSMEngine::full_compact</span><span class="params">(<span class="type">size_t</span> src_level)</span> </span>&#123;</span><br><span class="line">  <span class="comment">// 将 src_level 的 sst 全体压缩到 src_level + 1</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// 递归地判断下一级 level 是否需要 full compact</span></span><br><span class="line">  <span class="keyword">if</span> (level_sst_ids[src_level + <span class="number">1</span>].<span class="built_in">size</span>() &gt;= LSM_SST_LEVEL_RATIO) &#123;</span><br><span class="line">    <span class="built_in">full_compact</span>(src_level + <span class="number">1</span>);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 获取源level和目标level的 sst_id</span></span><br><span class="line">  <span class="keyword">auto</span> old_level_id_x = level_sst_ids[src_level];</span><br><span class="line">  <span class="keyword">auto</span> old_level_id_y = level_sst_ids[src_level + <span class="number">1</span>];</span><br><span class="line">  std::vector&lt;std::shared_ptr&lt;SST&gt;&gt; new_ssts;</span><br><span class="line">  <span class="function">std::vector&lt;<span class="type">size_t</span>&gt; <span class="title">lx_ids</span><span class="params">(old_level_id_x.begin(), old_level_id_x.end())</span></span>;</span><br><span class="line">  <span class="function">std::vector&lt;<span class="type">size_t</span>&gt; <span class="title">ly_ids</span><span class="params">(old_level_id_y.begin(), old_level_id_y.end())</span></span>;</span><br><span class="line">  <span class="keyword">if</span> (src_level == <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="comment">// l0这一层不同sst的key有重叠, 需要额外处理</span></span><br><span class="line">    new_ssts = <span class="built_in">full_l0_l1_compact</span>(lx_ids, ly_ids);</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    new_ssts = <span class="built_in">full_common_compact</span>(lx_ids, ly_ids, src_level + <span class="number">1</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 完成 compact 后移除旧的sst记录</span></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">auto</span> &amp;old_sst_id : old_level_id_x) &#123;</span><br><span class="line">    ssts[old_sst_id]-&gt;<span class="built_in">del_sst</span>();</span><br><span class="line">    ssts.<span class="built_in">erase</span>(old_sst_id);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">auto</span> &amp;old_sst_id : old_level_id_y) &#123;</span><br><span class="line">    ssts[old_sst_id]-&gt;<span class="built_in">del_sst</span>();</span><br><span class="line">    ssts.<span class="built_in">erase</span>(old_sst_id);</span><br><span class="line">  &#125;</span><br><span class="line">  level_sst_ids[src_level].<span class="built_in">clear</span>();</span><br><span class="line">  level_sst_ids[src_level + <span class="number">1</span>].<span class="built_in">clear</span>();</span><br><span class="line"></span><br><span class="line">  cur_max_level = std::<span class="built_in">max</span>(cur_max_level, src_level + <span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 添加新的sst</span></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">auto</span> &amp;new_sst : new_ssts) &#123;</span><br><span class="line">    level_sst_ids[src_level + <span class="number">1</span>].<span class="built_in">push_back</span>(new_sst-&gt;<span class="built_in">get_sst_id</span>());</span><br><span class="line">    ssts[new_sst-&gt;<span class="built_in">get_sst_id</span>()] = new_sst;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 此处没必要reverse了</span></span><br><span class="line">  std::<span class="built_in">sort</span>(level_sst_ids[src_level + <span class="number">1</span>].<span class="built_in">begin</span>(),</span><br><span class="line">            level_sst_ids[src_level + <span class="number">1</span>].<span class="built_in">end</span>());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>具体而言, <code>LSMEngine::full_compact</code> 函数的目标是将指定层级 <code>src_level</code> 的所有 SSTable 压缩到下一层级 <code>src_level + 1</code>，并通过合并和优化减少冗余数据，提升读写性能。其步骤为:</p><p><strong>a. 递归检查下一层是否需要压缩</strong></p><ul><li>在对当前层级（<code>src_level</code>）进行全量压缩之前，先检查下一层级（<code>src_level + 1</code>）是否也需要进行全量压缩。</li><li>如果 <code>src_level + 1</code> 层的 SSTable 数量超过阈值（<code>LSM_SST_LEVEL_RATIO</code>），则递归调用 <code>full_compact(src_level + 1)</code> 对下一层进行压缩。</li><li>这种递归机制确保了在压缩当前层之前，目标层已经处于优化状态。</li></ul><p><strong>b. 获取源层级和目标层级的 SSTable ID</strong></p><ul><li>从 <code>level_sst_ids[src_level]</code> 和 <code>level_sst_ids[src_level + 1]</code> 中分别获取当前层级和目标层级的所有 SSTable ID。</li><li>将这些 ID 转换为两个向量：<code>lx_ids</code>（源层级）和 <code>ly_ids</code>（目标层级），便于后续处理。</li></ul><p><strong>c. 根据层级选择不同的压缩方式</strong></p><ul><li>根据 <code>src_level</code> 是否为 Level 0，选择不同的压缩方法：<ul><li><strong>Level 0</strong>：由于 Level 0 的 SSTable 可能存在键范围重叠，调用 <code>full_l0_l1_compact(lx_ids, ly_ids)</code> 处理。</li><li><strong>其他层级</strong>：调用 <code>full_common_compact(lx_ids, ly_ids, src_level + 1)</code> 处理，这些层级的 SSTable 键范围不重叠。</li></ul></li></ul><p><strong>d. 移除旧的 SSTable</strong></p><ul><li>压缩完成后，删除源层级和目标层级中所有旧的 SSTable：<ul><li>调用 <code>del_sst()</code> 方法释放磁盘资源。</li><li>从 <code>ssts</code> 映射中移除这些 SSTable。</li></ul></li><li>清空 <code>level_sst_ids[src_level]</code> 和 <code>level_sst_ids[src_level + 1]</code>，为新生成的 SSTable 准备空间。</li></ul><p><strong>e. 更新最大层级</strong></p><ul><li>更新 <code>cur_max_level</code>，确保其始终表示当前 LSM 树中的最大层级。</li></ul><p><strong>f. 添加新的 SSTable</strong></p><ul><li>将新生成的 SSTable 添加到目标层级（<code>src_level + 1</code>）：<ul><li>将新 SSTable 的 ID 插入到 <code>level_sst_ids[src_level + 1]</code>。</li><li>将新 SSTable 本身插入到 <code>ssts</code> 映射中。</li></ul></li><li>对 <code>level_sst_ids[src_level + 1]</code> 进行排序，确保 SSTable 按顺序存储，便于高效访问。</li></ul><h3 id="4-2-2-L0-L1层的压缩"><a href="#4-2-2-L0-L1层的压缩" class="headerlink" title="4.2.2 L0-L1层的压缩"></a>4.2.2 L0-L1层的压缩</h3><p><code>full_l0_l1_compact</code>函数处理Level 0层和Level 1层之间的压缩，Level 0层和Level 1层之间的SSTable的<code>key</code>有重叠，因此需要处理键范围重叠的问题:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// src/lsm/engine.cpp</span></span><br><span class="line">std::vector&lt;std::shared_ptr&lt;SST&gt;&gt;</span><br><span class="line">LSMEngine::<span class="built_in">full_l0_l1_compact</span>(std::vector&lt;<span class="type">size_t</span>&gt; &amp;l0_ids,</span><br><span class="line">                              std::vector&lt;<span class="type">size_t</span>&gt; &amp;l1_ids) &#123;</span><br><span class="line">  <span class="comment">// <span class="doctag">TODO:</span> 这里需要补全的是对已经完成事务的删除</span></span><br><span class="line">  std::vector&lt;SstIterator&gt; l0_iters;</span><br><span class="line">  std::vector&lt;std::shared_ptr&lt;SST&gt;&gt; l1_ssts;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">auto</span> id : l0_ids) &#123;</span><br><span class="line">    <span class="keyword">auto</span> sst_it = ssts[id]-&gt;<span class="built_in">begin</span>(<span class="number">0</span>);</span><br><span class="line">    l0_iters.<span class="built_in">push_back</span>(sst_it);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">auto</span> id : l1_ids) &#123;</span><br><span class="line">    l1_ssts.<span class="built_in">push_back</span>(ssts[id]);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// l0 的sst之间的key有重叠, 需要合并</span></span><br><span class="line">  <span class="keyword">auto</span> [l0_begin, l0_end] = SstIterator::<span class="built_in">merge_sst_iterator</span>(l0_iters, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">  std::shared_ptr&lt;HeapIterator&gt; l0_begin_ptr = std::<span class="built_in">make_shared</span>&lt;HeapIterator&gt;();</span><br><span class="line">  *l0_begin_ptr = l0_begin;</span><br><span class="line"></span><br><span class="line">  std::shared_ptr&lt;ConcactIterator&gt; old_l1_begin_ptr =</span><br><span class="line">      std::<span class="built_in">make_shared</span>&lt;ConcactIterator&gt;(l1_ssts, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">  <span class="function">TwoMergeIterator <span class="title">l0_l1_begin</span><span class="params">(l0_begin_ptr, old_l1_begin_ptr, <span class="number">0</span>)</span></span>;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="built_in">gen_sst_from_iter</span>(l0_l1_begin,</span><br><span class="line">                           LSM_PER_MEM_SIZE_LIMIT * LSM_SST_LEVEL_RATIO, <span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里, 我们又复用了<code>HeapIterator</code>对<code>Level 0</code>不同的<code>sst</code>进行相同<code>key</code>的去重; 然后再与<code>Level 1</code>的<code>sst</code>的迭代器进行遍历, 随遍历流程构建新的<code>SST</code>(<code>gen_sst_from_iter</code>的处理)。需要注意的是，这里两个迭代器的遍历过程中，使用了<code>TwoMergeIterator</code>, 这里的迭代器泛型是<code>HeapIterator</code>和<code>ConcactIterator</code>的组合，实现了两个迭代器的按照不同优先级的遍历。</p><p>从这里也可以看出，我们为什么要在之前进行迭代器的统一， 就是为了借助<code>C++</code>的多态特性实现算法的高效复用。</p><h3 id="4-2-3-Lx-Lx-1-层的压缩"><a href="#4-2-3-Lx-Lx-1-层的压缩" class="headerlink" title="4.2.3 Lx-Lx+1 层的压缩"></a>4.2.3 Lx-Lx+1 层的压缩</h3><p><code>full_common_compact</code>函数完成其他层级的压缩, 其他的每一层, 不同<code>sst</code>之间的<code>key</code>都是从小大大排序的, 且无重叠。因此，则需要简单地迭代遍历即可：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// src/lsm/engine.cpp</span></span><br><span class="line">std::vector&lt;std::shared_ptr&lt;SST&gt;&gt;</span><br><span class="line">LSMEngine::<span class="built_in">full_common_compact</span>(std::vector&lt;<span class="type">size_t</span>&gt; &amp;lx_ids,</span><br><span class="line">                               std::vector&lt;<span class="type">size_t</span>&gt; &amp;ly_ids, <span class="type">size_t</span> level_y) &#123;</span><br><span class="line">  <span class="comment">// TODO 需要补全已完成事务的滤除</span></span><br><span class="line">  std::vector&lt;std::shared_ptr&lt;SST&gt;&gt; lx_iters;</span><br><span class="line">  std::vector&lt;std::shared_ptr&lt;SST&gt;&gt; ly_iters;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">auto</span> id : lx_ids) &#123;</span><br><span class="line">    lx_iters.<span class="built_in">push_back</span>(ssts[id]);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">auto</span> id : ly_ids) &#123;</span><br><span class="line">    ly_iters.<span class="built_in">push_back</span>(ssts[id]);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  std::shared_ptr&lt;ConcactIterator&gt; old_lx_begin_ptr =</span><br><span class="line">      std::<span class="built_in">make_shared</span>&lt;ConcactIterator&gt;(lx_iters, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">  std::shared_ptr&lt;ConcactIterator&gt; old_ly_begin_ptr =</span><br><span class="line">      std::<span class="built_in">make_shared</span>&lt;ConcactIterator&gt;(ly_iters, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">  <span class="function">TwoMergeIterator <span class="title">lx_ly_begin</span><span class="params">(old_lx_begin_ptr, old_ly_begin_ptr, <span class="number">0</span>)</span></span>;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// <span class="doctag">TODO:</span>如果目标 level 的下一级 level+1 不存在, 则为底层的level,</span></span><br><span class="line">  <span class="comment">// 可以清理掉删除标记</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="built_in">gen_sst_from_iter</span>(lx_ly_begin, LSMEngine::<span class="built_in">get_sst_size</span>(level_y),</span><br><span class="line">                           level_y);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里要做的, 就是使用<code>TwoMergeIterator</code>, 按照低层级优先的规则, 迭代遍历<code>lx_iters</code>(<code>Level x</code>层的迭代器)和<code>ly_iters</code>(<code>Level y</code>层的迭代器, <code>y = x + 1</code>)。</p><h2 id="4-3-SST的构建"><a href="#4-3-SST的构建" class="headerlink" title="4.3 SST的构建"></a>4.3 SST的构建</h2><p>最后， 就是遍历迭代器时新<code>SST</code>的构建:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// src/lsm/engine.cpp</span></span><br><span class="line">std::vector&lt;std::shared_ptr&lt;SST&gt;&gt;</span><br><span class="line">LSMEngine::<span class="built_in">gen_sst_from_iter</span>(BaseIterator &amp;iter, <span class="type">size_t</span> target_sst_size,</span><br><span class="line">                             <span class="type">size_t</span> target_level) &#123;</span><br><span class="line">  <span class="comment">// <span class="doctag">TODO:</span> 这里需要补全的是对已经完成事务的删除</span></span><br><span class="line"></span><br><span class="line">  std::vector&lt;std::shared_ptr&lt;SST&gt;&gt; new_ssts;</span><br><span class="line">  <span class="keyword">auto</span> new_sst_builder = <span class="built_in">SSTBuilder</span>(LSM_BLOCK_SIZE, <span class="literal">true</span>);</span><br><span class="line">  <span class="keyword">while</span> (iter.<span class="built_in">is_valid</span>() &amp;&amp; !iter.<span class="built_in">is_end</span>()) &#123;</span><br><span class="line"></span><br><span class="line">    new_sst_builder.<span class="built_in">add</span>((*iter).first, (*iter).second, <span class="number">0</span>);</span><br><span class="line">    ++iter;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (new_sst_builder.<span class="built_in">estimated_size</span>() &gt;= target_sst_size) &#123;</span><br><span class="line">      <span class="type">size_t</span> sst_id = next_sst_id++; <span class="comment">// <span class="doctag">TODO:</span> 后续优化并发性</span></span><br><span class="line">      std::string sst_path = <span class="built_in">get_sst_path</span>(sst_id, target_level);</span><br><span class="line">      <span class="keyword">auto</span> new_sst = new_sst_builder.<span class="built_in">build</span>(sst_id, sst_path, <span class="keyword">this</span>-&gt;block_cache);</span><br><span class="line">      new_ssts.<span class="built_in">push_back</span>(new_sst);</span><br><span class="line">      new_sst_builder = <span class="built_in">SSTBuilder</span>(LSM_BLOCK_SIZE, <span class="literal">true</span>); <span class="comment">// 重置builder</span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (new_sst_builder.<span class="built_in">estimated_size</span>() &gt; <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="type">size_t</span> sst_id = next_sst_id++; <span class="comment">// <span class="doctag">TODO:</span> 后续优化并发性</span></span><br><span class="line">    std::string sst_path = <span class="built_in">get_sst_path</span>(sst_id, target_level);</span><br><span class="line">    <span class="keyword">auto</span> new_sst = new_sst_builder.<span class="built_in">build</span>(sst_id, sst_path, <span class="keyword">this</span>-&gt;block_cache);</span><br><span class="line">    new_ssts.<span class="built_in">push_back</span>(new_sst);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> new_ssts;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里构建<code>SST</code>没有什么别的好讲的, 就是不断地将键值对插入到<code>SSTBuilder</code>中, 当超出阈值后就刷盘形成<code>SST</code></p><h1 id="4-Block缓存池的被动失效"><a href="#4-Block缓存池的被动失效" class="headerlink" title="4 Block缓存池的被动失效"></a>4 Block缓存池的被动失效</h1><p>这里大家可能注意到一个非常有意思的事情:<code>SST_id</code>的分配是单调递增的</p><p>也就是说, <code>SST_0</code>被压缩后, 其新合并的<code>SST</code>的<code>id</code>不会复用之前的<code>SST</code>的<code>id</code>, 举个例子:<br>假设初始时, 有三个<code>Level 0</code>的<code>SST</code>和一个<code>Level 1</code>的<code>SST</code>, 且<code>LSM_SST_LEVEL_RATIO=3</code>, 那么压缩前, 其状态为:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">L0: SST_6, SST_5, SST_4</span><br><span class="line">L1: SST_3</span><br></pre></td></tr></table></figure><p>压缩后, 其状态为:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">L1: SST_8, SST_7</span><br></pre></td></tr></table></figure><p>这里, <code>SST_6, SST_5, SST_4</code>和<code>SST_3</code>一起被压缩, 其新合并的<code>SST</code>的<code>id</code>为<code>SST_8, SST_7</code>, 他们不会复用之前的<code>SST_id</code>, 为什么如此设计呢? 主要是考虑到缓存池失效的问题, 这里回顾一下我的的缓存池设计:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">std::shared_ptr&lt;Block&gt; <span class="title">SST::read_block</span><span class="params">(<span class="type">size_t</span> block_idx)</span> </span>&#123;</span><br><span class="line">  <span class="comment">// ... 其他逻辑</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// 先从缓存中查找</span></span><br><span class="line">  <span class="keyword">if</span> (block_cache != <span class="literal">nullptr</span>) &#123;</span><br><span class="line">    <span class="keyword">auto</span> cache_ptr = block_cache-&gt;<span class="built_in">get</span>(<span class="keyword">this</span>-&gt;sst_id, block_idx);</span><br><span class="line">    <span class="keyword">if</span> (cache_ptr != <span class="literal">nullptr</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> cache_ptr;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">throw</span> std::<span class="built_in">runtime_error</span>(<span class="string">&quot;Block cache not set&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// ... 其他逻辑</span></span><br><span class="line">  <span class="keyword">return</span> block_res;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里, 从缓存池中查找时, 唯一标识<code>block</code>的信息就是<code>sst_id</code>和<code>block_idx</code>。 因此, 如果我们<code>compact</code>后复用之前的<code>SST_id</code>, 且<code>block_cache</code>中就存在了<code>compact</code>前的<code>SST</code>相同的<code>SST_id</code>, 在<code>compact</code>后<code>SST_id</code>已经发生了变化, 这是缓存池中的<code>block</code>就会索引到已经被删掉的<code>compact</code>前的<code>SST</code>中的<code>block</code>, 导致得到过时的缓存<code>block</code>。</p><p>为了解决这里缓存失效的问题，我将<code>sst_id</code>设计为单调递增的, 这样, 在<code>compact</code>后, <code>SST_id</code>不会复用之前的<code>SST_id</code>, 随着缓存池的访问, 之前已经被<code>compact</code>的<code>SST</code>的无效缓存记录逐渐被清空， 同时，没有<code>compact</code>部分的<code>SST</code>的缓存<code>block</code>依然有效。</p><p>假设我们复用<code>sst_id</code>的话, 那么在<code>compact</code>后我们需要手动清理掉旧<code>SST</code>的无效缓存, 这不但需要我们记录哪些<code>SST</code>已经被<code>compact</code>了, 且需要对我们的<code>Block Cache</code>进行侵入式的代码修改, 并且主动修改缓存的设计本来就不够优雅, 且耗费性能。</p><h1 id="5-总结"><a href="#5-总结" class="headerlink" title="5 总结"></a>5 总结</h1><p>本章主要讲了<code>LSM Tree</code>的<code>SST</code>压缩算法的设计, 并且实现了一个简单的<code>LSM Tree</code>的<code>SST</code>压缩算法。到此为止，我们的<code>LSM Tree</code>的所有组件都已经完成了， 本项目已经接近尾声了， 接下来可能会补充一点细节优化和<code>bug</code>修复的内容。如果大家感兴趣，可能也会更一些利用本项目进行简历包装的指导。</p><p>写到这里，本项目已经有20篇文章了（<code>Lec00-Lec19</code>），一开始自己只是打算对自己过往几年对存储和数据库领域学习的知识进行简单整理, 但没想到逐渐写出来一个较为复杂的项目, 也耗费了自己不少的心血， 项目统计代码行数都接近1W了：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">✗ cloc .</span><br><span class="line">-------------------------------------------------------------------------------</span><br><span class="line">Language                     files          blank        comment           code</span><br><span class="line">-------------------------------------------------------------------------------</span><br><span class="line">C++                             36           1487           1237           6660</span><br><span class="line">C/C++ Header                    23            287            211           1113</span><br><span class="line">Markdown                         2             91              0            557</span><br><span class="line">D                               25              0              0            545</span><br><span class="line">JSON                             4              0              0            382</span><br><span class="line">Lua                              1             29              8            157</span><br><span class="line">Python                           5             51             26            132</span><br><span class="line">Text                             6              1              0             77</span><br><span class="line">Bourne Shell                     1              3              0              8</span><br><span class="line">-------------------------------------------------------------------------------</span><br><span class="line">SUM:                           103           1949           1482           9631</span><br><span class="line">-------------------------------------------------------------------------------</span><br></pre></td></tr></table></figure><p>在此也非常感谢各位网友的支持, 如果你对本项目感兴趣, 欢迎点个<code>Star</code>, 也欢迎你将本项目推荐给更多的网友, 让更多人了解这个项目。同时, 将本项目用在自己实习或秋招的简历上也是可以的。如果想进一步支持一下一贫如洗的作者， 可以购买作者录制的付费版<a href="https://avo6166ew2u.feishu.cn/docx/LXmVdezdsoTBRaxC97WcHwGunOc">视频课程</a></p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;之前我们已经完成了数据库内存中事务和崩溃恢复相关的功能实现, 这节课不出意外应该是&lt;code&gt;LSM Tree&lt;/code&gt;核心组件的最后一节课, 即介绍&lt;code&gt;SSTable Compact&lt;/code&gt;相关功能的实现。&lt;/p&gt;
&lt;p&gt;代码仓库：&lt;a href=&quot;ht</summary>
      
    
    
    
    <category term="项目" scheme="http://example.com/categories/%E9%A1%B9%E7%9B%AE/"/>
    
    <category term="Tiny-LSM" scheme="http://example.com/categories/%E9%A1%B9%E7%9B%AE/Tiny-LSM/"/>
    
    
    <category term="C++" scheme="http://example.com/tags/C/"/>
    
    <category term="KV存储" scheme="http://example.com/tags/KV%E5%AD%98%E5%82%A8/"/>
    
  </entry>
  
  <entry>
    <title>C++从零开始实现LSM-Tree-KV存储-18-WAL与崩溃恢复</title>
    <link href="http://example.com/2025/04/16/%E9%A1%B9%E7%9B%AE/Tiny-LSM/C++%E4%BB%8E%E9%9B%B6%E5%BC%80%E5%A7%8B%E5%AE%9E%E7%8E%B0LSM-Tree-KV%E5%AD%98%E5%82%A8-18-WAL%E4%B8%8E%E5%B4%A9%E6%BA%83%E6%81%A2%E5%A4%8D/"/>
    <id>http://example.com/2025/04/16/%E9%A1%B9%E7%9B%AE/Tiny-LSM/C++%E4%BB%8E%E9%9B%B6%E5%BC%80%E5%A7%8B%E5%AE%9E%E7%8E%B0LSM-Tree-KV%E5%AD%98%E5%82%A8-18-WAL%E4%B8%8E%E5%B4%A9%E6%BA%83%E6%81%A2%E5%A4%8D/</id>
    <published>2025-04-16T02:41:57.000Z</published>
    <updated>2026-05-28T07:37:46.669Z</updated>
    
    <content type="html"><![CDATA[<p>之前我们已经完成了数据库内存中和编码文件中事务信息的融入, 现在我们还需要加上WAL与崩溃恢复的内容。</p><p>代码仓库：<a href="https://github.com/Vanilla-Beauty/tiny-lsm">Vanilla-Beauty&#x2F;tiny-lsm: A KV storage engine based on LSM Tree, supporting Redis RESP</a> 感谢您的 <code>Star</code>!</p><p>欢迎支持本项目同步更新的从零开始实现的视频教程：<a href="https://avo6166ew2u.feishu.cn/docx/LXmVdezdsoTBRaxC97WcHwGunOc">https://avo6166ew2u.feishu.cn/docx/LXmVdezdsoTBRaxC97WcHwGunOc</a></p><p>欢迎加入讨论群：<a href="https://qm.qq.com/q/lkIzCPvcdM">Tiny-LSM项目交流群</a></p><h1 id="1-WAL编码设计"><a href="#1-WAL编码设计" class="headerlink" title="1 WAL编码设计"></a>1 WAL编码设计</h1><h2 id="1-1-WAL简介"><a href="#1-1-WAL简介" class="headerlink" title="1.1 WAL简介"></a>1.1 WAL简介</h2><p>这里首先简单介绍下<strong>WAL（预写式日志）</strong> , <strong>WAL（预写式日志）</strong> 是数据库系统中保障数据一致性和事务可靠性的核心技术，核心思想是“<strong>日志先行</strong>”——任何数据修改必须先记录到日志中，确保日志持久化后，才允许实际数据写入磁盘。这种机制解决了两个关键问题：一是保证已提交的事务不会因系统崩溃而丢失（持久性），二是确保事务要么完全生效、要么完全回滚（原子性）。</p><p>具体来说，当一个事务提交时，数据库会先将事务的修改操作（例如数据修改前后的值、事务状态等）按顺序写入日志文件，并强制将日志刷到磁盘存储。由于日志是顺序写入，相比随机修改数据页的I&#x2F;O操作，这种设计大幅提升了性能。之后，数据库可以灵活地将内存中的脏页批量刷新到磁盘，减少磁盘操作次数。</p><p>如果系统崩溃，重启后可通过日志恢复数据。恢复分为两个阶段：<strong>Redo</strong> 阶段会重放所有已提交但未落盘的日志，确保事务修改生效；<strong>Undo</strong> 阶段则回滚未提交事务的部分修改，消除中间状态。为了加速恢复，数据库会定期创建<strong>检查点（Checkpoint）</strong>，将当前内存中的脏页刷盘，并记录日志位置，这样恢复时只需处理检查点之后的日志。</p><p>WAL的优势不仅在于数据安全，还在于其高性能和可扩展性。例如，PostgreSQL、MySQL InnoDB等数据库依赖WAL实现事务和崩溃恢复；分布式系统（如Raft算法）也借鉴类似思想，通过日志复制保证一致性。不过，WAL的日志文件可能快速增长，需要定期清理或归档，且频繁刷盘可能带来性能损耗，因此在实际应用中需权衡同步&#x2F;异步提交等策略。</p><h2 id="1-2-WAL编码设计"><a href="#1-2-WAL编码设计" class="headerlink" title="1.2 WAL编码设计"></a>1.2 WAL编码设计</h2><p>根据之前的介绍, 我们的<code>WAL</code>文件中需要满足如下功能:</p><ol><li>描述事务的开始和结束</li><li>描述每次事务的操作内容</li></ol><p>在<code>KV</code>存储引擎这个领域, <code>WAL</code>的设计已经比关系型数据库简单很多了, 因为其操作类型就只有简单的基于键值对的<code>put/get/remove</code>. 与之相反, 关系型数据库的<code>WAL</code>就复杂很多了, 还涉及到物理日志和逻辑日志的区别</p><p>在KV存储引擎中，<code>WAL</code>的设计需要满足简洁性和高效性。由于操作类型仅限于<code>put</code>、<code>remove</code>（<code>get</code>通常不涉及数据修改，因此无需记录），日志结构可以大幅简化。以下是具体的设计要点：</p><h3 id="1-2-1-日志条目设计"><a href="#1-2-1-日志条目设计" class="headerlink" title="1.2.1 日志条目设计"></a>1.2.1 日志条目设计</h3><p>每个日志条目需包含以下核心信息：</p><ol><li><strong>事务标识（Transaction ID）</strong>：唯一标识事务的ID，用于关联多个操作。</li><li><strong>操作类型（Operation Type）</strong>：如<code>PUT</code>、<code>REMOVE</code>、<code>GET</code>、<code>BEGIN</code>、<code>COMMIT</code>、<code>ABORT</code>等。</li><li><strong>键（Key）</strong>：操作的键值。</li><li><strong>值（Value）</strong>：对于<code>PUT</code>操作记录具体值；</li><li>**校验和（Checksum）(可选)**：用于验证日志条目的完整性（如CRC32）。</li><li><strong>时间戳（可选）</strong>：记录操作时间，用于多版本控制或冲突解决。</li></ol><p>这样，我们可以通过每一个日志条目判断这个操作类型和数据、是哪一个事务进行操作。同时， 由于我们在上一章中将数据库的事务完成状态也进行了持久化, 因此在崩溃恢复时, 我们可以通过检查当前<code>WAL</code>条目的事务<code>id</code>和以刷盘的事务的状态进行对比, 来判断是否需要重放操作。</p><p>因此我们不难进行如下的头文件定义:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">pragma</span> once</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cstdint&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdexcept&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;vector&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">enum class</span> <span class="title class_">OperationType</span> &#123;</span><br><span class="line">  CREATE,</span><br><span class="line">  COMMIT,</span><br><span class="line">  ROLLBACK,</span><br><span class="line">  PUT,</span><br><span class="line">  DELETE,</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Record</span> &#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">  <span class="comment">// 构造函数</span></span><br><span class="line">  <span class="built_in">Record</span>() = <span class="keyword">default</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="comment">// 操作类型枚举</span></span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="type">static</span> Record <span class="title">createRecord</span><span class="params">(<span class="type">uint64_t</span> tranc_id)</span></span>;</span><br><span class="line">  <span class="function"><span class="type">static</span> Record <span class="title">commitRecord</span><span class="params">(<span class="type">uint64_t</span> tranc_id)</span></span>;</span><br><span class="line">  <span class="function"><span class="type">static</span> Record <span class="title">rollbackRecord</span><span class="params">(<span class="type">uint64_t</span> tranc_id)</span></span>;</span><br><span class="line">  <span class="function"><span class="type">static</span> Record <span class="title">putRecord</span><span class="params">(<span class="type">uint64_t</span> tranc_id, <span class="type">const</span> std::string &amp;key,</span></span></span><br><span class="line"><span class="params"><span class="function">                          <span class="type">const</span> std::string &amp;value)</span></span>;</span><br><span class="line">  <span class="function"><span class="type">static</span> Record <span class="title">deleteRecord</span><span class="params">(<span class="type">uint64_t</span> tranc_id, <span class="type">const</span> std::string &amp;key)</span></span>;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 编码记录</span></span><br><span class="line">  <span class="function">std::vector&lt;<span class="type">uint8_t</span>&gt; <span class="title">encode</span><span class="params">()</span> <span class="type">const</span></span>;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 解码记录</span></span><br><span class="line">  <span class="function"><span class="type">static</span> std::vector&lt;Record&gt; <span class="title">decode</span><span class="params">(<span class="type">const</span> std::vector&lt;<span class="type">uint8_t</span>&gt; &amp;data)</span></span>;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 获取记录的各个部分</span></span><br><span class="line">  <span class="function"><span class="type">uint64_t</span> <span class="title">getTrancId</span><span class="params">()</span> <span class="type">const</span> </span>&#123; <span class="keyword">return</span> tranc_id_; &#125;</span><br><span class="line">  <span class="function">OperationType <span class="title">getOperationType</span><span class="params">()</span> <span class="type">const</span> </span>&#123; <span class="keyword">return</span> operation_type_; &#125;</span><br><span class="line">  <span class="function">std::string <span class="title">getKey</span><span class="params">()</span> <span class="type">const</span> </span>&#123; <span class="keyword">return</span> key_; &#125;</span><br><span class="line">  <span class="function">std::string <span class="title">getValue</span><span class="params">()</span> <span class="type">const</span> </span>&#123; <span class="keyword">return</span> value_; &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 打印记录（用于调试）</span></span><br><span class="line">  <span class="function"><span class="type">void</span> <span class="title">print</span><span class="params">()</span> <span class="type">const</span></span>;</span><br><span class="line"></span><br><span class="line">  <span class="type">bool</span> <span class="keyword">operator</span>==(<span class="type">const</span> Record &amp;other) <span class="type">const</span>;</span><br><span class="line">  <span class="type">bool</span> <span class="keyword">operator</span>!=(<span class="type">const</span> Record &amp;other) <span class="type">const</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">  <span class="type">uint64_t</span> tranc_id_;</span><br><span class="line">  OperationType operation_type_;</span><br><span class="line">  std::string key_;</span><br><span class="line">  std::string value_;</span><br><span class="line">  <span class="type">uint16_t</span> record_len_;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>这里的<code>Record</code>表示的就是<code>WAL</code>中的单个日志条目, 操作类型为<code>OperationType</code>, 通过<code>OperationType</code>可以判断其是否有<code>key</code>, <code>value</code>等附加数据信息。同时，我们通过静态成员函数<code>createRecord</code>, <code>putRecord</code>等构造类的实例。最后，<code>encode</code>和<code>decode</code>函数用于将记录转换为字节流和从字节流恢复记录。</p><h3 id="1-2-2-日志条目编解码"><a href="#1-2-2-日志条目编解码" class="headerlink" title="1.2.2 日志条目编解码"></a>1.2.2 日志条目编解码</h3><p>这里的编解码函数实现和之前<code>block</code>, <code>sst</code>文件的编解码方式非常类似, 每个条目的格式为:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">| record_len | tranc_id | operation_type | key_len(optional) | key(optional) | value_len(optional) | value(optional) |</span><br></pre></td></tr></table></figure><p>这里, 当<code>operation_type</code>是<code>CREATE</code>, <code>ROLLBACK</code>, <code>COMMIT</code>时, 只需要记录<code>tranc_id</code>和<code>operation_type</code>即可, 其余的<code>optional</code>部分不存在, 当<code>operation_type</code>是<code>PUT</code>时, 需要记录<code>tranc_id</code>, <code>operation_type</code>, <code>key</code>, <code>value</code>; 当<code>operation_type</code>是<code>DELETE</code>时, 需要记录<code>tranc_id</code>, <code>operation_type</code>, <code>key</code>。’</p><p>每个条目的第一部分是<code>record_len</code>, 其记录了整个日志条目的长度(16位)。这里的编解码需要注意一下，<code>encode</code>函数是以单个<code>Record</code>为单位, 将其编码为字节流, 而<code>decode</code>函数是以字节流为单位, 将其解码为<code>Record</code>数组。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">std::vector&lt;<span class="type">uint8_t</span>&gt; <span class="title">Record::encode</span><span class="params">()</span> <span class="type">const</span> </span>&#123;</span><br><span class="line">  std::vector&lt;<span class="type">uint8_t</span>&gt; record;</span><br><span class="line"></span><br><span class="line">  <span class="type">size_t</span> key_offset = <span class="built_in">sizeof</span>(<span class="type">uint16_t</span>) + <span class="built_in">sizeof</span>(<span class="type">uint64_t</span>) + <span class="built_in">sizeof</span>(<span class="type">uint8_t</span>);</span><br><span class="line">  <span class="comment">// 记录长度本身(16) + 事务id(64) +</span></span><br><span class="line">  <span class="comment">// 操作类型(8), 固有的编码部分</span></span><br><span class="line"></span><br><span class="line">  record.<span class="built_in">resize</span>(record_len_, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 编码 record_len</span></span><br><span class="line">  std::<span class="built_in">memcpy</span>(record.<span class="built_in">data</span>(), &amp;record_len_, <span class="built_in">sizeof</span>(<span class="type">uint16_t</span>));</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 编码 tranc_id</span></span><br><span class="line">  std::<span class="built_in">memcpy</span>(record.<span class="built_in">data</span>() + <span class="built_in">sizeof</span>(<span class="type">uint16_t</span>), &amp;tranc_id_, <span class="built_in">sizeof</span>(<span class="type">uint64_t</span>));</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 编码 operation_type</span></span><br><span class="line">  <span class="keyword">auto</span> type_byte = <span class="built_in">static_cast</span>&lt;<span class="type">uint8_t</span>&gt;(operation_type_);</span><br><span class="line">  std::<span class="built_in">memcpy</span>(record.<span class="built_in">data</span>() + <span class="built_in">sizeof</span>(<span class="type">uint16_t</span>) + <span class="built_in">sizeof</span>(<span class="type">uint64_t</span>), &amp;type_byte,</span><br><span class="line">              <span class="built_in">sizeof</span>(<span class="type">uint8_t</span>));</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">this</span>-&gt;operation_type_ == OperationType::PUT) &#123;</span><br><span class="line">    <span class="type">uint16_t</span> key_len = key_.<span class="built_in">size</span>();</span><br><span class="line">    std::<span class="built_in">memcpy</span>(record.<span class="built_in">data</span>() + key_offset, &amp;key_len, <span class="built_in">sizeof</span>(<span class="type">uint16_t</span>));</span><br><span class="line">    std::<span class="built_in">memcpy</span>(record.<span class="built_in">data</span>() + key_offset + <span class="built_in">sizeof</span>(<span class="type">uint16_t</span>), key_.<span class="built_in">data</span>(),</span><br><span class="line">                key_.<span class="built_in">size</span>());</span><br><span class="line"></span><br><span class="line">    <span class="type">size_t</span> value_offset = key_offset + <span class="built_in">sizeof</span>(<span class="type">uint16_t</span>) + key_.<span class="built_in">size</span>();</span><br><span class="line"></span><br><span class="line">    <span class="type">uint16_t</span> value_len = value_.<span class="built_in">size</span>();</span><br><span class="line">    std::<span class="built_in">memcpy</span>(record.<span class="built_in">data</span>() + value_offset, &amp;value_len, <span class="built_in">sizeof</span>(<span class="type">uint16_t</span>));</span><br><span class="line">    std::<span class="built_in">memcpy</span>(record.<span class="built_in">data</span>() + value_offset + <span class="built_in">sizeof</span>(<span class="type">uint16_t</span>), value_.<span class="built_in">data</span>(),</span><br><span class="line">                value_.<span class="built_in">size</span>());</span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="keyword">this</span>-&gt;operation_type_ == OperationType::DELETE) &#123;</span><br><span class="line">    <span class="type">uint16_t</span> key_len = key_.<span class="built_in">size</span>();</span><br><span class="line">    std::<span class="built_in">memcpy</span>(record.<span class="built_in">data</span>() + key_offset, &amp;key_len, <span class="built_in">sizeof</span>(<span class="type">uint16_t</span>));</span><br><span class="line">    std::<span class="built_in">memcpy</span>(record.<span class="built_in">data</span>() + key_offset + <span class="built_in">sizeof</span>(<span class="type">uint16_t</span>), key_.<span class="built_in">data</span>(),</span><br><span class="line">                key_.<span class="built_in">size</span>());</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> record;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function">std::vector&lt;Record&gt; <span class="title">Record::decode</span><span class="params">(<span class="type">const</span> std::vector&lt;<span class="type">uint8_t</span>&gt; &amp;data)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (data.<span class="built_in">size</span>() &lt; <span class="built_in">sizeof</span>(<span class="type">uint16_t</span>) + <span class="built_in">sizeof</span>(<span class="type">uint64_t</span>) + <span class="built_in">sizeof</span>(<span class="type">uint8_t</span>)) &#123;</span><br><span class="line">    <span class="keyword">return</span> &#123;&#125;;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  std::vector&lt;Record&gt; records;</span><br><span class="line">  <span class="type">size_t</span> pos = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">while</span> (pos &lt; data.<span class="built_in">size</span>()) &#123;</span><br><span class="line">    <span class="comment">// 读取 record_len</span></span><br><span class="line">    <span class="type">uint16_t</span> record_len;</span><br><span class="line">    std::<span class="built_in">memcpy</span>(&amp;record_len, data.<span class="built_in">data</span>() + pos, <span class="built_in">sizeof</span>(<span class="type">uint16_t</span>));</span><br><span class="line">    pos += <span class="built_in">sizeof</span>(<span class="type">uint16_t</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 检查数据长度是否足够</span></span><br><span class="line">    <span class="keyword">if</span> (data.<span class="built_in">size</span>() &lt; record_len) &#123;</span><br><span class="line">      <span class="keyword">throw</span> std::<span class="built_in">runtime_error</span>(<span class="string">&quot;Data length does not match record length&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 读取 tranc_id</span></span><br><span class="line">    <span class="type">uint64_t</span> tranc_id;</span><br><span class="line">    std::<span class="built_in">memcpy</span>(&amp;tranc_id, data.<span class="built_in">data</span>() + pos, <span class="built_in">sizeof</span>(<span class="type">uint64_t</span>));</span><br><span class="line">    pos += <span class="built_in">sizeof</span>(<span class="type">uint64_t</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 读取 operation_type</span></span><br><span class="line">    <span class="type">uint8_t</span> op_type = data[pos++];</span><br><span class="line">    OperationType operation_type = <span class="built_in">static_cast</span>&lt;OperationType&gt;(op_type);</span><br><span class="line"></span><br><span class="line">    Record record;</span><br><span class="line">    record.tranc_id_ = tranc_id;</span><br><span class="line">    record.operation_type_ = operation_type;</span><br><span class="line">    record.record_len_ = record_len;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (operation_type == OperationType::PUT) &#123;</span><br><span class="line">      <span class="comment">// 读取 key_len</span></span><br><span class="line">      <span class="type">uint16_t</span> key_len;</span><br><span class="line">      std::<span class="built_in">memcpy</span>(&amp;key_len, data.<span class="built_in">data</span>() + pos, <span class="built_in">sizeof</span>(<span class="type">uint16_t</span>));</span><br><span class="line">      pos += <span class="built_in">sizeof</span>(<span class="type">uint16_t</span>);</span><br><span class="line"></span><br><span class="line">      <span class="comment">// 读取 key</span></span><br><span class="line">      record.key_ = std::<span class="built_in">string</span>(</span><br><span class="line">          <span class="built_in">reinterpret_cast</span>&lt;<span class="type">const</span> <span class="type">char</span> *&gt;(data.<span class="built_in">data</span>() + pos), key_len);</span><br><span class="line">      pos += key_len;</span><br><span class="line"></span><br><span class="line">      <span class="comment">// 读取 value_len</span></span><br><span class="line">      <span class="type">uint16_t</span> value_len;</span><br><span class="line">      std::<span class="built_in">memcpy</span>(&amp;value_len, data.<span class="built_in">data</span>() + pos, <span class="built_in">sizeof</span>(<span class="type">uint16_t</span>));</span><br><span class="line">      pos += <span class="built_in">sizeof</span>(<span class="type">uint16_t</span>);</span><br><span class="line"></span><br><span class="line">      <span class="comment">// 读取 value</span></span><br><span class="line">      record.value_ = std::<span class="built_in">string</span>(</span><br><span class="line">          <span class="built_in">reinterpret_cast</span>&lt;<span class="type">const</span> <span class="type">char</span> *&gt;(data.<span class="built_in">data</span>() + pos), value_len);</span><br><span class="line">      pos += value_len;</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (operation_type == OperationType::DELETE) &#123;</span><br><span class="line">      <span class="comment">// 读取 key_len</span></span><br><span class="line">      <span class="type">uint16_t</span> key_len;</span><br><span class="line">      std::<span class="built_in">memcpy</span>(&amp;key_len, data.<span class="built_in">data</span>() + pos, <span class="built_in">sizeof</span>(<span class="type">uint16_t</span>));</span><br><span class="line">      pos += <span class="built_in">sizeof</span>(<span class="type">uint16_t</span>);</span><br><span class="line"></span><br><span class="line">      <span class="comment">// 读取 key</span></span><br><span class="line">      record.key_ = std::<span class="built_in">string</span>(</span><br><span class="line">          <span class="built_in">reinterpret_cast</span>&lt;<span class="type">const</span> <span class="type">char</span> *&gt;(data.<span class="built_in">data</span>() + pos), key_len);</span><br><span class="line">      pos += key_len;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    records.<span class="built_in">push_back</span>(record);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> records;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="2-崩溃恢复机制"><a href="#2-崩溃恢复机制" class="headerlink" title="2 崩溃恢复机制"></a>2 崩溃恢复机制</h1><h2 id="2-1-崩溃恢复运行机制"><a href="#2-1-崩溃恢复运行机制" class="headerlink" title="2.1 崩溃恢复运行机制"></a>2.1 崩溃恢复运行机制</h2><p>当某一时刻存储引擎发送崩溃时, 需要进行崩溃恢复，以恢复数据状态。崩溃恢复的关键是<strong>回放日志</strong>，以确定已提交的事务，并执行其操作。在这个描述中我们不难得到一个信息, 即成功执行的事务一定要先将操作记录持久化到日志中, 然后再在内存中进行操作, 最后返回成功信息给客户端或者调用者。这也是为什么这个机制称为<strong>预写式日志</strong>。其崩溃恢复的工作流程包括：</p><ol><li><strong>日志回放</strong>：从最后一个检查点开始扫描日志，按顺序处理所有已提交事务（<code>COMMIT</code>标记后的操作）。</li><li><strong>Redo阶段</strong>：重新执行所有已提交事务的<code>PUT/REMOVE</code>操作，覆盖当前数据状态。</li><li><strong>Undo阶段（可选）</strong>：若事务未提交（无<code>COMMIT</code>标记），则直接丢弃其操作记录。</li></ol><p><strong>示例场景</strong><br>假设事务<code>TX100</code>依次执行<code>PUT key1=value1</code>和<code>REMOVE key2</code>，其日志内容如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">BEGIN TX100</span><br><span class="line">PUT key1 5 value1</span><br><span class="line">DELETE key2</span><br><span class="line">COMMIT TX100</span><br></pre></td></tr></table></figure><p>事务<code>TX100</code>在调用<code>commit</code>函数后, 需要将上述日志刷入<code>wal</code>文件完成预写这一步骤后, 才会返回<code>client</code>事务提交成功。一开始提交的事务，其数据一定是只存在于<code>MemTable</code>中的, 此时如果存储引擎崩溃, 刚刚完成的事务是没法刷入到<code>sst</code>文件的, 但我们重启时可以检查<code>wal</code>文件的内容, 将其与数据库持久化的状态进行比对(持久化的状态包括最大已完成的事务id、最大已经刷盘的事务id， 见上一章的内容)，如果其事务<code>id</code>比目前已经持久化到<code>sst</code>的最大事务<code>id</code>大，则说明该事务需要进行重放, 另一方面, 如果事务记录的最后一个条目是<code>Rollback</code>，则说明该事务需要被回滚, 则不需要在崩溃恢复时进行重放.</p><p>将上述流程总结如下:</p><p><strong>情形1: 正常提交事务、SST刷盘正常</strong></p><ol><li>事务开始, 写入<code>BEGIN</code>标记到<code>WAL</code>日志中。(此时的<code>WAL</code>日志可能存在于缓冲区, 没有刷入文件)</li><li>执行若干<code>PUT/DELETE</code>操作, 将操作记录写入<code>WAL</code>日志中。(此时的<code>WAL</code>日志可能存在于缓冲区, 没有刷入文件)<ol><li>如果隔离级别是<code>Read Uncommitted</code>, 可以将<code>PUT/DELETE</code>操作直接应用到数据库</li><li>其他隔离级别则将<code>PUT/DELETE</code>操作暂存到事务管理的上下文内存中, 等待事务提交时再应用到数据库</li></ol></li><li>提交事务:<ol><li>将<code>COMMIT</code>标记写入<code>WAL</code>日志中。(此时的<code>WAL</code>日志可能存在于缓冲区, 没有刷入文件)</li><li>将<code>WAL</code>日志刷入磁盘。(此时<code>WAL</code>日志已经刷入磁盘)</li><li>如果隔离级别不是<code>Read Committed</code>, 则将暂存的<code>PUT/DELETE</code>操作应用到数据库</li><li>返回给<code>client</code>成功或失败</li></ol></li><li>之前事务的<code>PUT/DELETE</code>操作的变化应用到数据库仍然是位于<code>MemTable</code>中的, 其会稍后输入<code>SST</code></li></ol><p><strong>情形2: 正常提交事务、SST刷盘崩溃</strong></p><ol><li>事务开始, 写入<code>BEGIN</code>标记到<code>WAL</code>日志中。(此时的<code>WAL</code>日志可能存在于缓冲区, 没有刷入文件)</li><li>执行若干<code>PUT/DELETE</code>操作, 将操作记录写入<code>WAL</code>日志中。(此时的<code>WAL</code>日志可能存在于缓冲区, 没有刷入文件)<ol><li>如果隔离级别是<code>Read Uncommitted</code>, 可以将<code>PUT/DELETE</code>操作直接应用到数据库</li><li>其他隔离级别则将<code>PUT/DELETE</code>操作暂存到事务管理的上下文内存中, 等待事务提交时再应用到数据库</li></ol></li><li>提交事务:<ol><li>将<code>COMMIT</code>标记写入<code>WAL</code>日志中。(此时的<code>WAL</code>日志可能存在于缓冲区, 没有刷入文件)</li><li>将<code>WAL</code>日志刷入磁盘。(此时<code>WAL</code>日志已经刷入磁盘)</li><li>如果隔离级别不是<code>Read Committed</code>, 则将暂存的<code>PUT/DELETE</code>操作应用到数据库</li><li>返回给<code>client</code>成功或失败</li></ol></li><li>之前事务的<code>PUT/DELETE</code>操作的变化应用到数据库仍然是位于<code>MemTable</code>中的, 其稍后输入<code>SST</code>奔溃</li><li>数据库重启后执行崩溃回复<ol><li>检查<code>WAL</code>文件的记录</li><li>整合事务<code>id</code>每条记录, 忽略以<code>Rollback</code>结尾的事务</li><li>若事务以<code>Commit</code>结尾, 则将事务<code>id</code>与已经刷盘的<code>SST</code>中的最大事务<code>id</code>进行比对<ol><li>若事务<code>id</code>大于<code>SST</code>的最大事务<code>id</code>, 执行重放操作</li><li>若事务<code>id</code>小于<code>SST</code>的最大事务<code>id</code>, 则忽略该事务, 因为其已经被数据库执行过了</li></ol></li></ol></li></ol><p><strong>情形3: 事务回滚</strong></p><ol><li>事务开始, 写入<code>BEGIN</code>标记到<code>WAL</code>日志中。(此时的<code>WAL</code>日志可能存在于缓冲区, 没有刷入文件)</li><li>执行若干<code>PUT/DELETE</code>操作, 将操作记录写入<code>WAL</code>日志中。(此时的<code>WAL</code>日志可能存在于缓冲区, 没有刷入文件)<ol><li>如果隔离级别是<code>Read Uncommitted</code>, 可以将<code>PUT/DELETE</code>操作直接应用到数据库</li><li>其他隔离级别则将<code>PUT/DELETE</code>操作暂存到事务管理的上下文内存中, 等待事务提交时再应用到数据库</li></ol></li><li>回滚事务:<ol><li>将<code>Rollback</code>标记写入<code>WAL</code>日志中。(此时的<code>WAL</code>日志可能存在于缓冲区, 没有刷入文件)</li><li>将<code>WAL</code>日志刷入磁盘。(此时<code>WAL</code>日志已经刷入磁盘)</li><li>如果隔离级别不是<code>Read Committed</code>, 则将暂存的<code>PUT/DELETE</code>操作简单丢弃即可</li><li>如果隔离级别是<code>Read Committed</code>, 则将操作前的数据库状态进行还原(上一章介绍的<code>rollback_map_</code>)</li><li>返回给<code>client</code>成功或失败</li></ol></li></ol><h2 id="2-2-考虑WAL后的事务操作"><a href="#2-2-考虑WAL后的事务操作" class="headerlink" title="2.2 考虑WAL后的事务操作"></a>2.2 考虑WAL后的事务操作</h2><p>上一章中, 我们的事务设计没有考虑到或忽略了<code>WAL</code>的操作, 这里我们将对<code>WAL</code>的操作进行实现和讲解:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">TranContext::put</span><span class="params">(<span class="type">const</span> std::string &amp;key, <span class="type">const</span> std::string &amp;value)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">auto</span> isolation_level = tranManager_-&gt;<span class="built_in">isolation_level</span>();</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 所有隔离级别都需要先写入 operations 中</span></span><br><span class="line">  operations.<span class="built_in">emplace_back</span>(Record::<span class="built_in">putRecord</span>(<span class="keyword">this</span>-&gt;tranc_id_, key, value));</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (isolation_level == IsolationLevel::READ_UNCOMMITTED) &#123;</span><br><span class="line">    <span class="comment">// 1 如果隔离级别是 READ_UNCOMMITTED, 直接写入 memtable</span></span><br><span class="line">    <span class="comment">// 先查询以前的记录, 因为回滚时可能需要</span></span><br><span class="line">    <span class="keyword">auto</span> prev_record = engine_-&gt;<span class="built_in">get</span>(key, <span class="number">0</span>);</span><br><span class="line">    rollback_map_[key] = prev_record;</span><br><span class="line">    engine_-&gt;<span class="built_in">put</span>(key, value, tranc_id_);</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 2 其他隔离级别需要 暂存到 temp_map_ 中, 统一提交后才在数据库中生效</span></span><br><span class="line">  temp_map_[key] = value;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">TranContext::remove</span><span class="params">(<span class="type">const</span> std::string &amp;key)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">auto</span> isolation_level = tranManager_-&gt;<span class="built_in">isolation_level</span>();</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 所有隔离级别都需要先写入 operations 中</span></span><br><span class="line">  operations.<span class="built_in">emplace_back</span>(Record::<span class="built_in">deleteRecord</span>(<span class="keyword">this</span>-&gt;tranc_id_, key));</span><br><span class="line"></span><br><span class="line">  <span class="comment">// ... 具体的内存操作</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">TranContext::commit</span><span class="params">(<span class="type">bool</span> test_fail)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">auto</span> isolation_level = tranManager_-&gt;<span class="built_in">isolation_level</span>();</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (isolation_level == IsolationLevel::READ_UNCOMMITTED) &#123;</span><br><span class="line">    <span class="comment">// READ_UNCOMMITTED 随单次操作更新数据库, 不需要最后的统一更新</span></span><br><span class="line">    <span class="comment">// 因此也不需要使用到后面的锁保证正确性</span></span><br><span class="line">    operations.<span class="built_in">emplace_back</span>(Record::<span class="built_in">commitRecord</span>(<span class="keyword">this</span>-&gt;tranc_id_));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 先刷入wal</span></span><br><span class="line">    <span class="keyword">auto</span> wal_success = tranManager_-&gt;<span class="built_in">write_to_wal</span>(operations);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!wal_success) &#123;</span><br><span class="line">      <span class="keyword">throw</span> std::<span class="built_in">runtime_error</span>(<span class="string">&quot;write to wal failed&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    isCommited = <span class="literal">true</span>;</span><br><span class="line">    tranManager_-&gt;<span class="built_in">update_max_finished_tranc_id</span>(tranc_id_);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// ... 冲突检查</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// 其他隔离级别不检查, 直接运行到这里</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// 校验全部通过, 可以刷入</span></span><br><span class="line">  operations.<span class="built_in">emplace_back</span>(Record::<span class="built_in">commitRecord</span>(<span class="keyword">this</span>-&gt;tranc_id_));</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 先刷入wal</span></span><br><span class="line">  <span class="keyword">auto</span> wal_success = tranManager_-&gt;<span class="built_in">write_to_wal</span>(operations);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (!wal_success) &#123;</span><br><span class="line">    <span class="keyword">throw</span> std::<span class="built_in">runtime_error</span>(<span class="string">&quot;write to wal failed&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 将暂存数据应用到数据库</span></span><br><span class="line">  <span class="keyword">if</span> (!test_fail) &#123;</span><br><span class="line">    <span class="comment">// 这里是手动调用 memtable 的无锁版本的 put_, 因为之前手动加了写锁</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">auto</span> &amp;[k, v] : temp_map_) &#123;</span><br><span class="line">      memtable.<span class="built_in">put_</span>(k, v, tranc_id_);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// ... 持久化事务状态</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">TranContext::abort</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">auto</span> isolation_level = tranManager_-&gt;<span class="built_in">isolation_level</span>();</span><br><span class="line">  <span class="keyword">if</span> (isolation_level == IsolationLevel::READ_UNCOMMITTED) &#123;</span><br><span class="line">    <span class="comment">// 需要手动恢复之前的更改</span></span><br><span class="line">    <span class="comment">// <span class="doctag">TODO:</span> 需要使用批量化操作优化性能</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">auto</span> &amp;[k, res] : rollback_map_) &#123;</span><br><span class="line">      <span class="keyword">if</span> (res.<span class="built_in">has_value</span>()) &#123;</span><br><span class="line">        engine_-&gt;<span class="built_in">put</span>(k, res.<span class="built_in">value</span>().first, res.<span class="built_in">value</span>().second);</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// 之前本就不存在, 需要移除当前事务的新增操作</span></span><br><span class="line">        engine_-&gt;<span class="built_in">remove</span>(k, tranc_id_);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    isAborted = <span class="literal">true</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// ! 由于目前 records 是在 commit 时统一刷入 wal 的, 因此这个情况下, abort</span></span><br><span class="line">  <span class="comment">// ! 简单丢弃 operations 即可 ! 后续会将 records 分批次写入 wal</span></span><br><span class="line">  <span class="comment">// ! 这时就需要加上 ! rollback 标记了, 执行下面注释的逻辑</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// operations.emplace_back(Record::rollbackRecord(this-&gt;tranc_id_));</span></span><br><span class="line">  <span class="comment">// // 先刷入wal</span></span><br><span class="line">  <span class="comment">// auto wal_success = tranManager_-&gt;write_to_wal(operations);</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// if (!wal_success) &#123;</span></span><br><span class="line">  <span class="comment">//   throw std::runtime_error(&quot;write to wal failed&quot;);</span></span><br><span class="line">  <span class="comment">// &#125;</span></span><br><span class="line"></span><br><span class="line">  isAborted = <span class="literal">true</span>;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><blockquote><p>需要注意的是, 目前的实现中, 所有的操作记录是随<code>put/remove</code>添加到<code>TranContext</code>的成员变量<code>operations</code>中, 然后在<code>commit</code>时统一写入<code>WAL</code>中, 因此长事务在<code>commit</code>时的<code>IO</code>耗时较大, 后续应该改为分批次进行<code>WAL</code>刷入, 这里我留了个<code>issues</code>: <a href="https://github.com/Vanilla-Beauty/tiny-lsm/issues/6">https://github.com/Vanilla-Beauty/tiny-lsm/issues/6</a> ,感兴趣的朋友可以自己实现一下</p></blockquote><h2 id="2-3-WAL刷盘函数"><a href="#2-3-WAL刷盘函数" class="headerlink" title="2.3 WAL刷盘函数"></a>2.3 WAL刷盘函数</h2><p><code>commit</code>函数中的<code>write_to_wal</code>函数实现了<code>WAL</code>文件的刷盘操作, 其具体实现如下:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// src/lsm/transation.cpp</span></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">TranManager::write_to_wal</span><span class="params">(<span class="type">const</span> std::vector&lt;Record&gt; &amp;records)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    wal-&gt;<span class="built_in">log</span>(records, <span class="literal">true</span>);</span><br><span class="line">  &#125; <span class="built_in">catch</span> (<span class="type">const</span> std::exception &amp;e) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// src/wal/wal.cpp</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">WAL::log</span><span class="params">(<span class="type">const</span> std::vector&lt;Record&gt; &amp;records, <span class="type">bool</span> force_flush)</span> </span>&#123;</span><br><span class="line">  <span class="function">std::unique_lock&lt;std::mutex&gt; <span class="title">lock</span><span class="params">(mutex_)</span></span>;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 将 records 的所有记录添加到 log_buffer_</span></span><br><span class="line">  <span class="keyword">for</span> (<span class="type">const</span> <span class="keyword">auto</span> &amp;record : records) &#123;</span><br><span class="line">    log_buffer_.<span class="built_in">push_back</span>(record);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (log_buffer_.<span class="built_in">size</span>() &lt; buffer_size_ &amp;&amp; !force_flush) &#123;</span><br><span class="line">    <span class="comment">// 如果 log_buffer_ 的大小小于 buffer_size_ 且 force_flush 为 false,</span></span><br><span class="line">    <span class="comment">// 不进行写入</span></span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 否则写入 wal 文件</span></span><br><span class="line">  <span class="keyword">auto</span> pre_buffer = std::<span class="built_in">move</span>(log_buffer_);</span><br><span class="line">  <span class="keyword">for</span> (<span class="type">const</span> <span class="keyword">auto</span> &amp;record : pre_buffer) &#123;</span><br><span class="line">    std::vector&lt;<span class="type">uint8_t</span>&gt; encoded_record = record.<span class="built_in">encode</span>();</span><br><span class="line">    log_file_.<span class="built_in">append</span>(encoded_record);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (!log_file_.<span class="built_in">sync</span>()) &#123;</span><br><span class="line">    <span class="comment">// 确保日志立即写入磁盘</span></span><br><span class="line">    <span class="keyword">throw</span> std::<span class="built_in">runtime_error</span>(<span class="string">&quot;Failed to sync WAL file&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">auto</span> cur_file_size = log_file_.<span class="built_in">size</span>();</span><br><span class="line">  <span class="keyword">if</span> (cur_file_size &gt; file_size_limit_) &#123;</span><br><span class="line">    <span class="built_in">reset_file</span>();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里的核心函数是<code>WAL::log</code>, 其会将一个数组中的内存刷入到内存缓存数组<code>log_buffer_</code>, 当其数组大小超过指定阈值时, 就会进行刷盘操作, 同时会重置<code>log_buffer_</code>。同时，类似<code>commit</code>, <code>abort</code>这样的事务操作需要立即刷盘以完成持久化, 这是可以手动指定<code>force_flush</code>为<code>true</code>来强行刷入。这里的<code>log_file_</code>就是我们使用之前章节实现的<code>FileObj</code>包装的<code>wal</code>文件操作句柄。</p><h2 id="2-4-WAL重放检查函数"><a href="#2-4-WAL重放检查函数" class="headerlink" title="2.4 WAL重放检查函数"></a>2.4 WAL重放检查函数</h2><p>这是这章最核心的部分, 数据库启动时, 我们需要检查<code>WAL</code>文件是否需要被重放, 即检查<code>WAL</code>文件中的<code>Record</code>是否需要被应用到数据库中:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// src/lsm/engine.cpp</span></span><br><span class="line">LSM::<span class="built_in">LSM</span>(std::string path)</span><br><span class="line">    : <span class="built_in">engine</span>(std::<span class="built_in">make_shared</span>&lt;LSMEngine&gt;(path)),</span><br><span class="line">      <span class="built_in">tran_manager_</span>(std::<span class="built_in">make_shared</span>&lt;TranManager&gt;(path)) &#123;</span><br><span class="line">  tran_manager_-&gt;<span class="built_in">set_engine</span>(engine);</span><br><span class="line">  <span class="keyword">auto</span> check_recover_res = tran_manager_-&gt;<span class="built_in">check_recover</span>();</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">auto</span> &amp;[tranc_id, records] : check_recover_res) &#123;</span><br><span class="line">    tran_manager_-&gt;<span class="built_in">update_max_finished_tranc_id</span>(tranc_id);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">auto</span> &amp;record : records) &#123;</span><br><span class="line">      <span class="keyword">if</span> (record.<span class="built_in">getOperationType</span>() == OperationType::PUT) &#123;</span><br><span class="line">        engine-&gt;<span class="built_in">put</span>(record.<span class="built_in">getKey</span>(), record.<span class="built_in">getValue</span>(), tranc_id);</span><br><span class="line">      &#125; <span class="keyword">else</span> <span class="keyword">if</span> (record.<span class="built_in">getOperationType</span>() == OperationType::DELETE) &#123;</span><br><span class="line">        engine-&gt;<span class="built_in">remove</span>(record.<span class="built_in">getKey</span>(), tranc_id);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  tran_manager_-&gt;<span class="built_in">init_new_wal</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>数据库初始化时, 构造函数会调用<code>check_recover</code>判断是否需要进行<code>WAL</code>重放, 其执行的逻辑如下:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// src/lsm/transation.cpp</span></span><br><span class="line">std::map&lt;<span class="type">uint64_t</span>, std::vector&lt;Record&gt;&gt; TranManager::<span class="built_in">check_recover</span>() &#123;</span><br><span class="line">  std::map&lt;<span class="type">uint64_t</span>, std::vector&lt;Record&gt;&gt; wal_records =</span><br><span class="line">      WAL::<span class="built_in">recover</span>(data_dir_, max_flushed_tranc_id_);</span><br><span class="line">  <span class="keyword">return</span> wal_records;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// src/wal/wal.cpp</span></span><br><span class="line">std::map&lt;<span class="type">uint64_t</span>, std::vector&lt;Record&gt;&gt;</span><br><span class="line">WAL::<span class="built_in">recover</span>(<span class="type">const</span> std::string &amp;log_dir, <span class="type">uint64_t</span> max_flushed_tranc_id) &#123;</span><br><span class="line">  std::map&lt;<span class="type">uint64_t</span>, std::vector&lt;Record&gt;&gt; tranc_records&#123;&#125;;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 引擎启动时判断</span></span><br><span class="line">  <span class="keyword">if</span> (!std::filesystem::<span class="built_in">exists</span>(log_dir)) &#123;</span><br><span class="line">    <span class="keyword">return</span> tranc_records;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 遍历log_dir下的所有文件</span></span><br><span class="line">  std::vector&lt;std::string&gt; wal_paths;</span><br><span class="line">  <span class="keyword">for</span> (<span class="type">const</span> <span class="keyword">auto</span> &amp;entry : std::filesystem::<span class="built_in">directory_iterator</span>(log_dir)) &#123;</span><br><span class="line">    <span class="keyword">if</span> (entry.<span class="built_in">is_regular_file</span>()) &#123;</span><br><span class="line">      <span class="comment">// 获取/符号后的文件名</span></span><br><span class="line">      std::string filename = entry.<span class="built_in">path</span>().<span class="built_in">filename</span>().<span class="built_in">string</span>();</span><br><span class="line">      <span class="keyword">if</span> (filename.<span class="built_in">substr</span>(<span class="number">0</span>, <span class="number">4</span>) != <span class="string">&quot;wal.&quot;</span>) &#123;</span><br><span class="line">        <span class="keyword">continue</span>;</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      wal_paths.<span class="built_in">push_back</span>(entry.<span class="built_in">path</span>().<span class="built_in">string</span>());</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 按照seq升序排序</span></span><br><span class="line">  std::<span class="built_in">sort</span>(wal_paths.<span class="built_in">begin</span>(), wal_paths.<span class="built_in">end</span>(),</span><br><span class="line">            [](<span class="type">const</span> std::string &amp;a, <span class="type">const</span> std::string &amp;b) &#123;</span><br><span class="line">              <span class="keyword">auto</span> a_seq_str = a.<span class="built_in">substr</span>(a.<span class="built_in">find_last_of</span>(<span class="string">&quot;.&quot;</span>) + <span class="number">1</span>);</span><br><span class="line">              <span class="keyword">auto</span> b_seq_str = b.<span class="built_in">substr</span>(b.<span class="built_in">find_last_of</span>(<span class="string">&quot;.&quot;</span>) + <span class="number">1</span>);</span><br><span class="line">              <span class="keyword">return</span> std::<span class="built_in">stoi</span>(a_seq_str) &lt; std::<span class="built_in">stoi</span>(b_seq_str);</span><br><span class="line">            &#125;);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 读取所有的记录</span></span><br><span class="line">  <span class="keyword">for</span> (<span class="type">const</span> <span class="keyword">auto</span> &amp;wal_path : wal_paths) &#123;</span><br><span class="line">    <span class="keyword">auto</span> wal_file = FileObj::<span class="built_in">open</span>(wal_path, <span class="literal">false</span>);</span><br><span class="line">    <span class="keyword">auto</span> wal_records_slice = wal_file.<span class="built_in">read_to_slice</span>(<span class="number">0</span>, wal_file.<span class="built_in">size</span>());</span><br><span class="line">    <span class="keyword">auto</span> records = Record::<span class="built_in">decode</span>(wal_records_slice);</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">const</span> <span class="keyword">auto</span> &amp;record : records) &#123;</span><br><span class="line">      <span class="keyword">if</span> (record.<span class="built_in">getTrancId</span>() &gt; max_flushed_tranc_id) &#123;</span><br><span class="line">        <span class="comment">// 如果记录的 tranc_id 大于 max_finished_tranc_id, 才需要尝试恢复</span></span><br><span class="line">        tranc_records[record.<span class="built_in">getTrancId</span>()].<span class="built_in">push_back</span>(record);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> tranc_records;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>WAL::recover</code>是核心的<code>WAL</code>检测函数, 其会将<code>WAL</code>中不同事务的记录进行整理, 放到一个<code>map</code>中, <code>map</code>的每个<code>key</code>即为事务的<code>id</code>, <code>value</code>即为事务中包含的所有<code>Record</code>。<code>WAL::recover</code>将<code>key</code>与当前数据持久化信息中的<code>max_flushed_tranc_id</code>进行比较, 只保留记录的 <code>tranc_id</code> 大于 <code>max_finished_tranc_id</code>的记录, 否则其已经持久化到<code>sst</code>中, 因此不需要再进行恢复。</p><p><code>WAL::recover</code>检查只保留要重放的记录, 具体的重放操作由<code>LSM::LSM</code>中完成, 即构造函数中的<code>for</code>循环部分:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="keyword">auto</span> &amp;[tranc_id, records] : check_recover_res) &#123;</span><br><span class="line">    tran_manager_-&gt;<span class="built_in">update_max_finished_tranc_id</span>(tranc_id);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">auto</span> &amp;record : records) &#123;</span><br><span class="line">      <span class="keyword">if</span> (record.<span class="built_in">getOperationType</span>() == OperationType::PUT) &#123;</span><br><span class="line">        engine-&gt;<span class="built_in">put</span>(record.<span class="built_in">getKey</span>(), record.<span class="built_in">getValue</span>(), tranc_id);</span><br><span class="line">      &#125; <span class="keyword">else</span> <span class="keyword">if</span> (record.<span class="built_in">getOperationType</span>() == OperationType::DELETE) &#123;</span><br><span class="line">        engine-&gt;<span class="built_in">remove</span>(record.<span class="built_in">getKey</span>(), tranc_id);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure><h2 id="2-5-WAL清理函数"><a href="#2-5-WAL清理函数" class="headerlink" title="2.5 WAL清理函数"></a>2.5 WAL清理函数</h2><p><code>WAL</code>中的文件内容, 在数据运用到数据库并刷入了<code>SST</code>后就没有实际意义了, 这时我们需要清理<code>WAL</code>文件, 删除多余的<code>WAL</code>文件, 并且将<code>WAL</code>文件指针指向最新的<code>WAL</code>文件。这里我们如下设计:</p><ol><li><code>wal</code>文件格式为<code>wal.0</code>, <code>wal.1</code>, <code>wal.2</code>, …, <code>wal.n</code>, 其中<code>n</code>为文件序号, <code>wal.0</code>为最旧文件, <code>wal.n</code>为最新文件</li><li>单个<code>wal</code>文件有容量限制, 超出容量就需要创建新的<code>wal</code>文件, 并且将<code>wal</code>文件指针指向最新的<code>wal</code>文件</li><li>任意时刻只有1个<code>wal</code>文件处于允许写入状态, 其余的<code>wal</code>文件处于关闭状态</li><li>清理函数根据目前已经被持久化的事务的<code>id</code>, 周期性地从小到大遍历<code>wal</code>文件, 删除所有记录已经被持久化的<code>wal</code>文件,</li></ol><p>其清理函数为:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// src/wal/wal.cpp</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">WAL::cleanWALFile</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="comment">// 遍历log_file_所在的文件夹</span></span><br><span class="line">  std::string dir_path;</span><br><span class="line"></span><br><span class="line">  <span class="function">std::unique_lock&lt;std::mutex&gt; <span class="title">lock</span><span class="params">(mutex_)</span></span>; <span class="comment">// 只在获取当前文件路径时获取锁</span></span><br><span class="line">  <span class="keyword">if</span> (active_log_path_.<span class="built_in">find</span>(<span class="string">&quot;/&quot;</span>) != std::string::npos) &#123;</span><br><span class="line">    dir_path =</span><br><span class="line">        active_log_path_.<span class="built_in">substr</span>(<span class="number">0</span>, active_log_path_.<span class="built_in">find_last_of</span>(<span class="string">&quot;/&quot;</span>)) + <span class="string">&quot;/&quot;</span>;</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    dir_path = <span class="string">&quot;./&quot;</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  lock.<span class="built_in">unlock</span>();</span><br><span class="line"></span><br><span class="line">  <span class="comment">// wal文件格式为:</span></span><br><span class="line">  <span class="comment">// wal.seq</span></span><br><span class="line"></span><br><span class="line">  std::vector&lt;std::pair&lt;<span class="type">size_t</span>, std::string&gt;&gt; wal_paths;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">for</span> (<span class="type">const</span> <span class="keyword">auto</span> &amp;entry : std::filesystem::<span class="built_in">directory_iterator</span>(dir_path)) &#123;</span><br><span class="line">    <span class="keyword">if</span> (entry.<span class="built_in">is_regular_file</span>() &amp;&amp;</span><br><span class="line">        entry.<span class="built_in">path</span>().<span class="built_in">filename</span>().<span class="built_in">string</span>().<span class="built_in">substr</span>(<span class="number">0</span>, <span class="number">4</span>) == <span class="string">&quot;wal.&quot;</span>) &#123;</span><br><span class="line">      std::string filename = entry.<span class="built_in">path</span>().<span class="built_in">filename</span>().<span class="built_in">string</span>();</span><br><span class="line">      <span class="type">size_t</span> dot_pos = filename.<span class="built_in">find_last_of</span>(<span class="string">&quot;.&quot;</span>);</span><br><span class="line">      std::string seq_str = filename.<span class="built_in">substr</span>(dot_pos + <span class="number">1</span>);</span><br><span class="line">      <span class="type">uint64_t</span> seq = std::<span class="built_in">stoull</span>(seq_str);</span><br><span class="line">      wal_paths.<span class="built_in">push_back</span>(&#123;seq, entry.<span class="built_in">path</span>().<span class="built_in">string</span>()&#125;);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 按照seq升序排序</span></span><br><span class="line">  std::<span class="built_in">sort</span>(wal_paths.<span class="built_in">begin</span>(), wal_paths.<span class="built_in">end</span>(),</span><br><span class="line">            [](<span class="type">const</span> std::pair&lt;<span class="type">size_t</span>, std::string&gt; &amp;a,</span><br><span class="line">               <span class="type">const</span> std::pair&lt;<span class="type">size_t</span>, std::string&gt; &amp;b) &#123;</span><br><span class="line">              <span class="keyword">return</span> a.first &lt; b.first;</span><br><span class="line">            &#125;);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 判断是否可以删除</span></span><br><span class="line">  std::vector&lt;FileObj&gt; del_paths;</span><br><span class="line">  <span class="keyword">for</span> (<span class="type">int</span> idx = <span class="number">0</span>; idx &lt; wal_paths.<span class="built_in">size</span>() - <span class="number">1</span>; idx++) &#123;</span><br><span class="line">    <span class="keyword">auto</span> cur_path = wal_paths[idx].second;</span><br><span class="line">    <span class="keyword">auto</span> cur_file = FileObj::<span class="built_in">open</span>(cur_path, <span class="literal">false</span>);</span><br><span class="line">    <span class="comment">// 遍历文件记录, 读取所有的tranc_id,</span></span><br><span class="line">    <span class="comment">// 判断是否都小于等于max_finished_tranc_id_</span></span><br><span class="line">    <span class="type">size_t</span> offset = <span class="number">0</span>;</span><br><span class="line">    <span class="type">bool</span> has_unfinished = <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">while</span> (offset + <span class="built_in">sizeof</span>(<span class="type">uint16_t</span>) &lt; cur_file.<span class="built_in">size</span>()) &#123;</span><br><span class="line">      <span class="type">uint16_t</span> record_size = cur_file.<span class="built_in">read_uint16</span>(offset);</span><br><span class="line">      <span class="type">uint64_t</span> tranc_id = cur_file.<span class="built_in">read_uint64</span>(offset + <span class="built_in">sizeof</span>(<span class="type">uint16_t</span>));</span><br><span class="line">      <span class="keyword">if</span> (tranc_id &gt; max_finished_tranc_id_) &#123;</span><br><span class="line">        has_unfinished = <span class="literal">true</span>;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (!has_unfinished) &#123;</span><br><span class="line">      del_paths.<span class="built_in">push_back</span>(std::<span class="built_in">move</span>(cur_file));</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">auto</span> &amp;del_file : del_paths) &#123;</span><br><span class="line">    del_file.<span class="built_in">del_file</span>();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>其在构造函数初始化时, 会启动一个函数周期性地运行<code>cleanWALFile</code>:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// src/wal/wal.cpp</span></span><br><span class="line"><span class="comment">// 从零开始的初始化流程</span></span><br><span class="line">WAL::<span class="built_in">WAL</span>(<span class="type">const</span> std::string &amp;log_dir, <span class="type">size_t</span> buffer_size,</span><br><span class="line">         <span class="type">uint64_t</span> max_finished_tranc_id, <span class="type">uint64_t</span> clean_interval,</span><br><span class="line">         <span class="type">uint64_t</span> file_size_limit)</span><br><span class="line">    : <span class="built_in">buffer_size_</span>(buffer_size), <span class="built_in">max_finished_tranc_id_</span>(max_finished_tranc_id),</span><br><span class="line">      <span class="built_in">stop_cleaner_</span>(<span class="literal">false</span>), <span class="built_in">clean_interval_</span>(clean_interval),</span><br><span class="line">      <span class="built_in">file_size_limit_</span>(file_size_limit) &#123;</span><br><span class="line">  active_log_path_ = log_dir + <span class="string">&quot;/wal.0&quot;</span>;</span><br><span class="line">  log_file_ = FileObj::<span class="built_in">open</span>(active_log_path_, <span class="literal">true</span>);</span><br><span class="line"></span><br><span class="line">  cleaner_thread_ = std::<span class="built_in">thread</span>(&amp;WAL::cleaner, <span class="keyword">this</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">WAL::cleaner</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">    &#123;</span><br><span class="line">      <span class="comment">// 睡眠 clean_interval_ s</span></span><br><span class="line">      std::this_thread::<span class="built_in">sleep_for</span>(std::chrono::<span class="built_in">seconds</span>(clean_interval_));</span><br><span class="line">      <span class="keyword">if</span> (stop_cleaner_) &#123;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="built_in">cleanWALFile</span>();</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="3-总结"><a href="#3-总结" class="headerlink" title="3 总结"></a>3 总结</h1><p><code>WAL</code>是<code>LSM</code>树中一个重要的组件, 它的作用是保证数据的持久化, 并且<code>WAL</code>中的数据可以保证数据的一致性。至此，我们的<code>LSm Tree</code>的大部分基础功能都已经实现完毕, 包括基础的<code>api</code>, 事务, <code>MVCC</code>, <code>WAL</code>和崩溃恢复。下一章， 我们完成<code>SST</code>的<code>compact</code>后, <code>LSM Tree</code>这个项目就接近完结啦</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;之前我们已经完成了数据库内存中和编码文件中事务信息的融入, 现在我们还需要加上WAL与崩溃恢复的内容。&lt;/p&gt;
&lt;p&gt;代码仓库：&lt;a href=&quot;https://github.com/Vanilla-Beauty/tiny-lsm&quot;&gt;Vanilla-Beauty&amp;#x2F;</summary>
      
    
    
    
    <category term="项目" scheme="http://example.com/categories/%E9%A1%B9%E7%9B%AE/"/>
    
    <category term="Tiny-LSM" scheme="http://example.com/categories/%E9%A1%B9%E7%9B%AE/Tiny-LSM/"/>
    
    
    <category term="C++" scheme="http://example.com/tags/C/"/>
    
    <category term="KV存储" scheme="http://example.com/tags/KV%E5%AD%98%E5%82%A8/"/>
    
  </entry>
  
  <entry>
    <title>C++从零开始实现LSM-Tree-KV存储-17-事务与MVCC设计3</title>
    <link href="http://example.com/2025/04/15/%E9%A1%B9%E7%9B%AE/Tiny-LSM/C++%E4%BB%8E%E9%9B%B6%E5%BC%80%E5%A7%8B%E5%AE%9E%E7%8E%B0LSM-Tree-KV%E5%AD%98%E5%82%A8-17-%E4%BA%8B%E5%8A%A1%E4%B8%8EMVCC%E8%AE%BE%E8%AE%A13/"/>
    <id>http://example.com/2025/04/15/%E9%A1%B9%E7%9B%AE/Tiny-LSM/C++%E4%BB%8E%E9%9B%B6%E5%BC%80%E5%A7%8B%E5%AE%9E%E7%8E%B0LSM-Tree-KV%E5%AD%98%E5%82%A8-17-%E4%BA%8B%E5%8A%A1%E4%B8%8EMVCC%E8%AE%BE%E8%AE%A13/</id>
    <published>2025-04-15T03:17:39.000Z</published>
    <updated>2026-05-28T07:37:46.669Z</updated>
    
    <content type="html"><![CDATA[<p>之前我们已经对各个组件的接口进行了统一, 添加了<code>tranc_id</code>这个事务id参数, 接下来这个章节, 我们将介绍顶层的事务设计, 即事务id是如何生成的, 实现相关的事务管理器.</p><p>代码仓库：<a href="https://github.com/Vanilla-Beauty/tiny-lsm">Vanilla-Beauty&#x2F;tiny-lsm: A KV storage engine based on LSM Tree, supporting Redis RESP</a> 感谢您的 <code>Star</code>!</p><p>欢迎支持本项目同步更新的从零开始实现的视频教程：<a href="https://avo6166ew2u.feishu.cn/docx/LXmVdezdsoTBRaxC97WcHwGunOc">https://avo6166ew2u.feishu.cn/docx/LXmVdezdsoTBRaxC97WcHwGunOc</a></p><p>欢迎加入讨论群：<a href="https://qm.qq.com/q/lkIzCPvcdM">Tiny-LSM项目交流群</a></p><h1 id="1-事务的设计思想"><a href="#1-事务的设计思想" class="headerlink" title="1 事务的设计思想"></a>1 事务的设计思想</h1><p>我们先给出一个完成后的<code>demo</code>, 演示我们的事务设计是如何工作的, 代码如下:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;../include/lsm/engine.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="comment">// create lsm instance, data_dir is the directory to store data</span></span><br><span class="line">  <span class="function">LSM <span class="title">lsm</span><span class="params">(<span class="string">&quot;example_data&quot;</span>)</span></span>;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// put data</span></span><br><span class="line">  lsm.<span class="built_in">put</span>(<span class="string">&quot;key1&quot;</span>, <span class="string">&quot;value1&quot;</span>);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// Query data</span></span><br><span class="line">  <span class="keyword">auto</span> value1 = lsm.<span class="built_in">get</span>(<span class="string">&quot;key1&quot;</span>);</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;key1: &quot;</span> &lt;&lt; value1.<span class="built_in">value</span>() &lt;&lt; std::endl;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">  <span class="comment">// transaction</span></span><br><span class="line">  <span class="keyword">auto</span> tranc_hanlder = lsm.<span class="built_in">begin_tran</span>();</span><br><span class="line">  tranc_hanlder-&gt;<span class="built_in">put</span>(<span class="string">&quot;xxx&quot;</span>, <span class="string">&quot;yyy&quot;</span>);</span><br><span class="line">  tranc_hanlder-&gt;<span class="built_in">put</span>(<span class="string">&quot;yyy&quot;</span>, <span class="string">&quot;xxx&quot;</span>);</span><br><span class="line">  tranc_hanlder-&gt;<span class="built_in">commit</span>();</span><br><span class="line"></span><br><span class="line">  <span class="keyword">auto</span> res = lsm.<span class="built_in">get</span>(<span class="string">&quot;xxx&quot;</span>);</span><br><span class="line">  std::cout &lt;&lt; <span class="string">&quot;xxx: &quot;</span> &lt;&lt; res.<span class="built_in">value</span>() &lt;&lt; std::endl;</span><br><span class="line"></span><br><span class="line">  lsm.<span class="built_in">clear</span>();</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里我们可以通过一个<code>begin_tran</code>函数获取一个事务的处理句柄, 然后通过这个句柄进行增删改查操作, 最后通过<code>commit</code>或<code>abort</code>函数完成提交事务或终结事务的流程.</p><p>现在我们要完成的就是接受<code>begin_tran</code>的事务管理器. 这里有一些设计问题我们需要提前明确:</p><ol><li><code>begin_tran</code>获取的事务句柄肯定会分配一个事务<code>id</code>, 那么没有开启事务的<code>put/get/remove</code>操作的事务<code>id</code>是什么呢?</li><li><code>begin_tran</code>进行增删改查的操作如何保证不同事务的隔离性?</li></ol><p>首先回答第一个问题, 我们可以使用一个全局的<code>atomic</code>变量来作为事务id, 这个变量在每次调用<code>begin_tran</code>或<code>put/get/remove</code>时自增, 这样就可以保证每个事务或单次操作都有一个唯一的<code>tranc_id</code>(这里的<code>tranc_id</code>和事务<code>id</code>是同义词). 换句话说, 普通的<code>put/get/remove</code>就是操作数量为1的简单事务。</p><p>然后是第二个问题，这实际上取决于我们的事务隔离级别:</p><ol><li><code>Read Uncommitted</code>: 允许读取未提交的数据, 也就是脏读. 这种情况下, 我们可以将事务的句柄(案例代码中的<code>tranc_hanlder</code>)进行增删改查的数据直接写入到<code>memtable</code>中, 这样就可以让其他的事务可以从<code>memtable</code>中读取到未提交的数据, 速度肯定很快. 但是这里有一个场景需要尤其注意, 就是我们事务<code>rokkback</code>(或者是<code>abort</code>)时, 必须撤销已经写入到<code>memtable</code>的数据, 因此这里需要我们记录事务的操作记录和以前的历史记录, 然后在<code>abort</code>时, 将<code>memtable</code>中的数据进行回滚.</li><li><code>Read Committed</code>: 允许读取已提交的数据, 也就是不可脏读. 这种情况下, 我们可以将事务的句柄(案例代码中的<code>tranc_hanlder</code>)进行增删改查的数据暂存到句柄的上下文, 因此其他事务从<code>memtable</code>中是查不到这个事务未提交的数据的, 但这个事务自身查询时可以从自己的上下文中读取到未提交的数据.</li><li><code>Repeatable Read</code>: 在<code>Read Committed</code>的基础上解决了不可重复读的问题, 也就是在同一个事务中, 多次读取同一数据的结果是一样的. 这种情况下, 我们可以将每次<code>get</code>的数据同样暂存到句柄的上下文, 后续查询相同的<code>key</code>时, 从上下文中读取到相同的数据.</li><li><code>Serializable</code>: 这个这个事务隔离级别我们在关系型数据库中是进一步解决<code>幻读</code>现象的, 例如: 在<code>Repeatable Read</code>隔离级别下，事务A读取了年龄&gt;30的员工，得到10条记录。此时事务B插入了一个年龄31的新员工并提交。事务A再次读取同样的条件，可能会看到11条记录（幻读）。但在<code>Serializable</code>隔离级别下，事务B的插入会被阻塞或者事务A的两次读取结果保持一致，避免幻读。在我们的<code>KV</code>数据库中, 我们只需要保证事务提交时进程冲突检查、且按照事务<code>id</code>的顺序依次提交即可(虽然这样性能很低)</li></ol><blockquote><p>如果这里看不懂的话, 可以继续往下看, 随着代码的实现应该能理解, 再理解不了的话, 建议先复习或学习数据库中<code>MVCC</code>、事务、并发控制的基础知识</p></blockquote><h1 id="2-事务管理器的实现"><a href="#2-事务管理器的实现" class="headerlink" title="2 事务管理器的实现"></a>2 事务管理器的实现</h1><h2 id="2-1-组件关系设计"><a href="#2-1-组件关系设计" class="headerlink" title="2.1 组件关系设计"></a>2.1 组件关系设计</h2><p>还记得我们之前实现的<code>LSm</code>和<code>LSMEngine</code>吗? 当时我们将<code>LSMEngine</code>包裹在<code>LSM</code>中, <code>LSMEngine</code>中封装了<code>memtable</code>, <code>sst</code>等组件, 我们却进一步将其封装在<code>LSM</code>中, 这样的目的就是在后续中加入其他同级别的组件, 例如本章的事务管理器, 偷我二年级的定义为:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// include/lsm/engine.h</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">LSM</span> &#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">  std::shared_ptr&lt;LSMEngine&gt; engine;</span><br><span class="line">  std::shared_ptr&lt;TranManager&gt; tran_manager_; <span class="comment">// new</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="built_in">LSM</span>(std::string path);</span><br><span class="line">  ~<span class="built_in">LSM</span>();</span><br><span class="line"></span><br><span class="line">  <span class="function">std::optional&lt;std::string&gt; <span class="title">get</span><span class="params">(<span class="type">const</span> std::string &amp;key)</span></span>;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="type">void</span> <span class="title">put</span><span class="params">(<span class="type">const</span> std::string &amp;key, <span class="type">const</span> std::string &amp;value)</span></span>;</span><br><span class="line">  <span class="function"><span class="type">void</span> <span class="title">put_batch</span><span class="params">(<span class="type">const</span> std::vector&lt;std::pair&lt;std::string, std::string&gt;&gt; &amp;kvs)</span></span>;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="type">void</span> <span class="title">remove</span><span class="params">(<span class="type">const</span> std::string &amp;key)</span></span>;</span><br><span class="line">  <span class="function"><span class="type">void</span> <span class="title">remove_batch</span><span class="params">(<span class="type">const</span> std::vector&lt;std::string&gt; &amp;keys)</span></span>;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">using</span> LSMIterator = TwoMergeIterator; <span class="comment">// <span class="doctag">TODO:</span> 实现 compact后需要修改</span></span><br><span class="line">  <span class="function">LSMIterator <span class="title">begin</span><span class="params">(<span class="type">uint64_t</span> tranc_id)</span></span>; <span class="comment">// <span class="doctag">TODO:</span> 实现 compact后需要修改</span></span><br><span class="line">  <span class="function">LSMIterator <span class="title">end</span><span class="params">()</span></span>;</span><br><span class="line">  std::optional&lt;std::pair&lt;TwoMergeIterator, TwoMergeIterator&gt;&gt;</span><br><span class="line">  <span class="built_in">lsm_iters_monotony_predicate</span>(</span><br><span class="line">      <span class="type">uint64_t</span> tranc_id, std::function&lt;<span class="built_in">int</span>(<span class="type">const</span> std::string &amp;)&gt; predicate);</span><br><span class="line">  <span class="function"><span class="type">void</span> <span class="title">clear</span><span class="params">()</span></span>;</span><br><span class="line">  <span class="function"><span class="type">void</span> <span class="title">flush</span><span class="params">()</span></span>;</span><br><span class="line">  <span class="function"><span class="type">void</span> <span class="title">flush_all</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 开启一个事务</span></span><br><span class="line">  <span class="function">std::shared_ptr&lt;TranContext&gt; <span class="title">begin_tran</span><span class="params">()</span></span>; <span class="comment">// new</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>这里的数据成员中, 我们添加了<code>tran_manager_</code>, 其将作为分配事务<code>id</code>和<code>begin_tran</code>申请<code>TranContext</code>的管理器</p><h2 id="2-2-事务管理器的定义"><a href="#2-2-事务管理器的定义" class="headerlink" title="2.2 事务管理器的定义"></a>2.2 事务管理器的定义</h2><h3 id="2-2-1-TranContext的定义"><a href="#2-2-1-TranContext的定义" class="headerlink" title="2.2.1 TranContext的定义"></a>2.2.1 TranContext的定义</h3><p>这里首先回顾之前的定义：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">std::shared_ptr&lt;TranManager&gt; tran_manager_; <span class="comment">// new</span></span><br><span class="line"><span class="function">std::shared_ptr&lt;TranContext&gt; <span class="title">begin_tran</span><span class="params">()</span></span>; <span class="comment">// new</span></span><br></pre></td></tr></table></figure><p>这里的<code>TranContext</code>是向<code>TranManager</code>申请事务上下文后返回的类, 这个上下文中, 我们需要实现基本的事务管理功能, 包括增删改查、提交、回滚，我们初步将其头文件定义如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// include/lsm/transaction.h</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">LSMEngine</span>;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">TranManager</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">TranContext</span> &#123;</span><br><span class="line">  <span class="keyword">friend</span> <span class="keyword">class</span> <span class="title class_">TranManager</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="built_in">TranContext</span>(<span class="type">uint64_t</span> tranc_id, std::shared_ptr&lt;LSMEngine&gt; engine,</span><br><span class="line">              std::shared_ptr&lt;TranManager&gt; tranManager);</span><br><span class="line">  <span class="function"><span class="type">void</span> <span class="title">put</span><span class="params">(<span class="type">const</span> std::string &amp;key, <span class="type">const</span> std::string &amp;value)</span></span>;</span><br><span class="line">  <span class="function"><span class="type">void</span> <span class="title">remove</span><span class="params">(<span class="type">const</span> std::string &amp;key)</span></span>;</span><br><span class="line">  <span class="function">std::optional&lt;std::string&gt; <span class="title">get</span><span class="params">(<span class="type">const</span> std::string &amp;key)</span></span>;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// ! test_fail = true 是测试中手动触发的崩溃</span></span><br><span class="line">  <span class="function"><span class="type">bool</span> <span class="title">commit</span><span class="params">(<span class="type">bool</span> test_fail = <span class="literal">false</span>)</span></span>;</span><br><span class="line">  <span class="function"><span class="type">bool</span> <span class="title">abort</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">  std::shared_ptr&lt;LSMEngine&gt; engine_;</span><br><span class="line">  std::shared_ptr&lt;TranManager&gt; tranManager_;</span><br><span class="line">  <span class="type">uint64_t</span> tranc_id_;</span><br><span class="line">  std::vector&lt;Record&gt; operations;</span><br><span class="line">  std::unordered_map&lt;std::string, std::string&gt; temp_map_;</span><br><span class="line">  <span class="type">bool</span> isCommited = <span class="literal">false</span>;</span><br><span class="line">  <span class="type">bool</span> isAborted = <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">  std::unordered_map&lt;std::string,</span><br><span class="line">                     std::optional&lt;std::pair&lt;std::string, <span class="type">uint64_t</span>&gt;&gt;&gt;</span><br><span class="line">      read_map_;</span><br><span class="line">  std::unordered_map&lt;std::string,</span><br><span class="line">                     std::optional&lt;std::pair&lt;std::string, <span class="type">uint64_t</span>&gt;&gt;&gt;</span><br><span class="line">      rollback_map_;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>需要注意的是, 这里的设计考虑了不同的事务隔离级别, 例如:</p><ol><li>当我们使用<code>Repeatable Read</code>隔离级别时, 我们需要将每次<code>get</code>的数据暂存到句柄的上下文中, 后续查询相同的<code>key</code>时, 从上下文中读取到相同的数据. 这个暂存的数据就放在<code>read_map_</code>中</li><li>当我们使用非<code>Read Uncommitted</code>隔离级别时, 我们需要将每次<code>put</code>的数据暂存到句柄的上下文中, 后续查询相同的<code>key</code>时, 从上下文中读取到相同的数据. 这个暂存的数据就放在<code>temp_map_</code>中</li><li>当我们使用<code>Read Uncommitted</code>隔离级别时, 我们每次<code>put</code>的数据是直接应用到整个存储引擎中的, 因此这里我们存在一个存储引擎的指针<code>std::shared_ptr&lt;TranManager&gt; tranManager_</code>, 同时我们需要记录操作前的数据库状态以便于回滚, 因此其存放于<code>rollback_map_</code>中</li></ol><h3 id="2-2-2-TranManager的定义"><a href="#2-2-2-TranManager的定义" class="headerlink" title="2.2.2 TranManager的定义"></a>2.2.2 TranManager的定义</h3><p><code>TranManager</code>的作用包括:</p><ol><li>管理事务的<code>id</code>的生成</li><li>管理事务上下文<code>TranContext</code>的申请</li><li>负责记录当前事务完成状态:<ol><li><code>max_flushed_tranc_id_</code>: 最大的已经刷入<code>sst</code>中的事务</li><li><code>max_finished_tranc_id_</code>: 最大的已经完成的事务(但数据可能还存在于内存中)</li><li><code>nextTransactionId_</code>: 下一个分配事务的<code>id</code></li></ol></li><li>负责事务状态的持久化操作</li></ol><p>这里特别进行说明, 为什么要负责是事务的持久化操作, 因为我们后续会实现<code>WAL</code>(Write Ahead Log), 这个日志会记录每次的操作, 当我们的数据库崩溃后重启时, 会根据<code>WAL</code>中的操作记录进行恢复, 这里的操作记录是指<code>put</code>、<code>remove</code>等操作, 但是我们需要在重启时知道哪些操作是已经完成的, 哪些操作是未完成的, 因此我们需要在<code>WAL</code>中记录每个事务的状态, 这个状态就是<code>max_flushed_tranc_id_</code>, 因此在重放<code>WAL</code>的操作时, 我们需要根据这个状态来判断哪些操作是已经完成的, 哪些操作是未完成的.</p><blockquote><p><code>WAL</code>在实现<code>MVCC</code>后实现</p></blockquote><p>讲清楚这些概念后, 我们不难写出如下的<code>TranManager</code>定义:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// include/lsm/transaction.h</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">TranManager</span> : <span class="keyword">public</span> std::enable_shared_from_this&lt;TranManager&gt; &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="built_in">TranManager</span>(std::string data_dir, <span class="keyword">enum</span> IsolationLevel isolation_level =</span><br><span class="line">                                        IsolationLevel::REPEATABLE_READ);</span><br><span class="line">  ~<span class="built_in">TranManager</span>();</span><br><span class="line">  <span class="function"><span class="type">void</span> <span class="title">init_new_wal</span><span class="params">()</span></span>;</span><br><span class="line">  <span class="function"><span class="type">void</span> <span class="title">set_engine</span><span class="params">(std::shared_ptr&lt;LSMEngine&gt; engine)</span></span>;</span><br><span class="line">  <span class="function">std::shared_ptr&lt;TranContext&gt; <span class="title">new_tranc</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="type">uint64_t</span> <span class="title">getNextTransactionId</span><span class="params">()</span></span>;</span><br><span class="line">  <span class="function"><span class="type">uint64_t</span> <span class="title">get_max_flushed_tranc_id</span><span class="params">()</span></span>;</span><br><span class="line">  <span class="function"><span class="type">uint64_t</span> <span class="title">get_max_finished_tranc_id_</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="type">void</span> <span class="title">update_max_finished_tranc_id</span><span class="params">(<span class="type">uint64_t</span> tranc_id)</span></span>;</span><br><span class="line">  <span class="function"><span class="type">void</span> <span class="title">update_max_flushed_tranc_id</span><span class="params">(<span class="type">uint64_t</span> tranc_id)</span></span>;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="type">bool</span> <span class="title">write_to_wal</span><span class="params">(<span class="type">const</span> std::vector&lt;Record&gt; &amp;records)</span></span>;</span><br><span class="line"></span><br><span class="line">  std::map&lt;<span class="type">uint64_t</span>, std::vector&lt;Record&gt;&gt; <span class="built_in">check_recover</span>();</span><br><span class="line"></span><br><span class="line">  <span class="function">std::string <span class="title">get_tranc_id_file_path</span><span class="params">()</span></span>;</span><br><span class="line">  <span class="function"><span class="type">void</span> <span class="title">write_tranc_id_file</span><span class="params">()</span></span>;</span><br><span class="line">  <span class="function"><span class="type">void</span> <span class="title">read_tranc_id_file</span><span class="params">()</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">enum</span> IsolationLevel <span class="title">isolation_level</span><span class="params">()</span></span>;</span><br><span class="line">  <span class="comment">// void flusher();</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">  <span class="keyword">mutable</span> std::mutex mutex_;</span><br><span class="line">  std::shared_ptr&lt;LSMEngine&gt; engine_;</span><br><span class="line">  std::shared_ptr&lt;WAL&gt; wal;</span><br><span class="line">  std::string data_dir_;</span><br><span class="line">  <span class="keyword">enum</span> <span class="title class_">IsolationLevel</span> isolation_level_;</span><br><span class="line">  <span class="comment">// std::atomic&lt;bool&gt; flush_thread_running_ = true;</span></span><br><span class="line">  std::atomic&lt;<span class="type">uint64_t</span>&gt; nextTransactionId_ = <span class="number">1</span>;</span><br><span class="line">  std::atomic&lt;<span class="type">uint64_t</span>&gt; max_flushed_tranc_id_ = <span class="number">0</span>;</span><br><span class="line">  std::atomic&lt;<span class="type">uint64_t</span>&gt; max_finished_tranc_id_ = <span class="number">0</span>;</span><br><span class="line">  std::map&lt;<span class="type">uint64_t</span>, std::shared_ptr&lt;TranContext&gt;&gt; activeTrans_;</span><br><span class="line">  FileObj tranc_id_file_;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h2 id="2-3-事务管理器的实现"><a href="#2-3-事务管理器的实现" class="headerlink" title="2.3 事务管理器的实现"></a>2.3 事务管理器的实现</h2><p>这里我们进入今天的主题, 如何实现不同隔离级别下的事务操作</p><h3 id="2-3-1-put"><a href="#2-3-1-put" class="headerlink" title="2.3.1 put"></a>2.3.1 put</h3><p>这里的实现就是我们之前讲到的:</p><ol><li>如果隔离级别是<code>READ_UNCOMMITTED</code>, 直接写入<code>memtable</code></li><li>其他隔离级别需要 暂存到<code>temp_map_</code>中, 统一提交后才在数据库中生效</li></ol><blockquote><p>这里设计到的<code>operations</code>等操作可以先忽略, 其属于<code>wal</code>部分的内容</p></blockquote><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// src/lsm/transaction.cpp</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">TranContext::put</span><span class="params">(<span class="type">const</span> std::string &amp;key, <span class="type">const</span> std::string &amp;value)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">auto</span> isolation_level = tranManager_-&gt;<span class="built_in">isolation_level</span>();</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 所有隔离级别都需要先写入 operations 中</span></span><br><span class="line">  operations.<span class="built_in">emplace_back</span>(Record::<span class="built_in">putRecord</span>(<span class="keyword">this</span>-&gt;tranc_id_, key, value)); <span class="comment">// 可忽略, WAL部分的内容</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (isolation_level == IsolationLevel::READ_UNCOMMITTED) &#123;</span><br><span class="line">    <span class="comment">// 1 如果隔离级别是 READ_UNCOMMITTED, 直接写入 memtable</span></span><br><span class="line">    <span class="comment">// 先查询以前的记录, 因为回滚时可能需要</span></span><br><span class="line">    <span class="keyword">auto</span> prev_record = engine_-&gt;<span class="built_in">get</span>(key, <span class="number">0</span>);</span><br><span class="line">    rollback_map_[key] = prev_record;</span><br><span class="line">    engine_-&gt;<span class="built_in">put</span>(key, value, tranc_id_);</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 2 其他隔离级别需要 暂存到 temp_map_ 中, 统一提交后才在数据库中生效</span></span><br><span class="line">  temp_map_[key] = value;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="2-3-2-remove"><a href="#2-3-2-remove" class="headerlink" title="2.3.2 remove"></a>2.3.2 remove</h3><p><code>remove</code>在<code>LSM</code>中本质上就是<code>put</code>一个空值, 因此逻辑和<code>put</code>是一样的, 这里我们只需要注意:<br>隔离级别为<code>READ_UNCOMMITTED</code>时, 需要先查询以前的记录, 因为回滚时可能需要, 这里我们需要将以前的记录存储到<code>rollback_map_</code>中, 然后再执行删除操作.</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">TranContext::remove</span><span class="params">(<span class="type">const</span> std::string &amp;key)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">auto</span> isolation_level = tranManager_-&gt;<span class="built_in">isolation_level</span>();</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 所有隔离级别都需要先写入 operations 中</span></span><br><span class="line">  operations.<span class="built_in">emplace_back</span>(Record::<span class="built_in">deleteRecord</span>(<span class="keyword">this</span>-&gt;tranc_id_, key));</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (isolation_level == IsolationLevel::READ_UNCOMMITTED) &#123;</span><br><span class="line">    <span class="comment">// 1 如果隔离级别是 READ_UNCOMMITTED, 直接写入 memtable</span></span><br><span class="line">    <span class="comment">// 先查询以前的记录, 因为回滚时可能需要</span></span><br><span class="line">    <span class="keyword">auto</span> prev_record = engine_-&gt;<span class="built_in">get</span>(key, <span class="number">0</span>);</span><br><span class="line">    rollback_map_[key] = prev_record;</span><br><span class="line">    engine_-&gt;<span class="built_in">remove</span>(key, tranc_id_);</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 2 其他隔离级别需要 暂存到 temp_map_ 中, 统一提交后才在数据库中生效</span></span><br><span class="line">  temp_map_[key] = <span class="string">&quot;&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="2-3-3-get"><a href="#2-3-3-get" class="headerlink" title="2.3.3 get"></a>2.3.3 get</h3><p>这里<code>get</code>的逻辑相对复杂一点:</p><ol><li>所有隔离级别先就近在当前操作的临时缓存中查找(即使是<code>READ_UNCOMMITTED</code>隔离级别也可以直接从<code>temp_map_</code>中读取, 虽然它是空的…)</li><li>否则使用<code>engine</code>查询<ol><li>如果隔离级别是<code>READ_UNCOMMITTED</code>, 使用<code>engine</code>查询时不需要判断<code>tranc_id</code>, 直接获取最新值</li><li>如果隔离级别是<code>READ_COMMITTED</code>, 使用<code>engine</code>查询时需要传递当前事务的<code>tranc_id</code>表示最高的可见版本</li><li>如果隔离级别是<code>SERIALIZABLE</code>或<code>REPEATABLE_READ</code>, 第一次使用<code>engine</code>查询后还需要暂存, 从而避免不可重复读现象</li></ol></li></ol><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">std::optional&lt;std::string&gt; <span class="title">TranContext::get</span><span class="params">(<span class="type">const</span> std::string &amp;key)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">auto</span> isolation_level = tranManager_-&gt;<span class="built_in">isolation_level</span>();</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 1 所有隔离级别先就近在当前操作的临时缓存中查找</span></span><br><span class="line">  <span class="keyword">if</span> (temp_map_.<span class="built_in">find</span>(key) != temp_map_.<span class="built_in">end</span>()) &#123;</span><br><span class="line">    <span class="comment">// READ_UNCOMMITTED 随单次操作更新数据库, 不需要最后的统一更新</span></span><br><span class="line">    <span class="comment">// 这一步骤肯定会自然跳过的</span></span><br><span class="line">    <span class="keyword">return</span> temp_map_[key];</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 2 否则使用 engine 查询</span></span><br><span class="line">  std::optional&lt;std::pair&lt;std::string, <span class="type">uint64_t</span>&gt;&gt; query;</span><br><span class="line">  <span class="keyword">if</span> (isolation_level == IsolationLevel::READ_UNCOMMITTED) &#123;</span><br><span class="line">    <span class="comment">// 2.1 如果隔离级别是 READ_UNCOMMITTED, 使用 engine</span></span><br><span class="line">    <span class="comment">// 查询时不需要判断 tranc_id, 直接获取最新值</span></span><br><span class="line">    query = engine_-&gt;<span class="built_in">get</span>(key, <span class="number">0</span>);</span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> (isolation_level == IsolationLevel::READ_COMMITTED) &#123;</span><br><span class="line">    <span class="comment">// 2.2 如果隔离级别是 READ_COMMITTED, 使用 engine</span></span><br><span class="line">    <span class="comment">// 查询时判断 tranc_id</span></span><br><span class="line">    query = engine_-&gt;<span class="built_in">get</span>(key, <span class="keyword">this</span>-&gt;tranc_id_);</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">// 2.2 如果隔离级别是 SERIALIZABLE 或 REPEATABLE_READ, 第一次使用 engine</span></span><br><span class="line">    <span class="comment">// 查询后还需要暂存</span></span><br><span class="line">    <span class="keyword">if</span> (read_map_.<span class="built_in">find</span>(key) != read_map_.<span class="built_in">end</span>()) &#123;</span><br><span class="line">      query = read_map_[key];</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      query = engine_-&gt;<span class="built_in">get</span>(key, <span class="keyword">this</span>-&gt;tranc_id_);</span><br><span class="line">      read_map_[key] = query;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (query.<span class="built_in">has_value</span>()) &#123;</span><br><span class="line">    <span class="keyword">return</span> query-&gt;first;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> std::<span class="literal">nullopt</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="2-3-4-commit"><a href="#2-3-4-commit" class="headerlink" title="2.3.4 commit"></a>2.3.4 commit</h3><p><code>commit</code>可能是折柳最复杂的, 其逻辑是:</p><ol><li>如果隔离级别是<code>READ_UNCOMMITTED</code>, 因为之前就已经将更改的数据写入了<code>MemTable</code>, 现在只需要直接写入<code>wal</code>一个<code>Commit</code>记录(目前不涉及, 可先跳过)</li><li>如果隔离级别是<code>REPEATABLE_READ</code>或<code>SERIALIZABLE</code>, 需要遍历所有的操作记录, 判断是否存在冲突, 如果存在冲突则终止事务, 否则将所有的操作记录写入<code>wal</code>中, 然后将数据应用到数据库中</li><li>完成事务数据同步到<code>MemTable</code>后, 更新<code>max_finished_tranc_id_</code>并持久化数据</li></ol><blockquote><p>这里需要注意的是, 我们检查冲突侵入式地获取了其他组件的锁(<code>memtable.frozen_mtx</code>, <code>memtable.cur_mtx</code>, <code>engine_-&gt;ssts_mtx</code>), 这个写法可能有点丑陋, 但一开始没有设计好, 也只能这样了<br>同样地, 这里设计<code>WAL</code>的部分可以先跳过</p></blockquote><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">bool</span> <span class="title">TranContext::commit</span><span class="params">(<span class="type">bool</span> test_fail)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">auto</span> isolation_level = tranManager_-&gt;<span class="built_in">isolation_level</span>();</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (isolation_level == IsolationLevel::READ_UNCOMMITTED) &#123;</span><br><span class="line">    <span class="comment">// READ_UNCOMMITTED 随单次操作更新数据库, 不需要最后的统一更新</span></span><br><span class="line">    <span class="comment">// 因此也不需要使用到后面的锁保证正确性</span></span><br><span class="line">    operations.<span class="built_in">emplace_back</span>(Record::<span class="built_in">commitRecord</span>(<span class="keyword">this</span>-&gt;tranc_id_));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 先刷入wal</span></span><br><span class="line">    <span class="keyword">auto</span> wal_success = tranManager_-&gt;<span class="built_in">write_to_wal</span>(operations); <span class="comment">// WAL的部分, 现在可以忽略</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!wal_success) &#123;</span><br><span class="line">      <span class="keyword">throw</span> std::<span class="built_in">runtime_error</span>(<span class="string">&quot;write to wal failed&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    isCommited = <span class="literal">true</span>;</span><br><span class="line">    tranManager_-&gt;<span class="built_in">update_max_finished_tranc_id</span>(tranc_id_);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// commit 需要检查所有的操作是否合法</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// 遍历所有的记录, 判断是否合法</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// <span class="doctag">TODO:</span> 目前为检查冲突, 全局获取了读锁, 后续考虑性能优化方案</span></span><br><span class="line"></span><br><span class="line">  MemTable &amp;memtable = engine_-&gt;memtable;</span><br><span class="line">  <span class="function">std::unique_lock&lt;std::shared_mutex&gt; <span class="title">wlock1</span><span class="params">(memtable.frozen_mtx)</span></span>;</span><br><span class="line">  <span class="function">std::unique_lock&lt;std::shared_mutex&gt; <span class="title">wlock2</span><span class="params">(memtable.cur_mtx)</span></span>;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (isolation_level == IsolationLevel::REPEATABLE_READ ||</span><br><span class="line">      isolation_level == IsolationLevel::SERIALIZABLE) &#123;</span><br><span class="line">    <span class="comment">// REPEATABLE_READ 需要校验冲突</span></span><br><span class="line">    <span class="comment">// <span class="doctag">TODO:</span> 目前 SERIALIZABLE 还没有实现, 逻辑和 REPEATABLE_READ 相同</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 只要需要校验的 隔离级别 需要加sst的锁</span></span><br><span class="line">    <span class="function">std::shared_lock&lt;std::shared_mutex&gt; <span class="title">rlock3</span><span class="params">(engine_-&gt;ssts_mtx)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">auto</span> &amp;[k, v] : temp_map_) &#123;</span><br><span class="line">      <span class="comment">// 步骤1: 先在内存表中判断该 key 是否冲突</span></span><br><span class="line"></span><br><span class="line">      <span class="comment">// ! 注意第二个参数设置为0, 表示忽略事务可见性的查询</span></span><br><span class="line">      <span class="keyword">auto</span> res = memtable.<span class="built_in">get_</span>(k, <span class="number">0</span>);</span><br><span class="line">      <span class="keyword">if</span> (res.<span class="built_in">is_valid</span>() &amp;&amp; res.<span class="built_in">get_tranc_id</span>() &gt; tranc_id_) &#123;</span><br><span class="line">        <span class="comment">// 数据库中存在相同的 key , 且其 tranc_id 大于当前 tranc_id</span></span><br><span class="line">        <span class="comment">// 表示更晚创建的事务修改了相同的key, 并先提交, 发生了冲突</span></span><br><span class="line">        <span class="comment">// 需要终止事务</span></span><br><span class="line">        isAborted = <span class="literal">true</span>;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// 步骤2: 判断sst中是否是否存在冲突</span></span><br><span class="line">        <span class="keyword">if</span> (tranManager_-&gt;<span class="built_in">get_max_flushed_tranc_id</span>() &lt;= tranc_id_) &#123;</span><br><span class="line">          <span class="comment">// sst 中最大的 tranc_id 小于当前 tranc_id, 没有冲突</span></span><br><span class="line">          <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 否则要查询具体的key是否冲突</span></span><br><span class="line">        <span class="comment">// ! 注意第二个参数设置为0, 表示忽略事务可见性的查询</span></span><br><span class="line">        <span class="keyword">auto</span> res = engine_-&gt;<span class="built_in">sst_get_</span>(k, <span class="number">0</span>);</span><br><span class="line">        <span class="keyword">if</span> (res.<span class="built_in">has_value</span>()) &#123;</span><br><span class="line">          <span class="keyword">auto</span> [v, tranc_id] = res.<span class="built_in">value</span>();</span><br><span class="line">          <span class="keyword">if</span> (tranc_id &gt; tranc_id_) &#123;</span><br><span class="line">            <span class="comment">// 数据库中存在相同的 key , 且其 tranc_id 大于当前 tranc_id</span></span><br><span class="line">            <span class="comment">// 表示更晚创建的事务修改了相同的key, 并先提交, 发生了冲突</span></span><br><span class="line">            <span class="comment">// 需要终止事务</span></span><br><span class="line">            isAborted = <span class="literal">true</span>;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">          &#125;</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 其他隔离级别不检查, 直接运行到这里</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// 校验全部通过, 可以刷入</span></span><br><span class="line">  operations.<span class="built_in">emplace_back</span>(Record::<span class="built_in">commitRecord</span>(<span class="keyword">this</span>-&gt;tranc_id_));</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 先刷入wal</span></span><br><span class="line">  <span class="keyword">auto</span> wal_success = tranManager_-&gt;<span class="built_in">write_to_wal</span>(operations);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (!wal_success) &#123;</span><br><span class="line">    <span class="keyword">throw</span> std::<span class="built_in">runtime_error</span>(<span class="string">&quot;write to wal failed&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 将暂存数据应用到数据库</span></span><br><span class="line">  <span class="keyword">if</span> (!test_fail) &#123;</span><br><span class="line">    <span class="comment">// 这里是手动调用 memtable 的无锁版本的 put_, 因为之前手动加了写锁</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">auto</span> &amp;[k, v] : temp_map_) &#123;</span><br><span class="line">      memtable.<span class="built_in">put_</span>(k, v, tranc_id_);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  isCommited = <span class="literal">true</span>;</span><br><span class="line">  tranManager_-&gt;<span class="built_in">update_max_finished_tranc_id</span>(tranc_id_);</span><br><span class="line">  <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="2-3-5-abort"><a href="#2-3-5-abort" class="headerlink" title="2.3.5 abort"></a>2.3.5 abort</h3><p><code>abort</code> 方法用于回滚事务，其逻辑如下:</p><ol><li>如果隔离级别是<code>READ_UNCOMMITTED</code>, 需要手动恢复之前的更改, 这里我们需要遍历<code>rollback_map_</code>, 如果存在值, 则将其写入<code>memtable</code>, 否则删除当前事务的新增操作</li><li>如果是其他隔离级别, 由于数据都是暂存到本地的, 因此直接丢弃即可, 同时为<code>WAL</code>添加一个<code>Abort</code>标记即可(暂时先忽略, <code>wal</code>的内容)<figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">bool</span> <span class="title">TranContext::abort</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">auto</span> isolation_level = tranManager_-&gt;<span class="built_in">isolation_level</span>();</span><br><span class="line">  <span class="keyword">if</span> (isolation_level == IsolationLevel::READ_UNCOMMITTED) &#123;</span><br><span class="line">    <span class="comment">// 需要手动恢复之前的更改</span></span><br><span class="line">    <span class="comment">// <span class="doctag">TODO:</span> 需要使用批量化操作优化性能</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">auto</span> &amp;[k, res] : rollback_map_) &#123;</span><br><span class="line">      <span class="keyword">if</span> (res.<span class="built_in">has_value</span>()) &#123;</span><br><span class="line">        engine_-&gt;<span class="built_in">put</span>(k, res.<span class="built_in">value</span>().first, res.<span class="built_in">value</span>().second);</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// 之前本就不存在, 需要移除当前事务的新增操作</span></span><br><span class="line">        engine_-&gt;<span class="built_in">remove</span>(k, tranc_id_);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    isAborted = <span class="literal">true</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// ! 由于目前 records 是在 commit 时统一刷入 wal 的, 因此这个情况下, abort</span></span><br><span class="line">  <span class="comment">// ! 简单丢弃 operations 即可 ! 后续会将 records 分批次写入 wal</span></span><br><span class="line">  <span class="comment">// ! 这时就需要加上 ! rollback 标记了, 执行下面注释的逻辑</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// operations.emplace_back(Record::rollbackRecord(this-&gt;tranc_id_));</span></span><br><span class="line">  <span class="comment">// // 先刷入wal</span></span><br><span class="line">  <span class="comment">// auto wal_success = tranManager_-&gt;write_to_wal(operations);</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// if (!wal_success) &#123;</span></span><br><span class="line">  <span class="comment">//   throw std::runtime_error(&quot;write to wal failed&quot;);</span></span><br><span class="line">  <span class="comment">// &#125;</span></span><br><span class="line"></span><br><span class="line">  isAborted = <span class="literal">true</span>;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ol><h3 id="2-3-6-begin-tran"><a href="#2-3-6-begin-tran" class="headerlink" title="2.3.6 begin_tran"></a>2.3.6 begin_tran</h3><p><code>begin_tran</code>方法用于创建一个新的事务上下文, 在实现了前述的<code>TranContext</code> 之后, 我们只需要在<code>Engine</code>中添加一个<code>begin_tran</code>方法即可:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// src/lsm/engine.cpp</span></span><br><span class="line"><span class="comment">// 开启一个事务</span></span><br><span class="line"><span class="function">std::shared_ptr&lt;TranContext&gt; <span class="title">LSM::begin_tran</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">auto</span> tranc_context = tran_manager_-&gt;<span class="built_in">new_tranc</span>();</span><br><span class="line">  <span class="keyword">return</span> tranc_context;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// src/lsm/transation.cpp</span></span><br><span class="line"><span class="function">std::shared_ptr&lt;TranContext&gt; <span class="title">TranManager::new_tranc</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="comment">// 获取锁</span></span><br><span class="line">  <span class="function">std::unique_lock&lt;std::mutex&gt; <span class="title">lock</span><span class="params">(mutex_)</span></span>;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">auto</span> tranc_id = <span class="built_in">getNextTransactionId</span>();</span><br><span class="line">  activeTrans_[tranc_id] =</span><br><span class="line">      std::<span class="built_in">make_shared</span>&lt;TranContext&gt;(tranc_id, engine_, <span class="built_in">shared_from_this</span>());</span><br><span class="line">  <span class="keyword">return</span> activeTrans_[tranc_id];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="2-3-7-事务信息持久化"><a href="#2-3-7-事务信息持久化" class="headerlink" title="2.3.7 事务信息持久化"></a>2.3.7 事务信息持久化</h3><p>当每次进行事务<code>id</code>的更新时(主要是<code>update_max_flushed_tranc_id</code>), 都需要将最新的事务信息进行持久化, 由于我们的<code>update_max_flushed_tranc_id</code>是一个原子变量, 因此我们可以通过原子变量的特性无锁地完成其更新操作,:</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">TranManager::update_max_flushed_tranc_id</span><span class="params">(<span class="type">uint64_t</span> tranc_id)</span> </span>&#123;</span><br><span class="line">  <span class="comment">// ! max_finished_tranc_id_ 对于崩溃恢复有决定性的作用</span></span><br><span class="line">  <span class="comment">// ! 需要立即刷盘</span></span><br><span class="line">  <span class="type">uint64_t</span> expected = max_flushed_tranc_id_.<span class="built_in">load</span>(std::memory_order_relaxed);</span><br><span class="line">  <span class="keyword">while</span> (tranc_id &gt; expected) &#123;</span><br><span class="line">    <span class="keyword">if</span> (max_flushed_tranc_id_.<span class="built_in">compare_exchange_weak</span>(</span><br><span class="line">            expected, tranc_id, std::memory_order_acq_rel,</span><br><span class="line">            std::memory_order_relaxed)) &#123;</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="built_in">write_tranc_id_file</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">TranManager::write_tranc_id_file</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="comment">// 共4个8字节的整型id记录</span></span><br><span class="line">  <span class="comment">// std::atomic&lt;uint64_t&gt; nextTransactionId_;</span></span><br><span class="line">  <span class="comment">// std::atomic&lt;uint64_t&gt; max_flushed_tranc_id_;</span></span><br><span class="line">  <span class="comment">// std::atomic&lt;uint64_t&gt; max_finished_tranc_id_;</span></span><br><span class="line"></span><br><span class="line">  <span class="function">std::vector&lt;<span class="type">uint8_t</span>&gt; <span class="title">buf</span><span class="params">(<span class="number">3</span> * <span class="keyword">sizeof</span>(<span class="type">uint64_t</span>), <span class="number">0</span>)</span></span>;</span><br><span class="line">  <span class="type">uint64_t</span> nextTransactionId = nextTransactionId_.<span class="built_in">load</span>();</span><br><span class="line">  <span class="type">uint64_t</span> max_flushed_tranc_id = max_flushed_tranc_id_.<span class="built_in">load</span>();</span><br><span class="line">  <span class="type">uint64_t</span> max_finished_tranc_id = max_finished_tranc_id_.<span class="built_in">load</span>();</span><br><span class="line"></span><br><span class="line">  <span class="built_in">memcpy</span>(buf.<span class="built_in">data</span>(), &amp;nextTransactionId, <span class="built_in">sizeof</span>(<span class="type">uint64_t</span>));</span><br><span class="line">  <span class="built_in">memcpy</span>(buf.<span class="built_in">data</span>() + <span class="built_in">sizeof</span>(<span class="type">uint64_t</span>), &amp;max_flushed_tranc_id,</span><br><span class="line">         <span class="built_in">sizeof</span>(<span class="type">uint64_t</span>));</span><br><span class="line">  <span class="built_in">memcpy</span>(buf.<span class="built_in">data</span>() + <span class="number">2</span> * <span class="built_in">sizeof</span>(<span class="type">uint64_t</span>), &amp;max_finished_tranc_id,</span><br><span class="line">         <span class="built_in">sizeof</span>(<span class="type">uint64_t</span>));</span><br><span class="line"></span><br><span class="line">  tranc_id_file_.<span class="built_in">write</span>(<span class="number">0</span>, buf);</span><br><span class="line">  tranc_id_file_.<span class="built_in">sync</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里简单介绍下原子变量和内存顺序:</p><ol><li><strong>原子变量</strong>：<code>max_flushed_tranc_id_</code> 通过 <code>compare_exchange_weak</code> 循环更新最大值，确保多线程安全。  </li><li><strong>内存顺序</strong>：<code>acq_rel</code> 保证更新对其他线程可见且本线程读取最新值，失败时 <code>relaxed</code> 降低开销。  </li><li><strong>持久化</strong>：更新后立即调用 <code>write_tranc_id_file()</code> 刷盘，确保崩溃恢复时数据可靠。</li></ol><h1 id="3-小节"><a href="#3-小节" class="headerlink" title="3 小节"></a>3 小节</h1><p>本章完成了<code>MVCC</code>和事务设计的上层设计, 现在我们的存储引擎已经支持<code>MVCC</code>查询和不同隔离级别的事务了, 但现在我们的存储引擎还缺少<code>WAL</code>和崩溃恢复的设计, 这是我们下一章的内容。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;之前我们已经对各个组件的接口进行了统一, 添加了&lt;code&gt;tranc_id&lt;/code&gt;这个事务id参数, 接下来这个章节, 我们将介绍顶层的事务设计, 即事务id是如何生成的, 实现相关的事务管理器.&lt;/p&gt;
&lt;p&gt;代码仓库：&lt;a href=&quot;https://githu</summary>
      
    
    
    
    <category term="项目" scheme="http://example.com/categories/%E9%A1%B9%E7%9B%AE/"/>
    
    <category term="Tiny-LSM" scheme="http://example.com/categories/%E9%A1%B9%E7%9B%AE/Tiny-LSM/"/>
    
    
    <category term="C++" scheme="http://example.com/tags/C/"/>
    
    <category term="KV存储" scheme="http://example.com/tags/KV%E5%AD%98%E5%82%A8/"/>
    
  </entry>
  
</feed>
